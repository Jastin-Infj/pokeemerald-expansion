# Smart Gimmick AI Implementation

## Summary

The feature retains the existing smart timing, switching, Protect, partner, and
risk-governor layers, and now adds a bounded side-wide doubles board search.
Eligible read-mode 2v2 boards compare both AI actions as one move / switch /
supported-gimmick plan, simulate the realized board, and restore both commands
from the same selected root. Unsupported boards fail closed to the preserved
per-battler evaluator.

## September 2, 2026 Integration Staging

The implementation is staged in
`integration/goal-team-box-smart-ai-20260901`, based on
`integration/runtime-lab-current-1.16.1` at `4e960aecea`. Team Box PR #84 is
applied before Smart AI. Where the integration shelf differed from the newer
RH Hideout-side source, the newer source's APIs, command conventions, and
Battle Lab behavior were preserved; the reconciliation is `404cbd192d`.

The Smart AI source and handoff are `535f095fef` and `ca98c85ace`. The branch
passes the focused simulator, snapshot, joint planner, joint evaluation, and
joint runtime suites, plus debug/normal builds and the full test command. The
project-local direct mGBA runner also reached a live `Read Double` gauntlet
from a clean New Game: after both player commands were confirmed, the log
captured an opponent preserve switch and a Dynamax `Knock Off` response.
Evidence is `/tmp/smart-ai-route-20260902-final-battle-response.png`,
`/tmp/smart-ai-read-double-20260902.json`, and Lua marker
`smart_ai_read_double_runtime_20260902`; the exported v5 log contains five
valid actions, two AI plans, 16 candidates, and trace schema 5. No master
merge or external push was performed.

## Current Joint Board-Search Handoff - July 22, 2026

This implementation is the local commit `fe68cd2d9c` on
`integration/smart-ai-board-search-20260720`, based on
`integration/runtime-lab-current-1.16.1` at
`4e960aecea0185912e21a28f40e00fab1b388874`. It is playable integration and
has completed local review-fix validation; it is not a master-ready branch or
permission to merge runtime source into `master`. No feature PR exists yet.

The new runtime is split into these layers:

- `battle_ai_board_snapshot.c` captures immutable active, reserve, field, side,
  resource, counter, and confirmed-command state. With full knowledge it also
  derives prospective Mega / Ultra profiles from real party data and retains
  future gimmick eligibility for active and reserve party members.
- `battle_ai_board_damage.c` projects supported damage and effective Speed
  without mutating live battle globals.
- `battle_ai_board_sim.c` validates compact actions, enumerates exact weighted
  16-roll / critical / true-Speed-tie outcomes, applies the configured Mega and
  mid-turn order rules, merges equivalent post-turn boards, applies supported move /
  switch / gimmick transitions and forced replacements, and returns a realized
  board. A narrow proof groups equal raw-damage classes before whole-turn replay
  when exactly one ordinary single-target hit is present and all other actions
  are proven not to change its inputs. Repeated active Tailwind is a deterministic
  no-op that spends PP without refreshing the timer or emitting a field-change event.
- `battle_ai_joint_planner.c` ranks side-wide roots, completes depth 3, and can
  extend up to three leading or close roots to depth 5.
- `battle_ai_joint_runtime.c` generates bounded atomic candidates, advances the
  search in resumable 12-node slices, restores both commands, records trace
  evidence, and owns a 16,248-byte heap arena capped below 16 KiB.

Exact enumeration uses a separate 2,596-byte static EWRAM scratch under a
single-job/non-reentrant contract. Moving the full frontier workspace out of the
controller-task stack reduced the former 2,292-byte
`AiSim_EnumerateOutcomes` frame and eliminated task/window corruption during
fallback. Final `objdump` inspection shows local stack allocations of 240 bytes
in the normal object, 124 bytes in the debug object, and 244 bytes in the
`TESTING` object; the function also saves 36 bytes of registers.
Final turn-order review exposed a second nested stack boundary:
`AiSim_CheckJointTurn()` still carried a 540-byte validation board, producing an
approximately 592-byte frame beneath the enumerator. That board now lives in a
separate 540-byte single-job EWRAM validation scratch. Final local allocations
are 56 normal / 28 debug / 56 `TESTING` bytes, again plus 36 saved-register
bytes. Initial-order construction reuses the caller's output board,
and dynamic tie projection reuses the exact-outcome scratch rather than placing
another complete board on the fixed 1 KiB controller-task stack.

The joint route requires an ordinary admitted non-link NPC trainer 2v2 with no
trainer item inventory; Double Battle / Smart Mon Choices / Omniscient / Read
Player Move AI without `AI_FLAG_ATTACKS_PARTNER`; exactly two non-Commander
commandable battlers on each side; and exactly two confirmed live player move
or switch commands, with neither player battler forced into a multi-turn or
recharge state. Recorded, Palace, Safari, roaming, first-battle, multi,
two-opponent, and in-game-partner formats remain outside this gate. A
root-generation or simulator boundary rejects the whole side-wide result: the
two slots do not mix joint and legacy choices. Once a usable depth-3 root
completes, a limit reached on remaining roots or a selective continuation keeps
the deepest completed result; a budget failure before any usable base result
falls back coherently to legacy.

Current defaults are 32 generated atomic actions per actor, 12 retained actions
per actor, 32 joint roots, depth 3 with selective depth 5, 2,048 normal / 3,072
deep nodes, 160 normal / 220 deep frames, 12 nodes per slice, 32 merged outcomes
per joint turn, and 16 transposition entries. The simulator includes forced
replacement, configured priority / Speed order, all 16 single-target damage rolls,
configured critical odds, exact order permutations only for genuinely tied
actors, consecutive Protect odds, same-priority last-mover Protect / Quick Guard
/ Wide Guard failure, Geomancy, supported items and gimmicks, and realized
board-pressure scoring. Selecting Tailwind while that side's timer is already
active spends PP but neither refreshes the timer nor emits a new field-change
event; a new Tailwind uses the configured pre-Gen-5 three-turn or Gen-5+ four-turn
duration. It preserves the integer probability mass of every raw stochastic
branch. For exactly one ordinary single-target `EFFECT_HIT`, raw-damage classes
may be formed only after proving that the other actions (supported Tailwind /
Trick Room, safe physical-side Geomancy, or a non-target switch) cannot change
that hit's inputs. Special damage into the Geomancy user, Fairy damage with a
switchable Fairy Aura, multiple damage actions, and other unsafe combinations
remain on the raw path. Equivalent post-turn boards then have their weights added
before the 32-outcome frontier is enforced. A separate 4,096-application ceiling prevents a huge raw
Cartesian product that merges to few boards from monopolizing one controller task;
reaching it makes the whole turn unsupported. Public simulator entry points reject null,
invalid battler-count, invalid battler, and invalid item inputs without reading
outside the snapshot.

Turn order follows both generation settings used by the live engine. With
`B_MEGA_EVO_TURN_ORDER < GEN_7`, the initial sort and initial Speed-tie set use
the pre-Mega / pre-Ultra profile even though the gimmick still activates before
move effects; Gen 7+ uses the transformed profile for that initial sort. With
`B_RECALC_TURN_AFTER_ACTIONS < GEN_8`, the remaining actors consume that fixed
initial order. Gen 8+ keeps the switch bracket and first move in the initial
order, then recalculates remaining move actions after the board changes. Outcome
enumeration uses the same boundary and excludes actors that already acted from
new dynamic ties. Consecutive Protect weights likewise use the configured
pre-Gen-5 1/2 progression or Gen-5+ 1/3 progression, and Knock Off's 1.5x power
is present only when `B_KNOCK_OFF_DMG >= GEN_6` and the target item is removable.

Confirmed Fake Out handling is also composed-turn aware. The legacy read gate
separates a structurally committed Fake Out from the candidate-specific result,
then evaluates planned Mega / Ultra / Tera species, ability, type and Speed,
Gen-6 versus Gen-7 Mega ordering, type-changing abilities, Sheer Force,
Neutralizing Gas / Gastro Acid, Ability Shield, Dazzling-family protection,
Psychic Terrain, Ghost immunity overrides, Inner Focus / Shield Dust / Covert
Cloak / Substitute, accuracy, Quick Guard execution order, and the shared
Lagging Tail / Stall bracket. A confirmed-flinch rejection is retained as Fake
Out trace provenance but deferred during joint root generation: a paired faster
Quick Guard or faster ally Fake Out may make the full line legal. The immutable
turn simulator remains authoritative and still skips an unguarded victim, so
defer does not turn a real flinch into a successful action.

Active Dynamax, or any generated newly selected Dynamax / Max candidate or
dynamically derived Max Move power, makes the whole joint root set fail closed.
For full-knowledge active and reserve party members, prospective Mega and Ultra
profiles are now derived by copying the real party mon, resolving its
Mega-by-move, Mega-by-item, or Ultra form, and recalculating stats, types, and
ability. The snapshot retains per-mon Mega / Ultra / Z / Dynamax / Tera
eligibility; future generation can therefore switch and spend a supported
reserve resource on a later ply, and an Ultra-burst Necrozma remains eligible
for its later Z action. Unsupported entry abilities and unknown profiles still
reject the joint root set.

Snapshot hardening also makes the unused third profile type `TYPE_MYSTERY`, keeps
an already transformed Mega / Ultra profile valid while benched, and makes forced
replacement select the incoming member's persistent form instead of reusing the
outgoing slot's transformed flag. Under `TESTING`, a forced ability is reapplied to
both normal and prospective transformed profiles to mirror the live test battler;
this is production-neutral. Active Stellar and persistent Stellar reserves entering
through ordinary or forced replacement fail closed with gimmick and damage-modifier
flags because the compact board has no Stellar offensive ledger.

Board evaluation now uses `transformed.maxHp` when a persistent Mega / Ultra has no
normal profile and returns to the bench, so switching cannot turn its HP denominator
into one. Move flags that alter repeat legality or bypass target abilities / defense
stages (`cantUseTwice`, `ignoresTargetAbility`, and
`ignoresTargetDefenseEvasionStages`) are categorical simulator boundaries, including
their signature Z moves, rather than being evaluated as ordinary `EFFECT_HIT`.

Damaging spread moves, multi-hit damage, and pre-Generation-3 critical rules are
categorically unsupported because the compact per-actor key cannot represent
their independent target / strike RNG exactly. A frontier that still exceeds 32
distinct post-turn boards after merging is unsupported as well. Encountering
one of these legal actions marks generation unresolved and sends the whole AI
side to the legacy evaluator instead of searching a favorable subset. Targeted
Prankster status handling represented by the simulator now matches the relevant
live blockers: Psychic Terrain stops positive-priority actions into grounded
opponents, and Gen 7+ Dark immunity uses the target's current simulated type,
including Dark Tera and Tera-away states. Commander and other unmodeled
mechanics remain explicit legacy boundaries rather than approximate transitions.

The action log remains 128 28-byte entries plus its header (3,592 bytes). The
separate trace is schema version 5 with magic `0xA15C`: 32 32-byte plans, 256
28-byte candidates, 96 96-byte board snapshots, and a 16-byte header (17,424
bytes). The exporter emits `pokeemerald.battle_action_log.v5` only when the
trace signature matches; otherwise it safely emits the v4 action-only schema.
Each retained joint plan records before and predicted-after boards at planning
time; end-turn capture adds actual-after once the turn completes. It also keeps
the chosen root, top review candidates, rejected / forced facts, search depth,
termination reason, and links back to both confirmed AI commands. Legacy plans
and mid-turn exports may have only the phases available at capture time.

The pre-review 4v4 runtime run exposed a real heap-lifetime regression after
trace export: that earlier layout's 16,268-byte joint arena remained allocated
while Grassy Terrain background restore requested a 13,024-byte Smol
decompression buffer,
triggering `malloc.c:104`. `BattleAiJointRuntime_ReleaseData()` now frees the
arena immediately after every battler command is committed and the action log
has copied its plan links. Runtime scalar evidence is retained only under
`TESTING`. The same Grassy Terrain route then completed background restore,
faint / replacement handling, a second allocation-release cycle, and the next
command menu without an error; its turn-2 v5 export contained 6 plans, 48
candidates, 12 boards, and 14 linked move / switch / gimmick-aware actions.
All 6 plans were correctly marked `legacy_evaluator` because that route selected
unsupported Dynamax; this remains historical heap-lifecycle and trace/action-log
survival evidence, not final exact-outcome or joint-choice evidence. The current
heap arena is 16,248 bytes after reducing the transposition table from 64 to 16
entries; the exact-enumeration scratch is the separate static EWRAM object described
above.

The earlier `Party -> Joint Trace Double` acceptance run is also retained only
as a superseded wiring baseline. It used two no-item, no-gimmick, Sturdy Shuckle
with Tackle on each side. Session `smart-ai-joint-trace-final2-20260722` and
`/tmp/smart-ai-joint-trace-final-v5.json` showed a valid v5 / `0xA15C` signature,
one shared nonlegacy depth-3 plan, five candidates, three board snapshots, four
linked actions, and clean session shutdown. Every candidate score was zero and
the fixture had no switch, gimmick, or stochastic KO boundary, so it is not the
final tactical acceptance evidence for the review fixes.

The revised `Party -> Joint Trace Double` fixture omits explicit `Party Size`
lines so trainer generation preserves the listed player two / AI three members.
The player leads Intimidate Arcanine with only Tailwind and Power Herb Sturdy
Skarmory with only Geomancy. The AI leads a level-7, zero-Attack-IV Scizorite
Technician Scizor with only Bullet Punch and Focus Sash Prankster Whimsicott with
only Tailwind, keeps Power Herb Fairy Aura Xerneas with only Geomancy in reserve,
and receives Mega Ring access. The selected Skarmory target is deliberately bulky
and Steel-resistant so the production 160 / 220 frame budgets can complete a
depth-3 root. The live trace proves planner, prospective-Mega, reserve-switch,
linkage, and board-export behavior; focused C regressions remain the direct proof
for exact roll / critical weights.

Final review-fix handoff evidence:

- Focused C regressions passed: board simulator 44/44, snapshot 7/7, joint
  runtime 9/9, joint planner 18/18, joint board evaluation 5/5, Prankster /
  Parting Shot 1/1, Fake Out composition 3/3, Read Player Move 70/70, and final
  Trainer Party Pool 10/10. The final simulator log is
  `/tmp/smart-ai-board-simulator-tailwind-config-final-20260722.log`; the fixture-
  final pool log is `/tmp/smart-ai-trainer-party-pool-level7-final-20260722.log`.
- Final-source `rtk make -j16 -O check` exited 0 with 5,066 passed, 12 accepted
  known failing, 598 TODO, 6 expected failing, and 5,682 total; raw log:
  `/tmp/smart-ai-full-check-tailwind-config-final-20260722.log`.
- Normal ROM build passed with the existing RWX linker warning: EWRAM 251,288
  bytes (95.86%), IWRAM 28,384 bytes (86.62%), ROM 26,633,604 bytes (79.37%);
  `/tmp/smart-ai-normal-build-tailwind-config-final-20260722.log`. The debug build
  passed: EWRAM 251,280 bytes (95.86%), IWRAM 28,444 bytes (86.80%), ROM
  26,684,308 bytes (79.53%);
  `/tmp/smart-ai-debug-build-tailwind-config-final-20260722.log`.
- mGBA Live session `smart-ai-review-final19-20260722` used the script-capable
  wrapper at 120 FPS. `/tmp/smart-ai-review-final19-20260722-after.json` is a
  valid v5 / version-5 / `0xA15C` export with one joint nonlegacy plan, eight
  retained candidates, three boards, and four actions. The chosen plan completed
  depth 3, retained the deepest completed result, visited 60 nodes, and terminated
  on the frame budget while attempting depth 5. Candidate scores are nonzero;
  the review set contains both Mega Bullet Punch and reserve-Xerneas switch roots.
  Both AI commands link to plan 1 / rank 0, both confirmed player predictions and
  chosen AI actions match, and before / predicted-after / actual-after boards are
  present. The predicted and actual boards agree, including Skarmory at 171 HP,
  consumed Power Herb, and Tailwind timers `[3, 3]`.
- Command and post-turn screenshots are
  `/tmp/smart-ai-review-final19-20260722-command.png` and
  `/tmp/smart-ai-review-final19-20260722-after.png`. Stop returned `stopped:true`;
  final managed status was `[]`.
- A level-8 diagnostic intentionally preserved more live damage classes but
  completed zero depth-3 roots before the 160-frame cutoff (nine depth-1 roots,
  60 nodes, elapsed 165) and coherently used legacy. It was rejected in favor of
  the level-7 acceptance fixture; production budgets were not changed. An earlier
  direct MCP start with the script-build binary also exited `SIGABRT` because it
  lacked `DISPLAY`; the accepted wrapper path supplied `DISPLAY=:0`. No stale
  session remained.
- `rtk git diff --check` exited 0. `rtk mdbook build docs` exited 0 with the
  existing missing root `CHANGELOG.md` include, `CREDITS.md` closing-tag, and
  large search-index warnings; raw log:
  `/tmp/smart-ai-mdbook-tailwind-config-final-20260722.log`.

Long GitHub Actions were not re-waited. The local checks, final19 mGBA Live
evidence, and feature documents are the handoff basis.

## Historical Completion Handoff - July 16, 2026

Status on July 16, 2026: accepted as an interim-complete Smart Gimmick AI
snapshot. No further tactical tuning is planned on this branch before the
Battle Team integration line is created.

- Runtime branch: `feature/smart-gimmick-ai-16-20260604`.
- Runtime code completion commit: `46ed9d403f`.
- Current-master documentation ancestry merge: `dd55f9a45d`.
- Review shelf: draft PR #72, targeting `master` but not approved for runtime
  merge under the repository's master policy.
- The old PR check failure contained four AI thinking-time ceilings and one
  pre-desperation Eiscue move expectation. The completion commit keeps the
  ordinary no-flag ceilings unchanged, gives bounded Smart/read-mode analysis
  approximately ten percent headroom over measured frame counts, and records
  the intended no-clean-line flinch fallback.
- Remaining full two-to-three-turn search, richer candidate traces, broad ally
  order planning, and longer recovery/chip simulation are accepted backlog for
  a later planner feature. They are not silently represented as complete.

Completion validation:

- `rtk make -j16 -O check`: passed after the five old PR failures were closed.
- Focused thinking-time filter: all six tests passed; no-flag singles and
  doubles ceilings remain unchanged.
- Focused Eiscue fallback regression: passed with the intentional Icicle Crash
  flinch line when no clean damage race remains.
- `rtk make -j16 -O all`: passed.
- `rtk make -j16 -O debug`: passed.
- `rtk mdbook build docs`: passed with the existing missing root
  `CHANGELOG.md`, `CREDITS.md` closing-tag, and large search-index warnings.
- mGBA Live session `smart-ai-completion-20260716` booted the rebuilt debug ROM,
  continued the copied save, rendered the complete Gauntlet Battles menu,
  started `Read Single`, advanced through a live first turn to forced party
  replacement, returned to the overworld, and stopped cleanly. Final managed
  session status was `[]`.

The implemented integration order is Box NPC Party Pool, Battle Team Boxes,
then Smart Gimmick AI on `integration/runtime-lab-20260716`. Box and Battle Team
entered through PRs #76 and #77. Smart is squash-reapplied on
`integration/runtime-lab-smart-ai-pr-20260716` at `611933abf0` for its own PR
and combined validation. See
[Runtime Lab Integration](../runtime_lab_integration/implementation.md). This
does not change the docs/Lua-only policy for `master`.

Runtime files:

- `include/constants/battle_ai.h`
- `src/battle_controller_player.c`
- `src/battle_ai_switch.c`
- `src/battle_ai_util.c`
- `src/battle_script_commands.c`
- `src/data/debug_trainers.party`
- `src/debug.c`
- `test/battle/ai/ai_switching.c`
- `test/battle/ai/ai_smart_gimmick.c`
- `test/battle/ai/ai_partner_synergy.c`
- `test/battle/exp.c`

Documentation:

- `docs/tutorials/ai_flags.md`
- `docs/features/smart_gimmick_ai/`

Tooling:

- `tools/runtime_knowledge/`
- `tools/mgba_live/battle_action_log_export.lua`
- `tools/mgba_live/start_mgba_live.sh`
- `tools/mgba_live/start_mgba_live.bat`
- `tools/mgba_live/export_battle_action_log.sh`
- `tools/mgba_live/export_battle_action_log.bat`

## Runtime Behavior

`DecideGimmickBeforeMoveSelection()` still handles the early smart-gimmick gate. `ReconsiderSmartGimmick()` then checks the chosen move and cancels the gimmick when the selected turn is not worth spending.

For Dynamax, `ShouldUseSmartDynamax()` now accepts these reasons:

- Last available Pokemon, or one-reserve late-commit pressure when the active Pokemon is already low on HP or under immediate KO threat.
- The current target can otherwise KO the AI Pokemon.
- A known or predicted opposing move threatens Dynamax-blocked disruption while the AI selected a damaging move:
  - Fake Out-style flinch pressure.
  - Roar / Whirlwind-style forced-switch pressure.
- Dynamax converts the selected move into a KO that the regular move would miss.
- The selected Max Move creates strategic board value:
  - Speed control through `Max Airstream` or `Max Strike`.
  - Weather control through Fire, Water, Rock, or Ice Max Moves.
  - Terrain control through Electric, Grass, Fairy, or Psychic Max Moves.
  - Side-wide stat boosts or drops from Fighting, Poison, Ground, Steel, Dragon, Bug, Ghost, and Dark Max Moves.
  - Residual G-Max pressure from `G-Max Vine Lash`, `G-Max Wildfire`, `G-Max Cannonade`, and `G-Max Volcalith`.

Weather and terrain decisions call `ShouldSetWeather()`, `ShouldSetFieldStatus()`, and `ShouldClearFieldStatus()` so smart Dynamax follows the same field-state opinions used by normal AI move scoring.

When the AI side already has Tailwind, `Max Airstream` is not treated as an independent smart-Dynamax payoff. This prevents a known Tailwind board from spending Dynamax only to duplicate existing Speed control when a stronger G-Max residual line is available.

The Dynamax policy is intentionally not "use the strongest attack immediately." The current runtime model treats Dynamax as a board-presence resource: HP scaling can keep an active Pokemon on the field, Max Move effects can change Speed, weather, terrain, Defense, Special Defense, Attack, Special Attack, or opponent-side pressure, and Dynamax can deny disruption such as flinching or forced switching. This matches the Pokemon Wiki mechanics summary for Dynamax's HP increase, 3-turn duration, flinch / forced-switch immunity, Max Guard, and Max Move side effects, the official Sword / Shield Max Move explanation, and Victory Road VGC reports where Dynamax is discussed through Fake Out denial, Max Airstream tempo, Max Steelspike / Max Quake defensive value, and status / positioning pressure.

For Mega / Ultra Burst, `ShouldUseSmartMega()` now looks at the target form before spending the gimmick. It allows immediate use for target-form ability payoffs such as trapping or weather control, estimated Speed flips, meaningful defensive improvement under KO pressure, meaningful attacking-stat improvement for the selected damaging move, and one-reserve low-HP late-commit pressure. It still delays setup turns when there is no immediate pressure, and it can preserve `Air Lock` / `Cloud Nine` during active weather if the target form has no stronger immediate payoff.

For Z-Moves, `ShouldUseSmartZMove()` now wraps the existing Z-Move viability checks. Status Z-Moves keep their tactical checks. Damaging Z-Moves are conserved unless the AI is on its last Pokemon, has one reserve left and the active Pokemon is low on HP or under KO threat, the Z-Move converts the selected move into a KO, improves the damage race under immediate KO or trap pressure, or protects a low-accuracy KO line.

`AI_FLAG_AGGRESSIVE_GIMMICK` adds a debug / gauntlet spend route for competitive pressure tests. With this flag, smart Dynamax and damaging smart Z-Move decisions can spend after the selected gimmick-boosted move crosses `AI_AGGRESSIVE_GIMMICK_MIN_DAMAGE_PERCENT`, then passes the `AI_AGGRESSIVE_GIMMICK_USE_CHANCE` roll. Smart Tera runs the same 85% pressure route before the normal future-candidate conservation roll when Tera gives at least `AI_AGGRESSIVE_TERA_MIN_DAMAGE_BOOST_PERCENT` offensive improvement and the target is not protected by a hard punishing move. The route still rejects status moves, invalid moves, zero damage, partner attacks, and very weak attacks, so it is more proactive without becoming "always press the gimmick."

`AI_FLAG_READ_PLAYER_MOVE` is intentionally separate from `AI_FLAG_OMNISCIENT`. `Omniscient` gives full knowledge of the player's moves, abilities, and held items, but ordinary prediction still guesses the current command by re-running AI logic from the player position. `Read Player Move` lets non-link trainer AI use confirmed player commands from the command buffer; it began as a debug / gauntlet escalation and is now applied at runtime to trainers that already carry AI flags. Opponent controllers wait until every live player-side command is ready, rebuild `gAiLogicData`, and recompute action / move choice immediately before returning their own command. Confirmed moves feed `GetPredictedMove()` / `GetIncomingMove()`, confirmed switches are applied as the predicted switch-in, and selected player defensive gimmicks are passed into damage simulation. This fixes double-battle reads such as Koraidon scoring into a player Miraidon that has already selected Electric Tera; the AI should not continue scoring Dragon Claw as if Miraidon stayed Dragon-type. Eligible 2v2 doubles now use the bounded shared planner described above; singles and unsupported doubles boards retain the score-layer evaluator.

`Read Player Move` also treats a confirmed single-target `Protect` / Max Guard as a hard warning for ordinary single-target damage. The mGBA autosave log `/tmp/manual-ai-log-captured-20260606.json` showed strong reads early in a 4v4 double battle, including early pressure into the player right slot, player Tera capture, and four resolved switch-ins, but turn 3 still had the opponent right slot use `Collision Course` into a player right `Protect`. That is a poor full-information line when another opposing slot is available. `AI_CheckBadMove()` now applies a read-mode-only penalty to non-protect-bypassing, non-spread, single-target damaging moves aimed at a player battler whose confirmed command is single-target protection. Spread moves can still keep their partner pressure, and Feint / protect-bypassing moves are not penalized.

The later manual session `manual-ai-log` with archived pid `222151` produced `/tmp/manual-ai-log-battle-action-log-autosave.json` and `/tmp/manual-ai-log-latest-20260606.json` from the same non-empty autosave snapshot. That log recorded 20 entries through turn 3, including 14 move actions, 6 switch actions, two selected Tera markers, and resolved switch-ins. The important loss-pattern finding was turn 0: player left selected `Fake Out` into opponent right, but opponent right still planned an ordinary `Draco Meteor`. `Read Player Move` now scores slower ordinary actions down when a confirmed or currently unknown-but-visible player `Fake Out` can flinch the AI battler. This is a soft pressure penalty, not a forced `Protect` rule. It uses the actual threat move in `AI_WhoStrikesFirst()`, so priority order still matters: Gen 5+ `Fake Out` outruns `Extreme Speed`, while higher-priority protection or Dynamax can avoid the discount. The flinch threat is also ignored when the target would block it through priority blockers such as `Armor Tail` / `Dazzling` / `Queenly Majesty`, Psychic Terrain, type immunity, Substitute, Covert Cloak, Shield Dust, or Inner Focus.

The manual Read Double feedback for pid `252732` could not be matched to a live mGBA Live session record after the session closed, but the latest non-empty autosave at `/tmp/manual-ai-log-battle-action-log-autosave.json` was generated at `2026-06-06T11:39:11Z` and contained the relevant battle shape: 55 entries through turn 10, with opponent-side actions showing 9 commanded switches, 5 `Trick Room` uses, and only 1 `Protect`. That confirmed the reported pattern: Trick Room was too easy to bait or mirror, and optional doubles switching could outrank a simple protect-and-hold-position line. `AI_CheckBadMove()` now applies a read / predicted `Trick Room` toggle penalty when an opposing battler is also choosing Trick Room, and `AI_PowerfulStatus()` no longer adds the powerful-status boost to Trick Room in that mirror-read situation. The existing `AI_FLAG_POWERFUL_STATUS` behavior still allows initial proactive Trick Room when no opponent-side Trick Room is being read, but it no longer blindly rewards a same-turn toggle accident.

The follow-up pid `278133` loss was analyzed from `/tmp/manual-ai-log-battle-action-log-autosave.json` after the live session had closed. The final relevant shape was turn 7: player left selected `MOVE_GRASSY_GLIDE` into opponent right, while opponent right Mega Gengar stayed in and chose `MOVE_FOCUS_BLAST`; opponent right was then absent from the following turn, matching the reported Grassy Glide KO. This was not primarily a Fake Out valuation problem. The AI already had the player command, but `ShouldSwitch()` built its incoming-threat context from `GetOppositeBattler()`, so the opponent right slot mostly evaluated the player right slot instead of the cross-slot player-left attack targeting it. `SetKnownIncomingMoveForSwitchContext()` now scans every confirmed player-side move under `AI_FLAG_READ_PLAYER_MOVE`, keeps only known single-target damaging moves whose selected target is the current AI battler, and stores the threatening battler plus move slot in `SwitchAiContext`. `ShouldSwitchIfKnownSingleTargetKO()` then runs before the doubles Protect-preservation guard, but it is not a "survive at any cost" rule: if the confirmed selected move OHKOs the active battler, the AI only hard switches when an eligible reserve can comfortably absorb the same move as a defensive pivot, currently no damage or more than `AI_DEFENSIVE_KO_THRESHOLD` hits to KO after entry damage, healing, weather, status, and item effects. If the best reserve merely survives while taking a punishing hit, the route falls through and the existing Protect-preservation / stay-in logic can still handle the turn. This keeps Incineroar switching into the known Grassy Glide line, while avoiding a forced Incineroar switch into a known High Horsepower-style punish. Among safe switch-ins, Fake Out-style first-turn moves are only a tie-breaker, so Incineroar is chosen because it safely absorbs the known move first, not because Fake Out hides the immediate KO risk.

The later pid `297174` autosave at `/tmp/manual-ai-log-battle-action-log-autosave.json` was generated at `2026-06-06T13:58:27Z` with 28 entries through turn 6. It was not the reported no-Mega / no-gimmick 4v4 because it records one selected `GIMMICK_DYNAMAX` and one selected `GIMMICK_TERA`. The log did show generally stronger play after the cross-slot work: early Tailwind plus Dynamax pressure, a Protect / switch reaction around turn 1, and later pivoting rather than pure tunnel attacks. The only suspicious visible line was turn 5 `Make It Rain` into the protected player-right slot; because the action-log target for spread moves is still one battler, this needs a fuller replay before treating it as a bad single-target read. Searching `/tmp` and the archived `manual-ai-log-*` session metadata did not find a complete no-Mega / no-gimmick 4v4 action log. The only gimmick-free `/tmp` candidate was `/tmp/battle-action-log-export-test.json`, but it contained only six turn-0 entries from an export smoke, not a full 4v4 match.

The pid `317741` autosave from live session `manual-ai-log` matched pid `317741`, was generated at `2026-06-06T14:42:11Z`, and contained 42 entries through turn 8. The visible pattern was not only raw damage or Fake Out timing: `Amoonguss` had `Rage Powder` and `Pollen Puff`, but the recorded turns showed `Spore`, offensive `Pollen Puff`, and `Protect` winning the move choice instead of intentional partner support. `Rage Powder` had no read-mode score for a confirmed single-target player attack aimed at the partner, and `Pollen Puff` ally healing was only a weak generic partner-target exception. `AI_DoubleBattle()` now scores ally `Pollen Puff` / `Heal Pulse` from partner HP, setup value, active gimmick state, and speed; `EFFECT_FOLLOW_ME` now gets an explicit read-player-move protection score when a confirmed single-target damaging move is targeting the partner and redirection is survivable or meaningfully absorbs damage. This lets `Rage Powder` beat `Spore` / attacks in the exact "we know the player is targeting my partner" case, while keeping redirection tactical instead of automatic.

The same pid `317741` feedback expanded doubles ally-benefit play beyond pure healing. Intentional partner attacks now score `Steam Engine` plus `Weakness Policy` as a strong combined payoff, score `Stamina` when the partner can cash the Defense boost through `Body Press` or is under physical pressure, keep the existing Beat Up / Justified / Rage Fist coverage, and allow guaranteed-critical ally hits into `Anger Point`. Partner-target `Psych Up` is no longer rejected as a bad target when copying ally boosts is useful. Field stat reset now prefers `Haze` when ally stat drops from moves such as `Draco Meteor` / `Overheat` outweigh the cost of clearing ally boosts or enemy drops. Ally `Clear Smog` is intentionally weaker: it is only a score fallback when Haze is unavailable and the ally's drops clearly outweigh its boosts, matching the manual note that black haze is common while ally Clear Smog is rare.

The pid `528022` feedback refined that ally-trigger rule. The archived session matched pid `528022` and the autosave held 38 entries through turn 7; the visible `Water Gun` entries targeted player-right rather than the ally, but the manual point exposed a real fixture / scoring issue: `Partner Double` had been using `Pelipper` `Water Gun` as the visible `Steam Engine` / `Weakness Policy` trigger, and the AI's partner-activation score treated any survivable super-effective ally hit too generously. Single-target partner activation now uses a tactical trigger score. Spread moves that also hit opponents still work, priority moves such as `Aqua Jet` are strong candidates, and non-priority moves can still be candidates when the actual ally damage is harmless. Unsafe non-priority 4x Water hits into `Coalossal` no longer receive the same ally-trigger reward. The debug `Partner Double` reserve pair now uses special-attacking `Primarina` with low-damage `Aqua Jet` beside `Coalossal`, which better matches the intended Steam Engine setup shape without making priority the only legal route.

The pid `549295` session matched archived `manual-ai-log-20260607-055044/session.json`; `/tmp/manual-ai-log-battle-action-log-autosave.json` was generated at `2026-06-07T05:50:03Z` with 23 entries through turn 4. The read layer was active: turn 1 showed `Amoonguss` choosing `Rage Powder` into selected pressure. The bad line was turn 3, where player-right selected Dynamax `Wood Hammer` into opponent-right `Primarina`, but `Primarina` chose slower `Sparkling Aria` instead of valuing `Protect` enough. Root cause: `Protect` is a self-target move, so the normal incoming-move score table was not reliably tied to the player battler that had selected the threatening move, and selected-target helpers could prefer stale controller buffer target data over the confirmed `gBattleStruct->moveTarget`. `ProtectChecks()` now scans all confirmed player-side move commands under `AI_FLAG_READ_PLAYER_MOVE`, verifies whether the selected move targets the protecting battler, ignores protect-bypassing threats, and adds a strong Protect payoff for known high-damage or KO pressure. The selected-target readers in move scoring and switch scoring now prefer confirmed `gBattleStruct->moveTarget` before falling back to controller buffers. Ordinary non-Protect actions also get a read-mode penalty when known earlier damage would KO the acting battler before that action resolves.

`BS_TryHealPulse()` was also tightened so `Pollen Puff` ally healing does not call the `EFFECT_HEAL_PULSE` move-property accessor used by `Floral Healing`. `EFFECT_HIT_ENEMY_HEAL_ALLY` now takes the normal 50% heal path instead of risking an assertion when the move has no Heal Pulse property argument.

`gBattleActionLog` is the first runtime storage layer for battle command audits. It is a 128-entry in-ROM ring buffer cleared at battle start. Once all battlers confirm actions, it records one command entry per live battler with action, move, item, target, move slot, party switch-in index, selected gimmick, AI decision reason, compact AI threat flags, relevant AI risk kind, short-horizon line flags, stable line family, fallback risk line family, loss clock, turn, and sequence. `Cmd_getswitchedmondata()` also records resolved switch-ins; if the engine receives an invalid switch-in index, it now corrects to an arbitrary valid switch-in without showing a debug assert screen and marks that entry as corrected. `AI_FLAG_READ_PLAYER_MOVE` uses the current confirmed command first, then normal prediction, then the action log's last selected move as a fallback for current-battle reads.

`tools/mgba_live/battle_action_log_export.lua` is the persistent export layer for the action and planner buffers. It resolves `gBattleActionLog`, `gBattleAiTraceLog`, and `gBattlerPositions` from the matching `pokeemerald.map`, reads the packed EWRAM records, attaches names and linkage metadata, and validates trace version 5 plus magic `0xA15C`. A compatible run writes `pokeemerald.battle_action_log.v5` with `ai_trace_header`, `ai_plans`, `ai_candidates`, and `ai_board_snapshots`; `ai_plans[].board_comparison` now includes field-level differences and a diagnostic that separates confirmed command-link mismatches from simulator/runtime state divergence. Battler `status1` values are exported with condition names and sleep counters. A missing or mismatched trace signature deliberately writes the v4 action-only schema. `export_battle_action_log.sh` and `export_battle_action_log.bat` are convenience wrappers for WSL / Linux and Windows; both let manual test sessions dump the current battle state without editing Lua paths by hand. The WSL / Linux wrapper can omit `SESSION` when a live active session exists, and reports a direct `start_mgba_live.sh manual-ai-log 120` hint when no session is running. The exporter also supports `BATTLE_ACTION_LOG_SKIP_EMPTY` for startup autosave, so empty post-battle buffers do not overwrite the last useful host JSON.

`tools/mgba_live/battle_action_log_autosave.lua` is a startup Lua script for mGBA Live. `start_mgba_live.sh` and `start_mgba_live.bat` load it by default and set `BATTLE_ACTION_LOG_OUT` to `/tmp/<session>-battle-action-log-autosave.json` on WSL / Linux, unless the caller overrides the path. It periodically calls the exporter and keeps the latest non-empty action log on the host. This does not recover a log after a session has already been closed without autosave; it prevents that loss on later sessions.

`start_mgba_live.sh` and `start_mgba_live.bat` are the matching session-start shortcuts. They default to `pokeemerald.gba`, `videoSync=1`, a 120 FPS target, and battle action log autosave, with optional `SESSION`, `FPS`, and `ROM` arguments for manual battle-log sessions.

Player move-selection now clears transient gimmick UI state before drawing a new battler's move menu. Upstream `PlayerHandleChooseMove()` cleared `gBattleStruct->gimmick.playerSelect` after `InitMoveSelectionsVarsAndStrings()`, so double battles could draw the second player battler's move names as Z-Move or Max Move options if the first player battler had just selected a gimmick. The command returned by the controller was usually correct after the later reset, but the first rendered move-menu contents could be stale until the cursor moved or the menu was redrawn. The reset now happens before the move-name / move-type / PP windows are populated, and `zmove.viewing` is also cleared on entry so a previous Z-Move detail view cannot influence the next battler's initial draw.

`DecideTerastalAgainstTarget()` also keeps an explicit copy of damage dealt with Tera. Earlier no-Tera evaluation can rewrite `gAiLogicData->simulatedDmg` for follow-up decisions, so later Tera reconsideration now restores the Tera damage values when the AI commits. Without that restoration, the Tera branch could appear worse than it actually was after a previous conservation pass.

For double battle switching, `ShouldSwitchIfDoublePositionBad()` adds a VGC-style positioning check under `AI_FLAG_SMART_SWITCHING`. The AI may hard switch when the active Pokemon has no meaningful pressure into either opposing slot, is threatened by either opposing slot, has enough HP to be worth preserving, and its partner cannot cover the position. The check stays out of `AI_FLAG_SEQUENCE_SWITCHING`, respects existing no-switch gates, avoids overriding the existing Intimidate-blocker contract, and allows double switches when both active Pokemon are pinned.

Double-battle switch execution validates the final party index before emitting `B_ACTION_SWITCH` or returning a post-KO `ChoosePokemon` result. This fixes a debug ASSERT path where the smart-switch fallback could finish with no valid candidate (`-1`, `PARTY_SIZE`, a fainted mon, an active partner's mon, or a partner-reserved mon) and then pass that invalid index into `Cmd_getswitchedmondata()` / `IsValidSwitchIn()`. If an optional mid-turn switch has no valid target after validation, the AI clears the switch request and falls back to a normal move action instead of crashing the battle script. Post-KO / forced controller selection now rechecks the advanced result and falls back to the first valid reserve. The final script command is also non-asserting now: an invalid switch-in reaching `Cmd_getswitchedmondata()` is corrected and logged instead of stopping the debug ROM on the blue assert screen.

For board-control switching, `ShouldSwitchIfBoardControlBenefit()` lets `AI_FLAG_SMART_SWITCHING` identify reserve Pokemon that can immediately or soon change the board. The selector can choose weather setters (`Drizzle`, `Drought`, `Sand Stream`, `Snow Warning`), terrain setters (`Electric Surge`, `Grassy Surge`, `Misty Surge`, `Psychic Surge`, `Hadron Engine`), speed-control setters carrying `Tailwind` or `Trick Room`, status-pressure support, status-prevention / cure support, terrain seed plans, and ability bridge support such as `Skill Swap`, `Role Play`, and `Entrainment`.

The board-control switch is deliberately narrower in singles than doubles. Singles still need bad odds, a bad matchup, missing current pressure, an unfavorable field, or an immediate status-absorption payoff to replace. Non-immediate status pressure / support pivots are double-battle only in this slice so the AI does not abandon a winning singles 1v1 only because the bench has a utility move. Doubles can pivot more proactively once the reserve candidate itself has a clear weather, terrain, Tailwind, Trick Room, status, seed, or ability-bridge payoff, matching the VGC positioning model where a bench Pokemon can create pressure instead of merely absorbing damage.

The Read Double log also tightened the doubles switching gates. Optional all-moves-bad, board-control, double-position, and bad-odds switches now respect a `Protect` preservation guard: if the active double battler has a usable Protect-style move, is under pressure, is not trying to escape status / Yawn / perish / Encore / similar volatile pressure, and the incoming threat does not ignore Protect, the AI can stay in and let move scoring select Protect instead of immediately pivoting. Delayed Tailwind / Trick Room support pivots are still legal, but in doubles they now need a real pressure case when the switch-in has no immediate entry effect. Immediate weather / terrain / status-benefit pivots and dedicated switch reasons such as trapping, absorbers, and state-clearing still keep their existing routes.

Status-aware board-control support includes:

- Non-volatile status pressure, secondary status effects, and related pressure moves (`Spore`, `Yawn`, `Toxic`, `Will-O-Wisp`, paralysis moves, freeze / frostbite effects, `Toxic Spikes`, `Leech Seed`, `Swagger`, and confusion pressure).
- Team status care with `Heal Bell` / `Aromatherapy`.
- Status prevention with `Safeguard` or `Misty Terrain` when the opposing side has known status pressure.
- Self-status / status-benefit lines for `Guts`, `Quick Feet`, `Marvel Scale`, `Magic Guard`, `Poison Heal`, `Toxic Boost`, `Flare Boost`, `Facade`, and `Psycho Shift`.
- Terrain seed activation when the reserve setter creates the matching field.
- Aurora Veil only when snow / hail is already active or the reserve ability can create it.
- Skill Swap-style bridge moves when the reserve or active partner has a board-control ability worth moving or copying.

AI runtime knowledge now has a shared move / ability / item interpretation layer in `battle_ai_util`. The layer maps existing move flags into AI categories such as sound, bullet, powder, slicing, punching, biting, pulse, dance, wind, healing, Magic Coat, Snatch, ability-control, move-denial, and combo-state. It also maps abilities into strategy groups such as move immunity, move power, damage race, status interaction, field control, positioning, ability control, stat control, item control, priority, and form / state. Held items are mapped through hold effects into damage race, defensive race, stat control, Speed / order control, recovery, status cure, self-status, field duration, contact / hit punishment, move-shape modifiers, ability protection, choice lock, positioning, and gimmick unlock. It also exposes `AI_CanBattlerIgnorePredictedMove()`, which bridges active abilities and held items for prediction decisions using Mold Breaker-sanitized ability checks and enabled-item checks. Predicted-Taunt switching now calls this shared predicate instead of maintaining a local Taunt-only copy.

`tools/runtime_knowledge` adds the first offline catalog generator for larger knowledge collection. It is a Rust tool with no third-party crates. The generator reads the local expansion source and writes review JSON for moves, abilities, hold effects, items, gimmick policy, and a summary. This gives later Pokemon Wiki, VGC / official tournament, Champions usage, PartyGen, and observed-history adapters a local runtime baseline instead of requiring one-off source audits for every AI heuristic.

Predicted-Taunt support is intentionally separate from "bad move" switching. `ShouldSwitchIfPredictedTauntPunish()` only runs when `AI_FLAG_SMART_SWITCHING` and `AI_FLAG_PREDICT_MOVE` are both active and the incoming move is predicted as `Taunt`. It requires the current Pokemon to depend on important status moves, rejects positions where the current Pokemon can already damage-race or 2HKO the target, and skips Pokemon protected by `Aroma Veil`, Gen 6+ `Oblivious`, or an enabled Gen 5+ `Mental Herb`. If those gates pass, the selector evaluates eligible reserves as free switch-ins and chooses a damaging attacker that can win the immediate 1v1 or cross the switch-in damage threshold. The goal is to model "pivot an attacker into a predicted Taunt" without making the AI flee only because Taunt would block a utility move.

Protect scoring now separates singles and doubles payoff. In singles, first-turn Protect is no longer rewarded just because a damaging move is predicted. `ShouldUseSinglesProtect()` asks whether Protect gains the AI a turn or a future option: residual damage on the target, incoming Wish recovery, Poison Heal / Leftovers / Black Sludge recovery, Substitute-threshold recovery, choice-lock scouting, Disable / Encore follow-up, Explosion-style avoidance, or target secondary damage. It also avoids passive Protect against an already boosted attacker unless there is a Wish or Disable / Encore line to justify the stall.

Consecutive Protect remains possible. The second Protect is penalized for its reduced success rate, but it is not treated as impossible. Singles can still value a second Protect when the same payoff remains, and doubles get a lighter second-Protect penalty so high-pressure VGC-style "protect again" turns stay available. Third and later consecutive Protect attempts remain heavily discouraged.

Future usage / ranking data should come from source-tagged competitive data such as Pokemon Battle DataBase, official event reports, Pokemon Home / Champions-style usage when available, local PartyGen catalogs, official VGC / PJCS videos, and observed battle history. Pokemon Showdown articles should not be used as strategy source material for this feature; at most, raw data or team examples can be inspected with a clear source tag and lower confidence.

The debug Party menu exposes focused runtime fixtures and weighted gauntlet battles:

- `Battle 3v3 Single` builds a level-50 player team of `Dragonite`, `Gholdengo`, and `Garchomp`, then pits it against a 3-Pokemon AI side selected from a 6-Pokemon weighted singles pool.
- `Battle 4v4 Double` builds a level-50 player team of `Incineroar`, `Rillaboom`, `Flutter Mane`, and `Urshifu-Rapid-Strike`, then pits it against a 4-Pokemon AI side selected from a 7-Pokemon weighted doubles pool.
- `Battle Dmax/Z Single` builds a level-50 player-side Z-Move team, then pits it against a smart AI Dynamax / Gigantamax / Z-Crystal team.
- `Battle Dmax/Z Double` builds a level-50 player-side VGC-style Z-Move team, then pits it against a smart AI Dynamax / Gigantamax / Z-Crystal doubles team with Tailwind / weather pressure.
- `Battle Gimmick Single` builds a passive player side and an opponent audit party with lead `Gengarite` Mega, reserve `Mimikium Z`, reserve Gigantamax `Charizard`, and reserve Normal Tera `Dragonite`. It grants all debug gimmick access to prove the opponent's source data and runtime gates can expose every gimmick.
- `Battle Gimmick Double` builds a passive double player side and an opponent audit party with active Gigantamax `Charizard` plus `Electrium Z` `Tapu Koko`, then reserve `Gengarite` Mega and Normal Tera `Dragonite`. It is an availability audit, separate from the smarter Dmax/Z timing fixtures.
- `Gauntlet Battles` opens a dedicated submenu for the generation-specific and mixed gauntlet fixtures. Keeping the gauntlets in a submenu avoids overflowing the debug menu's 20-item list limit.
- `Mega Single` / `Mega Double` build level-50 3v3 singles and 4v4 doubles battles with Mega-only access.
- `MegaZ Single` / `MegaZ Double` build level-50 3v3 singles and 4v4 doubles battles with Gen 7-style Mega + Z-Move access.
- `Dmax Single` / `Dmax Double` build level-50 3v3 singles and 4v4 doubles battles with Dynamax-only access.
- `Tera Single` / `Tera Double` build level-50 3v3 singles and 4v4 doubles battles with Tera-only access.
- `Mix Single` / `Mix Double` build level-50 3v3 singles and 4v4 doubles battles with all debug gimmick access and mixed Mega / Z-Move / Dynamax / Tera weighted pools on both sides.
- `Read Single` builds a level-50 3v3 singles battle with all debug gimmick access from mirrored 6-Pokemon weighted pools. This is the singles reset-style manual read audit: both sides can roll `Miraidon`, `Koraidon`, `Gengar`, `Dragonite`, `Rillaboom`, and `Tapu Koko`, so player and AI grade stay close while the AI's advantage comes from read-mode flags and known-player-party logic.
- `Read Double` builds a level-50 4v4 doubles battle with all debug gimmick access from mirrored 8-Pokemon weighted pools. This is the reset-style manual read audit: both sides can roll `Tornadus`, `Incineroar`, `Farigiraf`, `Rillaboom`, `Miraidon`, `Koraidon`, `Gengar`, and `Tapu Koko`, so player and AI grade stay close while the AI's advantage comes from read-mode flags and known-player-party logic.
- `Partner Double` builds a fixed level-50 4v4 doubles battle with all debug gimmick access for intentional ally-benefit checks. Player side leads `Miraidon` plus `Incineroar` with `Rillaboom` and Mega `Gengar` in reserve. AI side leads `Amoonguss` plus `Xerneas` with `Primarina` and `Coalossal` in reserve, so manual replays can inspect `Rage Powder`, `Pollen Puff`, and `Aqua Jet` into ally `Steam Engine` / `Weakness Policy` without relying on a random pool roll.
- `Partner Tech` builds a level-50 4v4 doubles battle with all debug gimmick access from focused production-style pools. The AI side can roll `Whimsicott` + `Contrary` `Lurantis` for ally `Charm`, `Grimmsnarl` + Lum Berry `Staraptor` for ally `Swagger`, `Froslass` + `Anger Point` `Tauros` for guaranteed-critical `Frost Breath`, `Primarina` + `Coalossal` for priority `Aqua Jet` activation, and `Amoonguss` for `Rage Powder` / ally `Pollen Puff` checks.
- `Champs Single` builds a level-50 3v3 singles battle with Mega-only debug access. The player side stays fixed and compact, while the AI side selects 3 Pokemon from a 16-candidate Champion pool using `POOL_PRUNE_OPPONENT_ADAPTIVE`.
- `Champs Double` builds a level-50 4v4 doubles battle with Mega-only debug access. The player side stays fixed, while the AI side selects 4 Pokemon from a 17-candidate Champion pool using `POOL_PRUNE_OPPONENT_ADAPTIVE`.
- `Elite Single` builds a level-50 3v3 singles battle with no gimmick access. The AI side selects 3 Pokemon from a 17-candidate Elite pool using `POOL_PRUNE_OPPONENT_ADAPTIVE`.
- `Elite Double` builds a level-50 4v4 doubles battle with no gimmick access. The AI side selects 4 Pokemon from a 17-candidate Elite pool using `POOL_PRUNE_OPPONENT_ADAPTIVE`.

These AI fixtures enable `Smart Trainer`, `Prediction`, `Smart Gimmick`, `Know Opponent Party`, and `Powerful Status` AI flags. The gauntlet AI fixtures also include `Smart Switching`, `Smart Mon Choices`, explicit `Omniscient`, `Aggressive Gimmick`, and `Read Player Move` so the AI can test full-information move reads, stronger gimmick usage, and known player party species together. The pool entries carry `Pool Weight` plus role tags such as `Lead`, `Ace`, and `Support`, so the pool-based fixtures also exercise Trainer Party Pool role filtering and weighted selection.

The gauntlet battles are intended to be same-grade or near-same-grade matches rather than raw-stat stomps. Many player and AI sides are generated from weighted local pools on each start, and the player pools now include comparable restricted legends, mythicals, strong support, and high-value gimmick anchors for the selected format. Champion / Elite AI pools additionally adapt the tactic tag to the already-built player party: `Tag6` for fast pressure, `Tag7` for weather / legendary pressure, and `Tag8` for disruption / slow support counterplay. The AI's intended edge comes from its decision quality checks: it has `Omniscient` and `Know Opponent Party`, so the manual test target is whether full-information prediction, switching, and gimmick timing can beat a visible player team of roughly similar power. With `B_POOL_SETTING_CONSISTENT_RNG == FALSE`, repeated debug starts can produce different player and opponent teams. Mega and Mega+Z pools use `Lead` / `Ace` tags so the selected party reliably contains the intended Z or Mega slot while still changing the supporting Pokemon.

Elite Single and Champion Single pools are singles pools, even when they share role themes with doubles. Their Tag8 support members now avoid doubles-only utility such as `Helping Hand`, `Rage Powder`, ally `Pollen Puff`, and routine `Protect`, so a singles NPC does not roll a support answer that wastes turns. The adaptive Tag8 trigger also recognizes player-side disruption and sustain pressure such as `Spore` and `Recover`, matching the fixed Elite Single player party rather than only detecting explicit setup moves.

`AI_FLAG_READ_PLAYER_MOVE` now narrows singles switching damage checks to the confirmed selected player move when a player command is available. This prevents a singles NPC from switching only because the visible player set contains an unselected coverage move, while still allowing a switch when the selected single-target attack would actually KO the active Pokemon and a real defensive switch-in exists. The Elite Single trainer already carried `Read Player Move`; the bug was in the switching evaluation using broader coverage information than the selected command.

The read-player-move policy now applies to non-link NPC trainer AI at runtime instead of being copied into every `AI:` line in `trainers.party`. `GetAiFlags()` adds `AI_FLAG_READ_PLAYER_MOVE` after loading a trainer's existing flags when the battle is a non-recorded, non-link trainer battle and the trainer already has AI behavior. This covers Elite Four, Champion, ordinary NPCs, Frontier-style defaults, Trainer Hill, in-game partners, and debug trainer battles without creating a large trainer-data diff or duplicating partygen's ownership of generated Champions Challenge fragments. Trainers with no AI flags stay unchanged, and recorded / link battles keep their explicit flags.

Tera-only singles also use the selected-command switch guard before fleeing a known single-target KO. If the active Pokemon can Terastallize before the selected hit lands, maximum-roll damage after Tera no longer KOs, and the Tera form still has meaningful immediate damage, the AI stays in and lets the smart Tera logic spend the gimmick instead of cycling through reserves. If the selected hit can still KO after Tera at maximum roll, switching remains allowed; that covers the "random roll still kills through Tera" case as acceptable risk management rather than forcing a doomed Tera.

Near-term desperation risk is now separate from ordinary aggressive play. `AI_BuildBoardSnapshot()` records the active slot, partner, reserves, perish / trap state, selected read pressure, setup pressure, side speed-control mode, and reserve-entry potential, then `AI_ClassifyBoardThreat()` and `AI_EvaluateShortHorizon()` label the board before risky lines are considered. `AI_RiskGovernorAllows()` owns the shared risk policy for low-accuracy status / damage, secondary hax, OHKO fishing, delayed attacks, partner sacrifice, second Protect, and switch-survival risk. It treats a position as desperate only when the active AI battler is facing a three-turn-or-faster battle-level loss clock, not merely low HP: trapped or no-reserve Perish Song, a confirmed read Perish Song that the AI cannot switch out of, known same-turn KO with no reserve or visible opposing setup pressure, mode loss, or an opposing setup / damage clock that can finish the AI within `DESPERATION_NEAR_TERM_KO_TURNS`. Clean non-OHKO damage-race lines suppress hax. If no clean line exists, the AI can score low-accuracy status or damage swings, flinch chance, paralysis / freeze secondary effects, high-crit or OHKO fishing, delayed attacks when the payoff is large enough, and in doubles direct board stops such as Fake Out-style flinch, Taunt into a known status move, Tailwind, Trick Room, speed-dropping pressure, and Throat Chop against known sound moves. This is the "nearly lost, play to the remaining outs" layer, not a default preference for unreliable moves.

The short-horizon classifier is deliberately battle-level in doubles. A single active slot being KOed is not automatically desperation if the AI still has a live partner and reserves; the board must also show setup, side-collapse, Perish / trap pressure, reserve-entry payoff, or similar two-to-three-turn collapse. This prevents the AI from choosing weak freeze / flinch fishing over clean read-mode pressure just because one slot is exposed.

Double-battle spread friendly fire also has a desperation exception. A spread move that would normally be rejected for KOing a partner can become legal when the AI is under near-term pressure, has reserves to bring in afterward, and the spread damage actually KOs or heavily pressures the opposing side. This covers the deliberate "retire a role-complete support Pokemon to reset the next board" shape without making arbitrary ally KOs acceptable.

Perish Song and sound pressure now have a first switch-vs-move bridge. `ShouldSwitch()` can look at confirmed / predicted Perish Song and prefer a Soundproof reserve when the current active cannot stop the line with a clean faster KO. If switching is available and the board is not otherwise collapsing, Perish Song alone does not unlock flinch fishing. Broader sound-pressure routing and ally-order manipulation remain outside this slice.

Commander targeting is now handled as a mechanics exception. A swallowed `Commander` Tatsugiri is treated as an invalid direct target for ordinary moves, so the AI points attacks at Dondozo's slot instead; `Transform` remains the explicit exception. KOing Commander Dondozo gets a small extra score because the swallowed Tatsugiri returns after Dondozo faints, and that bonus increases if Tatsugiri is already poisoned / burned or is holding Toxic Orb / Flame Orb. This follows the Pokemon Wiki Commander behavior: swallowed Tatsugiri is semi-invulnerable to direct targeting, spread damage into Dondozo still uses spread damage rules, status and held-item effects continue while swallowed, Toxic Orb can activate there, and Dondozo fainting releases Tatsugiri.

The mixed gauntlets are intentionally noisier than the generation-specific fixtures. They grant `GIMMICK_ACCESS_ALL` and mix strong Tera attackers, explicit Z-Crystal users, Mega Stone users, Gigantamax users, support leads, and restricted legends in the same weighted pool. Use these when the manual question is "does the AI still read and sequence correctly when both teams may roll several gimmick families at once?"

`Read Double` is intentionally smaller and cleaner than `Mix Double`. It keeps the same 8-Pokemon pool on both sides, with lead / ace tags and weights, so repeated debug starts can test whether the AI is actually winning through command reads, target selection, switching, and gimmick timing rather than through a larger or stronger opponent roster.

`Read Single` is the singles counterpart to `Read Double`. It keeps a mirrored 6-Pokemon pool with Choice attackers, priority / terrain pressure, Mega, Z-Move, Dynamax / Gigantamax, and Tera candidates, so repeated starts can test whether the AI can read selected single-target pressure, switch timing, setup turns, and late gimmick spend without relying on doubles-only spread pressure.

`Partner Double` is a targeted manual replay route for the ally-benefit mechanics that are awkward to force from a weighted pool. It keeps production-style level-50 sets, items, abilities, natures, IVs, EVs, and doubles roles, but fixes the four Pokemon on each side so the first turns expose the support / setup board immediately.

`Partner Tech` is the broader teammate-targeting route requested after the fixed `Partner Double` checks. It keeps the battle same-grade and no-legendary, but gives the AI multiple tactical ally lines instead of a single scripted board: useful Contrary stat reversal, Lum-guarded `Swagger`, guaranteed-critical ally hits for `Anger Point`, low-damage priority activation, and ordinary redirection / heal support. The intent is that ally-target moves can appear when the payoff is concrete, not because arbitrary friendly fire survived a damage check.

`Champs Double` is the non-restricted follow-up route for manual read-quality testing when restricted legends and multi-gimmick formats are making the power level noisy. It grants only `GIMMICK_ACCESS_MEGA_RING`: the player side has Mega `Dragonite` support pressure, while the AI side has Mega `Lucario` plus non-legendary doubles support and spread-pressure pieces. This fixture is meant to answer whether the AI can win through confirmed player-command reads, target selection, Choice-role pivots, and Mega timing without relying on restricted legendary stat lines.

`Champs Single` is the singles counterpart to `Champs Double`. It removes restricted legends and mixed gimmicks while keeping a first-turn Mega Dragonite versus Mega Lucario-style setup race, so single-battle read quality can be checked without the doubles-specific partner and spread-pressure layers.

Gimmick access is split from AI timing:

- `B_FLAG_GIMMICK_ACCESS_ALL` grants Mega Ring, Z-Power Ring, Dynamax Band, and charged Tera Orb access for a special ruleset.
- `B_FLAG_GIMMICK_ACCESS_MEGA_RING`, `B_FLAG_GIMMICK_ACCESS_Z_POWER_RING`, `B_FLAG_GIMMICK_ACCESS_DYNAMAX_BAND`, and `B_FLAG_GIMMICK_ACCESS_TERA_ORB` allow individual formats such as Mega-only, Tera-only, or Z + Dynamax.
- `HasGimmickAccess()` centralizes Bag item checks and runtime/debug access overrides. Mega, Z, Ultra Burst, Dynamax, and Tera now read the same access layer.
- Debug Party Dmax/Z fixtures pass `GIMMICK_ACCESS_Z_POWER_RING | GIMMICK_ACCESS_DYNAMAX_BAND` through `gDebugGimmickAccessFlags` instead of using an AI flag as a ruleset unlock.
- `.party` supports `Z Move: Yes`, stored as `TrainerMon.shouldUseZMove` and copied into `gBattleStruct->opponentMonCanZMove` for opponent parties. Ordinary Z-Move availability still requires a matching Z-Crystal and Z-Power access.
- The earlier itemless Z-Move unlock was removed so AI flags remain tactical timing / ruleset-intent hints, not replacement access items.

Opponent gimmick use is not disabled. AI-side Mega / Z-Move / Dynamax / Tera availability still comes from trainer party data, held items, and the battle gimmick gates, and the focused AI tests confirm that the opponent can spend those gimmicks. The earlier `Battle Dmax/Z Single` and `Battle Dmax/Z Double` fixtures did not include an AI Mega candidate, and their Z-Move users were not both lead-visible, so they were poor "is the opponent allowed to use every gimmick?" audits. The new `Battle Gimmick Single` and `Battle Gimmick Double` fixtures are explicit availability audits. Under `AI_FLAG_SMART_GIMMICK_TIMING`, trainer gimmick data remains permission rather than an immediate command: Dynamax, Z-Move, Mega, and Tera can be conserved until the selected turn has KO, board-control, defensive, setup, late-commit, or last-Pokemon value.

AI-side debug availability does not require giving the opponent Bag key items such as Mega Ring, Z-Power Ring, Dynamax Band, or Tera Orb. Player-side access still uses those Bag items or `B_FLAG_GIMMICK_ACCESS_*` / `gDebugGimmickAccessFlags`, but opponent-side use is driven by the trainer-party fields: Mega Stone for Mega, Z-Crystal plus `Z Move: Yes` for Z-Move, `Dynamax Level` plus `Dynamax: Yes` for Dynamax / Gigantamax, and `Tera Type` for Tera. A debug-battle-only bug dropped the opponent `opponentMonCanDynamax`, `opponentMonCanZMove`, and `opponentMonCanTera` bits because the Party debug menu creates the opponent party before `gBattleStruct` is allocated, then `CB2_InitBattle()` zero-initializes the battle struct. The fix stores those opponent gimmick bits as pending data when `CreateNPCTrainerPartyFromTrainer()` runs before allocation and restores them immediately after `AllocateBattleResources()`. Normal trainer battles still set the bits directly during the normal battle-init party creation path.

`AssignUsableGimmicks()` still exposes one active gimmick candidate per battler in enum order. If one Pokemon is made eligible for multiple gimmicks, the earlier candidate wins: Mega / Ultra Burst before Z-Move, then Dynamax, then Tera. Availability audit parties and gauntlet pools should therefore keep Mega, Z-Move, Dynamax, and Tera candidates on separate Pokemon unless a test intentionally covers that priority rule.

Double-battle move comparison now treats 100% accurate moves and accuracy-bypassing moves as neutral for accuracy priority. This prevents rain-accurate `Thunder` from beating a 100% accurate move solely because the internal accuracy value is `101`. The same comparison pass now gives a small board-pressure preference to `TARGET_BOTH` spread damage when it can meaningfully pressure the other opposing battler too. This covers the Kyogre / rain case where `Water Spout` or `Origin Pulse` should remain a strong doubles line instead of tunneling `Thunder` into one target only.

Debug battles now skip `BattleTypeAllowsExp()` through `gIsDebugBattle`. This prevents the EXP bar, normal EXP gain, and EV gain in debug battles, including the level-100 EV-gain path that would otherwise still occur when EXP is disabled by level.

Battle ability popups are now guarded against duplicate same-battler instances. The popup renderer formats nicknames with the same nickname terminator handling used by normal battle text, and `CreateAbilityPopUp()` refreshes an existing popup for that battler instead of creating a second pair of sprites that shares the same tile tag and active flag. Mega Evolution, Wish Mega Evolution, Primal Reversion, Ultra Burst, Terastallization, and generic form-change entry scripts also close any active ability popup before starting their form-change message / animation. The normal ability popup script keeps the upstream `showabilitypopup` timing, including Fairy Aura / Dark Aura style switch-in popups, while palette ownership for the shared `TAG_ABILITY_POP_UP` palette is now centralized across Ability Popup, Last Used Ball Window, and Move Info Window. This prevents stale switch-in ability banners such as `Mewtwo's Pressure` from lingering over a Mega Evolution message, and avoids old / new popup sprite overlap or palette reuse that can leave black / wrong-color OBJ rectangles after Delta Stream / strong-winds state changes or when a move-info sprite closes during a switch-in ability popup.

The pid `354725` action log showed a concrete target-validity failure rather than a lack of read-player information: sequence 26 on turn 5 had opponent-right `Amoonguss` choose `MOVE_POLLEN_PUFF` into `B_POSITION_OPPONENT_RIGHT` itself, while Pollen Puff can damage enemies or heal allies but cannot target the user. The loss pattern was that this wasted the opponent-right action on the same turn the player-left slot had selected `Protect` and the player-right slot attacked with `Dazzling Gleam`, leaving the AI side to absorb pressure without a valid support action. `CanTargetBattler()` now rejects self-targeting for moves whose target class is not self-compatible, double-battle move comparison no longer lets move slot 0 bypass target validity, and `OpponentHandleChooseMove()` revalidates the final AI target before emitting the battle-controller command. If a healing / ally-benefit target is invalid, the controller fallback tries the partner first, then a live opposing slot, instead of sending an impossible self-target.

The follow-up teammate-targeting guard makes `Contrary` ally scoring require a real beneficial reversed stat change. `AI_CanAnyStatChange()` now adjusts stat stages against the target battler's ability instead of the user's ability, so moves such as ally `Charm` into a `Contrary` partner are scored only when the target can actually gain a useful stage. This prevents "any stat drop into Contrary" from becoming a blanket friendly-fire reward, while still allowing production patterns like Prankster `Charm` into a physical `Contrary` attacker.

The pid `368099` action log exposed a different read-mode damage-race gap. On turn 5, opponent-left `Kyogre` selected `MOVE_WATER_SPOUT` while player-right had a confirmed faster `MOVE_DRAGON_ASCENT` targeting that Kyogre. The AI scored Water Spout from Kyogre's current full HP, but if Rayquaza moved first the move's effective power would collapse before Kyogre attacked. `AI_CheckBadMove()` now applies a read-mode / known-partner penalty to HP-dependent damaging moves when a known earlier damaging move will reduce the relevant HP before the AI move resolves. For `EFFECT_POWER_BASED_ON_USER_HP` moves such as `Water Spout` / `Eruption`, the relevant HP is the AI user's HP. For `EFFECT_POWER_BASED_ON_TARGET_HP` moves such as `Wring Out` / `Crush Grip`, the relevant HP is the selected target's HP. The helper respects move target classes, spread moves, target validity, priority / Speed order, and existing AI damage rolls, so full-HP Water Spout remains valuable when Kyogre can actually fire it before being damaged.

The pid `493358` manual feedback highlighted the opposite Kyogre failure mode: opponent-left `Kyogre` correctly read an Incineroar-to-Amoonguss switch and considered `Ice Beam`, but it underweighted the nearby Geomancy-boosted `Xerneas` beside that switch. The immediate single-target read was plausible, especially because Xerneas had selected `Protect`, but Choice Specs `Water Spout` or `Origin Pulse` preserved much stronger board pressure: even if the first spread hit is partly covered by Protect, the next Water Spout turn can still hit both opposing slots, while a locked single-target Ice Beam only pressures one slot per turn. `AI_CheckViability()` now gives every damaging doubles candidate a lightweight future board-pressure score. The score compares selected-target single-hit pressure against safe opposing-side spread damage summed across both opposing battlers, and Choice / Gorilla Tactics users scale that comparison across a three-turn horizon because the selected move constrains follow-up turns. Boosted opposing slots still get explicit pressure recognition, but the new damage-total axis means the AI can prefer "two strong spread turns" over a narrow switch read even when the switch read itself is legal.

The pid `507917` autosave matched live session `manual-ai-log` and showed the next precision gap. Turn 3 had opponent-left `Tornadus` choose `MOVE_TAUNT` into player-left `Incineroar`; because Tornadus has `Prankster` and Incineroar is Dark-type, that target is not a valid payoff in Gen 7+ rules. The AI was checking whether the target had status moves, but it was not applying the same Prankster-Dark immunity layer used by the battle engine before rewarding Taunt. `AI_CheckBadMove()` and `AI_CalcMoveEffectScore()` now reject / withhold reward from Prankster status moves into Dark-type opposing targets, alongside the existing Magic Bounce / Mental Herb / Oblivious-style ignore checks. The same log also showed why Kyogre should not be treated as expendable or low-value when Tornadus has poor Incineroar damage: the future-pressure score now includes a bounded partner-coverage gap reward when the AI has reliable future context, including confirmed `AI_FLAG_READ_PLAYER_MOVE`, `AI_FLAG_OMNISCIENT`, move prediction, known opponent party, or assumption flags. A damaging candidate that meaningfully pressures a target the partner cannot damage, such as Kyogre's rain water pressure into Incineroar, receives extra value in both read-debug and normal predictive contexts without polluting ordinary low-information support-move tests.

The pid `565092` action log matched archived session `manual-ai-log-20260607-064700/session.json`; `/tmp/manual-ai-log-battle-action-log-autosave.json` was generated at `2026-06-07T06:44:51Z` with 58 entries through turn 12. The visible problem was not that the AI lacked the player's commands: opponent-right switched on turn 0, then used `MOVE_ICE_BEAM` nine consecutive times through turn 9 before finally switching. Several later turns showed the player mostly pressuring the opposite slot or using spread pressure, while the Choice-locked slot kept spending a low-impact single-target line. `ShouldSwitchIfReadChoiceRoleDone()` now adds a read-mode doubles pivot for Choice / Gorilla Tactics locks. It only runs when all live player commands are confirmed, the active AI Pokemon is already locked into a move, known player damage into that active slot is zero, and the locked move no longer has meaningful progress through a 2HKO or relevant spread-damage threshold. It then simulates eligible reserves and rejects switch-ins that would be punished by the confirmed player move set, including spread moves such as Earthquake hitting the incoming reserve even when the current active Pokemon is immune. This models the manual point that deliberately staying in can still be correct when the switch-in is too frail, while still allowing a role-done Choice attacker to pivot when it is clearly being ignored. The follow-up cushion guard is now explicit: if the confirmed player turn is already spending damage into the role-done locked Pokemon, the AI leaves that Pokemon available to absorb the hit instead of exposing the next reserve.

PJCS2026 and other VGC video reviews are accepted as future analysis sources for this switching layer. The official PJCS2026 streaming page lists the game-division YouTube Live pages for 6 June 2026 and 7 June 2026, and Victory Road records the event as the 2026 Japan Championships for `Pokemon Champions`, with a Best-of-1 Swiss plus Best-of-3 top-cut structure and live broadcast by TPC. These sources should be used to tag observed turn shapes such as role-done Pokemon staying as cushions, deliberate sacrifice turns, and delayed reserve entry before changing runtime weights: https://www.pokemon.co.jp/ex/pjcs/2026/news/260522_03/ and https://victoryroad.pro/2026-japan/

The pid `590887` live session matched `/home/jastin/.mgba-live-mcp/runtime/sessions/manual-ai-log/session.json`; `/tmp/manual-ai-log-battle-action-log-autosave.json` was generated at `2026-06-07T07:53:05Z` with 41 entries through turn 9. The biggest concrete read loss was turn 0 in the new `Champs Double` shape: player-left selected Mega `Dragon Dance`, player-right selected `Protect`, opponent-left selected Mega `Flash Cannon` into player-right's selected `Protect`, and opponent-right selected `Protect` despite no confirmed damaging move targeting it. This was not a party-power issue; it was a read-mode scoring gap. The selected-Protect single-target damage penalty is now large enough to force a retarget away from the protected slot, and `ProtectChecks()` now gives read-mode doubles Protect no passive bump when confirmed player commands show no protectable incoming damage. Low but real incoming damage still gets a weak Protect score, while high damage and KO pressure keep the stronger Protect payoff.

The pid `590887` follow-up audit used the active `manual-ai-log` session metadata at `/home/jastin/.mgba-live-mcp/runtime/sessions/manual-ai-log/session.json`, which had current process pid `597829`; `/tmp/manual-ai-log-battle-action-log-autosave.json` was generated at `2026-06-07T09:21:32Z` with 43 entries through turn 9. This run no longer had player-right `Protect` on turn 0: player-left selected Mega `Dragon Dance`, player-right selected `Moonblast` into opponent-left, opponent-left still failed to treat the known `Dragon Dance` as the highest future threat, and later Aegislash / Protect lines spent too many turns on `King's Shield` / `Protect` while the boosted Mega Dragonite kept attacking. The fix adds an offensive setup read axis. `IsOffensiveStatRaisingMove()` identifies moves that raise Attack, Special Attack, or Speed; read mode treats a selected self-target setup user as a future-pressure threat before the boost has resolved; damaging moves that can meaningfully hit that setup user gain score; narrow single-target damage into the other slot is discounted when the same move could pressure the setup threat; `ProtectChecks()` discounts a Protect that only shelters one slot while the partner-side player Pokemon is known to be boosting; and Aegislash's Stance Change `King's Shield` bonus no longer overrides that setup-threat read.

The later `Champs Single` action log `/tmp/more-debug-battles-20260630-champs-single-turn1.json` exposed the same idea in a stricter 1v1 shape. The player-side Mega Dragonite selected `MOVE_DRAGON_DANCE`, but opponent Mega Lucario still selected self-target `MOVE_NASTY_PLOT` instead of immediately attacking. The doubles setup-pressure scoring above rewarded damage into a known setup threat, but 1v1 bad-move scoring still allowed self offensive setup against an already selected opposing setup move. `ShouldAvoidSelfSetupIntoReadPlayerSetup()` now runs only in `AI_FLAG_READ_PLAYER_MOVE` 1v1 contexts, checks that the AI move is self-target offensive setup, confirms the player selected an offensive setup threat, and penalizes the AI setup move when the AI has meaningful direct damage into that target. This keeps ordinary proactive setup available outside read-mode pressure while making Mega Lucario attack selected `Dragon Dance` with `Flash Cannon`.

The pid `608191` autosave matched the active `manual-ai-log` session metadata for process pid `608191`; `/tmp/manual-ai-log-battle-action-log-autosave.json` was generated at `2026-06-07T09:59:58Z` with 32 entries through turn 6. The visible reversal started on turn 0: player-left selected `Protect`, player-right selected `Geomancy`, but opponent-right `Amoonguss` chose offensive `Pollen Puff` into Xerneas instead of using a real disruption line. The root cause was overbroad Encore fear: the AI saw Whimsicott had `Encore` somewhere on the player side and suppressed Encore-vulnerable support scoring even though Whimsicott had not selected Encore that turn. Read mode now only applies that "do not get baited into Encore" suppression when a selected player `Encore` actually targets the acting AI battler. Selected offensive setup targets also receive a disruption score for valid sleep, Taunt, slower Haze, and slower Clear Smog lines, so `Spore` can beat weak chip damage into a selected `Geomancy` user when it is legal.

The same pid `608191` log later showed player-left selecting `Rage Powder` while opponent single-target pressure was still aimed at the partner slot. Read mode now penalizes redirectable single-target moves aimed at a player battler whose partner has selected `Follow Me` / `Rage Powder`. This is not a blind redirect rule: it skips spread moves, self / ally targets, moves that prevent redirection such as Stalwart / Propeller Tail / Snipe Shot-style effects, and Rage Powder lines where the attacker is immune to powder redirection through Grass typing, Overcoat, or Safety Goggles. That keeps Grass-type `Amoonguss` free to ignore opposing Rage Powder when the battle engine would allow it, while making ordinary Mewtwo-style single-target pressure avoid a known redirected target.

Read-mode current-command helpers now also accept a current player move from the controller command buffer or the same-turn battle action log when `gChosenActionByBattler` has already returned to `B_ACTION_NONE`. This keeps score checks and late read helpers aligned with the command that was actually selected without falling back to stale prior-turn moves.

The 2026-06-30 `Read Double` mGBA recheck exposed a related all-gimmick timing gap. The player-selected commands are known before the opponent recomputes, but smart gimmick timing still mostly evaluated the currently selected opposing target rather than "can any confirmed player move remove this AI slot right now?" The read helpers now derive selected player moves from the live command buffer first, calculate known selected damage into each AI battler, and compare survival with and without the AI-side gimmick. Smart Mega, damaging Z-Move, Dynamax, and Tera timing can now spend when the gimmick prevents a confirmed same-turn KO or when that known KO pressure makes the turn a valid late-commit / damage-race turn. `GetSmartGimmickTarget()` also points the gimmick damage calc at the known KO-pressure source, so the AI is less likely to spend into the wrong slot just because the initial scoring target was elsewhere.

The 2026-07-04 manual watch session `manual-ai-watch-20260704` wrote `/tmp/manual-ai-watch-20260704-battle-action-log-autosave.json` with 44 entries through turn 9. The confirmed-command log records selected targets before move redirection, so turns 7 through 9 exposed a real ally-target bug: opponent-left `Coalossal` selected `MOVE_BODY_PRESS` into opponent-right `Amoonguss` on sequences 36 and 40, then selected `MOVE_EARTH_POWER` into the same ally on sequence 43. That was not a legal Rage Powder / Follow Me interaction. Pokemon Wiki documents `Rage Powder` / `Follow Me` as drawing opposing single-target moves toward the user, and its `ちゅうもくのまと` specification explicitly says same-side ally moves keep their original target. `AI_DoubleBattle()` now treats a single-target move into the partner as valid only when the partner-target block actually finds a payoff, such as healing, a useful ability / item trigger, ally stat utility, `Psych Up`, `Haze` / `Clear Smog`, or another explicit partner benefit. Ordinary non-`AI_FLAG_ATTACKS_PARTNER` single-target damage into an ally now receives a heavy score penalty when no payoff is found. Spread moves that merely include the ally remain on the older friendly-fire / payoff path, so Earthquake-style doubles pressure is not blanket-banned.

The same log also confirmed that the debug trainers used for manual audit should all carry `Read Player Move`; older `DEBUG_TRAINER_AI`, singles / doubles 3v3 / 4v4, Dmax / Z, and gimmick-audit routes still had weaker AI flag strings. `src/data/debug_trainers.party` now adds `Read Player Move` to those debug AI trainers so future manual captures exercise the same full-information route that the branch is trying to audit.

The same 2026-07-04 log also exposed a Max Guard-only spend: sequence 6 on turn 1 had opponent-left, the slot that had just used `Rage Powder`, select `GIMMICK_DYNAMAX` with `MOVE_PROTECT` while opponent-right also selected `Protect`. The older `/tmp/read-gimmick-pressure-20260630-turn1.json` audit showed the same class of mistake with `GIMMICK_DYNAMAX` plus `MOVE_TAILWIND`. That is a poor use of a limited Dynamax resource when the chosen move is not being converted into a damaging Max Move or a board-control Max Move. `ShouldUseSmartDynamax()` now rejects smart Dynamax timing when the selected move is missing or status-class, including `Protect` and `Tailwind`. The existing routes still allow Dynamax for damaging moves that block Fake Out / phazing disruption, answer confirmed read-mode KO pressure, improve a KO, or create Max Move board value.

The follow-up `manual-ai-watch-20260704b` Dmax/Z doubles log showed opponent Charizard selecting `GIMMICK_DYNAMAX` with `MOVE_AIR_SLASH` after Whimsicott had already established Tailwind. That line duplicated Speed control instead of using G-Max Charizard's residual pressure. `DoesDynamaxOfferStrategicMaxMovePayoff()` now suppresses redundant `Max Airstream` payoff under existing or partner-selected Tailwind and treats residual G-Max moves as strategic payoff when the opponent side can be pressured across future turns.

The older `AI_FLAG_ATTACKS_PARTNER is willing to kill either the partner or the player 4/6` broad probe was not a runtime target bug. Inspecting the scores showed the single-target `Mighty Cleave` still preferred the weak opposing slot, but the final move was `Overdrive`, which also took the weak-slot KO while adding spread damage into the other opponent. That matches the doubles spread-pressure policy. The legacy expectation now accepts `Overdrive` in that branch, and a separate production-style Iron Hands / Rillaboom / Iron Boulder / Garganacl regression preserves the single-target guarantee that, when spread pressure is unavailable, `Mighty Cleave` targets the low-HP foe rather than a healthy foe or ally.

Tournament review notes for this layer treat Mega Evolution, Z-Moves, Dynamax, and Terastallization as the same class of limited "gimmick" resource even though their exact mechanics differ. PJCS2026 / Pokemon Champions, NAIC, and older VGC seasons are useful for turn-shape review: when a player converts a known target into immediate KO pressure, a good AI should consider spending the format gimmick, repositioning, or sacrificing the correct Pokemon rather than conserving by default. The implementation should not blindly copy old generation mechanics across formats; use those videos and result pages to tag recurring board patterns, then encode only mechanics that this ROM actually supports.

## Tests Added

- Conservation baseline: keeps Dynamax unused when another Dynamax user remains and the move has no immediate payoff.
- Last-Pokemon baseline: spends Dynamax when no reserve remains.
- One-reserve late-commit baseline: spends Dynamax when the active Pokemon is low on HP and only one reserve remains, even if the opponent is passive.
- Max Geyser payoff: spends Dynamax to set rain even with a reserve remaining.
- Max Airstream payoff: spends Dynamax in doubles for Speed-control tempo even with a reserve remaining.
- G-Max residual payoff: with Tailwind already active, G-Max Charizard spends Dynamax on Wildfire pressure instead of redundant Airstream.
- Dynamax disruption payoff: spends Dynamax to keep a damaging move live through Fake Out-style flinch or Roar / Whirlwind-style phazing.
- Smart Z conserve: keeps a damaging Z-Move unused when another Pokemon remains and the Z-Move has no immediate payoff.
- Smart Z last Pokemon: spends a damaging Z-Move when no reserve remains.
- Smart Z one-reserve low-HP: spends a damaging Z-Move before it is stranded by over-conservation.
- Smart Z trap guard: conserves a damaging Z-Move against a trapping target when there is no damage-race payoff, while separate low-HP / last-Pokemon cases still spend.
- Low-HP pressure attackers: spends Dynamax and damaging Z-Move lines from production-style sets when the active Pokemon is low with one reserve remaining.
- Read-player-move debug prediction: with `AI_FLAG_READ_PLAYER_MOVE`, spends Dynamax into an already confirmed `Fake Out`, scores attacks against a selected player defensive Tera type, scores against the actual selected switch-in, and retargets ordinary single-target damage away from a confirmed selected `Protect`.
- Read-player-move Protect discipline: in the `Champs Double` turn-0 shape, does not send `Flash Cannon` into a player `Protect` and does not use passive `Protect` when the player is using setup plus Protect.
- Read-player-move offensive setup pressure: in the `Champs Double` turn-0 shape, sees selected Mega `Dragon Dance` plus partner `Moonblast` and still points both `Flash Cannon` and `Air Slash` at the setup Dragonite instead of hiding behind Protect or chasing only the partner attack.
- Read-player-move singles setup discipline: in the `Champs Single` Dragonite versus Lucario shape, attacks selected `Dragon Dance` with `Flash Cannon` instead of answering with self-target `Nasty Plot` when meaningful damage is available.
- Read-player-move King's Shield setup guard: with Aegislash Blade facing a selected `Dragon Dance` plus a known partner attack, does not let the Stance Change `King's Shield` bonus override the need to pressure the setup threat.
- Read-player-move selected setup disruption: with production-style Whimsicott / Xerneas / Kyogre / Amoonguss sets, chooses `Spore` into a selected `Geomancy` even though the player side has an unselected `Encore`.
- Read-player-move opposing redirection guard: with a selected `Rage Powder`, avoids aiming ordinary redirectable single-target pressure into the partner slot and targets the redirector instead.
- Read-player-move known KO gimmick timing: with production-style Miraidon versus Dynamax Koraidon singles and Incineroar / Miraidon versus Dynamax Koraidon / Tornadus doubles sets, spends Dynamax into a confirmed `Draco Meteor` KO threat even when the initial doubles attention is on the partner slot.
- Desperation comeback checks: uses a low-accuracy sleep move when the AI is about to be KOed with no stable line, logs the desperation reason, fishes for a faster flinch into a trapped / no-reserve read Perish Song only when no clean damage line exists, logs hax-out reason for that line, and does not fish for that flinch when a reserve can still switch out of Perish Song.
- Perish / Soundproof switching: switches to a Soundproof reserve against a read Perish Song when the current slot cannot stop it with clean faster damage, and logs `perish_escape` on the resolved switch-in action-log entry.
- Read-player-move cross-slot switching: with real VGC-style Rillaboom / Tornadus / Mega Gengar / Incineroar sets copied from `src/data/debug_trainers.party` conventions, switches Mega Gengar to Incineroar when player-left selected Grassy Glide is known to target and KO opponent-right, but does not burn the same Incineroar switch-in into a known High Horsepower punish.
- Partner synergy fixtures: with production-style level-50 doubles sets, heals a low-HP setup ally with `Pollen Puff`, rejects illegal self-target `Pollen Puff` even when the user wants healing, uses `Rage Powder` to protect a known targeted ally, intentionally activates ally `Steam Engine` plus `Weakness Policy` with priority `Aqua Jet`, still scores harmless non-priority Fire activation, rejects unsafe non-priority `Water Gun` into `Coalossal`, triggers ally `Stamina` for `Body Press`, triggers ally `Anger Point` with a guaranteed critical hit, copies ally boosts with `Psych Up`, uses `Haze` to reset ally `Draco Meteor` drops, and keeps ally `Clear Smog` as a lower-priority score fallback when Haze is unavailable.
- Commander targeting and desperation spread-pressure checks: targets Commander Dondozo instead of swallowed Tatsugiri, scores swallowed Tatsugiri as remaining value after Dondozo faints, preserves the Toxic Orb / Flame Orb Commander combo as a score path, and only permits partner-KO spread damage when near-term board pressure plus reserve-entry value justifies the sacrifice.
- Gimmick access separation: `AI_FLAG_GIMMICK_ENV_ALL` keeps Z-Move timing tactical while Z-Move availability remains tied to Z-Crystals and Z-Power access.
- Smart Mega Shadow Tag: spends Mega Evolution when the target form's ability creates immediate trapping pressure.
- Smart Mega low-HP late-commit: spends Mega Evolution with one reserve remaining, while a separate setup-delay test still verifies full-HP no-pressure conservation.
- Smart Switching doubles: can double switch out of bad double positions when neither partner can cover.
- Smart Switching doubles guard: stays in a bad position when the partner can cover the target.
- Smart Switching weather pivot: switches to a `Drizzle` reserve when rain improves reserve pressure.
- Smart Switching terrain pivot: switches to a `Grassy Surge` reserve that can change board control.
- Smart Switching speed-control pivots: switches to Tailwind and Trick Room reserves when those controls can flip the speed state.
- Smart Switching terrain seed pivot: switches to a terrain setter whose field triggers the reserve's seed plan.
- Smart Switching status pivots: switches into a status-benefit reserve under predicted burn, and in doubles can pivot to direct or secondary status / confusion pressure support.
- Smart Switching ability bridge pivot: switches in Skill Swap support when a partner board-control ability creates a bridge plan.
- Smart Switching Taunt reads: pivots an attacker into a predicted `Taunt`, stays in when the active Pokemon can punish with damage, and stays in when the active Pokemon ignores Taunt.
- Runtime knowledge layer: verifies AI move-category flags, ability-category flags, item / hold-effect category flags, and predicted-move immunity bridges for Magic Bounce, Safety Goggles / powder immunity, and ordinary non-ignored moves.
- Runtime knowledge catalog tool: generates local runtime review JSON for 935 moves, 319 abilities, 130 hold effects, 874 items, and 4 gimmick policies.
- Protect singles payoff scoring: avoids passive singles Protect with no turn-gain payoff, avoids passive Protect against boosted attackers, values Protect when residual damage creates payoff, and keeps a second singles Protect viable when the payoff remains.
- Protect doubles consecutive scoring: keeps a second double Protect as a risky but possible option instead of treating it as impossible.
- Kyogre doubles spread-pressure regressions: keeps rain-boosted `Water Spout` above `Thunder` when both opposing slots are pressured, including Tornadus + Incineroar boards where rain `Thunder` is internally accuracy-bypassing.
- HP-dependent power read regressions: discounts `Water Spout` when a confirmed faster `Dragon Ascent` will lower Kyogre's HP first, and discounts `Wring Out` when a confirmed faster ally-spread `Surf` will lower the selected target's HP before the AI move resolves.
- Future spread-pressure regression: with a post-Geomancy Xerneas protected beside an Incineroar-to-Amoonguss switch read, Kyogre scores `Water Spout` and `Origin Pulse` above `Ice Beam` under both Choice Specs and Mystic Water. The Choice Specs case verifies the three-turn same-move horizon where repeated spread pressure beats repeated single-target Ice Beam pressure.
- Prankster / coverage regressions: Tornadus no longer scores Prankster `Taunt` into Dark-type Incineroar, and Kyogre receives extra value as the Incineroar damage source when its partner has no meaningful hit into that slot. The Kyogre case is parametrized for both confirmed read-player context and normal prediction / known-party context.
- Choice-role-done switching regressions: with `AI_FLAG_READ_PLAYER_MOVE`, a Choice Scarf attacker pivots when it is ignored and its locked move no longer creates pressure, stays as a cushion when the confirmed player turn is already targeting it, and stays when the only useful reserve would be punished by known spread damage on entry.
- Dmax singles pivot guard: with production-style Calyrex-Shadow / Kyogre / Zacian / Rillaboom sets, the AI may pivot once into an Astral Barrage absorber, but the switch-in then attacks with `Grassy Glide` instead of repeatedly pivoting.
- Debug battle EXP gate: verifies a `gIsDebugBattle` trainer battle does not show the EXP bar and does not award EXP or EVs.
- Existing Tera, Mega, Z-Move, and combined environment tests remain in `ai_smart_gimmick.c`.

## Validation

Local validation highlights:

- `rtk make -j16 -O check TESTS='AI runtime knowledge'`: pass, 4 tests. Covers move-category, ability-category, item / hold-effect category mapping, and predicted-move immunity bridges.
- `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV'`: pass, 10 tests.
- 2026-07-04 `rtk make -j1 -O check TESTS='AI fishes for flinch against a read Perish Song only without a clean damage line'`: pass, 3 parametrized cases.
- 2026-07-04 `rtk make -j1 -O check TESTS='AI does not fish for flinch against read Perish Song when it can switch out'`: pass, 1 test.
- 2026-07-04 `rtk make -j1 -O check TESTS='AI accepts a low-accuracy sleep swing when it is about to be KOed'`: pass, 1 test.
- 2026-07-04 `rtk make -j1 -O check TESTS='AI logs hax out reason for desperate read Perish Song flinch'`: pass, 1 test.
- 2026-07-04 `rtk make -j1 -O check TESTS='AI switches to Soundproof against a read Perish Song when it cannot stop it'`: pass, 1 test.
- 2026-07-04 `rtk make -j1 -O check TESTS='AI targets Commander Dondozo instead of swallowed Tatsugiri'`: pass, 1 test.
- 2026-07-04 `rtk make -j1 -O check TESTS='AI only sacrifices a partner with spread damage for desperation board pressure'`: pass, 2 parametrized cases.
- 2026-07-04 `rtk make -j16 -O check TESTS='test/battle/ai/ai.c'`: exit 0 after the desperation comeback update.
- 2026-07-04 `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`: exit 0 after the Soundproof Perish switch update. Existing `KNOWN_FAILING` labels remain unchanged.
- 2026-07-04 `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`: exit 0 after the Commander targeting and desperation spread-sacrifice update. Existing `KNOWN_FAILING` / `TO_DO` labels remain unchanged.
- 2026-07-04 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI does not aim redirectable single-target pressure into selected Rage Powder'`: pass after narrowing doubles desperation so a lone exposed slot with a live partner is not enough to unlock hax over clean pressure.
- 2026-07-04 `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`: pass after the short-horizon risk governor update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV'`: pass, 11 tests. Includes the Z-Crystal-based all-gimmick Z-Move regression after access flags were split from AI flags.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV'`: pass, 13 tests. Includes Dynamax / Z / Mega late-commit regressions and the all-gimmick environment checks.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_ONLY'`: pass, 7 tests. Adds the one-reserve low-HP Dynamax late-commit regression.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_ALL'`: pass, 4 tests. Adds the one-reserve low-HP Mega late-commit regression while keeping full-HP setup-turn delay.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV'`: pass, 13 tests after preserving pre-battle debug opponent gimmick bits through `CB2_InitBattle()`.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI does not tunnel Kyogre'`: pass, 1 test after making rain-accurate `Thunder` neutral with 100% moves and preferring meaningful doubles spread pressure.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI prefers Kyogre'`: pass, 1 test after the same doubles spread-pressure comparison change.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 4 tests. Covers the read-player-move Fake Out Dynamax regression, the selected-player-Tera damage read, the actual selected switch-in read, and the in-ROM command log capture for all four choices in a double battle turn.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 5 tests. Adds the Protect-retarget regression from `/tmp/manual-ai-log-captured-20260606`, where a full-information gauntlet AI should avoid ordinary single-target damage into a confirmed player `Protect`.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 8 tests. Adds the pid `222151` manual-log Fake Out pressure regression, the `Extreme Speed` priority-order check, and the `Armor Tail` priority-blocker guard.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 11 tests. Adds the pid `278133` cross-slot Grassy Glide KO switch regression and the paired High Horsepower guard where Incineroar is not forced into a punishing known hit. The tests use real VGC-style Rillaboom / Tornadus / Mega Gengar / Incineroar moves, held items, abilities, natures, IVs, and fixed equivalent stats rather than placeholder Wobbuffet / Celebrate fixtures.
- 2026-06-06 `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`: pass, 31 tests after the pid `278133` fix.
- 2026-06-06 `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`: exit 0 after the pid `278133` fix. Existing known-failing labels remain known-failing; no new failures.
- 2026-06-06 `rtk make -j16 -O check TESTS='test/battle/ai/ai.c'`: exit 0 after the pid `278133` fix.
- 2026-06-06 `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`: exit 0 after the pid `278133` fix. Existing known-failing / TODO labels remain unchanged.
- 2026-06-06 `rtk make -j16 -O check TESTS='test/battle/ai/ai_choice.c'`: pass, 11 tests after the pid `278133` fix.
- 2026-06-06 `rtk make -j16 -O check TESTS='test/battle/ai/ai_calc_best_move_score.c'`: pass, 17 tests after the pid `278133` fix.
- 2026-06-06 `rtk make -j16 -O check TESTS='test/battle/ai'`: non-evidence, `No tests found`; AI directory validation used file-level filters above.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: selected Fake Out'`: pass, 2 tests. Confirms ordinary slower actions and lower-priority `Extreme Speed` are discounted under read-mode Fake Out pressure.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: blocked selected Fake Out does not discount ordinary actions'`: pass, 1 test. Confirms priority blockers stop the read-mode Fake Out pressure penalty.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_AGGRESSIVE_GIMMICK'`: non-evidence, `No tests found` after replacing the placeholder aggressive fixtures with deterministic production-style low-HP pressure cases in `ai_smart_gimmick.c`.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_TERA_ONLY'`: pass, 1 test.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_TERA'`: pass, 1 test.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_ALL'`: pass, 4 tests. Covers held Z-Crystal use, doubles Tera, setup-turn Mega delay, and low-HP one-reserve Mega late commit.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV'`: pass, 13 tests after the command-buffer read update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV'`: pass, 13 tests after the read-mode Fake Out priority-pressure update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 11 tests after the pid `317741` ally-benefit update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_ONLY'`: pass, 8 tests after the pid `317741` ally-benefit update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_SMART_Z_MOVE'`: pass, 5 tests after the pid `317741` ally-benefit update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_ALL'`: pass, 4 tests after the pid `317741` ally-benefit update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_TERA_ONLY'`: pass, 1 test after the pid `317741` ally-benefit update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_TERA'`: pass, 1 test after the pid `317741` ally-benefit update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_SMART_MEGA'`: pass, 1 test after the pid `317741` ally-benefit update. Together with the preceding focused `AI_FLAG_*` filters, this is the requested adjusted smart-gimmick / read target set; the current filter total is 31 tests.
- 2026-06-30 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: smart Dynamax answers selected single KO pressure'`: pass, 1 test after adding the singles known selected-KO Dynamax regression.
- 2026-07-04 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: Dmax singles switch-in stops pivoting after absorbing Astral Barrage'`: pass, 1 test after auditing `/tmp/single-gimmick-pressure-20260630-turn1.json` as a possible repeated-switch candidate.
- 2026-06-30 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 21 tests after adding singles known selected-KO gimmick timing coverage.
- 2026-06-30 `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`: pass, 38 tests after adding the read-mode single-slot and off-slot KO Dynamax regressions.
- 2026-06-30 `rtk make -j16 -O check TESTS='test/battle/ai/gimmick_dynamax.c'`: exit 0, with the existing `KNOWN_FAILING` / `TO_DO` labels unchanged.
- 2026-06-30 `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`: pass, 12 tests.
- 2026-06-30 `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`: exit 0, with existing `KNOWN_FAILING` / `TO_DO` labels unchanged.
- 2026-06-30 `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_tera.c'`: exited 2 on existing broad Tera test-runner issue, `AI_FLAG_SMART_TERA: AI might tera if it gets saved from a ko (1/?)`, because `PASSES_RANDOMLY` was specified without an executed matching random tag. This is tracked as pre-existing and is not counted against the known selected-KO gimmick update.
- 2026-06-30 `rtk make -j16 -O check TESTS='test/battle/ai/gimmick_z_move.c'`: exited 2 on existing broad Z-Move case `AI uses Z-Moves -- Z-Detect 1/2`, where the AI selected `Detect` without the Z-Move gimmick. Smart-gimmick Z timing checks remain covered by the focused smart-gimmick file.
- 2026-06-30 `rtk make -j16 -O all`: pass after the known selected-KO gimmick timing update.
- 2026-06-30 `rtk make -j16 -O debug`: pass after the known selected-KO gimmick timing update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI heals a low HP setup ally with Pollen Puff'`: pass, 1 test.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI uses Rage Powder'`: pass, 1 test.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI triggers ally'`: pass, 2 tests. Covers ally `Stamina` / `Body Press` and ally `Steam Engine` plus `Weakness Policy`.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI uses a guaranteed critical hit'`: pass, 1 test for ally `Anger Point`.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI uses Psych Up'`: pass, 1 test.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI uses Haze to reset an ally'`: pass, 1 test.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI can score Clear Smog'`: pass, 1 test.
- 2026-06-06 `rtk make -j16 -O check TESTS='Pollen Puff'`: pass, 1 existing multi-battle Pollen Puff scoring test.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI does not target itself'`: pass, 1 test. Covers the pid `354725` illegal Pollen Puff self-target regression with opponent-right `Amoonguss` damaged and `Pollen Puff` in the same move slot used by the manual log.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI heals'`: pass, 1 existing ally Pollen Puff healing test after the target-validity guard.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI uses Rage Powder'`: pass, 1 existing read-mode ally protection test after the target-validity guard.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI triggers ally'`: pass, 2 existing ally-benefit tests after the target-validity guard.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI uses a guaranteed critical hit'`: pass, 1 existing ally `Anger Point` test after the target-validity guard.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI uses Psych Up'`: pass, 1 existing ally boost-copy test after the target-validity guard.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI uses Haze'`: pass, 1 existing ally stat-reset test after the target-validity guard.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI can score Clear Smog'`: pass, 1 existing ally Clear Smog fallback test after the target-validity guard.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 11 tests after the target-validity guard.
- 2026-07-04 `rtk make -j16 -O check TESTS='AI_FLAG_ATTACKS_PARTNER'`: pass, 3 tests after the spread-pressure expectation cleanup and the production-style single-target target-selection regression.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI values future spread pressure over a switch read beside boosted Xerneas'`: pass, 1 parametrized future-pressure test. Confirms Kyogre scores `Water Spout` and `Origin Pulse` above `Ice Beam` into an Incineroar-to-Amoonguss switch read while a post-Geomancy Xerneas is protected beside it, under both Choice Specs and Mystic Water.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`: exit 0 after the future spread-pressure update. The new Kyogre future-pressure regression passed; existing known-failing / TO_DO labels remain unchanged.
- 2026-06-07 parallel `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'` plus `TESTS='test/battle/ai/ai_partner_synergy.c'`: non-evidence. The parallel run collided on the shared test ELF and produced a `Bus error`; sequential reruns are the accepted evidence.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`: pass, 31 tests after the future spread-pressure update.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`: pass, 9 tests after the future spread-pressure update.
- 2026-06-07 parallel focused `check` attempts for the two new pid `507917` test titles: non-evidence. Running both in parallel collided on shared hydra / temporary files and the long-title filters returned `No tests found`; file-level and sequential checks below are the accepted evidence.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI uses Guard Split to improve its stats'`: pass after gating the partner-coverage reward on reliable future context; this guards against the coverage heuristic polluting ordinary low-information support-move targeting while still allowing read, omniscient, prediction, known-party, and assumption-flag contexts.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`: exit 0 after the pid `507917` Prankster / coverage update. New tests passed for Prankster `Taunt` into Dark-type Incineroar and Kyogre as the Incineroar damage source when Tornadus lacks a good hit; existing known-failing / TO_DO labels remain unchanged.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`: pass, 31 tests after the pid `507917` update.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`: pass, 9 tests after the pid `507917` update.
- 2026-06-07 parallel focused `check` attempts for the pid `528022` partner-activation filters: non-evidence. Running filtered checks in parallel collided on the shared test ELF / temporary files, including `open tmpfd failed`, undefined references, and one `No tests found`; the sequential reruns below are the accepted evidence.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI triggers ally'`: pass, 2 tests after the pid `528022` partner-activation update. Covers priority `Aqua Jet` into ally `Steam Engine` plus `Weakness Policy`, and ally `Stamina` / `Body Press`.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI can score a harmless non-priority Fire hit'`: pass, 1 test. Confirms non-priority activation is not banned when the real ally damage is low.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI does not use unsafe non-priority Water Gun'`: pass, 1 test. Confirms 4x non-priority Water damage into `Coalossal` is not treated like a clean Steam Engine trigger.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`: pass, 11 tests after the pid `528022` update.
- 2026-06-07 `rtk make -j16 -O check TESTS="AI will trigger its ally's Weakness Policy"`: pass, 1 existing spread `Weakness Policy` test after the pid `528022` update.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`: exit 0 after the pid `528022` update. Existing known-failing / TO_DO labels remain unchanged.
- 2026-06-07 parallel `rtk make -j16 -O all` plus `rtk make -j16 -O debug`: non-evidence. Both builds write the same root `pokeemerald.elf` / `pokeemerald.gba`, causing objcopy string-table warnings; the sequential reruns below are the accepted evidence.
- 2026-06-07 `rtk make -j16 -O all`: pass after the pid `528022` partner-activation update.
- 2026-06-07 `rtk make -j16 -O debug`: pass after the pid `528022` partner-activation update.
- 2026-06-07 `rtk mdbook build docs`: exit 0 after documenting the pid `528022` tactical partner-activation update. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.
- 2026-06-07 mGBA Live session `partner-activation-ai-final2-20260607`: pass / boot plus bridge and input smoke after the final pid `528022` partner-activation source state. After rerunning the normal ROM build, `tools/mgba_live/start_mgba_live.sh partner-activation-ai-final2-20260607 120` started pid `541865` from `/home/jastin/dev/pokeemerald-expansion/pokeemerald.gba`; Lua returned `{ok=true, validation=partner_activation_ai_final2_20260607}`; screenshots `/tmp/partner-activation-ai-final2-20260607-boot.png` and `/tmp/partner-activation-ai-final2-20260607-after-a.png` were exported; `input-tap --key A` succeeded; `stop` returned `stopped:true`; and final `status --all` returned `[]`.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI protects instead of choosing a slower attack into a selected KO'`: pass, 1 new pid `549295` regression.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`: pass, 12 tests after the pid `549295` Protect read update.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 11 tests after the pid `549295` selected-target reader update.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`: pass, 31 tests after the pid `549295` update.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`: exit 0 after the pid `549295` update. Existing known-failing / TO_DO labels remain unchanged.
- 2026-06-07 `rtk make -j16 -O all`: pass after the pid `549295` Protect read update.
- 2026-06-07 `rtk make -j16 -O debug`: pass after the pid `549295` Protect read update.
- 2026-06-07 `rtk mdbook build docs`: exit 0 after documenting the pid `549295` update. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.
- 2026-06-07 mGBA Live session `protect-read-ai-20260607`: pass / boot plus bridge and input smoke after the pid `549295` source state. After rerunning the normal ROM build, `tools/mgba_live/start_mgba_live.sh protect-read-ai-20260607 120` started pid `562379` from `/home/jastin/dev/pokeemerald-expansion/pokeemerald.gba`; `status --all` showed the session alive at 120 FPS; Lua returned `{ok=true, validation=protect_read_ai_20260607}`; screenshots `/tmp/protect-read-ai-20260607-boot.png` and `/tmp/protect-read-ai-20260607-after-a.png` were exported; `input-tap --key A` succeeded; `stop` returned `stopped:true`; and final `status --all` returned `[]`.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: choice-locked'`: pass, 2 tests for the pid `565092` role-done Choice pivot and unsafe known-spread switch-in guard.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 13 tests after the pid `565092` Choice-role switch update.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`: exit 0 after the pid `565092` update. Existing known-failing labels remain known-failing; no new failures.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`: pass, 31 tests after the pid `565092` update.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`: exit 0 after the pid `565092` update. Existing known-failing / TO_DO labels remain unchanged.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`: pass, 12 tests after the pid `565092` update.
- 2026-06-07 `rtk make tools/trainerproc/trainerproc`: pass / no work needed after adding `Champs Double`.
- 2026-06-07 `rtk make -j16 -O all`: pass after the pid `565092` update and `Champs Double` fixture.
- 2026-06-07 `rtk make -j16 -O debug`: pass after the pid `565092` update and `Champs Double` fixture.
- 2026-06-07 `rtk mdbook build docs`: exit 0 after documenting the pid `565092` update. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.
- 2026-06-07 mGBA Live session `champs-choice-ai-20260607`: partial runtime smoke after the pid `565092` source state. `tools/mgba_live/start_mgba_live.sh champs-choice-ai-20260607 120` started pid `580288` from `/home/jastin/dev/pokeemerald-expansion/pokeemerald.gba`; `status --all` showed the session alive at 120 FPS; Lua returned `{ok=true, validation=champs_choice_ai_20260607}`; screenshots `/tmp/champs-choice-ai-20260607-boot.png` and `/tmp/champs-choice-ai-20260607-after-a.png` were exported; `input-tap --key A` succeeded; `stop` returned `stopped:true`; and final `status --all` returned `[]`. The boot screenshot was captured during a white fade, but the after-A screenshot reached the title screen. This did not validate `Champs Double` menu reachability because the current mGBA battery route only exposed `NEW GAME`, not a progressed overworld debug save; manual route validation remains recorded in the test plan.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: choice-locked'`: pass, 3 tests after the cushion follow-up. Adds the explicit guard that a role-done Choice-locked Pokemon stays in as a cushion when the confirmed player turn is already targeting it.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 14 tests after the cushion follow-up.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`: exit 0 after the cushion follow-up. Existing known-failing labels remain unchanged.
- 2026-06-07 `rtk make -j16 -O all`: pass after documenting the PJCS2026 / VGC video-source policy and adding the cushion regression.
- 2026-06-07 `rtk make -j16 -O debug`: pass after the same cushion follow-up.
- 2026-06-07 `rtk mdbook build docs`: exit 0 after documenting PJCS2026 / VGC video sources and the role-done cushion policy. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index. mGBA Live was not rerun for this follow-up because the C source change is a comment clarifying the already-tested guard; runtime behavior remains covered by `champs-choice-ai-20260607` plus the new test-runner regression.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI does not attack Protect or passively Protect into setup'`: pass, 1 test for the pid `590887` turn-0 read loss.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI pressures a selected offensive setup threat'`: pass, 1 test for the pid `590887` follow-up Dragon Dance read loss.
- 2026-06-07 `rtk make -j16 -O check TESTS="AI_FLAG_READ_PLAYER_MOVE: AI does not hide behind King's Shield while a setup threat is selected"`: pass, 1 test for the pid `590887` follow-up Aegislash / King's Shield setup guard.
- 2026-07-04 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: Mega Lucario attacks selected Dragon Dance instead of setting up'`: pass, 1 test for the `Champs Single` Dragon Dance versus Nasty Plot read loss.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 15 tests after the pid `590887` Protect-read update.
- 2026-06-07 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 17 tests after the pid `590887` offensive-setup read update.
- 2026-07-04 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 24 tests after the `Champs Single` singles setup read guard.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`: pass, 34 tests after the pid `590887` offensive-setup read update.
- 2026-07-04 `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`: pass, 41 tests after the `Champs Single` singles setup read guard.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`: pass, 12 tests after the pid `590887` update.
- 2026-06-07 `rtk make -j16 -O check TESTS='Protect: AI'`: pass, 9 tests after the pid `590887` update.
- 2026-06-07 `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`: exit 0 after the pid `590887` update. Existing known-failing / TO_DO labels remain unchanged.
- 2026-06-07 `rtk make -j16 -O all`: pass after the pid `590887` update.
- 2026-06-07 `rtk make -j16 -O debug`: pass after the pid `590887` update.
- 2026-06-07 `rtk mdbook build docs`: exit 0 after documenting the pid `590887` update. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.
- 2026-06-07 mGBA Live session `protect-read-discipline-20260607`: booted the current `pokeemerald.gba`, returned Lua `{ok=true, validation=protect_read_discipline_20260607}`, exported `/tmp/protect-read-discipline-20260607-boot.png`, `/tmp/protect-read-discipline-20260607-after-a.png`, and `/tmp/protect-read-discipline-20260607-after-input.png`, accepted an `A` input through `input-tap`, stopped with `stopped:true`, and final `status --all` returned `[]`. The analyzed `manual-ai-log` session was not stopped by this validation.
- 2026-06-07 `rtk make -j16 -O all`: pass after the pid `590887` offensive-setup read update. This was rerun after the debug build so mGBA Live booted the normal ROM.
- 2026-06-07 `rtk make -j16 -O debug`: pass after the pid `590887` offensive-setup read update.
- 2026-06-07 `rtk mdbook build docs`: exit 0 after documenting the pid `590887` offensive-setup read update. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.
- 2026-06-07 mGBA Live session `setup-threat-read-20260607`: booted the current normal `pokeemerald.gba`, returned Lua `{ok=true, validation=setup_threat_read_20260607}`, exported `/tmp/setup-threat-read-20260607-boot.png` and `/tmp/setup-threat-read-20260607-after-a.png`, accepted an `A` input through `input-tap`, stopped with `stopped:true`, and final `status --all` returned `[]`.
- 2026-07-04 `rtk make -j16 -O all`: pass after the `Champs Single` singles setup read guard.
- 2026-07-04 `rtk make -j16 -O debug`: pass after the `Champs Single` singles setup read guard.
- 2026-07-04 `rtk mdbook build docs`: exit 0 after documenting the `Champs Single` singles setup read guard. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.
- 2026-07-04 mGBA Live session `champs-single-setup-guard-20260704`: pass / boot plus Lua and screenshot smoke after the `Champs Single` singles setup read guard. `tools/mgba_live/start_mgba_live.sh champs-single-setup-guard-20260704 120` started pid `27945` from the updated `pokeemerald.gba`; `status --all` showed the session alive at 120 FPS; screenshot `/tmp/champs-single-setup-guard-20260704.png` was exported; the first 3-second Lua probe timed out, but a 10-second retry returned `{ok=true, validation=champs_single_setup_guard_20260704}`; `stop --grace 1` returned `stopped:true`; and final `status --all` listed only the older stale `read-gimmick-pressure-20260630` zombie session already recorded above.
- 2026-07-04 `rtk make -j16 -O check TESTS='Trainer Party Pool'`: pass, 10 tests after adding the adaptive Champion / Elite pools and `POOL_PRUNE_OPPONENT_ADAPTIVE`.
- 2026-07-04 `rtk make -j16 -O debug`: pass after adding the `Elite Single` and `Elite Double` debug NPC fixtures and regenerating debug trainer data.
- 2026-07-04 `rtk make -j16 -O all`: pass after correcting the adaptive tag mapping so fast pressure picks `Tag6`, weather / legendary pressure picks `Tag7`, and setup / support pressure picks `Tag8`.
- 2026-07-04 `rtk mdbook build docs`: exit 0 after documenting the NPC fixture-size rule. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.
- 2026-07-04 mGBA Live session `adaptive-pool-elite-double-smoke-20260704`: pass / boot plus Lua and screenshot smoke after the adaptive Champion / Elite fixture update. The session booted the updated ROM, exported `/tmp/adaptive-pool-elite-double-smoke-20260704.png`, returned Lua `{ok=true, validation=adaptive_pool_elite_double_smoke_20260704}`, and stopped with `stopped:true`. The active manual session `manual-ai-watch-20260704c` and stale `read-gimmick-pressure-20260630` status entry were left untouched.
- 2026-07-04 mGBA Live session `adaptive-tag-map-smoke-20260704`: pass / boot plus Lua and screenshot smoke after correcting the adaptive tag mapping. The session booted the updated ROM, exported `/tmp/adaptive-tag-map-smoke-20260704.png`, returned Lua `{ok=true, validation=adaptive_tag_map_smoke_20260704}`, and stopped with `stopped:true`. The active manual session `manual-ai-watch-20260704c` and stale `read-gimmick-pressure-20260630` status entry were left untouched.
- 2026-07-04 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: singles ignore unselected coverage'`: pass, 1 test for the Elite Single follow-up where the NPC must not switch merely because the visible player set contains an unselected Water move.
- 2026-07-04 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: singles switch on selected KO'`: pass, 1 test proving the same read layer can still switch when the selected move would actually KO.
- 2026-07-04 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 28 tests after the Elite Single selected-move switch update.
- 2026-07-04 `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`: exit 0 after the Elite Single selected-move switch update. Existing `KNOWN_FAILING` labels remain unchanged.
- 2026-07-04 `rtk make -j16 -O check TESTS='test/battle/trainer_control.c'`: pass, 24 tests after adding an Elite-style Metagross / Milotic / Breloom adaptive-pool fixture.
- 2026-07-04 `rtk make -j16 -O check TESTS='Trainer Slide: Multi: Last Switchin'`: pass after changing the slide fixture to avoid an unrelated `Memento` target tie.
- 2026-07-04 `rtk make -j16 -O check TESTS='AI_FLAG_SMART_TERA: AI will tera if it gets saved from a ko'`: pass after aligning the test with the deterministic saved-from-KO Tera path.
- 2026-07-04 `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`: pass, 42 tests after the Elite Single selected-move switch update.
- 2026-07-04 `rtk make -j16 -O debug`: pass after the Elite Single selected-move switch update.
- 2026-07-04 `rtk make -j16 -O all`: pass after the Elite Single selected-move switch update.
- 2026-07-04 `rtk mdbook build docs`: exit 0 after documenting the Elite Single selected-move switch update. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.
- 2026-07-04 mGBA Live session `elite-single-switch-smoke-20260704`: pass / boot plus Lua and screenshot smoke after the Elite Single selected-move switch update. The session started from the updated `pokeemerald.gba` at 120 FPS, `status --all` showed it active alongside the untouched `manual-ai-watch-20260704d` session and the pre-existing stale `read-gimmick-pressure-20260630` entry, Lua returned `{ok=true, validation=elite_single_switch_smoke_20260704}`, screenshot `/tmp/elite-single-switch-smoke-20260704.png` was exported, `stop --grace 1` returned `stopped:true`, and final `status --all` listed only `manual-ai-watch-20260704d` plus the stale `read-gimmick-pressure-20260630`.
- 2026-07-04 `/tmp/manual-ai-watch-20260704d-battle-action-log-autosave.json`: inspected after the Tera Single follow-up. The log was a singles battle where player Flutter Mane selected `Shadow Ball` on turn 0 and `Moonblast` on turns 1-2 while the AI switched three times before finally spending Tera with `Astral Barrage` on turn 3. The issue was not lack of Tera access; it was switching away before the existing smart Tera survival check could choose an immediate defensive / offensive Tera line.
- 2026-07-04 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI stays and teras instead of switching from a selected KO it can outpace'`: pass, 1 Tera Single-style Flutter Mane regression.
- 2026-07-04 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI may switch if tera still loses to the selected KO roll'`: pass, 1 guard for the maximum-roll KO-through-Tera case.
- 2026-07-04 `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_tera.c'`: pass, 6 tests after the Tera Single selected-KO switch guard.
- 2026-07-04 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 30 tests after the Tera Single selected-KO switch guard.
- 2026-07-04 `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`: exit 0 after the Tera Single selected-KO switch guard. Existing `KNOWN_FAILING` labels remain unchanged.
- 2026-07-04 `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`: pass, 42 tests after the Tera Single selected-KO switch guard.
- 2026-07-04 `rtk make -j16 -O debug`: pass after the Tera Single selected-KO switch guard.
- 2026-07-04 `rtk make -j16 -O all`: pass after the Tera Single selected-KO switch guard.
- 2026-07-04 `rtk mdbook build docs`: exit 0 after documenting the Tera Single selected-KO switch guard. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.
- 2026-07-04 mGBA Live session `tera-single-selected-ko-smoke-20260704`: pass / boot plus Lua and screenshot smoke after the Tera Single selected-KO switch guard. The session booted the updated normal ROM at 120 FPS, Lua returned `{ok=true, validation=tera_single_selected_ko_smoke_20260704}`, screenshot `/tmp/tera-single-selected-ko-smoke-20260704.png` was exported, `stop --grace 1` returned `stopped:true`, and final `status --all` listed only the pre-existing stale `read-gimmick-pressure-20260630` entry.
- 2026-07-04 `rtk make -j16 -O check TESTS='NPC trainer AI automatically reads confirmed player commands outside link battles'`: pass, 1 test after moving read-player policy to runtime `GetAiFlags()`.
- 2026-07-04 `rtk make -j16 -O check TESTS='AI runtime knowledge'`: pass, 4 tests after the non-link NPC read-player policy update.
- 2026-07-04 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 30 tests after the non-link NPC read-player policy update.
- 2026-07-04 `rtk make -j16 -O check TESTS='test/battle/ai/ai.c'`: exit 0 after adding the NPC policy boundary test.
- 2026-07-04 `rtk make -j16 -O all`: pass after the non-link NPC read-player policy update.
- 2026-07-04 `rtk make -j16 -O debug`: pass after the non-link NPC read-player policy update.
- 2026-07-04 `rtk mdbook build docs`: exit 0 after documenting the non-link NPC read-player policy. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.
- 2026-07-04 mGBA Live session `npc-read-policy-smoke-20260704`: pass / boot plus Lua, input, screenshot, and autosave smoke after the non-link NPC read-player policy update. A sandboxed start first failed because `mgba-live-cli` needed to refresh `~/.mgba-live-mcp/runtime/active_session`; rerunning with approval started the updated `pokeemerald.gba` at 120 FPS. Lua returned `{ok=true, validation=npc_read_policy_smoke_20260704}`, screenshots `/tmp/npc-read-policy-smoke-20260704-boot.png` and `/tmp/npc-read-policy-smoke-20260704-after-start.png` were exported, `START` input succeeded, `/tmp/npc-read-policy-smoke-20260704-battle-action-log-autosave.json` was written, and `stop` returned `stopped:true`. Final `status --all` listed only the pre-existing stale `read-gimmick-pressure-20260630` session.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI will choose Beat Up'`: pass, 2 existing ally `Beat Up` AI tests for `Justified` and `Rage Fist`.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI will only Beat Up'`: pass, 1 existing `Rage Fist` guard.
- 2026-06-06 `rtk make -j16 -O check TESTS="AI will trigger its ally's Weakness Policy"`: pass, 1 existing ally `Weakness Policy` AI test.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_'`: historical non-evidence broad filter. It picked unrelated existing AI-flag tests outside that change and failed at the then-unresolved `AI_FLAG_ATTACKS_PARTNER` legacy expectation. The `AI_FLAG_ATTACKS_PARTNER` filter was resolved separately on 2026-07-04.
- 2026-06-06 `rtk make -j16 -O debug`: pass after the command-buffer read update.
- 2026-06-06 `rtk make -j16 -O all`: pass after the command-buffer read update.
- 2026-06-06 `rtk make -j16 -O debug`: pass after the read-mode Fake Out priority-pressure update.
- 2026-06-06 `rtk make -j16 -O all`: pass after the read-mode Fake Out priority-pressure update.
- 2026-06-06 `rtk make -j16 -O all`: pass after the pid `278133` cross-slot switch update.
- 2026-06-06 `rtk make -j16 -O debug`: pass after the player double move-menu gimmick UI reset.
- 2026-06-06 `rtk make -j16 -O all`: pass after the player double move-menu gimmick UI reset.
- 2026-06-06 `rtk make -j16 -O all`: pass after the final pid `278133` cross-slot switch fix, production-style `ai_smart_gimmick.c` fixture rewrite, and documentation updates.
- 2026-06-07 `rtk make -j16 -O all`: pass after the future spread-pressure update.
- 2026-06-07 `rtk make -j16 -O debug`: pass after the future spread-pressure update.
- 2026-06-07 `rtk mdbook build docs`: exit 0 after documenting the future spread-pressure update. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.
- 2026-06-07 `rtk make -j16 -O all`: pass after the pid `507917` Prankster / coverage update.
- 2026-06-07 `rtk make -j16 -O debug`: pass after the pid `507917` Prankster / coverage update.
- 2026-06-07 `rtk mdbook build docs`: exit 0 after documenting the pid `507917` Prankster / coverage update. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.
- 2026-06-07 mGBA Live session `prankster-coverage-ai-20260607`: partial runtime smoke for the updated normal ROM. Booted `pokeemerald.gba`, returned Lua `{ok=true, validation=prankster_coverage_ai_20260607}`, exported `/tmp/prankster-coverage-ai-20260607-boot.png` and `/tmp/prankster-coverage-ai-20260607-after-a.png`, accepted an `A` input, stopped with `stopped:true`, and final `status --all` returned `[]`.
- 2026-06-06 `rtk make -j16 -O check TESTS='(Z-MOVE) Every battler can use Z-Moves - Doubles'`: pass, 1 test, after the player double move-menu gimmick UI reset.
- 2026-06-06 `rtk make -j16 -O check TESTS="Dynamax: Max Strike lowers both opponents' speed"`: pass, 1 test, after the player double move-menu gimmick UI reset.
- 2026-06-06 `rtk make -j16 -O check TESTS="Z-Moves don't leak when used - Doubles"`: non-evidence, `No tests found`; those animation leak fixtures are not available through this filtered check path.
- 2026-06-06 `rtk mdbook build docs`: exit 0 after docs updates. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` HTML end tag, and large search index.
- 2026-06-06 `rtk mdbook build docs`: exit 0 after documenting the read-mode Fake Out priority-pressure update. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` HTML end tag, and large search index.
- 2026-06-06 `rtk mdbook build docs`: exit 0 after the final pid `278133` / pid `297174` log-analysis documentation update. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` HTML end tag, and large search index.
- 2026-06-06 mGBA Live session `smart-gimmick-command-read-20260606`: pass for debug route and first-turn command-buffer runtime. Started the debug ROM with the project script-capable mGBA via `DISPLAY=:0 rtk ... mgba-live-cli`, opened `R+START` debug menu, navigated `Party -> Gauntlet Battles -> Tera Double`, reached battle, entered both player commands, observed the turn resolve to the party switch screen, exported `/tmp/smart-gimmick-command-read-after-turn.png`, and stopped cleanly. Direct MCP `mgba_live_start` without `DISPLAY` failed first with Qt `xcb` display initialization, so validation used CLI start plus MCP input / screenshot tools.
- 2026-06-06 mGBA Live session `smart-gimmick-dmax-double-log-20260606`: pass for the Dmax Double debug route, command-log runtime storage, and the non-assert switch-in fallback path. Continued from the existing save, opened `R+START`, navigated `Party -> Gauntlet Battles -> Dmax Double`, entered multiple double-battle turns, observed opponent switch-in resolution from `Kyogre` to `Charizard`, opponent `Protect`, a later `Landorus` board, and no `IsValidSwitchIn()` blue assert. Read `gBattleActionLog` from EWRAM at `0x020004cc`; the header showed `sequence=15`, `lastRecordedTurn=2`, `cursor=15`, and `count=15`, confirming runtime command / switch entries were being stored. Exported `/tmp/smart-gimmick-dmax-double-log-20260606.png`, stopped cleanly, and `mgba-live-cli status --all` returned `[]`.
- 2026-06-06 mGBA Live session `battle-log-export-20260606`: pass for host JSON export. Started the Dmax Double gauntlet from the existing save, confirmed one double-battle turn, then ran `tools/mgba_live/export_battle_action_log.sh battle-log-export-20260606 /tmp/battle-action-log-export-test.json`. The wrapper initially needed elevated execution because `mgba-live-cli` refreshes `~/.mgba-live-mcp/runtime/active_session`; after approval it returned schema `pokeemerald.battle_action_log.v1`, `entries=6`, `sequence=6`, `cursor=6`, `count=6`, and wrote valid JSON. `python3 -m json.tool` parsed the file, and the entries included named moves such as `MOVE_ASTRAL_BARRAGE` and `MOVE_FAKE_OUT`, opponent `B_ACTION_SWITCH` entries, and `resolved_switch_in` markers. Stopped cleanly and `mgba-live-cli status --all` returned `[]`.
- 2026-06-06 `/tmp/manual-battle-log-check-current.json`: pass for exported field coverage. The exported JSON used schema `pokeemerald.battle_action_log.v1`, header `count=17`, `cursor=17`, `sequence=17`, `last_recorded_turn=2`, and `skipped_invalid=0`. All 17 entries had the required raw fields and decoded names for action, move, item, gimmick, battler position, side / slot, target, party index, and flag markers. The sample covered 10 `B_ACTION_USE_MOVE` entries, 7 `B_ACTION_SWITCH` entries, selected `GIMMICK_Z_MOVE` and `GIMMICK_MEGA` markers, and 5 `resolved_switch_in` entries.
- 2026-06-06 mGBA Live session `fps-wrapper-check-20260606`: pass for the 120 FPS start helper. Ran `tools/mgba_live/start_mgba_live.sh fps-wrapper-check-20260606 120`; `mgba-live-cli status --all` reported alive session state with `fps_target=120.0`, ROM `/home/jastin/dev/pokeemerald-expansion/pokeemerald.gba`, and advancing heartbeat frame. A simple `run-lua` returned `{ result: 123 }`, confirming the Lua bridge responded. `stop --session fps-wrapper-check-20260606` returned `stopped:true`.
- 2026-06-06 mGBA Live session `manual-ai-retest-20260606`: action-log recovery after close was not possible. By the time export was attempted, `mgba-live-cli status --all` returned `[]` and the session had moved to `archived_sessions/manual-ai-retest-20260606-20260606-100152`. The archive contained session metadata, heartbeat, response, bridge script, and empty stdout / stderr logs, but no battle action JSON because host export had not been run before the session closed. Existing `/tmp/manual-battle-log-check-current.json` remained available as the last manual export, but it was not the just-finished double battle.
- 2026-06-06 mGBA Live session `autosave-wrapper-check-20260606`: pass for startup autosave registration. Ran `BATTLE_ACTION_LOG_OUT=/tmp/autosave-wrapper-check-20260606.json tools/mgba_live/start_mgba_live.sh autosave-wrapper-check-20260606 120`; `session.json` listed `tools/mgba_live/battle_action_log_autosave.lua` in `startup_scripts`, and `run-lua` confirmed `_G.BATTLE_ACTION_LOG_AUTOSAVE` existed. No battle was started in this smoke, so `last_sequence=-1` was expected. Stopped cleanly.
- 2026-06-06 mGBA Live session `player-gimmick-menu-reset-20260606`: partial visual evidence for the player double move-menu gimmick UI reset. Started `pokeemerald.gba` at 120 FPS through `tools/mgba_live/start_mgba_live.sh`, `status --all` reported the session alive, `run-lua` returned `{ok=true}`, and screenshot `/tmp/player-gimmick-menu-reset-20260606.png` was exported. The session stopped cleanly. A pre-existing unrelated `manual-ai-log` session remained active and was intentionally not stopped. This was boot / bridge evidence only; the exact double-battle "first player selects Z-Move or Dynamax, second player opens normal move menu" route still needs manual visual confirmation.
- 2026-06-06 mGBA Live session `ai-protect-retarget-20260606`: partial runtime evidence for the Protect-retarget update. Started the updated `pokeemerald.gba` at 120 FPS through `tools/mgba_live/start_mgba_live.sh`, `run-lua` returned `{ok=true}`, screenshot `/tmp/ai-protect-retarget-20260606.png` was exported, and the session stopped cleanly. This was boot / bridge evidence only; the behavior itself is covered by the focused `AI_FLAG_READ_PLAYER_MOVE` test because a full manual gauntlet replay would interrupt the user's active play route.
- 2026-06-06 mGBA Live session `read-fakeout-priority-smoke-20260606`: pass / boot smoke for the read-mode Fake Out priority-pressure update. Started `pokeemerald.gba` at 120 FPS through `tools/mgba_live/start_mgba_live.sh`, `status --all` reported pid `242810` alive with `fps_target=120.0`, `run-lua` returned `{ok=true}`, screenshot `/tmp/read-fakeout-priority-smoke-20260606.png` was exported, `stop` returned `stopped:true`, and final `status --all` returned `[]`. This was boot / bridge evidence; the behavior itself is covered by focused test-runner validation.
- 2026-06-06 mGBA Live session `known-cross-slot-ko-switch-guard-20260606`: pass / boot smoke for the pid `278133` cross-slot switch update. Started updated `pokeemerald.gba` at 120 FPS through `tools/mgba_live/start_mgba_live.sh`, `status --all` reported pid `299495` alive with ROM `/home/jastin/dev/pokeemerald-expansion/pokeemerald.gba`, screenshot `/tmp/known-cross-slot-ko-switch-guard-20260606.png` showed a nonblank in-game field frame, `run-lua` returned `{ok=true, frame=1217}`, and `stop` returned `stopped:true`. Final `status --all` still listed the unrelated `manual-ai-log` session alive, so only the validation session was stopped. This was boot / bridge evidence; the exact behavior is covered by focused test-runner validation.
- 2026-06-06 mGBA Live session `final-smart-gimmick-ai-20260606`: pass / boot smoke after the final source, test, and docs state. Direct MCP start returned Lua `{ok=true, validation=smart_gimmick_ai_final_20260606}`, screenshots reached the title path and exported `/tmp/final-smart-gimmick-ai-20260606.png`, and `stop` returned `stopped:true`.
- 2026-06-06 mGBA Live session `read-double-menu-20260606`: pass for the new `Read Double` gauntlet menu entry. Rebuilt the debug ROM, started at 120 FPS through `tools/mgba_live/start_mgba_live.sh`, captured `/tmp/read-double-menu-20260606-boot.png`, sent `START`, reached `Party -> Gauntlet Battles`, scrolled to the bottom, and captured `/tmp/read-double-menu-20260606-read-double.png` showing `Read Double` rendered without mojibake. `stop` returned `stopped:true`, and final `status --all` returned `[]`. This validates menu availability; the user will run battle rechecks and provide session IDs for action-log review.
- 2026-06-06 mGBA Live session `smart-gimmick-read-aggressive-20260606`: pass for debug route and in-battle reachability. Booted the debug ROM, progressed from `NEW GAME` to field control, opened `R+START` debug menu, navigated `Party -> Gauntlet Battles -> Dmax Double`, reached battle, selected player moves, captured `/tmp/smart-gimmick-read-aggressive-after-input.png`, then stopped cleanly with `status --all` returning `[]`. This is not counted as a visible opponent gimmick-animation proof because early manual inputs KO'd opposing slots before a visible Dynamax / Tera / Z-Move animation was captured.
- 2026-06-06 `rtk make -j16 -O check`: attempted after the debug opponent gimmick-bit preservation and Kyogre spread-pressure fix. It exited 2 with the same broad-run non-fatal test-runner / known-failing output pattern already recorded for this branch, including `Tests resume after CRASH` and `Pokemon level up learnsets fit within MAX_LEVEL_UP_MOVES and MAX_RELEARNER_MOVES`. Focused AI checks above are the validation evidence for this update.
- `rtk make -j16 -O check TESTS='AI_FLAG_SMART_Z_MOVE'`: pass, 3 tests.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_SMART_Z_MOVE'`: pass, 4 tests. Adds the one-reserve low-HP Z-Move late-commit regression.
- 2026-07-04 `rtk make -j16 -O check TESTS='AI uses Z-Moves'`: pass, 14 tests, after resolving the older broad `Z-Detect` status-Z gap. The AI now treats a confirmed / predicted `Fake Out` as a singles Protect payoff, prefers normal `Detect` when a same-type damaging Z-Move such as `Close Combat` should be preserved, and spends Z-Detect when the Z-Crystal has no usable same-type damaging payoff. A focused `AI uses Z-Moves -- Z-Detect` rerun passed first.
- 2026-07-04 `rtk make -j16 -O all`: pass after the `Z-Detect` / Fake Out Protect payoff update. Existing linker warning remains for RWX permissions on `pokeemerald.elf`.
- 2026-07-04 `rtk mdbook build docs`: exit 0 after documenting the `Z-Detect` / Fake Out Protect payoff update. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>`, and large search index.
- 2026-07-04 mGBA Live session `zdetect-runtime-20260704`: pass / boot plus input smoke after the `Z-Detect` / Fake Out Protect payoff update. `tools/mgba_live/start_mgba_live.sh zdetect-runtime-20260704 120` started pid `76537` from the updated `pokeemerald.gba`; screenshot `/tmp/zdetect-runtime-20260704-boot.png` showed a nonblank intro frame; `input-tap --key START` succeeded; screenshot `/tmp/zdetect-runtime-20260704-after-start.png` reached the title screen; and `stop --session zdetect-runtime-20260704` returned `stopped:true`.
- `rtk make -j16 -O check TESTS='AI_FLAG_SMART_MEGA'`: pass, 1 test.
- `rtk make -j16 -O check TESTS='AI_FLAG_SMART_TERA'`: pass, 4 tests.
- `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_ONLY'`: historical pass, 6 tests. Includes Fake Out and phazing disruption-prevention Dynamax checks before the late-commit regression was added.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_SMART_SWITCHING'`: pass. Includes bad-position double switching, weather / terrain / Tailwind / Trick Room board-control pivots, terrain seed, predicted burn status-benefit, direct and secondary status / confusion support, Skill Swap bridge pivots, predicted-Taunt attacker pivot / stay-in guards, and the double-switch execution guard through the shared predicted-move immunity predicate.
- `rtk make -j16 -O check TESTS='Protect: AI'`: pass, 9 tests. Covers ignore-protection moves, Unseen Fist, passive singles Protect rejection, boosted-attacker rejection, residual payoff, and second Protect scoring in singles and doubles.
- 2026-06-06 `rtk make -j16 -O check TESTS='Protect: AI'`: pass, 9 tests after the Protect-retarget update.
- 2026-06-06 `rtk make -j16 -O check TESTS='Protect: AI'`: pass, 9 tests after the read-mode Fake Out priority-pressure update.
- 2026-06-06 `rtk make -j16 -O check TESTS='Fake Out'`: pass, 2 move-effect tests after the read-mode Fake Out priority-pressure update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI will Fake Out'`: pass, 4 existing AI Fake Out selection tests after the read-mode Fake Out priority-pressure update.
- `rtk make tools/trainerproc/trainerproc`: pass. Regenerated trainer data from `.party` fixtures, including `Pool Weight`.
- 2026-06-06 `rtk make tools/trainerproc/trainerproc`: pass. Regenerated debug trainer data for `Battle Gimmick Single` / `Battle Gimmick Double`.
- 2026-06-06 `rtk make tools/trainerproc/trainerproc`: pass after adding the 8 weighted gauntlet debug battles.
- 2026-06-06 `rtk make tools/trainerproc/trainerproc`: pass after moving the gauntlets into a submenu and rebalancing player-side gauntlet pools toward same-grade battles.
- 2026-06-06 `rtk make tools/trainerproc/trainerproc`: pass after adding `Mix Single` / `Mix Double` all-gimmick gauntlets.
- 2026-06-06 `rtk make tools/trainerproc/trainerproc`: pass after adding the `Read Double` mirrored 8-Pokemon gauntlet pool.
- 2026-06-30 `rtk make tools/trainerproc/trainerproc`: pass after adding `Read Single` and `Champs Single`.
- 2026-06-06 `rtk make -j16 -O check TESTS='Debug battles do not give exp or EVs'`: pass, 1 test. Covers no EXP bar, no EXP gain, and no EV gain while `gIsDebugBattle` is set.
- 2026-06-30 `rtk make -j16 -O check TESTS='Debug battles do not give exp or EVs'`: pass, 1 test after adding `Read Single` and `Champs Single`.
- 2026-06-06 `rtk make -j16 -O check TESTS='Trainer Party Pool'`: pass, 9 tests. Regresses pool role filtering and weighted selection behavior used by the debug fixtures.
- 2026-06-06 `rtk make -j16 -O check TESTS='Trainer Party Pool'`: pass, 9 tests after adding the gauntlet pools. Covers weighted eligible candidates, tag constraints, runtime RNG variation, custom rules, and fallback.
- 2026-06-06 `rtk make -j16 -O check TESTS='Trainer Party Pool'`: pass, 9 tests after the gauntlet submenu and same-grade player-pool rebalance.
- 2026-06-06 `rtk make -j16 -O check TESTS='Trainer Party Pool'`: pass, 9 tests after adding the mixed all-gimmick gauntlet pools.
- 2026-06-06 `rtk make -j16 -O check TESTS='Trainer Party Pool'`: pass, 9 tests after adding `Read Double`.
- 2026-06-30 `rtk make -j16 -O check TESTS='test/battle/trainer_control.c'`: pass, 23 tests after adding `Read Single` and `Champs Single`.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 8 tests after adding `Read Double`.
- 2026-06-06 `rtk make -j16 -O check`: exited 2 after adding `Read Double`; `/tmp/read-double-check.log` showed three unrelated existing broad-run AI failures: the then-unresolved `AI_FLAG_ATTACKS_PARTNER is willing to kill either the partner or the player 4/6`, `AI_FLAG_SMART_TERA: AI might tera if it gets saved from a ko (1/?)`, and `AI uses Z-Moves -- Z-Detect 1/2`. The focused Trainer Party Pool and read-mode checks above are the accepted evidence for this debug-fixture addition; the `AI_FLAG_ATTACKS_PARTNER` filter was resolved separately on 2026-07-04.
- 2026-06-06 `rtk make -j16 -O check TESTS='Mega Evolution'`: pass, 5 tests after the ability-popup refresh / form-change cleanup fix. The first parallel attempt conflicted with another `make check` process and produced `open tmpfd failed: File exists`; the sequential rerun passed.
- 2026-06-06 `rtk make -j16 -O check TESTS='Pressure'`: pass, 5 tests after the ability-popup refresh / form-change cleanup fix.
- 2026-06-06 `rtk make -j16 -O check TESTS='Delta Stream'`: pass, 1 test after the ability-popup refresh / form-change cleanup fix. The first parallel attempt conflicted with another `make check` process and produced a test-runner segmentation fault; the sequential rerun passed.
- 2026-06-06 `rtk make -j16 -O check TESTS='Terastallization'`: pass, 3 tests after the form-change popup cleanup hook.
- 2026-06-06 `rtk make -j16 -O check TESTS='Ultra Burst'`: pass, 5 tests after the form-change popup cleanup hook.
- 2026-06-06 `rtk make -j16 -O check TESTS='Primal Reversion'`: pass, 15 tests after the form-change popup cleanup hook.
- `rtk cargo check --manifest-path tools/runtime_knowledge/Cargo.toml`: pass.
- `rtk cargo run --manifest-path tools/runtime_knowledge/Cargo.toml -- --out /tmp/runtime_knowledge_catalog_rust --pretty`: pass. Generated 935 moves, 319 abilities, 130 hold effects, 874 items, 4 gimmick policies, and a valid summary.
- `rtk make -j16 -O check`: pass. Existing known-failing / expected-failing labels remained non-fatal.
- `rtk make -j16 -O check`: attempted again after the Protect / Dmax-vs-Z debug-fixture update and exited 2. The captured output only exposed existing test-runner / known-failing labels (`Tests resume after CRASH`, `Pokemon level up learnsets fit within MAX_LEVEL_UP_MOVES and MAX_RELEARNER_MOVES`); both pass as expected when filtered individually. The focused AI, debug EXP / EV, and Trainer Party Pool checks above are the validation evidence for this update.
- `rtk make -j1 -O check`: attempted to rule out make-job parallelism, but the run did not progress beyond the initial link warning and was abandoned as non-evidence. The stale `mgba-rom-test-hydra` / `mgba-rom-test` children were killed before handoff.
- `rtk make -j16 -O debug`: pass.
- 2026-06-06 `rtk make -j16 -O debug`: pass after adding `Battle Gimmick Single` / `Battle Gimmick Double`.
- 2026-06-06 `rtk make -j16 -O debug`: pass after adding the 8 weighted gauntlet debug battles.
- 2026-06-06 `rtk make -j16 -O debug`: pass after moving gauntlets under `Gauntlet Battles` and rebalancing the player gauntlet pools.
- 2026-06-06 `rtk make -j16 -O debug`: pass after adding `Mix Single` / `Mix Double` under `Gauntlet Battles`.
- 2026-06-06 `rtk make -j16 -O debug`: pass after the ability-popup refresh / form-change cleanup fix.
- 2026-06-06 `rtk make -j16 -O debug`: pass. Confirms the debug Dmax/Z fixtures build with debug Z-Power / Dynamax access and `.party` `Z Move: Yes` candidates.
- 2026-06-06 `rtk make -j16 -O debug`: pass after the double-switch execution guard.
- 2026-06-06 `rtk make -j16 -O debug`: pass after preserving debug opponent gimmick bits through battle allocation.
- 2026-06-06 `rtk make -j16 -O debug`: pass after the Protect-retarget update.
- 2026-06-06 `rtk make -j16 -O debug`: pass after adding `Read Double`.
- 2026-06-06 `rtk make -j16 -O all`: pass.
- 2026-06-06 `rtk make -j16 -O all`: pass after adding late-commit smart-gimmick checks and debug audit fixtures.
- 2026-06-06 `rtk make -j16 -O all`: pass after the double-switch execution guard.
- 2026-06-06 `rtk make -j16 -O all`: pass after adding the 8 weighted gauntlet debug battles.
- 2026-06-06 `rtk make -j16 -O all`: pass after the gauntlet menu overflow fix and pool rebalance.
- 2026-06-06 `rtk make -j16 -O all`: pass after the ability-popup refresh / form-change cleanup fix.
- 2026-06-06 `rtk make -j16 -O all`: pass after the debug opponent gimmick-bit preservation and Kyogre spread-pressure fix.
- 2026-06-06 `rtk make -j16 -O all`: pass after the Protect-retarget update.
- 2026-06-06 `rtk make -j16 -O all`: pass after adding `Read Double`.
- 2026-06-06 `rtk mdbook build docs`: pass with existing missing-root-`CHANGELOG.md` include warning, `CREDITS.md` `</img>` warning, and large search index warning.
- 2026-06-06 `rtk mdbook build docs`: pass after the double-switch execution guard, with the same existing warnings.
- 2026-06-06 `rtk mdbook build docs`: pass after the late-commit and debug audit update, with the same existing warnings.
- 2026-06-06 `rtk mdbook build docs`: pass after documenting the gauntlet debug battles, with the same existing warnings.
- 2026-06-06 `rtk mdbook build docs`: pass after the gauntlet menu overflow fix and pool rebalance, with the same existing missing-root-`CHANGELOG.md`, `CREDITS.md` `</img>`, and large search index warnings.
- 2026-06-06 `rtk mdbook build docs`: pass after documenting the ability-popup refresh / form-change cleanup fix, with the same existing missing-root-`CHANGELOG.md`, `CREDITS.md` `</img>`, and large search index warnings.
- 2026-06-06 `rtk mdbook build docs`: pass after documenting the debug opponent gimmick-bit preservation and Kyogre spread-pressure fix, with the same existing missing-root-`CHANGELOG.md`, `CREDITS.md` `</img>`, and large search index warnings.
- 2026-06-06 `rtk mdbook build docs`: pass after documenting `Read Double`, with the same existing missing-root-`CHANGELOG.md`, `CREDITS.md` `</img>`, and large search index warnings.
- mGBA Live: wrapper `/home/jastin/.local/bin/mgba-qt` booted `pokeemerald.gba` to the title screen and captured a screenshot in session `smart-ai-ability-item-knowledge-20260605`. `mgba_live_stop` returned `stopped:true`.
- mGBA Live: current ROM booted to the title screen and captured `/tmp/debug-vgc-fixtures-20260605.png` in session `debug-vgc-fixtures-20260605`. `mgba_live_stop` returned `stopped:true`, and CLI `status --all` returned `[]`. The debug Party menu battle itself still needs a progressed save or a focused input route for visual confirmation.
- mGBA Live: current ROM booted in session `smart-ai-protect-dmaxz-20260605`. `mgba_live_start_with_lua_and_view` reported a Lua bridge invalid-context error after starting, but `mgba_live_get_view` returned a rendered frame, `mgba_live_export_screenshot` saved `/tmp/smart-ai-protect-dmaxz-20260605.png`, and `mgba_live_stop` returned `stopped:true`.
- 2026-06-06 mGBA Live: MCP startup without `DISPLAY` failed with Qt `xcb` display initialization. CLI startup with `DISPLAY=:0` booted `pokeemerald.gba` to the title / demo screen in session `smart-gimmick-access-cli-smoke`, saved `/tmp/smart-gimmick-access-smoke.png`, and `mgba-live-cli stop` returned `stopped:true`.
- 2026-06-06 mGBA Live: sandboxed CLI startup first failed because the tool could not create `~/.mgba-live-mcp/runtime/sessions/...`. Rerunning with approval and `DISPLAY=:0` booted `pokeemerald.gba` in session `smart-gimmick-switch-guard-20260606`, saved `/tmp/smart-gimmick-switch-guard-20260606.png`, and `mgba-live-cli stop` returned `stopped:true`. `status --all` returned `[]`. This was a boot smoke only; the exact debug double-battle ASSERT path still needs manual progression to the Party debug fixture.
- 2026-06-06 mGBA Live: sandboxed CLI startup first failed because it could not create `~/.mgba-live-mcp/runtime/sessions/smart-gimmick-late-audit-20260606`. Rerunning with approval and `DISPLAY=:0` booted `pokeemerald.gba` to the title screen, saved `/tmp/smart-gimmick-late-audit-20260606.png`, and `mgba-live-cli stop` returned `stopped:true`. `status --all` returned `[]`. This was a boot smoke only; the new `Battle Gimmick Single` / `Battle Gimmick Double` menu entries still need manual progression from a debug-enabled save.
- 2026-06-06 mGBA Live: sandboxed CLI startup first failed because it could not create `~/.mgba-live-mcp/runtime/sessions/smart-gimmick-gauntlet-20260606`. Rerunning with approval and `DISPLAY=:0` booted `pokeemerald.gba` to the title screen, saved `/tmp/smart-gimmick-gauntlet-20260606.png`, and `mgba-live-cli stop` returned `stopped:true`. `status --all` returned `[]`. This was a boot smoke only; the 8 gauntlet menu entries still need manual progression from a debug-enabled save.
- 2026-06-06 mGBA Live: sandboxed CLI startup first failed because it could not create `~/.mgba-live-mcp/runtime/sessions/smart-gimmick-gauntlet-menu-fix2-20260606`. Rerunning with approval and `DISPLAY=:0` booted `pokeemerald.gba` to the title screen, saved `/tmp/smart-gimmick-gauntlet-menu-fix2-20260606.png`, and `mgba-live-cli stop` returned `stopped:true`. `status --all` returned `[]`. This was a boot smoke only; the `Party -> Gauntlet Battles` submenu still needs manual progression from a debug-enabled save.
- 2026-06-06 mGBA Live: sandboxed CLI startup first failed because it could not create `~/.mgba-live-mcp/runtime/sessions/ability-popup-formchange-20260606`. Rerunning with approval and `DISPLAY=:0` booted `pokeemerald.gba` to the title screen, saved `/tmp/ability-popup-formchange-20260606.png`, and `mgba-live-cli stop` returned `stopped:true`. `status --all` returned `[]`. This was a boot smoke only; the Mega Mewtwo / Delta Stream visual sequence still needs manual progression through a debug gauntlet save.
- 2026-06-06 mGBA Live: `DISPLAY=:0` booted `pokeemerald.gba` in session `smart-gimmick-pending-bits-20260606`, saved `/tmp/smart-gimmick-pending-bits-20260606.png` at the title screen, `mgba-live-cli stop` returned `stopped:true`, and `status --all` returned `[]`. This was a boot smoke only; `Battle Gimmick Double` still needs manual progression from a debug-enabled save to visually confirm Charizard Gigantamax and Tapu Koko Z-Move activation after the opponent-bit preservation fix.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 9 tests after the Read Double Trick Room mirror guard.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI uses Trick Room'`: pass, 3 tests after the Trick Room mirror guard.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_SMART_SWITCHING'`: pass after the Protect preservation guard for optional double switches.
- 2026-06-06 `rtk make -j16 -O check TESTS='Protect: AI'`: pass, 9 tests after the Protect preservation guard. An attempted parallel rerun of two other `check` filters collided on `pokeemerald-test.elf` and produced `mmap elffd failed`; sequential reruns passed.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI prefers to set up a powerful Status'`: pass. Confirms proactive `AI_FLAG_POWERFUL_STATUS` Trick Room is preserved when there is no read / predicted opposing Trick Room.
- 2026-06-06 `rtk make -j16 -O all`: pass after the Read Double Trick Room / Protect over-pivot tuning.
- 2026-06-06 `rtk make -j16 -O debug`: pass after the Read Double Trick Room / Protect over-pivot tuning.
- 2026-06-06 mGBA Live session `read-trick-protect-smoke-20260606`: pass for boot / bridge smoke after the Read Double Trick Room / Protect over-pivot tuning. `tools/mgba_live/start_mgba_live.sh read-trick-protect-smoke-20260606 120` started pid `274593`, `status --all` showed `fps_target=120.0`, screenshot `/tmp/read-trick-protect-smoke-20260606.png` was exported, `run-lua --code 'return {ok=true}'` returned `{ok=true}`, `stop` returned `stopped:true`, and final `status --all` returned `[]`. The behavior itself is covered by focused tests; this was ROM boot / Lua bridge evidence.
- 2026-06-06 `rtk make -j16 -O debug`: pass after adding `Partner Double`, ally-benefit scoring, and the Pollen Puff heal guard.
- 2026-06-06 `rtk make -j16 -O all`: pass after adding `Partner Double`, ally-benefit scoring, and the Pollen Puff heal guard.
- 2026-06-07 `rtk make -j16 -O all`: pass after the pid `354725` Pollen Puff target-validity guard.
- 2026-06-07 `rtk make -j16 -O debug`: pass after the pid `354725` Pollen Puff target-validity guard.
- 2026-06-07 `rtk mdbook build docs`: exit 0 after documenting the pid `354725` Pollen Puff target-validity guard. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.
- 2026-06-06 mGBA Live session `partner-tech-ai-20260606`: pass / boot plus input smoke after the pid `317741` ally-benefit update. A sandboxed start first failed because `mgba-live-cli` needed to update `~/.mgba-live-mcp/runtime/active_session`; rerunning with approval started pid `348899` at 120 FPS from `/home/jastin/dev/pokeemerald-expansion/pokeemerald.gba`. Screenshot `/tmp/partner-tech-ai-20260606-boot.png` showed a nonblank boot/title frame, `input-tap --key A` reached the title menu, screenshot `/tmp/partner-tech-ai-20260606-after-a.png` showed `CONTINUE / NEW GAME / OPTION`, and `stop` returned `stopped:true`. A final status probe for that session returned `session_not_found`, which is expected after clean stop/archive. The unrelated `manual-ai-log` session was not stopped.
- 2026-06-07 mGBA Live session `pollen-self-target-guard-20260607`: pass / boot plus input smoke after the pid `354725` Pollen Puff target-validity guard. `tools/mgba_live/start_mgba_live.sh pollen-self-target-guard-20260607 120` started pid `363185` from the updated `pokeemerald.gba`; `status --all` showed the session alive at 120 FPS; screenshot `/tmp/pollen-self-target-guard-boot.png` was exported; `input-tap --key A` succeeded; `stop` returned `stopped:true`; and final `status --all` returned `[]`.
- 2026-06-07 mGBA Live session `future-pressure-ai-20260607`: pass / boot plus input smoke after the future spread-pressure update. `tools/mgba_live/start_mgba_live.sh future-pressure-ai-20260607 120` started pid `505206` from the updated `pokeemerald.gba`; `status --all` showed the session alive at 120 FPS; Lua returned `{ok=true, validation=future_pressure_ai_20260607}`; screenshots `/tmp/future-pressure-ai-20260607-boot.png` and `/tmp/future-pressure-ai-20260607-after-a.png` were exported; `input-tap --key A` succeeded; `stop` returned `stopped:true`; and final `status --all` returned `[]`.
- 2026-06-30 `rtk make -j16 -O all`: pass after adding `Read Single` and `Champs Single`.
- 2026-06-30 `rtk make -j16 -O debug`: pass after adding `Read Single` and `Champs Single`.
- 2026-06-30 mGBA Live session `double-battle-mgba-20260630b`: pass for a pre-fix `Read Double` action-log audit. The debug route reached `Party -> Gauntlet Battles -> Read Double`, started a doubles battle with player `Tornadus` + `Incineroar` versus opponent `Tornadus` + `Tapu Koko`, selected player `Tailwind` plus `Fake Out`, resolved to a switch screen, exported `/tmp/double-battle-mgba-20260630-read-double-turn1.json`, and captured `/tmp/double-battle-mgba-20260630-read-double-switch.png`. The log confirmed the opponent-side `Tapu Koko` selected `GIMMICK_Z_MOVE` + `MOVE_VOLT_SWITCH` into player-left while the earlier verbal note incorrectly named Xerneas; the board was actually Tornadus / Tapu Koko.
- 2026-06-30 mGBA Live session `read-gimmick-pressure-20260630`: pass / partial runtime for the post-fix `Read Double` route and visible opponent gimmick spend. The route reached `Party -> Gauntlet Battles -> Read Double`, started player `Tapu Koko` + `Incineroar` versus opponent `Tornadus` + `Incineroar`, selected player `Dazzling Gleam` plus `Fake Out`, and exported `/tmp/read-gimmick-pressure-20260630-turn1.json` plus screenshots through `/tmp/read-gimmick-pressure-20260630-turn1-final.png`. The final action-log window included opponent-left `MOVE_PROTECT` after selecting `GIMMICK_DYNAMAX` in the recompute log, opponent-right switching to `Rillaboom`, and `Rillaboom` using `MOVE_WOOD_HAMMER` into player-left, which KO'd Koko and reached the party switch screen. Lua bridge returned `{ok=true}`. Cleanup caveat: `mgba-live-cli stop --session read-gimmick-pressure-20260630` returned `stopped:false`; `ps` showed pid `726608` as `Zs [mgba-qt] <defunct>` under the mGBA Live MCP parent, and `status --all` still listed a stale alive session. This is recorded as a zombie cleanup state per the branch runtime instructions, not a clean stop.
- 2026-06-30 mGBA Live session `single-gimmick-pressure-20260630`: pass for a `Dmax Single` action-log audit after the singles regression. The route booted the debug ROM, opened `R+START`, navigated `Party -> Gauntlet Battles -> Dmax Single`, reached player `Calyrex` versus opponent `Kyogre`, selected `MOVE_ASTRAL_BARRAGE`, and exported `/tmp/single-gimmick-pressure-20260630-turn1.json` plus screenshots through `/tmp/single-gimmick-pressure-20260630-turn1-progress.png`. The log recorded player `MOVE_ASTRAL_BARRAGE` and opponent-side switch actions, including resolved switch-ins to `Zacian`, showing the single gauntlet can read and reposition against selected damage pressure. The action log alone does not include enough HP / damage outcome state to prove the later switch was incorrect, so this remains a monitor candidate rather than a runtime bug. A 2026-07-04 fixed production-style regression confirms that, with the same Calyrex pressure shape, the current AI can pivot once into Rillaboom and then attack with `Grassy Glide` instead of repeatedly switching. `mgba-live-cli stop --session single-gimmick-pressure-20260630 --grace 1` returned `stopped:true`; final `status --all` still listed only the older stale `read-gimmick-pressure-20260630` zombie session.
- 2026-06-30 mGBA Live session `more-debug-battles-20260630`: pass for the added `Champs Single` route and visible submenu expansion. The session reached `Party -> Gauntlet Battles`, captured `/tmp/more-debug-battles-20260630-read-single-menu.png` with `Read Single` visible, captured `/tmp/more-debug-battles-20260630-champs-single-selected.png` with `Champs Single` selected, started `CHAMPION Champ Lab`, reached `Dragonite` versus `Lucario`, selected `MOVE_DRAGON_DANCE`, and exported `/tmp/more-debug-battles-20260630-champs-single-turn1.json`. The action log recorded player `MOVE_DRAGON_DANCE` and opponent `MOVE_NASTY_PLOT` on turn 0. `mgba-live-cli stop --session more-debug-battles-20260630 --grace 1` returned `stopped:true`; final status still listed only the older stale `read-gimmick-pressure-20260630` session, and a repeat stop for that old session still returned `stopped:false`.
- 2026-06-30 `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`: pass, 15 tests, after adding the `Contrary` target-side stat-stage guard and `Charm` / `Swagger` production-style ally-target regressions.
- 2026-06-30 `rtk make -j16 -O check TESTS='test/battle/trainer_control.c'`: pass, 23 tests, after adding `Partner Tech`.
- 2026-06-30 `rtk make -j16 -O check TESTS='Debug battles do not give exp or EVs'`: pass, 1 test, after adding `Partner Tech`.
- 2026-06-30 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 21 tests, after the `Partner Tech` teammate-target scoring update.
- 2026-06-30 `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`: pass, 38 tests, after the `Partner Tech` teammate-target scoring update.
- 2026-06-30 `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`: exit 0 with existing known-failing / TO_DO labels unchanged after the `Partner Tech` teammate-target scoring update.
- 2026-07-04 `rtk make -j16 -O check TESTS='AI_FLAG_ATTACKS_PARTNER'`: pass, 3 tests. The former `Expected target playerRight, got playerLeft` mismatch was a legacy expectation around `Overdrive` spread pressure, not an ally-target runtime regression; single-target target selection is now covered by a production-style `Mighty Cleave` board.
- 2026-06-30 `rtk make -j16 -O all`: pass after adding `Partner Tech`.
- 2026-06-30 `rtk make -j16 -O debug`: pass after adding `Partner Tech`.
- 2026-06-30 mGBA Live session `partner-tech-menu-20260630`: pass for the added `Partner Tech` route and visible runtime trainer load. The session booted the ROM, continued from the debug save, opened `R+START`, reached `Party -> Gauntlet Battles`, captured `/tmp/partner-tech-menu-20260630-partner-tech-menu.png` with `Partner Tech` visible, captured `/tmp/partner-tech-menu-20260630-partner-tech-selected.png` with the entry selected, started `CHAMPION Tech Lab`, and reached player `Staraptor` + `Rillaboom` versus opponent `Lurantis` + `Staraptor`. `mgba-live-cli stop --session partner-tech-menu-20260630 --grace 1` returned `stopped:true`; final status still listed the older stale `read-gimmick-pressure-20260630` zombie session.
- 2026-06-30 `rtk mdbook build docs`: exit 0 after documenting `Partner Tech`. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.
- 2026-06-06 `rtk mdbook build docs`: exit 0 after documenting the pid `317741` ally-benefit update and `Partner Double`. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.
- 2026-06-06 `rtk mdbook build docs`: exit 0 after documenting the Read Double Trick Room / Protect over-pivot tuning. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.

## Strategy Sources

The runtime policy above was cross-checked against:

- [Pokemon Wiki / Pokemon battle consideration wiki Dynamax mechanics](https://poke-wiki.net/%E3%83%80%E3%82%A4%E3%83%9E%E3%83%83%E3%82%AF%E3%82%B9): 3-turn duration, switch cancellation, HP scaling by Dynamax level, flinch and forced-switch immunity, Max Guard, Max Move side effects, and double-battle side effects.
- [Official Pokemon Sword / Shield Dynamax and Max Moves page](https://swordshield.pokemon.com/en-us/gameplay/dynamaxing-max-moves/): Max Moves are powerful but also carry additional effects; status moves become Max Guard; held item freedom means the Dynamax choice depends on the battle state.
- [Pokemon Wiki / ちゅうもくのまと](https://wiki.pokemonwiki.com/wiki/%E3%81%A1%E3%82%85%E3%81%86%E3%82%82%E3%81%8F%E3%81%AE%E3%81%BE%E3%81%A8) and [いかりのこな](https://wiki.pokemonwiki.com/wiki/%E3%81%84%E3%81%8B%E3%82%8A%E3%81%AE%E3%81%93%E3%81%AA): Follow Me / Rage Powder create the attention-target state for opposing single-target moves; same-side ally moves are not retargeted by that state.
- [Pokemon Wiki / しれいとう](https://wiki.pokemonwiki.com/wiki/%E3%81%97%E3%82%8C%E3%81%84%E3%81%A8%E3%81%86): Commander swallows Tatsugiri into Dondozo, direct target moves into swallowed Tatsugiri fail except special cases such as Transform, spread damage into Dondozo remains spread-modified, status / volatile / item effects continue while swallowed, Toxic Orb can activate there, and Tatsugiri returns when Dondozo faints.
- [Pokemon Wiki / ねらいのまと](https://wiki.pokemonwiki.com/wiki/%E3%81%AD%E3%82%89%E3%81%84%E3%81%AE%E3%81%BE%E3%81%A8): Ring Target is a held-item type-immunity rule, not the attention-target state. It only removes type-effectiveness immunities on the holder and does not bypass ability / powder / Prankster-style immunities.
- Victory Road VGC writing: [Incineroar / Fake Out discussion](https://victoryroad.pro/2020/03/02/incineroar-vgc-column/) for Dynamax flinch immunity; [Durant team report](https://victoryroad.pro/2020/01/31/bingjie-dallas-report-finalist/) and [Series 10 report](https://victoryroad.pro/2021/07/20/series-10-introduction/) for Max Steelspike / Max Quake defensive value; [Corviknight / Max Airstream report](https://victoryroad.pro/2020/03/14/zach-kelly-ocic20-report/) for speed and positioning value; team reports as examples that Dynamax is frequently a board-control and survival resource, not only a damage button.
- PJCS2026 / Pokemon Champions streams, NAIC tournament coverage, and older VGC Worlds / International replays are accepted for turn-shape review across Mega, Z-Move, Dynamax, and Tera formats. Use them to classify recurring doubles choices such as spending a format gimmick under known KO pressure, protecting a key slot while the partner pivots, sacrificing a role-done Pokemon as a cushion, or using spread pressure over narrow switch reads. Do not import mechanic numbers directly when the source format differs from this ROM.
- Pokemon Battle DataBase / official usage-style data remains the preferred future source for species, item, and move priors. Pokemon Showdown articles remain excluded as strategy source material per project policy.

## Known Gaps

- Status Z-Move effect families are now exposed as AI move knowledge and the existing status Z decision branch is separated into a shared helper. Fine-grained tactics for `Z-Haze`, `Z-Tailwind`, `Z-Trick Room`, and `Z-Parting Shot` still need targeted scoring.
- Tera doubles support uses the selected target and explicit candidate data. It now rejects pure defensive Tera lines that introduce a new large-hit weakness, but it does not fully simulate every partner threat or all possible double-target lines.
- G-Max unique, residual, and hazard effects are exposed through AI move knowledge flags. G-Max Stonesurge / Steelsurge can now spend Dynamax as hazard payoff when the existing hazard setup checks agree. Other non-residual G-Max unique secondary effects still need tactic-specific scoring before relying on them as strategic reasons.
- The preserved fallback evaluator uses a shared desperation risk governor and a
  small short-horizon classifier. Eligible supported doubles boards now search
  depth 3 with selective depth 5, but unencoded Perish, pivot, recovery, or
  mechanic combinations still fail closed to that narrower score layer.
- Soundproof switching is implemented for the narrow read-Perish escape case.
  Broader sound-pressure switching and most ally-order manipulation remain
  outside the current compact simulator envelope. Ally-targeted Speed drops
  such as Scary Face under active Trick Room retain a narrow immediate-KO score
  bridge on the fallback path; supporting broader versions requires explicit
  survival, order, switch / Protect, and next-board transitions.
- The new 3v3 / 4v4 debug fixtures are runtime smoke fixtures, not exhaustive balance fixtures. They provide quick repeated checks for competitive-style teams, weighted pool selection, smart gimmick timing, and no-EXP debug battles.

## 2026-07-04 AI Debt Pass

- `ShouldSmartMegaPreserveCurrentAbility()` now checks raw `gBattleWeather` instead of `AI_GetWeather()` when deciding whether to preserve `Air Lock` / `Cloud Nine`, because the latter intentionally returns no active weather while those abilities suppress weather effects. Focused `AI_FLAG_SMART_MEGA` coverage now checks both abilities against active Primal rain.
- Smart Tera now tracks whether Tera introduces a new large-hit weakness. Pure defensive Tera lines that reduce one big hit but make another known opponent move exceed the large-hit threshold are rejected unless a stronger reason such as KO conversion or true KO prevention applies.
- `AI_GetMoveKnowledgeFlags()` now exposes Max Move speed control, weather, terrain, stat-control, G-Max unique, and residual G-Max categories. The `tools/runtime_knowledge` generator mirrors those tags into `moves.json`.
- Validation: `AI_FLAG_SMART_MEGA`, `test/battle/ai/ai_smart_tera.c`, `AI runtime knowledge`, full `test/battle/ai/ai_smart_gimmick.c`, runtime catalog generation, `rtk mdbook build docs`, `rtk make -j16 -O all`, and mGBA Live session `ai-debt-smoke-20260704` all passed on 2026-07-04. mdBook and linker warnings were the existing warnings recorded in the test plan.

## 2026-07-04 Status Z Knowledge Pass

- Added status Z-Move knowledge flags for status Z, stat reset, stat boost, critical boost, redirection, HP recovery, and replacement healing. `AI_GetMoveKnowledgeFlags()` now derives those flags from `GetMoveZEffect()` for status moves, including dual potential for `Z_EFFECT_CURSE`.
- Split the status branch of `ShouldUseZMove()` into a three-way helper: use, decline, or defer to the existing generic Z-Move fallback. This keeps the previous "selected status move may still be Z-upgraded" behavior while making the status Z tactical decision surface reusable.
- `tools/runtime_knowledge` now reads `.zMove = { .effect = ... }` from `moves_info.h`, emits `zMoveEffect` in `moves.json`, and mirrors the new status Z knowledge tags. Spot checks confirmed `MOVE_HAZE`, `MOVE_DESTINY_BOND`, `MOVE_TAILWIND`, and `MOVE_PARTING_SHOT` carry the expected `z_*` tags.
- Validation: `rtk make -j16 -O check TESTS='AI runtime knowledge maps move categories from move data'`, `rtk make -j16 -O check TESTS='AI runtime knowledge'`, `rtk make -j16 -O check TESTS='AI uses Z-Moves'`, `rtk cargo run --manifest-path tools/runtime_knowledge/Cargo.toml -- --out /tmp/runtime_knowledge_catalog_z_status --pretty`, generated JSON spot checks, `rtk mdbook build docs`, `rtk make -j16 -O all`, `rtk git diff --check`, and mGBA Live session `z-status-knowledge-smoke-20260704` passed on 2026-07-04. The mGBA Live smoke booted `pokeemerald.gba`, returned Lua `{ok=true, validation=z_status_knowledge_smoke_20260704}`, accepted `START`, saved `/tmp/z-status-knowledge-smoke-20260704-after-start.png`, stopped with `stopped:true`, and final `status --all` returned `[]`. mdBook and linker warnings were the existing warnings recorded in the test plan.

## 2026-07-04 G-Max Hazard Payoff Pass

- Added `AI_MOVE_KNOWLEDGE_GMAX_HAZARD` for `G-Max Stonesurge` and `G-Max Steelsurge`, mirrored by `tools/runtime_knowledge` as the `gmax_hazard` tag.
- `DoesDynamaxOfferStrategicMaxMovePayoff()` now treats G-Max Stonesurge / Steelsurge as hazard payoff by reusing `AI_ShouldSetUpHazards()` and checking that the corresponding hazard is not already active on the opposing side.
- Focused coverage confirms G-Max Stonesurge spends Dynamax to set rocks when the opposing side has reserves, and conserves Dynamax when Stealth Rock is already set.
- Validation: `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_ONLY: G-Max Stonesurge'`, `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`, `rtk make -j16 -O check TESTS='AI runtime knowledge'`, `rtk cargo run --manifest-path tools/runtime_knowledge/Cargo.toml -- --out /tmp/runtime_knowledge_catalog_gmax_hazard --pretty`, and generated JSON spot checks for `MOVE_G_MAX_STONESURGE` / `MOVE_G_MAX_STEELSURGE` passed on 2026-07-04. A parallel `make check` attempt for the Stonesurge and runtime-knowledge filters is non-evidence because the two checks collided on shared test temp files with `open tmpfd failed`; sequential reruns passed.

## 2026-07-04 Desperation / Commander Pass

- Added a near-term desperation gate for "nearly lost" positions. It keys off battle-ending pressure over roughly three turns, Perish Song trap / no-reserve clocks, selected setup pressure, and known KO pressure only when the broader board is collapsing. It is not an HP-only trigger.
- Added clean damage-race suppression before hax scoring. If a normal non-OHKO move can KO or win the short damage race, the AI takes that line instead of fishing for flinch, status, crit, or OHKO luck.
- Added comeback scoring for low-accuracy status swings, faster flinch, paralysis / freeze secondary effects, high-crit moves, OHKO moves, and doubles-only stop / speed-control tools such as Taunt, Tailwind, Trick Room, speed drops, and Throat Chop.
- Added a desperation-only spread sacrifice exception for doubles. It can accept KOing a partner only when the move creates immediate opposing-side board pressure and the AI has reserve value to bring in afterward.
- Added Commander targeting awareness so swallowed Tatsugiri is avoided as a direct target, Dondozo is targeted instead, and KOing Dondozo gets small extra value because Tatsugiri remains afterward, especially when poisoned / burned or holding Toxic Orb / Flame Orb.
- Validation: focused `make check` filters passed for low-accuracy sleep, read Perish Song flinch fishing with and without switch escape, Commander Dondozo targeting, and desperation spread sacrifice. A Toxic Orb Commander score regression was attempted in this pass but removed because the Commander / residual test harness advanced an extra turn and failed with `1 TURNs specified, but 2 ran`; that specific coverage is now handled by the 2026-07-05 lower-level Commander score coverage pass.
- Handoff validation: `rtk git diff --check`, `rtk mdbook build docs`, `rtk make -j16 -O all`, and mGBA Live session `desperation-commander-smoke2-20260704` passed on 2026-07-04. mdBook and linker warnings were the existing warnings recorded in the test plan. The mGBA Live smoke booted the updated normal ROM, captured boot and post-`START` screenshots, accepted input, returned Lua `{ok=true, validation=desperation_commander_smoke2_20260704}`, stopped cleanly, and final `status --all` returned `[]`.

## 2026-07-04 Short-Horizon Risk Governor Pass

- Added `AiBoardSnapshot`, `AiThreatClass`, and `AiShortHorizon` helpers so risky move scoring, switch scoring, and sacrifice scoring can ask the same question: is this board actually collapsing within the next few turns?
- Centralized risk unlocks behind `AI_RiskGovernorAllows()`. The governor covers high-variance comeback moves, low-accuracy status / damage, secondary hax, OHKO fishing, delayed attacks, partner sacrifice, second Protect, and switch survival. Existing `AI_IsNearTermDesperationPressure()` and `AI_ShouldAcceptDesperationRisk()` now route through that shared policy.
- Narrowed doubles desperation. An AI slot being pinned is not by itself battle-level desperation when the AI still has a live partner; this keeps clean read-mode pressure ahead of weak freeze or flinch fishing on otherwise playable boards.
- Added a switch-vs-move Perish escape path. When a confirmed / predicted Perish Song is the loss line, the AI can choose a Soundproof reserve and mark the action log with `perish_escape`, while clean faster damage still wins when it can stop the line directly.
- Added `BattleActionLog` v2 AI reason tags. Commanded and resolved switch entries can now expose `desperation_comeback`, `hax_out`, `ally_sacrifice_board_reset`, `perish_escape`, `commander_slot_correction`, and the other compact reasons defined in `enum AiDecisionReason`.
- Commander scoring still treats Toxic Orb / Flame Orb swallowed Tatsugiri as extra remaining value after Dondozo faints. The mechanics were rechecked against Pokemon Wiki: swallowed Tatsugiri cannot be hit by normal direct moves, spread damage into Dondozo remains spread-modified, item / status effects continue while swallowed, and Tatsugiri returns when Dondozo faints. The lower-level Toxic Orb / poison score regression is now covered by the 2026-07-05 Commander score coverage pass.
- Validation: focused `make check` filters passed for low-accuracy sleep, Perish flinch with and without switch escape, hax-out reason logging, Soundproof Perish switching, Commander Dondozo targeting, partner-sacrifice spread pressure, `ai.c`, `ai_switching.c`, `ai_doubles.c`, selected-Rage-Powder read mode, and full `ai_smart_gimmick.c`. Handoff validation passed with `rtk git diff --check`, `rtk mdbook build docs`, `rtk make -j16 -O all`, and mGBA Live session `short-horizon-risk-smoke-20260704`; existing mdBook and linker warnings are recorded in `test_plan.md`.

## 2026-07-04 Focused Slot Collapse Switch Pass

- Read-mode switching now recalculates confirmed player damage with the selected player gimmick. If the player already selected a Z-Move, Dynamax / Max Move, or Tera-relevant damaging action, `ShouldSwitch()` uses `AI_CalcDamage(... USE_GIMMICK ...)` for the known incoming slot pressure instead of relying only on precomputed non-gimmick `AI_GetDamage()` rolls.
- Known player damage into a slot is now summarized with total damage, hit count, and whether Fake Out-style flinch pressure is part of the confirmed turn. This lets doubles switching recognize "Z-Move plus Fake Out focus" as a slot-collapse line even when no single move alone is the whole tactical story.
- Added a focused-slot collapse switch route after the doubles Protect-preservation guard. The order is intentional: if the active Pokemon has a safe usable Protect / Detect style line, move scoring can still buy the turn. If it cannot safely Protect and confirmed focus damage would remove a high-value slot, the AI compares switch-ins.
- Switch-in ranking still prefers real defensive answers first: no damage, immunity / absorption, or enough repeated-hit survival after hazards, healing, weather, status, items, and incoming Wish. If no safe answer exists, the route can choose a lower-value Focus Sash or support-style reserve as a deliberate cushion when that preserves a higher-value active Pokemon.
- The first production-style regression covers the manual Read Double shape: Choice Specs Kyogre is pinned by selected Electric Z `Thunderbolt` plus `Fake Out`, has no Protect, and has no Electric-immune reserve. The AI now switches Kyogre to Focus Sash Whimsicott, preserving Kyogre and consuming the one-turn Fake Out pressure instead of staying in with `Water Spout`.
- Validation: the new focused regression failed before the fix with `Expected SWITCH, got MOVE`, then passed after the switch update. `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`, `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`, `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`, `rtk git diff --check`, `rtk make -j16 -O all`, and `rtk mdbook build docs` all passed on 2026-07-04. `ai_switching.c` still reports its existing `KNOWN_FAILING` label while exiting 0. mGBA Live session `focused-slot-collapse-smoke-20260704` booted the rebuilt ROM, returned Lua bridge checks, exported a boot screenshot, accepted `START`, stopped cleanly, and left `status --all` empty; the post-START screenshot timed out, so runtime evidence is boot / bridge / input rather than a feature-specific battle screen.

## 2026-07-04 Safe Protect Counterplay Switch Pass

- Doubles switch preservation now treats Protect / Detect as safe only after the known counterplay is checked. A selected Feint, a move that ignores protection, or an Unseen Fist-style contact bypass can make the pinned slot fall through to the imperfect-switch ladder instead of staying in behind a false Protect line.
- The check scans all confirmed player-side move choices under `AI_FLAG_READ_PLAYER_MOVE`, not only the single `incomingMove` stored in the switch context. This covers the important doubles shape where one player slot supplies the major damage and the other selected move breaks Protect on the same AI target.
- `BattlerHasUsableProtectMove()` now respects the battler's current move limitations before declaring a Protect line available. Disabled, Imprisoned, locked, or otherwise unusable Protect should no longer block switch preservation just because the move exists in the set and has PP.
- Added a production-style regression where Mystic Water Kyogre has `Protect`, but selected Electric Z `Thunderbolt` plus selected `Feint` still pins it. Before the fix the AI tried to move through the turn with `Expected SWITCH, got MOVE`; after the fix it switches to Whimsicott and preserves Kyogre.
- Validation: `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI does not trust Protect when selected Feint breaks the pinned Kyogre line'`, `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`, `rtk make -j16 -O check TESTS='Protect: AI'`, `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`, `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`, `rtk git diff --check`, `rtk make -j16 -O all`, and `rtk mdbook build docs` passed on 2026-07-04. `ai_switching.c` still reports its existing `KNOWN_FAILING` label while exiting 0; mdBook and linker warnings were the existing warnings recorded in the test plan. mGBA Live session `safe-protect-counterplay-smoke-20260704` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/safe-protect-counterplay-smoke-20260704-boot.png` and `/tmp/safe-protect-counterplay-smoke-20260704-after-start.png`, stopped cleanly, and final `status --all` returned `[]`.

## 2026-07-05 End-of-Turn Recovery Protect Pass

- Added a shared one-turn Protect recovery estimate for deterministic end-of-turn healing. Singles Protect payoff now sees Leftovers, Black Sludge, Grassy Terrain, Poison Heal, Rain Dish, Ice Body, Aqua Ring, and Ingrain before deciding whether a protected turn restores a useful HP band.
- The same Grassy Terrain recovery is now included in smart switch-in repeated-hit survival estimates. Existing switch-in recovery already handled recurring item recovery, Poison Heal, weather healing, single-use item healing, hazards, status, weather damage, and incoming Wish; this closes the terrain-recovery gap for switch survival thresholds.
- Added a focused singles regression where Grassy Terrain recovery makes Protect score above default instead of being treated as a passive no-payoff turn. This covers the manual concern that terrain / item recovery can change whether a stall turn or survival roll is acceptable.
- This is still a one-turn heuristic, not the full requested two-to-three-turn recovery planner. It does not yet simulate combined recovery plus future opponent choices, timer expiry, Leech Seed ownership swings, or exact 14/16 and 15/16 roll policy across multiple turns.
- Validation: `rtk make -j1 -O check TESTS='Protect: AI values singles Protect when Grassy Terrain recovery creates payoff'`, `rtk make -j16 -O check TESTS='Protect: AI'`, `rtk make -j16 -O check TESTS='test/battle/ai/check_bad_move.c'`, `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`, `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`, `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. mGBA Live session `end-turn-recovery-protect-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/end-turn-recovery-protect-smoke-20260705-boot.png` and `/tmp/end-turn-recovery-protect-smoke-20260705-after-start.png`, stopped cleanly, and final `status --all` returned `[]`.

## 2026-07-05 Tailwind Timer-Stall Protect Pass

- Singles Protect can now deliberately burn the opponent's final Tailwind turn when the incoming selected / predicted damaging move is meaningful and the AI will outspeed that opponent after Tailwind expires. This turns Protect from a pure recovery / residual payoff into a narrow board-building stall option.
- The speed check recalculates the two relevant `gAiLogicData->speedStats` entries under the current side statuses and again with the opposing Tailwind temporarily removed. This matters because normal AI speed comparisons use the cached turn snapshot rather than recomputing after a hypothetical field-status change.
- `ProtectChecks()` now maps self-target singles Protect back to the actual opposite battler before asking about Protect payoff or contact-bypass counterplay. This keeps Unseen Fist-style contact and ignore-Protect checks tied to the real threat source rather than the self-target slot.
- Added a focused singles regression where the player side has final-turn Tailwind, the AI is about to be KOed by fixed damage, and Protect lets the AI take speed control on the following turn. The test asserts actual move choice rather than score-comparison internals because the test DSL's score helper evaluates singles move scores through a target slot, while the move-choice path uses the runtime self-target Protect path.
- This is still the first timer-stall slice. It does not yet cover Trick Room expiry, Dynamax turn burn, the AI wasting its own favorable Tailwind / Trick Room turns, weather / terrain / screen timer tradeoffs, or doubles lines where one slot protects while the partner pivots or attacks.
- Validation: `rtk make -j1 -O check TESTS='Protect: AI values singles Protect to burn the last opposing Tailwind turn'`, `rtk make -j16 -O check TESTS='Protect: AI'`, `rtk make -j16 -O check TESTS='test/battle/ai/check_bad_move.c'`, `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`, `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`, `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. One parallel `make check` attempt is non-evidence because concurrent checks collided on shared temp files with `open tmpfd failed`; sequential reruns passed. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. The normal ROM build passed with the existing linker RWX warning. mGBA Live session `tailwind-timer-protect-smoke-20260705` booted the rebuilt ROM, `status --all` showed pid `511906` alive at 120 FPS, Lua returned `{ok=true, validation=tailwind_timer_protect_smoke_20260705}`, screenshots `/tmp/tailwind-timer-protect-smoke-20260705-boot.png` and `/tmp/tailwind-timer-protect-smoke-20260705-after-start.png` were captured, `START` input was accepted, a follow-up Lua bridge check returned `{ok=true, validation=tailwind_timer_protect_after_start_20260705}`, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`.

## 2026-07-05 Commander Toxic Orb Score Coverage

- Added a low-level test accessor for the Commander Dondozo faint bonus so the Toxic Orb / poison score path can be verified without executing a full KO turn. The earlier high-level regression was unstable because Dondozo fainting, Commander release, and residual processing advanced the harness by an extra turn.
- `GetCommanderDondozoFaintBonus()` now reads the Commander Tatsugiri hold effect from `gAiLogicData` during normal AI scoring, and falls back to `GetBattlerHoldEffect()` only when called outside an AI scoring context. This preserves the runtime scoring path while making the low-level regression deterministic.
- Added coverage for swallowed Tatsugiri with Toxic Orb, toxic poison, and regular poison. Toxic Orb and toxic poison receive the stronger Commander remaining-value bonus; regular poison receives the weaker remaining-value bonus.
- Validation: `rtk make -j1 -O check TESTS='AI scores Commander Dondozo KO knowing poisoned Tatsugiri remains'`, `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`, `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`, `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. `ai_doubles.c` still reports its existing `KNOWN_FAILING` / `TO_DO` labels while exiting 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. The normal ROM build passed with the existing linker RWX warning. mGBA Live session `commander-toxic-orb-score-smoke-20260705` booted the rebuilt ROM, `status --all` showed pid `525287` alive at 120 FPS, captured `/tmp/commander-toxic-orb-score-smoke-20260705-boot.png`, accepted `START`, returned Lua bridge checks after a single non-evidence parallel `run_lua` timeout, captured `/tmp/commander-toxic-orb-score-smoke-20260705-after-start.png`, stopped cleanly, and final `status --all` returned `[]`.

## 2026-07-05 Trick Room Timer-Stall Protect Pass

- Singles Protect can now deliberately burn the final Trick Room turn when the incoming selected / predicted damaging move is meaningful and the AI will outspeed after Trick Room expires. The speed check temporarily removes final Trick Room, and also removes final Tailwind on either side when modeling the next board.
- Added a narrow self-sabotage guard: if the AI's own Tailwind is also on its final turn, Protect is penalized as a no-damage / fail line unless there is a concrete payoff such as Wish, deterministic end-of-turn recovery, Disable / Encore follow-up, opposing residual damage, or an Explosion-style dodge. This prevents "Protect to burn Trick Room" from also wasting the AI's own mode when the post-turn board is still bad.
- Added an explicit Leftovers Protect regression alongside the existing Grassy Terrain recovery regression, so both item and field recovery are covered as one-turn Protect payoffs.
- The timer-stall tests use low-level `ShouldUseSinglesProtect()` / `ProtectChecks()` assertions for final-turn field states. The high-level test DSL cannot reliably create arbitrary one-turn timers before AI scoring without running unrelated AI turns, so the focused regression pins the scoring functions directly.
- This is still not the full timer planner. Dynamax turns, weather, terrain, screens, Perish counters, doubles partner-board Protect / pivot lines, and multi-turn recovery / chip comparisons remain in the short-horizon planner backlog.
- Validation: the focused Leftovers, Grassy Terrain, opposing Tailwind, final Trick Room, and own-final-Tailwind guard regressions passed, followed by `Protect: AI`, full `check_bad_move.c`, `AI_FLAG_READ_PLAYER_MOVE`, `ai_smart_gimmick.c`, `ai_switching.c`, `ai_doubles.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` on 2026-07-05. Existing mdBook and linker warnings are recorded in `test_plan.md`. mGBA Live session `trick-room-timer-protect-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/trick-room-timer-protect-smoke-20260705-boot.png` and `/tmp/trick-room-timer-protect-smoke-20260705-after-start.png`, stopped cleanly, and final `status --all` returned `[]`.

## 2026-07-05 End-of-Turn Berry / Leech Recovery Pass

- Singles Protect recovery now includes HP-restoring item thresholds after expected end-turn chip. This covers the common line where Protect avoids the incoming hit, then poison / weather / Leech Seed-style chip drops the AI into Sitrus or confusion-berry range and the item restores a survival band before the next turn.
- The item estimate checks the same important blockers and modifiers as runtime item activation: Klutz, disabled held item, Heal Block, opposing Unnerve for berries, Gluttony threshold widening, Ripen doubling, fixed HP berries, percentage HP berries, and confusion berries. The focused Protect regression covers a Sitrus user at 51% that becomes eligible only after poison chip.
- Protect recovery also includes Leech Seed drain recovery when the AI is the seeder and a seeded target will be drained at end turn. The estimate skips false healing from Magic Guard targets, Liquid Ooze targets, and Heal Block on the receiver.
- Smart switch-in survival now fixes the same Sitrus percentage bug for single-use healing items. `HOLD_EFFECT_RESTORE_PCT_HP` was being estimated as `maxHP / 25` for Sitrus instead of runtime's `maxHP * 25 / 100`, which could make a real Sitrus pivot look much less durable than it is. The helper now also uses inclusive HP thresholds and accounts for Gluttony and Ripen.
- Because the corrected Sitrus estimate made the old High Horsepower negative fixture too safe, the read-mode "do not switch into a punishing cross-slot hit" regression now uses Choice Band Rillaboom so Incineroar remains a punished switch-in even with correct Sitrus healing.
- Validation: `rtk make -j1 -O check TESTS='Protect: AI values singles Protect when Sitrus Berry threshold follows end-turn chip'`, `rtk make -j1 -O check TESTS='Protect: AI recovery estimate includes Leech Seed drain from a seeded target'`, `rtk make -j1 -O check TESTS='Switch AI counts Sitrus Berry as twenty-five percent single-use healing'`, the strengthened High Horsepower negative read-mode regression, `Protect: AI`, full `check_bad_move.c`, full `ai_switching.c`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_smart_gimmick.c`, full `ai_doubles.c`, `rtk git diff --check`, `rtk make -j16 -O all`, and `rtk mdbook build docs` passed on 2026-07-05. Existing mdBook and linker warnings are recorded in `test_plan.md`. mGBA Live session `end-turn-berry-leech-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/end-turn-berry-leech-smoke-20260705-boot.png` and `/tmp/end-turn-berry-leech-smoke-20260705-after-start.png`, stopped cleanly, and final `status --all` returned `[]`.

## 2026-07-05 Final Screen Timer-Stall Protect Pass

- Singles Protect can now deliberately burn the opponent's final Reflect, Light Screen, or Aurora Veil turn when the incoming damaging move is dangerous and the wall expiry creates a real follow-up attack payoff.
- The check compares current AI damage against a hypothetical next board with only the final-turn opposing screens removed. It requires the post-expiry damage to improve meaningfully, including KO lines where the current screened damage cannot KO. It also avoids recommending this Protect line when the AI can already attack with a screen-breaking move such as Brick Break.
- The next-turn check keeps the line practical: if the known incoming move would KO the AI after Protect, the candidate attack must be able to move first under the current speed / priority state.
- This is still a narrow singles screen slice. It does not model doubles partner-board screen stalling, nonfinal screen countdowns, screen stalling alongside weather / terrain / Perish clocks, or full two-to-three-turn candidate lines.
- Validation in this slice: `rtk make -j1 -O check TESTS='Protect: AI values singles Protect to burn the last opposing screen turn'`, `rtk make -j1 -O check TESTS='Protect: AI does not burn the last opposing screen turn when it can break screens'`, `rtk make -j16 -O check TESTS='Protect: AI'`, full `check_bad_move.c`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_smart_gimmick.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. Existing mdBook and linker warnings are recorded in `test_plan.md`. mGBA Live session `final-screen-timer-protect-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START` after one non-evidence parallel Lua timeout, captured `/tmp/final-screen-timer-protect-smoke-20260705-boot.png` and `/tmp/final-screen-timer-protect-smoke-20260705-after-start.png`, accepted `START`, stopped cleanly, and final `status --all` returned `[]`.

## 2026-07-05 Final Weather / Terrain Timer-Stall Protect Pass

- Singles Protect can now deliberately burn a final temporary weather or terrain turn when the selected / predicted incoming move is dangerous and the expiry improves the next board.
- The helper compares incoming damage under the current weather / terrain against damage after the final timer expires. This covers defensive stall lines such as final rain making an incoming Water attack lethal or final Electric Terrain making an incoming Electric attack lethal.
- The same helper compares AI follow-up damage before and after expiry, so it can also catch cases where weather / terrain is suppressing the AI's own follow-up damage. If the AI would still be KOed after the timer expires, the attack must move first and KO; otherwise the line is rejected.
- This is still a narrow singles slice. It does not model permanent weather / terrain, nonfinal timers, terrain priority blocking, doubles partner-board timer lines, terrain seeds, or longer two-to-three-turn weather / terrain plans.
- Validation in this slice: `rtk make -j1 -O check TESTS='Protect: AI values singles Protect to burn final rain before a boosted attack'`, `rtk make -j1 -O check TESTS='Protect: AI values singles Protect to burn final Electric Terrain before a boosted attack'`, `rtk make -j16 -O check TESTS='Protect: AI'`, full `check_bad_move.c`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_smart_gimmick.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. A parallel `ai_smart_gimmick.c` run is non-evidence because it collided on shared temporary test files with `open tmpfd failed`; the sequential rerun passed. Existing mdBook and linker warnings are recorded in `test_plan.md`. mGBA Live session `final-weather-terrain-protect-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/final-weather-terrain-protect-smoke-20260705-boot.png` and `/tmp/final-weather-terrain-protect-smoke-20260705-after-start.png`, accepted `START`, stopped cleanly, and final `status --all` returned `[]`.

## 2026-07-05 Final Perish Timer-Stall Protect Pass

- Singles Protect can now deliberately wait out an already-active opposing Perish Song count when the opponent's Perish timer is at zero, the AI is not also falling this turn, and the selected / predicted incoming move would otherwise threaten the AI through the turn.
- The helper still respects Protect counterplay. Moves that ignore protection and Unseen Fist-style contact bypasses reject this line, so the AI does not pretend Protect is safe against known counterplay.
- Added the matching negative guard for same-count Perish endings. If both active singles battlers would fall at the same Perish count this turn, Protect is treated as no payoff instead of being boosted by the opponent's residual loss.
- This is still a final-count singles slice. It does not model nonfinal Perish counters, switch / Protect / trap sequences, doubles partner preservation, or Perish Song lines where a partner or reserve creates the actual next board.
- Validation in this slice: `rtk make -j1 -O check TESTS='Protect: AI values singles Protect while opposing Perish count expires'`, `rtk make -j1 -O check TESTS='Protect: AI does not value singles Protect when both Perish counts expire'`, `rtk make -j16 -O check TESTS='Protect: AI'`, full `check_bad_move.c`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_switching.c`, full `ai.c`, full `ai_smart_gimmick.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. Existing `KNOWN_FAILING` labels in broader AI switch tests, existing mdBook warnings, and the existing linker RWX warning remain unchanged. mGBA Live session `perish-timer-protect-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/perish-timer-protect-smoke-20260705-boot.png` and `/tmp/perish-timer-protect-smoke-20260705-after-start.png`, accepted `START`, the after-START screenshot showed the title screen, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`.

## 2026-07-05 Final Dynamax Timer-Stall Protect / Max Guard Pass

- Singles Protect / Max Guard can now deliberately burn the opponent's final Dynamax turn when the opponent is still threatening through a Max Move and the post-Dynamax board creates a concrete damage or survival payoff.
- The helper models the local Dynamax protection rules instead of treating every Protect-like move as a full block. Max Guard blocks ordinary Max Moves, while regular single-target Protect / Detect-style moves only qualify if the AI survives the maximum 1/4 Max Move damage that leaks through protection.
- The follow-up comparison accounts for the opponent's HP returning to its non-Dynamax band after the timer expires. This lets the AI value lines where the same Max Move cannot KO a Dynamaxed target now but can KO after the opponent returns to normal HP.
- Added a matching self-mode guard for Max Guard: if the AI's own Dynamax is also on its final turn, this narrow helper does not boost Max Guard just to trade final Dynamax turns. Those simultaneous-mode lines still need the broader short-horizon planner.
- Validation in this widened slice: `rtk make -j1 -O check TESTS='Protect: AI values Max Guard to burn the last opposing Dynamax turn'`, `rtk make -j1 -O check TESTS='Protect: AI values Protect to burn final opposing Dynamax when chip damage is survivable'`, `rtk make -j1 -O check TESTS='Protect: AI does not spend its own final Max Guard only to burn opposing Dynamax'`, `rtk make -j16 -O check TESTS='Protect: AI'`, full `check_bad_move.c`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_smart_gimmick.c`, full `ai/gimmick_dynamax.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. The AI Dynamax file still reports its existing `KNOWN_FAILING` / `TO_DO` labels while exiting 0. Existing mdBook warnings and the linker RWX warning remain unchanged. mGBA Live session `final-dynamax-protect-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/final-dynamax-protect-smoke-20260705-boot.png` and `/tmp/final-dynamax-protect-smoke-20260705-after-start.png`, accepted `START`, the after-START screenshot showed the title screen, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`.

## 2026-07-05 Protect Chip Counterplay Switch Pass

- Doubles switch preservation now treats selected Z-Moves and selected Dynamax attacks as Protect counterplay when their maximum damage through single-target Protect would still KO the protected slot. This closes the case where a Pokemon stayed in only because it knew Protect, even though the selected gimmick hit would leak enough damage through Protect to remove it.
- The check stays narrow: it only applies to confirmed player commands under read mode, only when the selected move targets the protected battler slot, and it does not mark the line unsafe when the AI battler is already Dynamaxed and would be using Max Guard instead of ordinary Protect.
- Added a production-shaped Kyogre regression next to the existing Feint counterplay test. Low-HP Mystic Water Kyogre has Protect, but selected Electric Z `Thunderbolt` plus selected `Fake Out` still pins it because the Z chip through Protect is lethal; the AI now switches Kyogre to Whimsicott instead of trusting Protect.
- Validation in this slice: `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI does not trust Protect when selected Z chip KOs through it'`, full `ai_smart_gimmick.c`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_switching.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. `ai_switching.c` still reports its existing `KNOWN_FAILING` labels while exiting 0. Existing mdBook warnings and the linker RWX warning remain unchanged. mGBA Live session `protect-chip-counterplay-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/protect-chip-counterplay-smoke-20260705-boot.png` and `/tmp/protect-chip-counterplay-smoke-20260705-after-start.png`, accepted `START`, the after-START screenshot showed the title screen, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`.

## 2026-07-05 Focused Roll-Survival Switch Pass

- Focused-slot collapse switching now keeps a middle tier between "safe pivot" and "pure sacrifice." When confirmed player commands would remove a high-value doubles slot, the switch-in evaluator sums selected incoming damage at minimum, median, estimated 14/16, estimated 15/16, and maximum rolls.
- A reserve that dies only to the maximum roll can now be selected if it survives at the 15/16 threshold, the shared `AI_RISK_SWITCH_SURVIVAL` governor allows the risk, and the reserve has real value such as immediate progress, most-suitable switch-in status, support value, or Focus Sash. If the broader board is already in shared desperation, the same path may fall back to a 14/16 survival threshold.
- The scoring order remains conservative: fully safe pivots still score far above roll-survival pivots, and roll-survival pivots score above low-value sacrifice cushions. This keeps the new behavior from replacing real immunities / absorbers / durable resists.
- The first regression uses a production-shaped doubles board instead of a perfect immunity. Choice Specs Kyogre is pinned by selected `Thunderbolt` plus `Fake Out`; the reserve Koraidon is a Choice item holder and not a free sacrifice, but can survive the focused damage unless the opponent hits the maximum roll, so the AI switches Kyogre out instead of accepting the high-value loss.
- Validation in this slice: `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI accepts a 15-of-16 survival switch when focus pressure leaves no clean pivot'`, full `ai_smart_gimmick.c`, full `ai_switching.c`, `AI_FLAG_READ_PLAYER_MOVE`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. `ai_switching.c` still reports its existing `KNOWN_FAILING` label while exiting 0. Existing mdBook warnings and the linker RWX warning remain unchanged. mGBA Live session `focused-roll-survival-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/focused-roll-survival-smoke-20260705-boot.png` and `/tmp/focused-roll-survival-smoke-20260705-after-start.png`, accepted `START`, the after-START screenshot showed the title screen, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`.

## 2026-07-05 Focused Delayed-Attack Risk Pass

- Non-semi-invulnerable two-turn attacks now use the same delayed-attack risk policy instead of being either always rejected under pressure or always preferred by raw damage. `Solar Beam`, `Meteor Beam`, `Electro Shot`, and similar moves remain ordinary immediate attacks when Power Herb or their accelerating weather is active.
- The unsafe case stays blocked: if the opponent can KO the AI before a non-semi-invulnerable charge move resolves, the bad-move early rejection still applies. The new allowance only opens when the AI survives the charge turn, the board is already in shared desperation, and the delayed attack can create a 2-turn-or-faster payoff.
- Move comparison no longer automatically awards the "not charging" priority against an accepted delayed-risk move. Stable boards still prefer the non-charging move, but desperation boards can compare the delayed attack by KO value, accuracy, and effect instead of discarding it only because it charges.
- Added a focused regression where Typhlosion is being 2HKOed by selected pressure, `Grass Pledge` cannot close the race, and delayed `Solar Beam` is the only short-horizon attack payoff. The AI now accepts the delayed Solar Beam line. Existing Solar Beam / Power Herb / weather tests still pass, preserving the usual "do not charge for no reason" behavior.
- Validation in this slice: `rtk make -j1 -O check TESTS='AI accepts delayed Solar Beam when near-term loss has no clean hit'`, `rtk make -j1 -O check TESTS=\"AI won't use Solar Beam if there is no Sun up or the user is not holding Power Herb\"`, `rtk make -j1 -O check TESTS='AI scores KOs with two turn moves correctly, considering Power Herb'`, full `ai.c`, full `check_bad_move.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. Existing mdBook warnings and the linker RWX warning remain unchanged. A first attempted parallel focused run is non-evidence because concurrent `make check` jobs collided on shared temporary files; the sequential reruns above are the accepted evidence. mGBA Live session `delayed-attack-risk-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/delayed-attack-risk-smoke-20260705-boot.png` and `/tmp/delayed-attack-risk-smoke-20260705-after-start.png`, accepted `START`, showed a nonblank after-START field frame, stopped cleanly, and final `status --all` returned `[]`.

## 2026-07-05 Read Sound-Pressure Soundproof Switch Pass

- The Soundproof switch bridge now covers lethal read sound-move pressure, not only Perish Song. If a confirmed player sound move such as `Boomburst` would remove the active AI battler and the active battler cannot cleanly KO the source first, smart switching can choose an eligible Soundproof reserve.
- Spread sound moves are included through the same known-slot targeting helper, so doubles boards can use Soundproof as a real switch answer instead of only treating sound denial as a move-scoring trick.
- The guard remains narrow: nonlethal sound pressure, status sound moves other than Perish Song, and broader Throat Chop-vs-switch line comparison still belong to the short-horizon planner.
- Validation in this slice: `rtk make -j1 -O check TESTS='AI switches to Soundproof against'`, `rtk make -j1 -O check TESTS='AI does not switch to Soundproof against read sound pressure when it can KO first'`, full `ai_switching.c`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_smart_gimmick.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. Existing mdBook warnings and the linker RWX warning remain unchanged. mGBA Live session `read-soundproof-switch-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/read-soundproof-switch-smoke-20260705-boot.png` and `/tmp/read-soundproof-switch-smoke-20260705-after-start.png`, accepted `START`, showed a nonblank after-START field frame, stopped cleanly, and final `status --all` returned `[]`.

## 2026-07-05 Focused Nonfatal Cushion Switch Pass

- Focused slot-collapse switch ranking now has an explicit nonfatal-cushion tier between accepted roll-survival pivots and deliberate sacrifice. A reserve that survives the confirmed focused hit now outranks a lower-value support or Focus Sash sacrifice that is expected to be lost immediately.
- The existing safe-pivot and 15/16 / 14/16 roll-survival tiers remain higher priority. The new middle tier is only for candidates that are not fully safe but still leave a body on the board, matching the requested immunity / resistance / roll survival / nonfatal cushion / sacrifice ladder.
- Added a production-shaped read-mode regression where Choice Specs Kyogre is pinned by selected Electric Z plus Fake Out, Whimsicott is a doomed support sacrifice, and Hariyama is a low-offense but nonfatal cushion. The AI now preserves Kyogre by switching to Hariyama rather than spending Whimsicott.
- Validation in this slice: `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI prefers a nonfatal cushion over a doomed support sacrifice'`, full `ai_smart_gimmick.c`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_switching.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. Existing mdBook warnings and the linker RWX warning remain unchanged. mGBA Live session `nonfatal-cushion-switch-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/nonfatal-cushion-switch-smoke-20260705-boot.png` and `/tmp/nonfatal-cushion-switch-smoke-20260705-after-start.png`, accepted `START`, showed a nonblank after-START title frame, stopped cleanly, and final `status --all` returned `[]`.

## 2026-07-05 Pivot-Capable Preserve Switch Pass

- Preserve and focused-collapse switch-in scoring now treats `EFFECT_HIT_ESCAPE` moves such as `U-turn`, `Volt Switch`, and `Flip Turn` as follow-up value, alongside existing `Parting Shot` support. This does not simulate the future pivot turn, but it stops the ladder from treating a pivot-capable Focus Sash / cushion exactly the same as a dead-end cushion when both otherwise preserve the pinned slot.
- The selected-KO switch-in helper also uses the pivot flag as a tie-break beside Fake Out, because that narrower helper can fire before the focused-collapse ladder when one selected move is already lethal.
- Added a read-mode regression where Choice Specs Kyogre is pinned by selected Electric Z plus Fake Out and two otherwise identical Focus Sash Crobat reserves are available; the AI now chooses the Crobat with `U-turn` over the one with only `Tackle`.
- Validation in this slice: `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI values a pivot-capable sacrifice in focused collapse'`, the existing cross-slot selected-KO regression, the nonfatal cushion regression, full `ai_smart_gimmick.c`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_switching.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. Existing mdBook warnings and the linker RWX warning remain unchanged. mGBA Live session `pivot-preserve-switch-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/pivot-preserve-switch-smoke-20260705-boot.png` and `/tmp/pivot-preserve-switch-smoke-20260705-after-start.png`, accepted `START`, showed a nonblank after-START title frame, stopped cleanly, and final `status --all` returned `[]`.

## 2026-07-05 Battle Action Log Trace v3 Pass

- Battle action log entries now retain a compact AI trace alongside the existing reason tag. The ROM entry remains 24 bytes; the two previously unused trailing bytes now store `aiThreatFlags` and `aiRiskKind`, with a compile-time size assert so the Lua exporter layout cannot silently drift.
- Move and switch decision finalization now copies the active board threat flags from `AiBoardSnapshot`. Desperation move logs also identify the relevant risk family when it can be inferred from the chosen move: low-accuracy status / damage, secondary hax, OHKO fishing, delayed attack, or generic high-variance comeback. Switch-preserve logs mark switch-survival risk when the chosen switch reason is tied to known KO pressure.
- The mGBA Live JSON exporter now writes schema `pokeemerald.battle_action_log.v3` and includes raw plus named `ai_threat_flags` / `ai_threat_names` and `ai_risk_kind` / `ai_risk_name` fields. This is still a compact trace; it does not yet emit candidate scores, rejected alternatives, or full two-to-three-turn line facts.
- Validation in this slice: `rtk make -j1 -O check TESTS='AI logs hax out reason for desperate read Perish Song flinch'`, `rtk make -j1 -O check TESTS='AI accepts a low-accuracy sleep swing when it is about to be KOed'`, full `ai.c`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_smart_gimmick.c`, full `ai_switching.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. Existing mdBook warnings, the existing linker RWX warning, and the existing `ai_switching.c` `KNOWN_FAILING` label remain unchanged. mGBA Live session `battle-log-trace-v3-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/battle-log-trace-v3-smoke-20260705-boot.png` and `/tmp/battle-log-trace-v3-smoke-20260705-after-start.png`, exported an empty but valid `/tmp/battle-log-trace-v3-smoke-20260705-empty.json` with schema `pokeemerald.battle_action_log.v3` and `entry_size=24`, parsed that JSON successfully, stopped cleanly, and final `status --all` returned `[]`. This smoke did not enter a battle, so the Lua evidence covers schema / layout execution; the populated trace-byte coverage is from the C battle tests.

## 2026-07-05 Read Sound-Pressure Throat Chop Pass

- `Throat Chop` scoring now checks confirmed read-mode player commands before falling back to generic best-move prediction. If the target has selected a sound pressure move and is not already throat-chopped, the AI can value `Throat Chop` as the immediate answer.
- The new status-sound allowance is intentionally narrow. It covers selected Perish Song as a direct stop, while other status sound moves remain future planner work so the AI does not overvalue blocking low-impact utility.
- Added focused regressions for selected `Hyper Voice` and selected `Perish Song`. The Perish Song regression failed before this pass with `Expected Throat Chop, got Psychic Fangs`, confirming that the existing best-damage path did not handle selected status sound pressure.
- Validation in this slice: `rtk make -j1 -O check TESTS='AI reads a selected sound status move and uses Throat Chop immediately'`, `rtk make -j1 -O check TESTS='AI reads a selected sound move and uses Throat Chop immediately'`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. Full `ai_calc_best_move_score.c` is not accepted evidence because its unrelated existing Ice Face / Belly Drum check failed with `Expected Liquidation, got Icicle Crash`; the same check fails alone, while the Throat Chop checks pass. Existing mdBook warnings and the linker RWX warning remain unchanged. mGBA Live session `throat-chop-sound-pressure-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before input and after `START` retry, captured `/tmp/throat-chop-sound-pressure-smoke-20260705-boot.png` and `/tmp/throat-chop-sound-pressure-smoke-20260705-after-start-late.png`, stopped cleanly, and final `status --all` returned `[]`. One parallel after-START Lua bridge timed out and is non-evidence; the single-command retry passed.

## 2026-07-05 Exact Multi-Move Roll Distribution Switch Pass

- Focused switch survival no longer adds independent per-hit 14/16 or 15/16 estimates when multiple selected player attacks hit the same slot. The switch roll summary now expands each incoming damage range into its 16 roll values, convolves the normal one- or two-source incoming damage cases, sorts the joint sums, and takes minimum, median, 14/16, 15/16, and maximum from that exact distribution.
- This matters for focused collapse lines where the reserve only needs to survive the combined turn often enough. For two independent 85-100 damage ranges, the old additive 15/16 estimate would be `99 + 99 = 198`, while the exact 256-outcome distribution has a 15/16 threshold of `195`.
- Added a low-level switch AI regression for the distribution primitive. It verifies both the single-hit `85, 93, 98, 99, 100` summary and the two-hit `170, 185, 193, 195, 200` summary, so future changes cannot accidentally return to additive quantiles.
- The helper is intentionally bounded to the normal doubles-sized selected-damage case to avoid large stack allocations in the runtime AI path. Broader multi-turn reserve-entry, recovery / chip, and three-plus-source modeling remain planner work rather than this focused primitive.
- Validation in this slice: `rtk make -j1 -O check TESTS='Switch AI combines multiple damage rolls as an exact distribution'`, `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI accepts a 15-of-16 survival switch when focus pressure leaves no clean pivot'`, full `ai_switching.c`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_smart_gimmick.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. `ai_switching.c` still reports its existing `KNOWN_FAILING` label while exiting 0. Existing mdBook warnings and the linker RWX warning remain unchanged. mGBA Live session `exact-roll-distribution-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/exact-roll-distribution-smoke-20260705-boot.png` and `/tmp/exact-roll-distribution-smoke-20260705-after-start.png`, accepted `START`, stopped cleanly, and final `status --all` returned `[]`.

## 2026-07-05 Field-Expiry Speed Guard Protect Pass

- Final weather / terrain Protect burn now checks next-turn move order under the field state after expiry. `FieldExpiryLeavesUsableNextTurn()` no longer trusts the current cached `gAiLogicData->speedStats` when the AI only survives if it attacks first after the timer is gone; it temporarily applies the post-expiry weather / terrain, recomputes both battlers' Speed, then calls `AI_IsFaster()`.
- This closes the case where a Swift Swim user under final rain could Protect to burn the rain, then still be treated as faster on the next turn even though the same Protect removed its own speed boost. The same primitive also covers terrain-speed dependencies such as Surge Surfer because it recomputes Speed under the post-expiry field status.
- Added a focused regression where the AI's Swift Swim lets it outrun the player only during final rain. Non-rain `Surf` still KOs the AI, so burning rain is not a real line unless the AI remains faster afterward. The regression failed before the fix because `ShouldUseSinglesProtect()` accepted the Protect line, then passed after the post-expiry Speed recalculation.
- Added a paired own-final-screen regression for the same self-sabotage family. That case was already safe in the current scoring path, but the regression keeps final-screen burn from later ignoring that the AI's own screen can expire at the same time.
- Validation in this slice: `rtk make -j1 -O check TESTS='Protect: AI does not burn final rain if Swift Swim speed expires too'`, `rtk make -j1 -O check TESTS='Protect: AI values singles Protect to burn final rain before a boosted attack'`, `rtk make -j1 -O check TESTS='Protect: AI values singles Protect to burn final Electric Terrain before a boosted attack'`, `rtk make -j1 -O check TESTS='Protect: AI does not burn opposing screens when its own final screen is needed'`, `Protect: AI`, full `check_bad_move.c`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_smart_gimmick.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. A parallel focused check attempt is non-evidence because two `make check` processes collided on the shared test ELF and one returned `No tests found`; the sequential rerun passed. Existing mdBook warnings and the linker RWX warning remain unchanged. mGBA Live session `field-expiry-speed-guard-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/field-expiry-speed-guard-smoke-20260705-boot.png` and `/tmp/field-expiry-speed-guard-smoke-20260705-after-start.png`, accepted `START`, stopped cleanly, and final `status --all` returned `[]`.

## 2026-07-05 Nonlethal Soundproof Switch Pass

- The Soundproof switch bridge now covers selected nonlethal sound damage when the current battler is still losing the short race. The read-mode helper accepts a selected sound move if it is lethal or if its damage is at least a two-hit collapse, then compares the current battler's own damaging moves against that sound-move clock before switching.
- The current slot still stays in when it can win the race in place, including the old clean-faster-KO case and the new faster damage-race case. This prevents Soundproof from becoming a default answer to harmless chip or to boards where the active Pokemon can simply attack through the sound move.
- Soundproof candidates must have immediate attack or board value for nonlethal pressure. A lethal sound line may still use Soundproof as preserve value, but the nonlethal path requires a useful reserve rather than switching to a dead slot only because it is immune.
- Added focused regressions for selected nonlethal `Hyper Voice`: one where the active AI cannot race the sound pressure and switches to a Soundproof `Thunderbolt` reserve, and one where the active AI wins the race with faster `Strength` and stays in. Existing lethal sound, spread sound, and clean-KO-first Soundproof tests still pass.
- Validation in this slice: `rtk make -j1 -O check TESTS='AI switches to Soundproof against read nonlethal sound pressure'`, `rtk make -j1 -O check TESTS='AI does not switch to Soundproof against nonlethal sound pressure when it wins the race'`, existing lethal / spread / clean-KO Soundproof focused checks, full `ai_switching.c`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_smart_gimmick.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. `ai_switching.c` still reports its existing `KNOWN_FAILING` label while exiting 0. Existing mdBook warnings and the linker RWX warning remain unchanged. mGBA Live session `nonlethal-soundproof-switch-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/nonlethal-soundproof-switch-smoke-20260705-boot.png` and `/tmp/nonlethal-soundproof-switch-smoke-20260705-after-start.png`, accepted `START`, stopped cleanly, and final `status --all` returned `[]`.

## 2026-07-05 Ally Speed Drop Trick Room Score Pass

- Ally-target stat-change scoring now has a narrow Trick Room order bridge. If an active Trick Room is up, a move such as `Scary Face` can score on the AI's partner when the Speed drop makes that partner slower than a live foe, which means the partner can move before that foe under Trick Room.
- The bridge is payoff-gated. It rejects White Herb / Clear Amulet cases, minimum-Speed dead ends, and drops that do not actually flip order. It then requires an immediate payoff: either the slowed partner can KO the newly outsped foe using on-demand `AI_CalcDamage`, or the foe already threatens to KO the partner.
- Generic Speed-drop scoring into opposing targets no longer receives its ordinary Speed-control bonus while Trick Room is active. This avoids helping an opponent become faster in Trick Room order.
- This is intentionally still not the full ally-order planner requested in `goal.md`. It does not compare switching, Protect, sacrifice entry, or two-to-three-turn survival lines; it only covers the immediate, local "slow partner under Trick Room so it acts first and cashes out" case.
- Validation in this slice: `rtk make -j1 -O check TESTS='AI scores ally Speed drop under Trick Room to reverse move order'`, full `ai_doubles.c`, `AI_FLAG_ATTACKS_PARTNER`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_partner_synergy.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. Existing mdBook warnings and the linker RWX warning remain unchanged. A first attempted parallel check involving `AI_FLAG_ATTACKS_PARTNER` and `ai_smart_gimmick.c` is non-evidence because parallel `make check` jobs collided on shared test output; the accepted reruns were sequential. mGBA Live session `ally-speed-drop-trick-room-smoke-20260705` booted the rebuilt ROM, Lua returned `{ok=true, validation=ally_speed_drop_trick_room_smoke_20260705}` before input and `{ok=true, validation=ally_speed_drop_trick_room_after_start_20260705}` after `START`, screenshots `/tmp/ally-speed-drop-trick-room-smoke-20260705-boot.png` and `/tmp/ally-speed-drop-trick-room-smoke-20260705-after-start.png` were captured, `START` input was accepted, the after-START screenshot showed the continue menu, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. The first screenshot attempt used the wrong `--output` flag and is non-evidence; the retry with `--out` passed.

## 2026-07-05 Focused Switch Candidate Payoff Pass

- Focused slot-collapse switching now builds an explicit local candidate shape instead of scoring every reserve through one inline branch. The candidate families are safe pivot, accepted roll survival, nonfatal cushion, and deliberate sacrifice.
- The deliberate sacrifice family is now payoff-gated. A doomed switch-in is only considered when it consumes selected `Fake Out` pressure, carries support / pivot-style role value, or has Focus Sash-style preserve value. A no-item, no-support reserve that simply dies to the same selected two-hit pressure is rejected because it does not create a better next board.
- Existing positive preserve lines are retained: Focus Sash Whimsicott still preserves Choice Specs Kyogre from selected Electric Z plus Fake Out, nonfatal cushions still outrank doomed support sacrifice, pivot-capable sacrifices still beat dead-end sacrifices, and 15/16 roll-survival switch-ins still work through the shared risk governor.
- Added a negative production-shaped read-mode regression where low-HP Choice Specs Kyogre is pinned by selected `Thunderbolt` plus selected `Bleakwind Storm`, but the only reserve is a no-item `Magikarp` with no support or pivot value. The AI now rejects that doomed sacrifice and attacks with Kyogre instead of cycling into a body that does not change the following board.
- Validation in this slice: `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI rejects a doomed sacrifice when it does not improve next board'`, `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI sacrifices Whimsicott to preserve Choice Kyogre from selected Z plus Fake Out focus'`, `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI prefers a nonfatal cushion over a doomed support sacrifice'`, `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI values a pivot-capable sacrifice in focused collapse'`, `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI accepts a 15-of-16 survival switch when focus pressure leaves no clean pivot'`, full `ai_smart_gimmick.c`, full `ai_switching.c`, `AI_FLAG_READ_PLAYER_MOVE`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. Each focused check was run sequentially; `ai_switching.c` still reports its existing `KNOWN_FAILING` label while exiting 0. Existing mdBook warnings and the linker RWX warning remain unchanged. mGBA Live session `focused-switch-candidate-payoff-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/focused-switch-candidate-payoff-smoke-20260705-boot.png` and `/tmp/focused-switch-candidate-payoff-smoke-20260705-after-start.png`, accepted `START`, showed a nonblank continue-menu screenshot after input, stopped cleanly, and final `status --all` returned `[]`. A first Lua / screenshot attempt omitted `--session`, and a first input attempt omitted `--key`; those CLI-usage attempts are non-evidence and the corrected retries passed.

## 2026-07-05 Short-Horizon Candidate Line Suppression Pass

- `AiShortHorizon` now keeps a compact `AiCandidateLine` for the first stable line and first fallback risk line. The initial line families cover clean damage, switch escape, setup denial, mode control, reserve entry, and high-variance fallback.
- The shared risk governor now blocks high-variance comeback, low-accuracy status / damage, secondary hax, OHKO fishing, and delayed-attack risks when the short-horizon evaluator already found a stable line. Partner sacrifice and switch-survival still use their dedicated board checks.
- Move scoring now withholds ordinary secondary-hax scoring in desperation boards when the same short horizon already contains a stable clean answer. This closes the case where a weaker flinch line could tie or beat a clear KO while the AI was not actually out of stable options.
- Added a read-mode Perish Song regression where the player is threatening a short forced clock, `Stomp` can fish for flinch, but `Boomburst` cleanly answers the board. The AI now chooses the clean damage line and logs `AI_DECISION_REASON_CLEAN_DAMAGE_PREFERRED` with no risk family.
- This remains a narrow metadata / suppression pass, not the full two-to-three-turn line comparator. It does not yet emit rejected candidate details into the battle action log or compare complete move / switch / Protect / sacrifice trees.
- Validation in this slice: `rtk make -j1 -O check TESTS='AI short horizon suppresses hax when clean damage answers read Perish Song'`, `rtk make -j1 -O check TESTS='AI fishes for flinch against a read Perish Song only without a clean damage line'`, `rtk make -j1 -O check TESTS='AI logs hax out reason for desperate read Perish Song flinch'`, `rtk make -j1 -O check TESTS='AI accepts a low-accuracy sleep swing when it is about to be KOed'`, full `ai.c`, full `ai_smart_gimmick.c`, `AI_FLAG_READ_PLAYER_MOVE`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. Existing mdBook warnings and the linker RWX warning remain unchanged. A parallel focused `make check` attempt is non-evidence because concurrent checks collided on shared test output; the accepted reruns were sequential. mGBA Live session `short-horizon-candidate-line-smoke-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/short-horizon-candidate-line-smoke-20260705-boot.png` and `/tmp/short-horizon-candidate-line-smoke-20260705-after-start.png`, accepted `START`, stopped cleanly, and final `status --all` returned `[]`.

## 2026-07-05 Battle Action Log Short-Horizon Trace v4 Pass

- `BattleActionLogEntry` now carries the short-horizon facts that were previously only available inside `AiShortHorizon`: line flags, preferred stable line family, fallback risk line family, and loss clock. The entry size is now 28 bytes with a compile-time assert.
- Move and switch decision finalization copy this compact trace from `AI_EvaluateShortHorizon()` alongside the existing reason, threat flags, and risk kind. This does not log full candidate scores or rejected alternatives yet, but manual logs can now show when a clean stable line and high-variance fallback were both visible.
- The mGBA Live exporter now writes schema `pokeemerald.battle_action_log.v4`, reads the 28-byte layout, and emits named `ai_line_names`, `ai_stable_line_name`, `ai_fallback_line_name`, and `ai_loss_clock` fields.
- The focused Perish Song clean-damage regression now asserts the populated trace: clean damage and high variance are both present, clean damage is the stable line family, high variance is the fallback family, and the loss clock is 2.
- Validation in this slice: `rtk make -j1 -O check TESTS='AI short horizon suppresses hax when clean damage answers read Perish Song'`, `rtk make -j1 -O check TESTS='AI logs hax out reason for desperate read Perish Song flinch'`, `rtk make -j1 -O check TESTS='AI accepts a low-accuracy sleep swing when it is about to be KOed'`, full `ai.c`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_smart_gimmick.c`, and full `ai_switching.c` passed on 2026-07-05. The first focused run triggered a broad header rebuild and produced no output for several minutes; it was interrupted and rerun successfully. `ai_switching.c` still reports its existing `KNOWN_FAILING` label while exiting 0. Final docs, ROM build, and mGBA Live exporter v4 evidence are recorded in `test_plan.md`.

## 2026-07-05 Doubles Partner Timer-Stall Protect Pass

- Doubles Protect scoring now has a narrow partner-board timer bridge. If a protected AI slot is threatened by a damaging move, final opposing Tailwind or final Trick Room is the reason the slot is pinned, and the AI's live partner will move first after the timer expires with an immediate KO move, `ProtectChecks()` gives Protect a stronger payoff than the generic doubles Protect bump.
- The helper stays legality-gated through the existing Protect counterplay checks. It refuses protect-bypassing moves, Unseen Fist-style contact bypass, status / unavailable predicted moves, nonthreatening incoming moves, missing partners, unusable partner moves, invalid targets, and partner lines that cannot KO after the timer expires.
- A paired doubles self-sabotage guard rejects the opposite case: final Trick Room plus the AI side's own final Tailwind is not treated as a good Protect line unless the protected slot will gain post-expiry Speed payoff, the partner can KO immediately before the threat, or the partner can KO after the timer expires. This keeps the AI from spending Protect only to erase its own remaining Speed-control mode.
- This is intentionally a score-layer bridge, not the full doubles line planner. It covers the local "protect the pinned slot so the partner can punish after speed control expires" case for final Tailwind and final Trick Room, plus the paired no-payoff self-sabotage case. Nonfinal timers, weather / terrain / screens in doubles, Perish / Dynamax partner-board lines, reserve-entry comparison, and multi-turn Protect / switch / sacrifice trees remain planner work.
- Validation in this slice: `rtk make -j1 -O check TESTS='Protect: AI values doubles Protect when the partner can punish final opposing Tailwind'`, `rtk make -j1 -O check TESTS='Protect: AI values doubles Protect when the partner can punish final Trick Room'`, `rtk make -j1 -O check TESTS='Protect: AI does not waste doubles final Tailwind while burning Trick Room without partner payoff'`, `Protect: AI`, full `check_bad_move.c`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_smart_gimmick.c`, full `ai_doubles.c`, `rtk git diff --check`, `rtk mdbook build docs`, and `rtk make -j16 -O all` passed on 2026-07-05. `ai_doubles.c` still reports its existing `KNOWN_FAILING` / `TO_DO` labels while exiting 0. Existing mdBook warnings and the linker RWX warning remain unchanged. mGBA Live session `doubles-partner-timer-protect-smoke2-20260705` booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/doubles-partner-timer-protect-smoke2-20260705-boot.png` and `/tmp/doubles-partner-timer-protect-smoke2-20260705-after-start.png`, accepted `START`, stopped cleanly, and final `status --all` returned `[]`. A first attempted parallel focused rerun of the two positive doubles Protect checks collided on shared test output with `open tmpfd failed` / missing temp file errors and is non-evidence; both checks passed when rerun sequentially.

## 2026-07-05 Doubles Perish Timer-Stall Protect Pass

- Doubles Protect scoring now recognizes the narrow final-Perish payoff that singles already handled locally. If the protected slot is threatened by a damaging move and the target alone will fall to an already-active Perish Song count this turn, `ProtectChecks()` upgrades the Protect line above the generic doubles Protect bump.
- The same Protect counterplay gates still apply: protect-bypassing moves and Unseen Fist-style contact bypass reject the line before the Perish payoff can score. This keeps Perish burn from becoming a false safety answer into `Feint`-like or protection-bypassing pressure.
- A paired doubles guard rejects the self-nullifying case where the protected slot and the target both fall to Perish this turn. In that position Protect does not preserve the slot or improve the next board, so it should not receive the ordinary doubles Protect value.
- This is still a final-count score-layer bridge, not a full Perish line planner. Nonfinal Perish counts, switching to reset Perish, partner / reserve sequencing, and multi-turn Protect risk remain short-horizon planner work.
- Validation for this slice is recorded in `test_plan.md`.

## 2026-07-05 Doubles Partner Screen Timer-Stall Protect Pass

- Doubles Protect scoring now has a second partner-board timer bridge for final opposing screens. If the protected slot is threatened by a damaging move, the opposing side's Reflect / Light Screen / Aurora Veil is on its last turn, and the AI partner has a damaging move whose payoff appears only after that screen expires, `ProtectChecks()` upgrades Protect above the generic doubles Protect bump.
- The bridge mirrors the singles screen logic: it compares partner damage with the final screen still active against damage after removing the expiring screen, and requires a KO or meaningful large-hit gain. It also skips partner moves that already break screens, because those should solve the board directly instead of spending Protect to wait.
- The existing Protect counterplay checks still gate the line, so protect-bypassing moves and Unseen Fist-style contact bypass do not get treated as safe screen-burn turns.
- This remains a score-layer bridge, not full doubles screen planning. It covers the local "protect the pinned slot so the partner can cash out after the screen expires" case. Weather in doubles, broader terrain cases, nonfinal screens, reserve-entry sequencing, and multi-turn Protect / switch / sacrifice comparisons remain planner work.
- Validation for this slice is recorded in `test_plan.md`.

## 2026-07-05 Doubles Partner Terrain Timer-Stall Protect Pass

- Doubles Protect scoring now has a narrow final-terrain partner-board bridge. If the protected slot is threatened by a damaging move while terrain is on its last turn, and the AI partner has enough post-expiry pressure to punish after the terrain disappears, `ProtectChecks()` upgrades the Protect line above the generic doubles Protect bump.
- The first regression covers final Electric Terrain. The protected slot is under Electric-boosted `Thunderbolt` pressure; after Protect burns the final terrain turn, the incoming damage drops and the partner's large hit becomes a real next-board payoff. A paired negative keeps nonfinal terrain from receiving the same wait-line value.
- The helper is deliberately terrain-only in this slice. Weather in doubles, nonfinal terrain, reserve-entry sequencing, and broader multi-turn Protect / switch / sacrifice comparisons remain planner work.
- Validation for this slice is recorded in `test_plan.md`.

## 2026-07-20 Known-KO Switch / Sacrifice Cost Pass

- Runtime work is on `integration/smart-ai-switch-sacrifice-risk-20260720`, based on `integration/runtime-lab-current-1.16.1` at `4e960aecea0185912e21a28f40e00fab1b388874`. This keeps the change on a fresh integration branch and leaves `master` source trees untouched.
- The selected single-target KO route no longer switches merely because some reserve can take more than `AI_DEFENSIVE_KO_THRESHOLD` hits. It now returns an explicit stay / switch / unresolved decision and compares the value preserved by switching against the exposure imposed on the best eligible reserve. The no-switch path is rechecked after move scoring so the later all-scores-bad pass cannot silently undo a deliberate sacrifice, while switches already chosen by higher-priority rules retain their existing precedence.
- Active value includes current HP, immediate offensive pressure, strategic move roles, Choice commitment, useful stat boosts, and active or still-available gimmick value. Reserve exposure scales its full role value by confirmed incoming damage, then adds bounded risk for exposing an unused gimmick or breaking an intact Focus Sash. Eligible reserves are ranked by this exposure cost before the existing survival, Fake Out, and pivot tie-breakers.
- This deliberately uses battle state and resource value rather than Pokemon level as a policy threshold. The regressions use sharply different levels only to create clearly separated low- and high-value resources; level itself is not consulted by the decision.
- Added inverse and boundary coverage: the AI sacrifices a spent 1-HP active instead of chipping its unused Dynamax win condition, still accepts reserve chip to preserve a valuable active Mega Pokemon, and chooses a lower-exposure cushion before the higher-value gimmick reserve when both can survive.
- Focused and file-level checks passed, including all of `ai_smart_gimmick.c`, all `AI_FLAG_READ_PLAYER_MOVE` cases, and `ai_switching.c` with its existing accepted `KNOWN_FAILING` label unchanged. The normal ROM build also passed.
- Accepted mGBA Live session `smart-ai-switch-sacrifice-risk-final-20260720` booted the final rebuilt normal ROM through the required wrapper, returned Lua bridge checks before and after `START`, captured `/tmp/smart-ai-switch-sacrifice-risk-final-20260720-boot.png` and `/tmp/smart-ai-switch-sacrifice-risk-final-20260720-after-start.png`, reached the continue menu, stopped cleanly, and left final managed status `[]`. An initial direct-binary launch is non-evidence because it lacked `DISPLAY` and exited with `SIGABRT`; the wrapper supplied `DISPLAY=:0` for the accepted run.
- Runtime smoke proves ROM boot, rendering, input, and bridge health, while the C battle regressions are the direct behavior evidence. No scripted interactive route reproduced the exact switch / sacrifice board in this pass. The comparator remains a bounded one-turn heuristic: it does not yet simulate the free-entry move, full endgame matchups, or a unified two-to-three-turn switch / Protect / sacrifice tree.
