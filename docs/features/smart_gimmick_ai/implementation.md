# Smart Gimmick AI Implementation

## Summary

The feature adds smart timing flags for battle gimmicks, expands Dynamax timing beyond immediate damage, adds Mega / Ultra Burst payoff checks, adds a smart conservation layer for damaging Z-Moves, tunes Protect as a board-payoff move instead of a passive default, and provides debug battle fixtures for competitive-style 3v3 singles, 4v4 doubles, and Dmax-vs-Z checks.

Runtime files:

- `include/constants/battle_ai.h`
- `src/battle_controller_player.c`
- `src/battle_ai_switch.c`
- `src/battle_ai_util.c`
- `src/battle_script_commands.c`
- `src/data/debug_trainers.party`
- `src/debug.c`
- `test/battle/ai/ai_switching.c`
- `test/battle/ai/ai_smart_gimmick.c`
- `test/battle/ai/ai_partner_synergy.c`
- `test/battle/exp.c`

Documentation:

- `docs/tutorials/ai_flags.md`
- `docs/features/smart_gimmick_ai/`

Tooling:

- `tools/runtime_knowledge/`
- `tools/mgba_live/battle_action_log_export.lua`
- `tools/mgba_live/start_mgba_live.sh`
- `tools/mgba_live/start_mgba_live.bat`
- `tools/mgba_live/export_battle_action_log.sh`
- `tools/mgba_live/export_battle_action_log.bat`

## Runtime Behavior

`DecideGimmickBeforeMoveSelection()` still handles the early smart-gimmick gate. `ReconsiderSmartGimmick()` then checks the chosen move and cancels the gimmick when the selected turn is not worth spending.

For Dynamax, `ShouldUseSmartDynamax()` now accepts these reasons:

- Last available Pokemon, or one-reserve late-commit pressure when the active Pokemon is already low on HP or under immediate KO threat.
- The current target can otherwise KO the AI Pokemon.
- A known or predicted opposing move threatens Dynamax-blocked disruption while the AI selected a damaging move:
  - Fake Out-style flinch pressure.
  - Roar / Whirlwind-style forced-switch pressure.
- Dynamax converts the selected move into a KO that the regular move would miss.
- The selected Max Move creates strategic board value:
  - Speed control through `Max Airstream` or `Max Strike`.
  - Weather control through Fire, Water, Rock, or Ice Max Moves.
  - Terrain control through Electric, Grass, Fairy, or Psychic Max Moves.
  - Side-wide stat boosts or drops from Fighting, Poison, Ground, Steel, Dragon, Bug, Ghost, and Dark Max Moves.

Weather and terrain decisions call `ShouldSetWeather()`, `ShouldSetFieldStatus()`, and `ShouldClearFieldStatus()` so smart Dynamax follows the same field-state opinions used by normal AI move scoring.

The Dynamax policy is intentionally not "use the strongest attack immediately." The current runtime model treats Dynamax as a board-presence resource: HP scaling can keep an active Pokemon on the field, Max Move effects can change Speed, weather, terrain, Defense, Special Defense, Attack, Special Attack, or opponent-side pressure, and Dynamax can deny disruption such as flinching or forced switching. This matches the Pokemon Wiki mechanics summary for Dynamax's HP increase, 3-turn duration, flinch / forced-switch immunity, Max Guard, and Max Move side effects, the official Sword / Shield Max Move explanation, and Victory Road VGC reports where Dynamax is discussed through Fake Out denial, Max Airstream tempo, Max Steelspike / Max Quake defensive value, and status / positioning pressure.

For Mega / Ultra Burst, `ShouldUseSmartMega()` now looks at the target form before spending the gimmick. It allows immediate use for target-form ability payoffs such as trapping or weather control, estimated Speed flips, meaningful defensive improvement under KO pressure, meaningful attacking-stat improvement for the selected damaging move, and one-reserve low-HP late-commit pressure. It still delays setup turns when there is no immediate pressure, and it can preserve `Air Lock` / `Cloud Nine` during active weather if the target form has no stronger immediate payoff.

For Z-Moves, `ShouldUseSmartZMove()` now wraps the existing Z-Move viability checks. Status Z-Moves keep their tactical checks. Damaging Z-Moves are conserved unless the AI is on its last Pokemon, has one reserve left and the active Pokemon is low on HP or under KO threat, the Z-Move converts the selected move into a KO, improves the damage race under immediate KO or trap pressure, or protects a low-accuracy KO line.

`AI_FLAG_AGGRESSIVE_GIMMICK` adds a debug / gauntlet spend route for competitive pressure tests. With this flag, smart Dynamax and damaging smart Z-Move decisions can spend after the selected gimmick-boosted move crosses `AI_AGGRESSIVE_GIMMICK_MIN_DAMAGE_PERCENT`, then passes the `AI_AGGRESSIVE_GIMMICK_USE_CHANCE` roll. Smart Tera runs the same 85% pressure route before the normal future-candidate conservation roll when Tera gives at least `AI_AGGRESSIVE_TERA_MIN_DAMAGE_BOOST_PERCENT` offensive improvement and the target is not protected by a hard punishing move. The route still rejects status moves, invalid moves, zero damage, partner attacks, and very weak attacks, so it is more proactive without becoming "always press the gimmick."

`AI_FLAG_READ_PLAYER_MOVE` is intentionally separate from `AI_FLAG_OMNISCIENT`. `Omniscient` gives full knowledge of the player's moves, abilities, and held items, but ordinary prediction still guesses the current command by re-running AI logic from the player position. `Read Player Move` lets debug / gauntlet AI use confirmed player commands from the command buffer. Opponent controllers now wait until every live player-side command is ready, rebuild `gAiLogicData`, and recompute action / move choice immediately before returning their own command. Confirmed moves feed `GetPredictedMove()` / `GetIncomingMove()`, confirmed switches are applied as the predicted switch-in, and selected player defensive gimmicks are passed into damage simulation. This fixes double-battle reads such as Koraidon scoring into a player Miraidon that has already selected Electric Tera; the AI should not continue scoring Dragon Claw as if Miraidon stayed Dragon-type. This is still a debug / gauntlet input-read layer, not a normal NPC policy or a full turn-tree solver.

`Read Player Move` also treats a confirmed single-target `Protect` / Max Guard as a hard warning for ordinary single-target damage. The mGBA autosave log `/tmp/manual-ai-log-captured-20260606.json` showed strong reads early in a 4v4 double battle, including early pressure into the player right slot, player Tera capture, and four resolved switch-ins, but turn 3 still had the opponent right slot use `Collision Course` into a player right `Protect`. That is a poor full-information line when another opposing slot is available. `AI_CheckBadMove()` now applies a read-mode-only penalty to non-protect-bypassing, non-spread, single-target damaging moves aimed at a player battler whose confirmed command is single-target protection. Spread moves can still keep their partner pressure, and Feint / protect-bypassing moves are not penalized.

The later manual session `manual-ai-log` with archived pid `222151` produced `/tmp/manual-ai-log-battle-action-log-autosave.json` and `/tmp/manual-ai-log-latest-20260606.json` from the same non-empty autosave snapshot. That log recorded 20 entries through turn 3, including 14 move actions, 6 switch actions, two selected Tera markers, and resolved switch-ins. The important loss-pattern finding was turn 0: player left selected `Fake Out` into opponent right, but opponent right still planned an ordinary `Draco Meteor`. `Read Player Move` now scores slower ordinary actions down when a confirmed or currently unknown-but-visible player `Fake Out` can flinch the AI battler. This is a soft pressure penalty, not a forced `Protect` rule. It uses the actual threat move in `AI_WhoStrikesFirst()`, so priority order still matters: Gen 5+ `Fake Out` outruns `Extreme Speed`, while higher-priority protection or Dynamax can avoid the discount. The flinch threat is also ignored when the target would block it through priority blockers such as `Armor Tail` / `Dazzling` / `Queenly Majesty`, Psychic Terrain, type immunity, Substitute, Covert Cloak, Shield Dust, or Inner Focus.

The manual Read Double feedback for pid `252732` could not be matched to a live mGBA Live session record after the session closed, but the latest non-empty autosave at `/tmp/manual-ai-log-battle-action-log-autosave.json` was generated at `2026-06-06T11:39:11Z` and contained the relevant battle shape: 55 entries through turn 10, with opponent-side actions showing 9 commanded switches, 5 `Trick Room` uses, and only 1 `Protect`. That confirmed the reported pattern: Trick Room was too easy to bait or mirror, and optional doubles switching could outrank a simple protect-and-hold-position line. `AI_CheckBadMove()` now applies a read / predicted `Trick Room` toggle penalty when an opposing battler is also choosing Trick Room, and `AI_PowerfulStatus()` no longer adds the powerful-status boost to Trick Room in that mirror-read situation. The existing `AI_FLAG_POWERFUL_STATUS` behavior still allows initial proactive Trick Room when no opponent-side Trick Room is being read, but it no longer blindly rewards a same-turn toggle accident.

The follow-up pid `278133` loss was analyzed from `/tmp/manual-ai-log-battle-action-log-autosave.json` after the live session had closed. The final relevant shape was turn 7: player left selected `MOVE_GRASSY_GLIDE` into opponent right, while opponent right Mega Gengar stayed in and chose `MOVE_FOCUS_BLAST`; opponent right was then absent from the following turn, matching the reported Grassy Glide KO. This was not primarily a Fake Out valuation problem. The AI already had the player command, but `ShouldSwitch()` built its incoming-threat context from `GetOppositeBattler()`, so the opponent right slot mostly evaluated the player right slot instead of the cross-slot player-left attack targeting it. `SetKnownIncomingMoveForSwitchContext()` now scans every confirmed player-side move under `AI_FLAG_READ_PLAYER_MOVE`, keeps only known single-target damaging moves whose selected target is the current AI battler, and stores the threatening battler plus move slot in `SwitchAiContext`. `ShouldSwitchIfKnownSingleTargetKO()` then runs before the doubles Protect-preservation guard, but it is not a "survive at any cost" rule: if the confirmed selected move OHKOs the active battler, the AI only hard switches when an eligible reserve can comfortably absorb the same move as a defensive pivot, currently no damage or more than `AI_DEFENSIVE_KO_THRESHOLD` hits to KO after entry damage, healing, weather, status, and item effects. If the best reserve merely survives while taking a punishing hit, the route falls through and the existing Protect-preservation / stay-in logic can still handle the turn. This keeps Incineroar switching into the known Grassy Glide line, while avoiding a forced Incineroar switch into a known High Horsepower-style punish. Among safe switch-ins, Fake Out-style first-turn moves are only a tie-breaker, so Incineroar is chosen because it safely absorbs the known move first, not because Fake Out hides the immediate KO risk.

The later pid `297174` autosave at `/tmp/manual-ai-log-battle-action-log-autosave.json` was generated at `2026-06-06T13:58:27Z` with 28 entries through turn 6. It was not the reported no-Mega / no-gimmick 4v4 because it records one selected `GIMMICK_DYNAMAX` and one selected `GIMMICK_TERA`. The log did show generally stronger play after the cross-slot work: early Tailwind plus Dynamax pressure, a Protect / switch reaction around turn 1, and later pivoting rather than pure tunnel attacks. The only suspicious visible line was turn 5 `Make It Rain` into the protected player-right slot; because the action-log target for spread moves is still one battler, this needs a fuller replay before treating it as a bad single-target read. Searching `/tmp` and the archived `manual-ai-log-*` session metadata did not find a complete no-Mega / no-gimmick 4v4 action log. The only gimmick-free `/tmp` candidate was `/tmp/battle-action-log-export-test.json`, but it contained only six turn-0 entries from an export smoke, not a full 4v4 match.

The pid `317741` autosave from live session `manual-ai-log` matched pid `317741`, was generated at `2026-06-06T14:42:11Z`, and contained 42 entries through turn 8. The visible pattern was not only raw damage or Fake Out timing: `Amoonguss` had `Rage Powder` and `Pollen Puff`, but the recorded turns showed `Spore`, offensive `Pollen Puff`, and `Protect` winning the move choice instead of intentional partner support. `Rage Powder` had no read-mode score for a confirmed single-target player attack aimed at the partner, and `Pollen Puff` ally healing was only a weak generic partner-target exception. `AI_DoubleBattle()` now scores ally `Pollen Puff` / `Heal Pulse` from partner HP, setup value, active gimmick state, and speed; `EFFECT_FOLLOW_ME` now gets an explicit read-player-move protection score when a confirmed single-target damaging move is targeting the partner and redirection is survivable or meaningfully absorbs damage. This lets `Rage Powder` beat `Spore` / attacks in the exact "we know the player is targeting my partner" case, while keeping redirection tactical instead of automatic.

The same pid `317741` feedback expanded doubles ally-benefit play beyond pure healing. Intentional partner attacks now score `Steam Engine` plus `Weakness Policy` as a strong combined payoff, score `Stamina` when the partner can cash the Defense boost through `Body Press` or is under physical pressure, keep the existing Beat Up / Justified / Rage Fist coverage, and allow guaranteed-critical ally hits into `Anger Point`. Partner-target `Psych Up` is no longer rejected as a bad target when copying ally boosts is useful. Field stat reset now prefers `Haze` when ally stat drops from moves such as `Draco Meteor` / `Overheat` outweigh the cost of clearing ally boosts or enemy drops. Ally `Clear Smog` is intentionally weaker: it is only a score fallback when Haze is unavailable and the ally's drops clearly outweigh its boosts, matching the manual note that black haze is common while ally Clear Smog is rare.

`BS_TryHealPulse()` was also tightened so `Pollen Puff` ally healing does not call the `EFFECT_HEAL_PULSE` move-property accessor used by `Floral Healing`. `EFFECT_HIT_ENEMY_HEAL_ALLY` now takes the normal 50% heal path instead of risking an assertion when the move has no Heal Pulse property argument.

`gBattleActionLog` is the first runtime storage layer for battle command audits. It is a 128-entry in-ROM ring buffer cleared at battle start. Once all battlers confirm actions, it records one command entry per live battler with action, move, item, target, move slot, party switch-in index, selected gimmick, turn, and sequence. `Cmd_getswitchedmondata()` also records resolved switch-ins; if the engine receives an invalid switch-in index, it now corrects to an arbitrary valid switch-in without showing a debug assert screen and marks that entry as corrected. `AI_FLAG_READ_PLAYER_MOVE` uses the current confirmed command first, then normal prediction, then the action log's last selected move as a fallback for current-battle reads.

`tools/mgba_live/battle_action_log_export.lua` is the persistent export layer for that runtime buffer. It resolves `gBattleActionLog` and `gBattlerPositions` from `pokeemerald.map`, reads the 24-byte packed entries from EWRAM, attaches names for actions, moves, items, gimmicks, battler positions, and flag markers, then writes host JSON with schema `pokeemerald.battle_action_log.v1`. `export_battle_action_log.sh` and `export_battle_action_log.bat` are convenience wrappers for WSL / Linux and Windows; both let manual test sessions dump the current battle log without editing Lua paths by hand. The WSL / Linux wrapper can now omit `SESSION` when a live active session exists, and reports a direct `start_mgba_live.sh manual-ai-log 120` hint when no session is running. The exporter also supports `BATTLE_ACTION_LOG_SKIP_EMPTY` for startup autosave, so empty post-battle buffers do not overwrite the last useful host JSON.

`tools/mgba_live/battle_action_log_autosave.lua` is a startup Lua script for mGBA Live. `start_mgba_live.sh` and `start_mgba_live.bat` load it by default and set `BATTLE_ACTION_LOG_OUT` to `/tmp/<session>-battle-action-log-autosave.json` on WSL / Linux, unless the caller overrides the path. It periodically calls the exporter and keeps the latest non-empty action log on the host. This does not recover a log after a session has already been closed without autosave; it prevents that loss on later sessions.

`start_mgba_live.sh` and `start_mgba_live.bat` are the matching session-start shortcuts. They default to `pokeemerald.gba`, `videoSync=1`, a 120 FPS target, and battle action log autosave, with optional `SESSION`, `FPS`, and `ROM` arguments for manual battle-log sessions.

Player move-selection now clears transient gimmick UI state before drawing a new battler's move menu. Upstream `PlayerHandleChooseMove()` cleared `gBattleStruct->gimmick.playerSelect` after `InitMoveSelectionsVarsAndStrings()`, so double battles could draw the second player battler's move names as Z-Move or Max Move options if the first player battler had just selected a gimmick. The command returned by the controller was usually correct after the later reset, but the first rendered move-menu contents could be stale until the cursor moved or the menu was redrawn. The reset now happens before the move-name / move-type / PP windows are populated, and `zmove.viewing` is also cleared on entry so a previous Z-Move detail view cannot influence the next battler's initial draw.

`DecideTerastalAgainstTarget()` also keeps an explicit copy of damage dealt with Tera. Earlier no-Tera evaluation can rewrite `gAiLogicData->simulatedDmg` for follow-up decisions, so later Tera reconsideration now restores the Tera damage values when the AI commits. Without that restoration, the Tera branch could appear worse than it actually was after a previous conservation pass.

For double battle switching, `ShouldSwitchIfDoublePositionBad()` adds a VGC-style positioning check under `AI_FLAG_SMART_SWITCHING`. The AI may hard switch when the active Pokemon has no meaningful pressure into either opposing slot, is threatened by either opposing slot, has enough HP to be worth preserving, and its partner cannot cover the position. The check stays out of `AI_FLAG_SEQUENCE_SWITCHING`, respects existing no-switch gates, avoids overriding the existing Intimidate-blocker contract, and allows double switches when both active Pokemon are pinned.

Double-battle switch execution validates the final party index before emitting `B_ACTION_SWITCH` or returning a post-KO `ChoosePokemon` result. This fixes a debug ASSERT path where the smart-switch fallback could finish with no valid candidate (`-1`, `PARTY_SIZE`, a fainted mon, an active partner's mon, or a partner-reserved mon) and then pass that invalid index into `Cmd_getswitchedmondata()` / `IsValidSwitchIn()`. If an optional mid-turn switch has no valid target after validation, the AI clears the switch request and falls back to a normal move action instead of crashing the battle script. Post-KO / forced controller selection now rechecks the advanced result and falls back to the first valid reserve. The final script command is also non-asserting now: an invalid switch-in reaching `Cmd_getswitchedmondata()` is corrected and logged instead of stopping the debug ROM on the blue assert screen.

For board-control switching, `ShouldSwitchIfBoardControlBenefit()` lets `AI_FLAG_SMART_SWITCHING` identify reserve Pokemon that can immediately or soon change the board. The selector can choose weather setters (`Drizzle`, `Drought`, `Sand Stream`, `Snow Warning`), terrain setters (`Electric Surge`, `Grassy Surge`, `Misty Surge`, `Psychic Surge`, `Hadron Engine`), speed-control setters carrying `Tailwind` or `Trick Room`, status-pressure support, status-prevention / cure support, terrain seed plans, and ability bridge support such as `Skill Swap`, `Role Play`, and `Entrainment`.

The board-control switch is deliberately narrower in singles than doubles. Singles still need bad odds, a bad matchup, missing current pressure, an unfavorable field, or an immediate status-absorption payoff to replace. Non-immediate status pressure / support pivots are double-battle only in this slice so the AI does not abandon a winning singles 1v1 only because the bench has a utility move. Doubles can pivot more proactively once the reserve candidate itself has a clear weather, terrain, Tailwind, Trick Room, status, seed, or ability-bridge payoff, matching the VGC positioning model where a bench Pokemon can create pressure instead of merely absorbing damage.

The Read Double log also tightened the doubles switching gates. Optional all-moves-bad, board-control, double-position, and bad-odds switches now respect a `Protect` preservation guard: if the active double battler has a usable Protect-style move, is under pressure, is not trying to escape status / Yawn / perish / Encore / similar volatile pressure, and the incoming threat does not ignore Protect, the AI can stay in and let move scoring select Protect instead of immediately pivoting. Delayed Tailwind / Trick Room support pivots are still legal, but in doubles they now need a real pressure case when the switch-in has no immediate entry effect. Immediate weather / terrain / status-benefit pivots and dedicated switch reasons such as trapping, absorbers, and state-clearing still keep their existing routes.

Status-aware board-control support includes:

- Non-volatile status pressure, secondary status effects, and related pressure moves (`Spore`, `Yawn`, `Toxic`, `Will-O-Wisp`, paralysis moves, freeze / frostbite effects, `Toxic Spikes`, `Leech Seed`, `Swagger`, and confusion pressure).
- Team status care with `Heal Bell` / `Aromatherapy`.
- Status prevention with `Safeguard` or `Misty Terrain` when the opposing side has known status pressure.
- Self-status / status-benefit lines for `Guts`, `Quick Feet`, `Marvel Scale`, `Magic Guard`, `Poison Heal`, `Toxic Boost`, `Flare Boost`, `Facade`, and `Psycho Shift`.
- Terrain seed activation when the reserve setter creates the matching field.
- Aurora Veil only when snow / hail is already active or the reserve ability can create it.
- Skill Swap-style bridge moves when the reserve or active partner has a board-control ability worth moving or copying.

AI runtime knowledge now has a shared move / ability / item interpretation layer in `battle_ai_util`. The layer maps existing move flags into AI categories such as sound, bullet, powder, slicing, punching, biting, pulse, dance, wind, healing, Magic Coat, Snatch, ability-control, move-denial, and combo-state. It also maps abilities into strategy groups such as move immunity, move power, damage race, status interaction, field control, positioning, ability control, stat control, item control, priority, and form / state. Held items are mapped through hold effects into damage race, defensive race, stat control, Speed / order control, recovery, status cure, self-status, field duration, contact / hit punishment, move-shape modifiers, ability protection, choice lock, positioning, and gimmick unlock. It also exposes `AI_CanBattlerIgnorePredictedMove()`, which bridges active abilities and held items for prediction decisions using Mold Breaker-sanitized ability checks and enabled-item checks. Predicted-Taunt switching now calls this shared predicate instead of maintaining a local Taunt-only copy.

`tools/runtime_knowledge` adds the first offline catalog generator for larger knowledge collection. It is a Rust tool with no third-party crates. The generator reads the local expansion source and writes review JSON for moves, abilities, hold effects, items, gimmick policy, and a summary. This gives later Pokemon Wiki, VGC / official tournament, Champions usage, PartyGen, and observed-history adapters a local runtime baseline instead of requiring one-off source audits for every AI heuristic.

Predicted-Taunt support is intentionally separate from "bad move" switching. `ShouldSwitchIfPredictedTauntPunish()` only runs when `AI_FLAG_SMART_SWITCHING` and `AI_FLAG_PREDICT_MOVE` are both active and the incoming move is predicted as `Taunt`. It requires the current Pokemon to depend on important status moves, rejects positions where the current Pokemon can already damage-race or 2HKO the target, and skips Pokemon protected by `Aroma Veil`, Gen 6+ `Oblivious`, or an enabled Gen 5+ `Mental Herb`. If those gates pass, the selector evaluates eligible reserves as free switch-ins and chooses a damaging attacker that can win the immediate 1v1 or cross the switch-in damage threshold. The goal is to model "pivot an attacker into a predicted Taunt" without making the AI flee only because Taunt would block a utility move.

Protect scoring now separates singles and doubles payoff. In singles, first-turn Protect is no longer rewarded just because a damaging move is predicted. `ShouldUseSinglesProtect()` asks whether Protect gains the AI a turn or a future option: residual damage on the target, incoming Wish recovery, Poison Heal / Leftovers / Black Sludge recovery, Substitute-threshold recovery, choice-lock scouting, Disable / Encore follow-up, Explosion-style avoidance, or target secondary damage. It also avoids passive Protect against an already boosted attacker unless there is a Wish or Disable / Encore line to justify the stall.

Consecutive Protect remains possible. The second Protect is penalized for its reduced success rate, but it is not treated as impossible. Singles can still value a second Protect when the same payoff remains, and doubles get a lighter second-Protect penalty so high-pressure VGC-style "protect again" turns stay available. Third and later consecutive Protect attempts remain heavily discouraged.

Future usage / ranking data should come from source-tagged competitive data such as Pokemon Battle DataBase, official event reports, Pokemon Home / Champions-style usage when available, local PartyGen catalogs, and observed battle history. Pokemon Showdown articles should not be used as strategy source material for this feature; at most, raw data or team examples can be inspected with a clear source tag and lower confidence.

The debug Party menu exposes focused runtime fixtures and weighted gauntlet battles:

- `Battle 3v3 Single` builds a level-50 player team of `Dragonite`, `Gholdengo`, and `Garchomp`, then pits it against a 3-Pokemon AI side selected from a 6-Pokemon weighted singles pool.
- `Battle 4v4 Double` builds a level-50 player team of `Incineroar`, `Rillaboom`, `Flutter Mane`, and `Urshifu-Rapid-Strike`, then pits it against a 4-Pokemon AI side selected from a 7-Pokemon weighted doubles pool.
- `Battle Dmax/Z Single` builds a level-50 player-side Z-Move team, then pits it against a smart AI Dynamax / Gigantamax / Z-Crystal team.
- `Battle Dmax/Z Double` builds a level-50 player-side VGC-style Z-Move team, then pits it against a smart AI Dynamax / Gigantamax / Z-Crystal doubles team with Tailwind / weather pressure.
- `Battle Gimmick Single` builds a passive player side and an opponent audit party with lead `Gengarite` Mega, reserve `Mimikium Z`, reserve Gigantamax `Charizard`, and reserve Normal Tera `Dragonite`. It grants all debug gimmick access to prove the opponent's source data and runtime gates can expose every gimmick.
- `Battle Gimmick Double` builds a passive double player side and an opponent audit party with active Gigantamax `Charizard` plus `Electrium Z` `Tapu Koko`, then reserve `Gengarite` Mega and Normal Tera `Dragonite`. It is an availability audit, separate from the smarter Dmax/Z timing fixtures.
- `Gauntlet Battles` opens a dedicated submenu for the generation-specific and mixed gauntlet fixtures. Keeping the gauntlets in a submenu avoids overflowing the debug menu's 20-item list limit.
- `Mega Single` / `Mega Double` build level-50 3v3 singles and 4v4 doubles battles with Mega-only access.
- `MegaZ Single` / `MegaZ Double` build level-50 3v3 singles and 4v4 doubles battles with Gen 7-style Mega + Z-Move access.
- `Dmax Single` / `Dmax Double` build level-50 3v3 singles and 4v4 doubles battles with Dynamax-only access.
- `Tera Single` / `Tera Double` build level-50 3v3 singles and 4v4 doubles battles with Tera-only access.
- `Mix Single` / `Mix Double` build level-50 3v3 singles and 4v4 doubles battles with all debug gimmick access and mixed Mega / Z-Move / Dynamax / Tera weighted pools on both sides.
- `Read Double` builds a level-50 4v4 doubles battle with all debug gimmick access from mirrored 8-Pokemon weighted pools. This is the reset-style manual read audit: both sides can roll `Tornadus`, `Incineroar`, `Farigiraf`, `Rillaboom`, `Miraidon`, `Koraidon`, `Gengar`, and `Tapu Koko`, so player and AI grade stay close while the AI's advantage comes from read-mode flags and known-player-party logic.
- `Partner Double` builds a fixed level-50 4v4 doubles battle with all debug gimmick access for intentional ally-benefit checks. Player side leads `Miraidon` plus `Incineroar` with `Rillaboom` and Mega `Gengar` in reserve. AI side leads `Amoonguss` plus `Xerneas` with `Pelipper` and `Coalossal` in reserve, so manual replays can inspect `Rage Powder`, `Pollen Puff`, and `Water Gun` into ally `Steam Engine` / `Weakness Policy` without relying on a random pool roll.

These AI fixtures enable `Smart Trainer`, `Prediction`, `Smart Gimmick`, `Know Opponent Party`, and `Powerful Status` AI flags. The gauntlet AI fixtures also include `Smart Switching`, `Smart Mon Choices`, explicit `Omniscient`, `Aggressive Gimmick`, and `Read Player Move` so the AI can test full-information move reads, stronger gimmick usage, and known player party species together. The pool entries carry `Pool Weight` plus role tags such as `Lead`, `Ace`, and `Support`, so the pool-based fixtures also exercise Trainer Party Pool role filtering and weighted selection.

The gauntlet battles are intended to be same-grade or near-same-grade matches rather than raw-stat stomps. Player and AI sides are both generated from weighted local pools on each start, and the player pools now include comparable restricted legends, mythicals, strong support, and high-value gimmick anchors for the selected format. The AI's intended edge comes from its decision quality checks: it has `Omniscient` and `Know Opponent Party`, so the manual test target is whether full-information prediction, switching, and gimmick timing can beat a visible player team of roughly similar power. With `B_POOL_SETTING_CONSISTENT_RNG == FALSE`, repeated debug starts can produce different player and opponent teams. Mega and Mega+Z pools use `Lead` / `Ace` tags so the selected party reliably contains the intended Z or Mega slot while still changing the supporting Pokemon.

The mixed gauntlets are intentionally noisier than the generation-specific fixtures. They grant `GIMMICK_ACCESS_ALL` and mix strong Tera attackers, explicit Z-Crystal users, Mega Stone users, Gigantamax users, support leads, and restricted legends in the same weighted pool. Use these when the manual question is "does the AI still read and sequence correctly when both teams may roll several gimmick families at once?"

`Read Double` is intentionally smaller and cleaner than `Mix Double`. It keeps the same 8-Pokemon pool on both sides, with lead / ace tags and weights, so repeated debug starts can test whether the AI is actually winning through command reads, target selection, switching, and gimmick timing rather than through a larger or stronger opponent roster.

`Partner Double` is a targeted manual replay route for the ally-benefit mechanics that are awkward to force from a weighted pool. It keeps production-style level-50 sets, items, abilities, natures, IVs, EVs, and doubles roles, but fixes the four Pokemon on each side so the first turns expose the support / setup board immediately.

Gimmick access is split from AI timing:

- `B_FLAG_GIMMICK_ACCESS_ALL` grants Mega Ring, Z-Power Ring, Dynamax Band, and charged Tera Orb access for a special ruleset.
- `B_FLAG_GIMMICK_ACCESS_MEGA_RING`, `B_FLAG_GIMMICK_ACCESS_Z_POWER_RING`, `B_FLAG_GIMMICK_ACCESS_DYNAMAX_BAND`, and `B_FLAG_GIMMICK_ACCESS_TERA_ORB` allow individual formats such as Mega-only, Tera-only, or Z + Dynamax.
- `HasGimmickAccess()` centralizes Bag item checks and runtime/debug access overrides. Mega, Z, Ultra Burst, Dynamax, and Tera now read the same access layer.
- Debug Party Dmax/Z fixtures pass `GIMMICK_ACCESS_Z_POWER_RING | GIMMICK_ACCESS_DYNAMAX_BAND` through `gDebugGimmickAccessFlags` instead of using an AI flag as a ruleset unlock.
- `.party` supports `Z Move: Yes`, stored as `TrainerMon.shouldUseZMove` and copied into `gBattleStruct->opponentMonCanZMove` for opponent parties. Ordinary Z-Move availability still requires a matching Z-Crystal and Z-Power access.
- The earlier itemless Z-Move unlock was removed so AI flags remain tactical timing / ruleset-intent hints, not replacement access items.

Opponent gimmick use is not disabled. AI-side Mega / Z-Move / Dynamax / Tera availability still comes from trainer party data, held items, and the battle gimmick gates, and the focused AI tests confirm that the opponent can spend those gimmicks. The earlier `Battle Dmax/Z Single` and `Battle Dmax/Z Double` fixtures did not include an AI Mega candidate, and their Z-Move users were not both lead-visible, so they were poor "is the opponent allowed to use every gimmick?" audits. The new `Battle Gimmick Single` and `Battle Gimmick Double` fixtures are explicit availability audits. Under `AI_FLAG_SMART_GIMMICK_TIMING`, trainer gimmick data remains permission rather than an immediate command: Dynamax, Z-Move, Mega, and Tera can be conserved until the selected turn has KO, board-control, defensive, setup, late-commit, or last-Pokemon value.

AI-side debug availability does not require giving the opponent Bag key items such as Mega Ring, Z-Power Ring, Dynamax Band, or Tera Orb. Player-side access still uses those Bag items or `B_FLAG_GIMMICK_ACCESS_*` / `gDebugGimmickAccessFlags`, but opponent-side use is driven by the trainer-party fields: Mega Stone for Mega, Z-Crystal plus `Z Move: Yes` for Z-Move, `Dynamax Level` plus `Dynamax: Yes` for Dynamax / Gigantamax, and `Tera Type` for Tera. A debug-battle-only bug dropped the opponent `opponentMonCanDynamax`, `opponentMonCanZMove`, and `opponentMonCanTera` bits because the Party debug menu creates the opponent party before `gBattleStruct` is allocated, then `CB2_InitBattle()` zero-initializes the battle struct. The fix stores those opponent gimmick bits as pending data when `CreateNPCTrainerPartyFromTrainer()` runs before allocation and restores them immediately after `AllocateBattleResources()`. Normal trainer battles still set the bits directly during the normal battle-init party creation path.

`AssignUsableGimmicks()` still exposes one active gimmick candidate per battler in enum order. If one Pokemon is made eligible for multiple gimmicks, the earlier candidate wins: Mega / Ultra Burst before Z-Move, then Dynamax, then Tera. Availability audit parties and gauntlet pools should therefore keep Mega, Z-Move, Dynamax, and Tera candidates on separate Pokemon unless a test intentionally covers that priority rule.

Double-battle move comparison now treats 100% accurate moves and accuracy-bypassing moves as neutral for accuracy priority. This prevents rain-accurate `Thunder` from beating a 100% accurate move solely because the internal accuracy value is `101`. The same comparison pass now gives a small board-pressure preference to `TARGET_BOTH` spread damage when it can meaningfully pressure the other opposing battler too. This covers the Kyogre / rain case where `Water Spout` or `Origin Pulse` should remain a strong doubles line instead of tunneling `Thunder` into one target only.

Debug battles now skip `BattleTypeAllowsExp()` through `gIsDebugBattle`. This prevents the EXP bar, normal EXP gain, and EV gain in debug battles, including the level-100 EV-gain path that would otherwise still occur when EXP is disabled by level.

Battle ability popups are now guarded against duplicate same-battler instances. The popup renderer formats nicknames with the same nickname terminator handling used by normal battle text, and `CreateAbilityPopUp()` refreshes an existing popup for that battler instead of creating a second pair of sprites that shares the same tile tag and active flag. Mega Evolution, Wish Mega Evolution, Primal Reversion, Ultra Burst, Terastallization, and generic form-change entry scripts also close any active ability popup before starting their form-change message / animation. The normal ability popup script keeps the upstream `showabilitypopup` timing, including Fairy Aura / Dark Aura style switch-in popups, while palette ownership for the shared `TAG_ABILITY_POP_UP` palette is now centralized across Ability Popup, Last Used Ball Window, and Move Info Window. This prevents stale switch-in ability banners such as `Mewtwo's Pressure` from lingering over a Mega Evolution message, and avoids old / new popup sprite overlap or palette reuse that can leave black / wrong-color OBJ rectangles after Delta Stream / strong-winds state changes or when a move-info sprite closes during a switch-in ability popup.

## Tests Added

- Conservation baseline: keeps Dynamax unused when another Dynamax user remains and the move has no immediate payoff.
- Last-Pokemon baseline: spends Dynamax when no reserve remains.
- One-reserve late-commit baseline: spends Dynamax when the active Pokemon is low on HP and only one reserve remains, even if the opponent is passive.
- Max Geyser payoff: spends Dynamax to set rain even with a reserve remaining.
- Max Airstream payoff: spends Dynamax in doubles for Speed-control tempo even with a reserve remaining.
- Dynamax disruption payoff: spends Dynamax to keep a damaging move live through Fake Out-style flinch or Roar / Whirlwind-style phazing.
- Smart Z conserve: keeps a damaging Z-Move unused when another Pokemon remains and the Z-Move has no immediate payoff.
- Smart Z last Pokemon: spends a damaging Z-Move when no reserve remains.
- Smart Z one-reserve low-HP: spends a damaging Z-Move before it is stranded by over-conservation.
- Smart Z trap guard: conserves a damaging Z-Move against a trapping target when there is no damage-race payoff, while separate low-HP / last-Pokemon cases still spend.
- Low-HP pressure attackers: spends Dynamax and damaging Z-Move lines from production-style sets when the active Pokemon is low with one reserve remaining.
- Read-player-move debug prediction: with `AI_FLAG_READ_PLAYER_MOVE`, spends Dynamax into an already confirmed `Fake Out`, scores attacks against a selected player defensive Tera type, scores against the actual selected switch-in, and retargets ordinary single-target damage away from a confirmed selected `Protect`.
- Read-player-move cross-slot switching: with real VGC-style Rillaboom / Tornadus / Mega Gengar / Incineroar sets copied from `src/data/debug_trainers.party` conventions, switches Mega Gengar to Incineroar when player-left selected Grassy Glide is known to target and KO opponent-right, but does not burn the same Incineroar switch-in into a known High Horsepower punish.
- Partner synergy fixtures: with production-style level-50 doubles sets, heals a low-HP setup ally with `Pollen Puff`, uses `Rage Powder` to protect a known targeted ally, intentionally activates ally `Steam Engine` plus `Weakness Policy`, triggers ally `Stamina` for `Body Press`, triggers ally `Anger Point` with a guaranteed critical hit, copies ally boosts with `Psych Up`, uses `Haze` to reset ally `Draco Meteor` drops, and keeps ally `Clear Smog` as a lower-priority score fallback when Haze is unavailable.
- Gimmick access separation: `AI_FLAG_GIMMICK_ENV_ALL` keeps Z-Move timing tactical while Z-Move availability remains tied to Z-Crystals and Z-Power access.
- Smart Mega Shadow Tag: spends Mega Evolution when the target form's ability creates immediate trapping pressure.
- Smart Mega low-HP late-commit: spends Mega Evolution with one reserve remaining, while a separate setup-delay test still verifies full-HP no-pressure conservation.
- Smart Switching doubles: can double switch out of bad double positions when neither partner can cover.
- Smart Switching doubles guard: stays in a bad position when the partner can cover the target.
- Smart Switching weather pivot: switches to a `Drizzle` reserve when rain improves reserve pressure.
- Smart Switching terrain pivot: switches to a `Grassy Surge` reserve that can change board control.
- Smart Switching speed-control pivots: switches to Tailwind and Trick Room reserves when those controls can flip the speed state.
- Smart Switching terrain seed pivot: switches to a terrain setter whose field triggers the reserve's seed plan.
- Smart Switching status pivots: switches into a status-benefit reserve under predicted burn, and in doubles can pivot to direct or secondary status / confusion pressure support.
- Smart Switching ability bridge pivot: switches in Skill Swap support when a partner board-control ability creates a bridge plan.
- Smart Switching Taunt reads: pivots an attacker into a predicted `Taunt`, stays in when the active Pokemon can punish with damage, and stays in when the active Pokemon ignores Taunt.
- Runtime knowledge layer: verifies AI move-category flags, ability-category flags, item / hold-effect category flags, and predicted-move immunity bridges for Magic Bounce, Safety Goggles / powder immunity, and ordinary non-ignored moves.
- Runtime knowledge catalog tool: generates local runtime review JSON for 935 moves, 319 abilities, 130 hold effects, 874 items, and 4 gimmick policies.
- Protect singles payoff scoring: avoids passive singles Protect with no turn-gain payoff, avoids passive Protect against boosted attackers, values Protect when residual damage creates payoff, and keeps a second singles Protect viable when the payoff remains.
- Protect doubles consecutive scoring: keeps a second double Protect as a risky but possible option instead of treating it as impossible.
- Kyogre doubles spread-pressure regressions: keeps rain-boosted `Water Spout` above `Thunder` when both opposing slots are pressured, including Tornadus + Incineroar boards where rain `Thunder` is internally accuracy-bypassing.
- Debug battle EXP gate: verifies a `gIsDebugBattle` trainer battle does not show the EXP bar and does not award EXP or EVs.
- Existing Tera, Mega, Z-Move, and combined environment tests remain in `ai_smart_gimmick.c`.

## Validation

Local validation highlights:

- `rtk make -j16 -O check TESTS='AI runtime knowledge'`: pass, 4 tests. Covers move-category, ability-category, item / hold-effect category mapping, and predicted-move immunity bridges.
- `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV'`: pass, 10 tests.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV'`: pass, 11 tests. Includes the Z-Crystal-based all-gimmick Z-Move regression after access flags were split from AI flags.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV'`: pass, 13 tests. Includes Dynamax / Z / Mega late-commit regressions and the all-gimmick environment checks.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_ONLY'`: pass, 7 tests. Adds the one-reserve low-HP Dynamax late-commit regression.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_ALL'`: pass, 4 tests. Adds the one-reserve low-HP Mega late-commit regression while keeping full-HP setup-turn delay.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV'`: pass, 13 tests after preserving pre-battle debug opponent gimmick bits through `CB2_InitBattle()`.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI does not tunnel Kyogre'`: pass, 1 test after making rain-accurate `Thunder` neutral with 100% moves and preferring meaningful doubles spread pressure.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI prefers Kyogre'`: pass, 1 test after the same doubles spread-pressure comparison change.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 4 tests. Covers the read-player-move Fake Out Dynamax regression, the selected-player-Tera damage read, the actual selected switch-in read, and the in-ROM command log capture for all four choices in a double battle turn.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 5 tests. Adds the Protect-retarget regression from `/tmp/manual-ai-log-captured-20260606`, where a full-information gauntlet AI should avoid ordinary single-target damage into a confirmed player `Protect`.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 8 tests. Adds the pid `222151` manual-log Fake Out pressure regression, the `Extreme Speed` priority-order check, and the `Armor Tail` priority-blocker guard.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 11 tests. Adds the pid `278133` cross-slot Grassy Glide KO switch regression and the paired High Horsepower guard where Incineroar is not forced into a punishing known hit. The tests use real VGC-style Rillaboom / Tornadus / Mega Gengar / Incineroar moves, held items, abilities, natures, IVs, and fixed equivalent stats rather than placeholder Wobbuffet / Celebrate fixtures.
- 2026-06-06 `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`: pass, 31 tests after the pid `278133` fix.
- 2026-06-06 `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`: exit 0 after the pid `278133` fix. Existing known-failing labels remain known-failing; no new failures.
- 2026-06-06 `rtk make -j16 -O check TESTS='test/battle/ai/ai.c'`: exit 0 after the pid `278133` fix.
- 2026-06-06 `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`: exit 0 after the pid `278133` fix. Existing known-failing / TODO labels remain unchanged.
- 2026-06-06 `rtk make -j16 -O check TESTS='test/battle/ai/ai_choice.c'`: pass, 11 tests after the pid `278133` fix.
- 2026-06-06 `rtk make -j16 -O check TESTS='test/battle/ai/ai_calc_best_move_score.c'`: pass, 17 tests after the pid `278133` fix.
- 2026-06-06 `rtk make -j16 -O check TESTS='test/battle/ai'`: non-evidence, `No tests found`; AI directory validation used file-level filters above.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: selected Fake Out'`: pass, 2 tests. Confirms ordinary slower actions and lower-priority `Extreme Speed` are discounted under read-mode Fake Out pressure.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: blocked selected Fake Out does not discount ordinary actions'`: pass, 1 test. Confirms priority blockers stop the read-mode Fake Out pressure penalty.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_AGGRESSIVE_GIMMICK'`: non-evidence, `No tests found` after replacing the placeholder aggressive fixtures with deterministic production-style low-HP pressure cases in `ai_smart_gimmick.c`.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_TERA_ONLY'`: pass, 1 test.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_TERA'`: pass, 1 test.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_ALL'`: pass, 4 tests. Covers held Z-Crystal use, doubles Tera, setup-turn Mega delay, and low-HP one-reserve Mega late commit.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV'`: pass, 13 tests after the command-buffer read update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV'`: pass, 13 tests after the read-mode Fake Out priority-pressure update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 11 tests after the pid `317741` ally-benefit update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_ONLY'`: pass, 8 tests after the pid `317741` ally-benefit update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_SMART_Z_MOVE'`: pass, 5 tests after the pid `317741` ally-benefit update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_ALL'`: pass, 4 tests after the pid `317741` ally-benefit update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_TERA_ONLY'`: pass, 1 test after the pid `317741` ally-benefit update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_TERA'`: pass, 1 test after the pid `317741` ally-benefit update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_SMART_MEGA'`: pass, 1 test after the pid `317741` ally-benefit update. Together with the preceding focused `AI_FLAG_*` filters, this is the requested adjusted smart-gimmick / read target set; the current filter total is 31 tests.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI heals a low HP setup ally with Pollen Puff'`: pass, 1 test.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI uses Rage Powder'`: pass, 1 test.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI triggers ally'`: pass, 2 tests. Covers ally `Stamina` / `Body Press` and ally `Steam Engine` plus `Weakness Policy`.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI uses a guaranteed critical hit'`: pass, 1 test for ally `Anger Point`.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI uses Psych Up'`: pass, 1 test.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI uses Haze to reset an ally'`: pass, 1 test.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI can score Clear Smog'`: pass, 1 test.
- 2026-06-06 `rtk make -j16 -O check TESTS='Pollen Puff'`: pass, 1 existing multi-battle Pollen Puff scoring test.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI will choose Beat Up'`: pass, 2 existing ally `Beat Up` AI tests for `Justified` and `Rage Fist`.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI will only Beat Up'`: pass, 1 existing `Rage Fist` guard.
- 2026-06-06 `rtk make -j16 -O check TESTS="AI will trigger its ally's Weakness Policy"`: pass, 1 existing ally `Weakness Policy` AI test.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_'`: non-evidence broad filter. It picked unrelated existing AI-flag tests outside this change and failed at `AI_FLAG_ATTACKS_PARTNER is willing to kill either the partner or the player 4/6`; the focused target filters above are the accepted evidence.
- 2026-06-06 `rtk make -j16 -O debug`: pass after the command-buffer read update.
- 2026-06-06 `rtk make -j16 -O all`: pass after the command-buffer read update.
- 2026-06-06 `rtk make -j16 -O debug`: pass after the read-mode Fake Out priority-pressure update.
- 2026-06-06 `rtk make -j16 -O all`: pass after the read-mode Fake Out priority-pressure update.
- 2026-06-06 `rtk make -j16 -O all`: pass after the pid `278133` cross-slot switch update.
- 2026-06-06 `rtk make -j16 -O debug`: pass after the player double move-menu gimmick UI reset.
- 2026-06-06 `rtk make -j16 -O all`: pass after the player double move-menu gimmick UI reset.
- 2026-06-06 `rtk make -j16 -O all`: pass after the final pid `278133` cross-slot switch fix, production-style `ai_smart_gimmick.c` fixture rewrite, and documentation updates.
- 2026-06-06 `rtk make -j16 -O check TESTS='(Z-MOVE) Every battler can use Z-Moves - Doubles'`: pass, 1 test, after the player double move-menu gimmick UI reset.
- 2026-06-06 `rtk make -j16 -O check TESTS="Dynamax: Max Strike lowers both opponents' speed"`: pass, 1 test, after the player double move-menu gimmick UI reset.
- 2026-06-06 `rtk make -j16 -O check TESTS="Z-Moves don't leak when used - Doubles"`: non-evidence, `No tests found`; those animation leak fixtures are not available through this filtered check path.
- 2026-06-06 `rtk mdbook build docs`: exit 0 after docs updates. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` HTML end tag, and large search index.
- 2026-06-06 `rtk mdbook build docs`: exit 0 after documenting the read-mode Fake Out priority-pressure update. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` HTML end tag, and large search index.
- 2026-06-06 `rtk mdbook build docs`: exit 0 after the final pid `278133` / pid `297174` log-analysis documentation update. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` HTML end tag, and large search index.
- 2026-06-06 mGBA Live session `smart-gimmick-command-read-20260606`: pass for debug route and first-turn command-buffer runtime. Started the debug ROM with the project script-capable mGBA via `DISPLAY=:0 rtk ... mgba-live-cli`, opened `R+START` debug menu, navigated `Party -> Gauntlet Battles -> Tera Double`, reached battle, entered both player commands, observed the turn resolve to the party switch screen, exported `/tmp/smart-gimmick-command-read-after-turn.png`, and stopped cleanly. Direct MCP `mgba_live_start` without `DISPLAY` failed first with Qt `xcb` display initialization, so validation used CLI start plus MCP input / screenshot tools.
- 2026-06-06 mGBA Live session `smart-gimmick-dmax-double-log-20260606`: pass for the Dmax Double debug route, command-log runtime storage, and the non-assert switch-in fallback path. Continued from the existing save, opened `R+START`, navigated `Party -> Gauntlet Battles -> Dmax Double`, entered multiple double-battle turns, observed opponent switch-in resolution from `Kyogre` to `Charizard`, opponent `Protect`, a later `Landorus` board, and no `IsValidSwitchIn()` blue assert. Read `gBattleActionLog` from EWRAM at `0x020004cc`; the header showed `sequence=15`, `lastRecordedTurn=2`, `cursor=15`, and `count=15`, confirming runtime command / switch entries were being stored. Exported `/tmp/smart-gimmick-dmax-double-log-20260606.png`, stopped cleanly, and `mgba-live-cli status --all` returned `[]`.
- 2026-06-06 mGBA Live session `battle-log-export-20260606`: pass for host JSON export. Started the Dmax Double gauntlet from the existing save, confirmed one double-battle turn, then ran `tools/mgba_live/export_battle_action_log.sh battle-log-export-20260606 /tmp/battle-action-log-export-test.json`. The wrapper initially needed elevated execution because `mgba-live-cli` refreshes `~/.mgba-live-mcp/runtime/active_session`; after approval it returned schema `pokeemerald.battle_action_log.v1`, `entries=6`, `sequence=6`, `cursor=6`, `count=6`, and wrote valid JSON. `python3 -m json.tool` parsed the file, and the entries included named moves such as `MOVE_ASTRAL_BARRAGE` and `MOVE_FAKE_OUT`, opponent `B_ACTION_SWITCH` entries, and `resolved_switch_in` markers. Stopped cleanly and `mgba-live-cli status --all` returned `[]`.
- 2026-06-06 `/tmp/manual-battle-log-check-current.json`: pass for exported field coverage. The exported JSON used schema `pokeemerald.battle_action_log.v1`, header `count=17`, `cursor=17`, `sequence=17`, `last_recorded_turn=2`, and `skipped_invalid=0`. All 17 entries had the required raw fields and decoded names for action, move, item, gimmick, battler position, side / slot, target, party index, and flag markers. The sample covered 10 `B_ACTION_USE_MOVE` entries, 7 `B_ACTION_SWITCH` entries, selected `GIMMICK_Z_MOVE` and `GIMMICK_MEGA` markers, and 5 `resolved_switch_in` entries.
- 2026-06-06 mGBA Live session `fps-wrapper-check-20260606`: pass for the 120 FPS start helper. Ran `tools/mgba_live/start_mgba_live.sh fps-wrapper-check-20260606 120`; `mgba-live-cli status --all` reported alive session state with `fps_target=120.0`, ROM `/home/jastin/dev/pokeemerald-expansion/pokeemerald.gba`, and advancing heartbeat frame. A simple `run-lua` returned `{ result: 123 }`, confirming the Lua bridge responded. `stop --session fps-wrapper-check-20260606` returned `stopped:true`.
- 2026-06-06 mGBA Live session `manual-ai-retest-20260606`: action-log recovery after close was not possible. By the time export was attempted, `mgba-live-cli status --all` returned `[]` and the session had moved to `archived_sessions/manual-ai-retest-20260606-20260606-100152`. The archive contained session metadata, heartbeat, response, bridge script, and empty stdout / stderr logs, but no battle action JSON because host export had not been run before the session closed. Existing `/tmp/manual-battle-log-check-current.json` remained available as the last manual export, but it was not the just-finished double battle.
- 2026-06-06 mGBA Live session `autosave-wrapper-check-20260606`: pass for startup autosave registration. Ran `BATTLE_ACTION_LOG_OUT=/tmp/autosave-wrapper-check-20260606.json tools/mgba_live/start_mgba_live.sh autosave-wrapper-check-20260606 120`; `session.json` listed `tools/mgba_live/battle_action_log_autosave.lua` in `startup_scripts`, and `run-lua` confirmed `_G.BATTLE_ACTION_LOG_AUTOSAVE` existed. No battle was started in this smoke, so `last_sequence=-1` was expected. Stopped cleanly.
- 2026-06-06 mGBA Live session `player-gimmick-menu-reset-20260606`: partial visual evidence for the player double move-menu gimmick UI reset. Started `pokeemerald.gba` at 120 FPS through `tools/mgba_live/start_mgba_live.sh`, `status --all` reported the session alive, `run-lua` returned `{ok=true}`, and screenshot `/tmp/player-gimmick-menu-reset-20260606.png` was exported. The session stopped cleanly. A pre-existing unrelated `manual-ai-log` session remained active and was intentionally not stopped. This was boot / bridge evidence only; the exact double-battle "first player selects Z-Move or Dynamax, second player opens normal move menu" route still needs manual visual confirmation.
- 2026-06-06 mGBA Live session `ai-protect-retarget-20260606`: partial runtime evidence for the Protect-retarget update. Started the updated `pokeemerald.gba` at 120 FPS through `tools/mgba_live/start_mgba_live.sh`, `run-lua` returned `{ok=true}`, screenshot `/tmp/ai-protect-retarget-20260606.png` was exported, and the session stopped cleanly. This was boot / bridge evidence only; the behavior itself is covered by the focused `AI_FLAG_READ_PLAYER_MOVE` test because a full manual gauntlet replay would interrupt the user's active play route.
- 2026-06-06 mGBA Live session `read-fakeout-priority-smoke-20260606`: pass / boot smoke for the read-mode Fake Out priority-pressure update. Started `pokeemerald.gba` at 120 FPS through `tools/mgba_live/start_mgba_live.sh`, `status --all` reported pid `242810` alive with `fps_target=120.0`, `run-lua` returned `{ok=true}`, screenshot `/tmp/read-fakeout-priority-smoke-20260606.png` was exported, `stop` returned `stopped:true`, and final `status --all` returned `[]`. This was boot / bridge evidence; the behavior itself is covered by focused test-runner validation.
- 2026-06-06 mGBA Live session `known-cross-slot-ko-switch-guard-20260606`: pass / boot smoke for the pid `278133` cross-slot switch update. Started updated `pokeemerald.gba` at 120 FPS through `tools/mgba_live/start_mgba_live.sh`, `status --all` reported pid `299495` alive with ROM `/home/jastin/dev/pokeemerald-expansion/pokeemerald.gba`, screenshot `/tmp/known-cross-slot-ko-switch-guard-20260606.png` showed a nonblank in-game field frame, `run-lua` returned `{ok=true, frame=1217}`, and `stop` returned `stopped:true`. Final `status --all` still listed the unrelated `manual-ai-log` session alive, so only the validation session was stopped. This was boot / bridge evidence; the exact behavior is covered by focused test-runner validation.
- 2026-06-06 mGBA Live session `final-smart-gimmick-ai-20260606`: pass / boot smoke after the final source, test, and docs state. Direct MCP start returned Lua `{ok=true, validation=smart_gimmick_ai_final_20260606}`, screenshots reached the title path and exported `/tmp/final-smart-gimmick-ai-20260606.png`, and `stop` returned `stopped:true`.
- 2026-06-06 mGBA Live session `read-double-menu-20260606`: pass for the new `Read Double` gauntlet menu entry. Rebuilt the debug ROM, started at 120 FPS through `tools/mgba_live/start_mgba_live.sh`, captured `/tmp/read-double-menu-20260606-boot.png`, sent `START`, reached `Party -> Gauntlet Battles`, scrolled to the bottom, and captured `/tmp/read-double-menu-20260606-read-double.png` showing `Read Double` rendered without mojibake. `stop` returned `stopped:true`, and final `status --all` returned `[]`. This validates menu availability; the user will run battle rechecks and provide session IDs for action-log review.
- 2026-06-06 mGBA Live session `smart-gimmick-read-aggressive-20260606`: pass for debug route and in-battle reachability. Booted the debug ROM, progressed from `NEW GAME` to field control, opened `R+START` debug menu, navigated `Party -> Gauntlet Battles -> Dmax Double`, reached battle, selected player moves, captured `/tmp/smart-gimmick-read-aggressive-after-input.png`, then stopped cleanly with `status --all` returning `[]`. This is not counted as a visible opponent gimmick-animation proof because early manual inputs KO'd opposing slots before a visible Dynamax / Tera / Z-Move animation was captured.
- 2026-06-06 `rtk make -j16 -O check`: attempted after the debug opponent gimmick-bit preservation and Kyogre spread-pressure fix. It exited 2 with the same broad-run non-fatal test-runner / known-failing output pattern already recorded for this branch, including `Tests resume after CRASH` and `Pokemon level up learnsets fit within MAX_LEVEL_UP_MOVES and MAX_RELEARNER_MOVES`. Focused AI checks above are the validation evidence for this update.
- `rtk make -j16 -O check TESTS='AI_FLAG_SMART_Z_MOVE'`: pass, 3 tests.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_SMART_Z_MOVE'`: pass, 4 tests. Adds the one-reserve low-HP Z-Move late-commit regression.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI uses Z-Moves'`: failed 1 existing broad Z-Move AI case, `AI uses Z-Moves -- Z-Detect 1/2`, where the AI selected `Detect` without the Z-Move gimmick. The smart-gimmick filters passed; this broad status-Z Protect heuristic remains a separate follow-up.
- `rtk make -j16 -O check TESTS='AI_FLAG_SMART_MEGA'`: pass, 1 test.
- `rtk make -j16 -O check TESTS='AI_FLAG_SMART_TERA'`: pass, 4 tests.
- `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_ONLY'`: historical pass, 6 tests. Includes Fake Out and phazing disruption-prevention Dynamax checks before the late-commit regression was added.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_SMART_SWITCHING'`: pass. Includes bad-position double switching, weather / terrain / Tailwind / Trick Room board-control pivots, terrain seed, predicted burn status-benefit, direct and secondary status / confusion support, Skill Swap bridge pivots, predicted-Taunt attacker pivot / stay-in guards, and the double-switch execution guard through the shared predicted-move immunity predicate.
- `rtk make -j16 -O check TESTS='Protect: AI'`: pass, 9 tests. Covers ignore-protection moves, Unseen Fist, passive singles Protect rejection, boosted-attacker rejection, residual payoff, and second Protect scoring in singles and doubles.
- 2026-06-06 `rtk make -j16 -O check TESTS='Protect: AI'`: pass, 9 tests after the Protect-retarget update.
- 2026-06-06 `rtk make -j16 -O check TESTS='Protect: AI'`: pass, 9 tests after the read-mode Fake Out priority-pressure update.
- 2026-06-06 `rtk make -j16 -O check TESTS='Fake Out'`: pass, 2 move-effect tests after the read-mode Fake Out priority-pressure update.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI will Fake Out'`: pass, 4 existing AI Fake Out selection tests after the read-mode Fake Out priority-pressure update.
- `rtk make tools/trainerproc/trainerproc`: pass. Regenerated trainer data from `.party` fixtures, including `Pool Weight`.
- 2026-06-06 `rtk make tools/trainerproc/trainerproc`: pass. Regenerated debug trainer data for `Battle Gimmick Single` / `Battle Gimmick Double`.
- 2026-06-06 `rtk make tools/trainerproc/trainerproc`: pass after adding the 8 weighted gauntlet debug battles.
- 2026-06-06 `rtk make tools/trainerproc/trainerproc`: pass after moving the gauntlets into a submenu and rebalancing player-side gauntlet pools toward same-grade battles.
- 2026-06-06 `rtk make tools/trainerproc/trainerproc`: pass after adding `Mix Single` / `Mix Double` all-gimmick gauntlets.
- 2026-06-06 `rtk make tools/trainerproc/trainerproc`: pass after adding the `Read Double` mirrored 8-Pokemon gauntlet pool.
- 2026-06-06 `rtk make -j16 -O check TESTS='Debug battles do not give exp or EVs'`: pass, 1 test. Covers no EXP bar, no EXP gain, and no EV gain while `gIsDebugBattle` is set.
- 2026-06-06 `rtk make -j16 -O check TESTS='Trainer Party Pool'`: pass, 9 tests. Regresses pool role filtering and weighted selection behavior used by the debug fixtures.
- 2026-06-06 `rtk make -j16 -O check TESTS='Trainer Party Pool'`: pass, 9 tests after adding the gauntlet pools. Covers weighted eligible candidates, tag constraints, runtime RNG variation, custom rules, and fallback.
- 2026-06-06 `rtk make -j16 -O check TESTS='Trainer Party Pool'`: pass, 9 tests after the gauntlet submenu and same-grade player-pool rebalance.
- 2026-06-06 `rtk make -j16 -O check TESTS='Trainer Party Pool'`: pass, 9 tests after adding the mixed all-gimmick gauntlet pools.
- 2026-06-06 `rtk make -j16 -O check TESTS='Trainer Party Pool'`: pass, 9 tests after adding `Read Double`.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 8 tests after adding `Read Double`.
- 2026-06-06 `rtk make -j16 -O check`: exited 2 after adding `Read Double`; `/tmp/read-double-check.log` showed three unrelated existing broad-run AI failures: `AI_FLAG_ATTACKS_PARTNER is willing to kill either the partner or the player 4/6`, `AI_FLAG_SMART_TERA: AI might tera if it gets saved from a ko (1/?)`, and `AI uses Z-Moves -- Z-Detect 1/2`. The focused Trainer Party Pool and read-mode checks above are the accepted evidence for this debug-fixture addition.
- 2026-06-06 `rtk make -j16 -O check TESTS='Mega Evolution'`: pass, 5 tests after the ability-popup refresh / form-change cleanup fix. The first parallel attempt conflicted with another `make check` process and produced `open tmpfd failed: File exists`; the sequential rerun passed.
- 2026-06-06 `rtk make -j16 -O check TESTS='Pressure'`: pass, 5 tests after the ability-popup refresh / form-change cleanup fix.
- 2026-06-06 `rtk make -j16 -O check TESTS='Delta Stream'`: pass, 1 test after the ability-popup refresh / form-change cleanup fix. The first parallel attempt conflicted with another `make check` process and produced a test-runner segmentation fault; the sequential rerun passed.
- 2026-06-06 `rtk make -j16 -O check TESTS='Terastallization'`: pass, 3 tests after the form-change popup cleanup hook.
- 2026-06-06 `rtk make -j16 -O check TESTS='Ultra Burst'`: pass, 5 tests after the form-change popup cleanup hook.
- 2026-06-06 `rtk make -j16 -O check TESTS='Primal Reversion'`: pass, 15 tests after the form-change popup cleanup hook.
- `rtk cargo check --manifest-path tools/runtime_knowledge/Cargo.toml`: pass.
- `rtk cargo run --manifest-path tools/runtime_knowledge/Cargo.toml -- --out /tmp/runtime_knowledge_catalog_rust --pretty`: pass. Generated 935 moves, 319 abilities, 130 hold effects, 874 items, 4 gimmick policies, and a valid summary.
- `rtk make -j16 -O check`: pass. Existing known-failing / expected-failing labels remained non-fatal.
- `rtk make -j16 -O check`: attempted again after the Protect / Dmax-vs-Z debug-fixture update and exited 2. The captured output only exposed existing test-runner / known-failing labels (`Tests resume after CRASH`, `Pokemon level up learnsets fit within MAX_LEVEL_UP_MOVES and MAX_RELEARNER_MOVES`); both pass as expected when filtered individually. The focused AI, debug EXP / EV, and Trainer Party Pool checks above are the validation evidence for this update.
- `rtk make -j1 -O check`: attempted to rule out make-job parallelism, but the run did not progress beyond the initial link warning and was abandoned as non-evidence. The stale `mgba-rom-test-hydra` / `mgba-rom-test` children were killed before handoff.
- `rtk make -j16 -O debug`: pass.
- 2026-06-06 `rtk make -j16 -O debug`: pass after adding `Battle Gimmick Single` / `Battle Gimmick Double`.
- 2026-06-06 `rtk make -j16 -O debug`: pass after adding the 8 weighted gauntlet debug battles.
- 2026-06-06 `rtk make -j16 -O debug`: pass after moving gauntlets under `Gauntlet Battles` and rebalancing the player gauntlet pools.
- 2026-06-06 `rtk make -j16 -O debug`: pass after adding `Mix Single` / `Mix Double` under `Gauntlet Battles`.
- 2026-06-06 `rtk make -j16 -O debug`: pass after the ability-popup refresh / form-change cleanup fix.
- 2026-06-06 `rtk make -j16 -O debug`: pass. Confirms the debug Dmax/Z fixtures build with debug Z-Power / Dynamax access and `.party` `Z Move: Yes` candidates.
- 2026-06-06 `rtk make -j16 -O debug`: pass after the double-switch execution guard.
- 2026-06-06 `rtk make -j16 -O debug`: pass after preserving debug opponent gimmick bits through battle allocation.
- 2026-06-06 `rtk make -j16 -O debug`: pass after the Protect-retarget update.
- 2026-06-06 `rtk make -j16 -O debug`: pass after adding `Read Double`.
- 2026-06-06 `rtk make -j16 -O all`: pass.
- 2026-06-06 `rtk make -j16 -O all`: pass after adding late-commit smart-gimmick checks and debug audit fixtures.
- 2026-06-06 `rtk make -j16 -O all`: pass after the double-switch execution guard.
- 2026-06-06 `rtk make -j16 -O all`: pass after adding the 8 weighted gauntlet debug battles.
- 2026-06-06 `rtk make -j16 -O all`: pass after the gauntlet menu overflow fix and pool rebalance.
- 2026-06-06 `rtk make -j16 -O all`: pass after the ability-popup refresh / form-change cleanup fix.
- 2026-06-06 `rtk make -j16 -O all`: pass after the debug opponent gimmick-bit preservation and Kyogre spread-pressure fix.
- 2026-06-06 `rtk make -j16 -O all`: pass after the Protect-retarget update.
- 2026-06-06 `rtk make -j16 -O all`: pass after adding `Read Double`.
- 2026-06-06 `rtk mdbook build docs`: pass with existing missing-root-`CHANGELOG.md` include warning, `CREDITS.md` `</img>` warning, and large search index warning.
- 2026-06-06 `rtk mdbook build docs`: pass after the double-switch execution guard, with the same existing warnings.
- 2026-06-06 `rtk mdbook build docs`: pass after the late-commit and debug audit update, with the same existing warnings.
- 2026-06-06 `rtk mdbook build docs`: pass after documenting the gauntlet debug battles, with the same existing warnings.
- 2026-06-06 `rtk mdbook build docs`: pass after the gauntlet menu overflow fix and pool rebalance, with the same existing missing-root-`CHANGELOG.md`, `CREDITS.md` `</img>`, and large search index warnings.
- 2026-06-06 `rtk mdbook build docs`: pass after documenting the ability-popup refresh / form-change cleanup fix, with the same existing missing-root-`CHANGELOG.md`, `CREDITS.md` `</img>`, and large search index warnings.
- 2026-06-06 `rtk mdbook build docs`: pass after documenting the debug opponent gimmick-bit preservation and Kyogre spread-pressure fix, with the same existing missing-root-`CHANGELOG.md`, `CREDITS.md` `</img>`, and large search index warnings.
- 2026-06-06 `rtk mdbook build docs`: pass after documenting `Read Double`, with the same existing missing-root-`CHANGELOG.md`, `CREDITS.md` `</img>`, and large search index warnings.
- mGBA Live: wrapper `/home/jastin/.local/bin/mgba-qt` booted `pokeemerald.gba` to the title screen and captured a screenshot in session `smart-ai-ability-item-knowledge-20260605`. `mgba_live_stop` returned `stopped:true`.
- mGBA Live: current ROM booted to the title screen and captured `/tmp/debug-vgc-fixtures-20260605.png` in session `debug-vgc-fixtures-20260605`. `mgba_live_stop` returned `stopped:true`, and CLI `status --all` returned `[]`. The debug Party menu battle itself still needs a progressed save or a focused input route for visual confirmation.
- mGBA Live: current ROM booted in session `smart-ai-protect-dmaxz-20260605`. `mgba_live_start_with_lua_and_view` reported a Lua bridge invalid-context error after starting, but `mgba_live_get_view` returned a rendered frame, `mgba_live_export_screenshot` saved `/tmp/smart-ai-protect-dmaxz-20260605.png`, and `mgba_live_stop` returned `stopped:true`.
- 2026-06-06 mGBA Live: MCP startup without `DISPLAY` failed with Qt `xcb` display initialization. CLI startup with `DISPLAY=:0` booted `pokeemerald.gba` to the title / demo screen in session `smart-gimmick-access-cli-smoke`, saved `/tmp/smart-gimmick-access-smoke.png`, and `mgba-live-cli stop` returned `stopped:true`.
- 2026-06-06 mGBA Live: sandboxed CLI startup first failed because the tool could not create `~/.mgba-live-mcp/runtime/sessions/...`. Rerunning with approval and `DISPLAY=:0` booted `pokeemerald.gba` in session `smart-gimmick-switch-guard-20260606`, saved `/tmp/smart-gimmick-switch-guard-20260606.png`, and `mgba-live-cli stop` returned `stopped:true`. `status --all` returned `[]`. This was a boot smoke only; the exact debug double-battle ASSERT path still needs manual progression to the Party debug fixture.
- 2026-06-06 mGBA Live: sandboxed CLI startup first failed because it could not create `~/.mgba-live-mcp/runtime/sessions/smart-gimmick-late-audit-20260606`. Rerunning with approval and `DISPLAY=:0` booted `pokeemerald.gba` to the title screen, saved `/tmp/smart-gimmick-late-audit-20260606.png`, and `mgba-live-cli stop` returned `stopped:true`. `status --all` returned `[]`. This was a boot smoke only; the new `Battle Gimmick Single` / `Battle Gimmick Double` menu entries still need manual progression from a debug-enabled save.
- 2026-06-06 mGBA Live: sandboxed CLI startup first failed because it could not create `~/.mgba-live-mcp/runtime/sessions/smart-gimmick-gauntlet-20260606`. Rerunning with approval and `DISPLAY=:0` booted `pokeemerald.gba` to the title screen, saved `/tmp/smart-gimmick-gauntlet-20260606.png`, and `mgba-live-cli stop` returned `stopped:true`. `status --all` returned `[]`. This was a boot smoke only; the 8 gauntlet menu entries still need manual progression from a debug-enabled save.
- 2026-06-06 mGBA Live: sandboxed CLI startup first failed because it could not create `~/.mgba-live-mcp/runtime/sessions/smart-gimmick-gauntlet-menu-fix2-20260606`. Rerunning with approval and `DISPLAY=:0` booted `pokeemerald.gba` to the title screen, saved `/tmp/smart-gimmick-gauntlet-menu-fix2-20260606.png`, and `mgba-live-cli stop` returned `stopped:true`. `status --all` returned `[]`. This was a boot smoke only; the `Party -> Gauntlet Battles` submenu still needs manual progression from a debug-enabled save.
- 2026-06-06 mGBA Live: sandboxed CLI startup first failed because it could not create `~/.mgba-live-mcp/runtime/sessions/ability-popup-formchange-20260606`. Rerunning with approval and `DISPLAY=:0` booted `pokeemerald.gba` to the title screen, saved `/tmp/ability-popup-formchange-20260606.png`, and `mgba-live-cli stop` returned `stopped:true`. `status --all` returned `[]`. This was a boot smoke only; the Mega Mewtwo / Delta Stream visual sequence still needs manual progression through a debug gauntlet save.
- 2026-06-06 mGBA Live: `DISPLAY=:0` booted `pokeemerald.gba` in session `smart-gimmick-pending-bits-20260606`, saved `/tmp/smart-gimmick-pending-bits-20260606.png` at the title screen, `mgba-live-cli stop` returned `stopped:true`, and `status --all` returned `[]`. This was a boot smoke only; `Battle Gimmick Double` still needs manual progression from a debug-enabled save to visually confirm Charizard Gigantamax and Tapu Koko Z-Move activation after the opponent-bit preservation fix.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`: pass, 9 tests after the Read Double Trick Room mirror guard.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI uses Trick Room'`: pass, 3 tests after the Trick Room mirror guard.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI_FLAG_SMART_SWITCHING'`: pass after the Protect preservation guard for optional double switches.
- 2026-06-06 `rtk make -j16 -O check TESTS='Protect: AI'`: pass, 9 tests after the Protect preservation guard. An attempted parallel rerun of two other `check` filters collided on `pokeemerald-test.elf` and produced `mmap elffd failed`; sequential reruns passed.
- 2026-06-06 `rtk make -j16 -O check TESTS='AI prefers to set up a powerful Status'`: pass. Confirms proactive `AI_FLAG_POWERFUL_STATUS` Trick Room is preserved when there is no read / predicted opposing Trick Room.
- 2026-06-06 `rtk make -j16 -O all`: pass after the Read Double Trick Room / Protect over-pivot tuning.
- 2026-06-06 `rtk make -j16 -O debug`: pass after the Read Double Trick Room / Protect over-pivot tuning.
- 2026-06-06 mGBA Live session `read-trick-protect-smoke-20260606`: pass for boot / bridge smoke after the Read Double Trick Room / Protect over-pivot tuning. `tools/mgba_live/start_mgba_live.sh read-trick-protect-smoke-20260606 120` started pid `274593`, `status --all` showed `fps_target=120.0`, screenshot `/tmp/read-trick-protect-smoke-20260606.png` was exported, `run-lua --code 'return {ok=true}'` returned `{ok=true}`, `stop` returned `stopped:true`, and final `status --all` returned `[]`. The behavior itself is covered by focused tests; this was ROM boot / Lua bridge evidence.
- 2026-06-06 `rtk make -j16 -O debug`: pass after adding `Partner Double`, ally-benefit scoring, and the Pollen Puff heal guard.
- 2026-06-06 `rtk make -j16 -O all`: pass after adding `Partner Double`, ally-benefit scoring, and the Pollen Puff heal guard.
- 2026-06-06 mGBA Live session `partner-tech-ai-20260606`: pass / boot plus input smoke after the pid `317741` ally-benefit update. A sandboxed start first failed because `mgba-live-cli` needed to update `~/.mgba-live-mcp/runtime/active_session`; rerunning with approval started pid `348899` at 120 FPS from `/home/jastin/dev/pokeemerald-expansion/pokeemerald.gba`. Screenshot `/tmp/partner-tech-ai-20260606-boot.png` showed a nonblank boot/title frame, `input-tap --key A` reached the title menu, screenshot `/tmp/partner-tech-ai-20260606-after-a.png` showed `CONTINUE / NEW GAME / OPTION`, and `stop` returned `stopped:true`. A final status probe for that session returned `session_not_found`, which is expected after clean stop/archive. The unrelated `manual-ai-log` session was not stopped.
- 2026-06-06 `rtk mdbook build docs`: exit 0 after documenting the pid `317741` ally-benefit update and `Partner Double`. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.
- 2026-06-06 `rtk mdbook build docs`: exit 0 after documenting the Read Double Trick Room / Protect over-pivot tuning. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index.

## Strategy Sources

The runtime policy above was cross-checked against:

- [Pokemon Wiki / Pokemon battle consideration wiki Dynamax mechanics](https://poke-wiki.net/%E3%83%80%E3%82%A4%E3%83%9E%E3%83%83%E3%82%AF%E3%82%B9): 3-turn duration, switch cancellation, HP scaling by Dynamax level, flinch and forced-switch immunity, Max Guard, Max Move side effects, and double-battle side effects.
- [Official Pokemon Sword / Shield Dynamax and Max Moves page](https://swordshield.pokemon.com/en-us/gameplay/dynamaxing-max-moves/): Max Moves are powerful but also carry additional effects; status moves become Max Guard; held item freedom means the Dynamax choice depends on the battle state.
- Victory Road VGC writing: [Incineroar / Fake Out discussion](https://victoryroad.pro/2020/03/02/incineroar-vgc-column/) for Dynamax flinch immunity; [Durant team report](https://victoryroad.pro/2020/01/31/bingjie-dallas-report-finalist/) and [Series 10 report](https://victoryroad.pro/2021/07/20/series-10-introduction/) for Max Steelspike / Max Quake defensive value; [Corviknight / Max Airstream report](https://victoryroad.pro/2020/03/14/zach-kelly-ocic20-report/) for speed and positioning value; team reports as examples that Dynamax is frequently a board-control and survival resource, not only a damage button.
- Pokemon Battle DataBase / official usage-style data remains the preferred future source for species, item, and move priors. Pokemon Showdown articles remain excluded as strategy source material per project policy.

## Known Gaps

- Air Lock / Cloud Nine preservation is source-derived, but not covered by a focused unit test yet because the AI test DSL does not have a direct pre-turn weather fixture in this file.
- Z-Move status tactics still rely on the existing Z-Move viability checks. More status Z-Move tactics can be modeled later.
- Tera doubles support uses the selected target and explicit candidate data. It does not fully simulate every partner threat or all possible double-target lines.
- G-Max unique secondary effects are not separately modeled in this slice. Regular Max Move effects are the current runtime focus.
- The new 3v3 / 4v4 debug fixtures are runtime smoke fixtures, not exhaustive balance fixtures. They provide quick repeated checks for competitive-style teams, weighted pool selection, smart gimmick timing, and no-EXP debug battles.
