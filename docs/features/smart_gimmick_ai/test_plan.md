# Smart Gimmick AI Test Plan

## Required Validation

| Check | Command | Status |
| --- | --- | --- |
| Doubles partner timer-stall Protect pass | `rtk make -j1 -O check TESTS='Protect: AI values doubles Protect when the partner can punish final opposing Tailwind'`; `rtk make -j1 -O check TESTS='Protect: AI values doubles Protect when the partner can punish final Trick Room'`; `rtk make -j1 -O check TESTS='Protect: AI does not waste doubles final Tailwind while burning Trick Room without partner payoff'`; `rtk make -j16 -O check TESTS='Protect: AI'`; `rtk make -j16 -O check TESTS='test/battle/ai/check_bad_move.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `doubles-partner-timer-protect-smoke2-20260705` | Pass on 2026-07-05. Adds a narrow doubles score-layer bridge where a pinned slot can Protect to burn final opposing Tailwind or final Trick Room when the AI partner will move first after expiry and has an immediate KO move. It also adds the paired no-payoff guard for final Trick Room plus the AI side's own final Tailwind: Protect is rejected when neither the active slot nor its partner gains an immediate or post-expiry KO payoff. The focused checks compare `ProtectChecks()` with and without the expiring timer and assert the self-sabotage line returns `NO_DAMAGE_OR_FAILS`, so the regressions verify the new partner-board payoff rather than the pre-existing generic doubles Protect bump. After the self-sabotage extension, `Protect: AI` passed 30 tests, full `check_bad_move.c` passed 39 tests, `AI_FLAG_READ_PLAYER_MOVE` passed 37 tests, full `ai_smart_gimmick.c` passed, and full `ai_doubles.c` exited 0 with its existing `KNOWN_FAILING` / `TO_DO` labels unchanged. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt ROM, `status --all` showed pid `1160614` alive at 120 FPS, Lua returned `{ok=true, validation=doubles_partner_timer_protect_smoke2_20260705}` before input and `{ok=true, validation=doubles_partner_timer_protect_smoke2_after_start_20260705}` after `START`, screenshots `/tmp/doubles-partner-timer-protect-smoke2-20260705-boot.png` and `/tmp/doubles-partner-timer-protect-smoke2-20260705-after-start.png` were captured, `START` input was accepted, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. A first attempted parallel focused rerun of the two positive checks collided on shared test output with `open tmpfd failed` / missing temp file errors and is non-evidence; both checks passed when rerun sequentially. |
| Focused runtime knowledge tests | `rtk make -j16 -O check TESTS='AI runtime knowledge'` | Pass on 2026-06-05; 4 tests passed. Covers move-category, ability-category, item / hold-effect category mapping, and predicted-move immunity bridges. |
| AI debt pass: weather suppression, Tera weakness, Max/G-Max knowledge | `rtk make -j16 -O check TESTS='AI_FLAG_SMART_MEGA'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_tera.c'`; `rtk make -j16 -O check TESTS='AI runtime knowledge'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk cargo run --manifest-path tools/runtime_knowledge/Cargo.toml -- --out /tmp/runtime_knowledge_catalog_ai_debt --pretty`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `ai-debt-smoke-20260704` | Pass on 2026-07-04. `AI_FLAG_SMART_MEGA` passed 2 tests, including focused `Air Lock` / `Cloud Nine` preservation against active Primal rain. `ai_smart_tera.c` passed 7 tests, including a new Water Tera guard that rejects fixing a Fire weakness while introducing a new Electric large-hit weakness. `AI runtime knowledge` passed with Max Move speed / weather / terrain / stat-control tags and G-Max unique / residual tags. Full `ai_smart_gimmick.c` passed 43 tests. The runtime catalog generator exited 0 and `/tmp/runtime_knowledge_catalog_ai_debt/moves.json` contains the new Max / G-Max knowledge flags. Normal ROM build passed with the existing linker RWX warning. mdbook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>`, and large-search-index warnings. mGBA Live booted the updated normal ROM, `run-lua` returned `{ok=true, validation=ai_debt_smoke_20260704}`, screenshots were exported to `/tmp/ai-debt-smoke-20260704-boot.png` and `/tmp/ai-debt-smoke-20260704-after-start.png`, `START` input was accepted, and final `status --all` returned `[]`. |
| Status Z runtime knowledge pass | `rtk make -j16 -O check TESTS='AI runtime knowledge maps move categories from move data'`; `rtk make -j16 -O check TESTS='AI runtime knowledge'`; `rtk make -j16 -O check TESTS='AI uses Z-Moves'`; `rtk cargo run --manifest-path tools/runtime_knowledge/Cargo.toml -- --out /tmp/runtime_knowledge_catalog_z_status --pretty`; generated `moves.json` spot checks for `MOVE_HAZE`, `MOVE_DESTINY_BOND`, `MOVE_TAILWIND`, and `MOVE_PARTING_SHOT`; `rtk mdbook build docs`; `rtk make -j16 -O all`; `rtk git diff --check`; mGBA Live session `z-status-knowledge-smoke-20260704` | Pass on 2026-07-04. The runtime knowledge test covers status Z families for stat reset, stat boost, critical boost, redirection, HP recovery, replacement healing, and `Curse` dual stat / recovery potential. The full runtime knowledge filter passed 4 tests, and the broad existing Z-Move AI filter still passed 14 tests after splitting status Z viability into a shared use / decline / defer helper. The runtime catalog generator emitted `zMoveEffect` and `z_*` tags; spot checks confirmed `Haze` as `z_recovery`, `Destiny Bond` as `z_redirection`, `Tailwind` as `z_crit_boost`, and `Parting Shot` as `z_replacement_heal`. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>`, and large-search-index warnings. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted `pokeemerald.gba`, Lua returned `{ok=true, validation=z_status_knowledge_smoke_20260704}`, accepted `START`, saved `/tmp/z-status-knowledge-smoke-20260704-after-start.png`, stopped with `stopped:true`, and final `status --all` returned `[]`. |
| G-Max hazard payoff pass | `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_ONLY: G-Max Stonesurge'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='AI runtime knowledge'`; `rtk cargo run --manifest-path tools/runtime_knowledge/Cargo.toml -- --out /tmp/runtime_knowledge_catalog_gmax_hazard --pretty`; generated `moves.json` spot checks for `MOVE_G_MAX_STONESURGE` and `MOVE_G_MAX_STEELSURGE` | Pass on 2026-07-04. G-Max Stonesurge now spends Dynamax as hazard payoff when Stealth Rock is not already on the player side and conserves Dynamax when rocks are already set. `ai_smart_gimmick.c` passed 45 tests after adding the two Stonesurge checks. `AI runtime knowledge` passed 4 tests with the new `AI_MOVE_KNOWLEDGE_GMAX_HAZARD` flag, and the runtime catalog emitted `gmax_hazard` tags for both Stonesurge and Steelsurge. One parallel `make check` attempt is non-evidence because two checks collided on shared temp files with `open tmpfd failed`; sequential reruns passed. |
| Desperation comeback / Commander pass | `rtk make -j1 -O check TESTS='AI fishes for flinch against a read Perish Song only without a clean damage line'`; `rtk make -j1 -O check TESTS='AI does not fish for flinch against read Perish Song when it can switch out'`; `rtk make -j1 -O check TESTS='AI accepts a low-accuracy sleep swing when it is about to be KOed'`; `rtk make -j1 -O check TESTS='AI targets Commander Dondozo instead of swallowed Tatsugiri'`; `rtk make -j1 -O check TESTS='AI only sacrifices a partner with spread damage for desperation board pressure'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'` | Pass on 2026-07-04. Covers near-term desperation risk for low-accuracy status, read Perish Song flinch fishing only when no clean damage line or switch escape exists, Commander target correction to Dondozo over swallowed Tatsugiri, and the doubles-only partner-KO spread exception when near-term board pressure justifies a sacrifice. File-level `ai.c` and `ai_doubles.c` checks passed after the focused filters; existing `KNOWN_FAILING` / `TO_DO` labels in doubles remain unchanged. A parallel focused `make check` attempt for low-sleep plus Commander targeting is non-evidence because both checks wrote the shared test ELF / temp files and produced `not an ELF file` / input-file errors; sequential reruns are the accepted evidence. A Toxic Orb Commander score regression was attempted in this pass but removed because the Commander / residual test harness advanced an extra turn and failed with `1 TURNs specified, but 2 ran`; that gap is closed by the 2026-07-05 lower-level Commander score coverage row. |
| Desperation / Commander runtime handoff | `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `desperation-commander-smoke2-20260704` | Pass / partial runtime on 2026-07-04. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>`, and large-search-index warnings. Normal ROM build passed with the existing linker RWX warning. mGBA Live started pid `388869`, standalone Lua returned `{ok=true, validation=desperation_commander_smoke2_20260704}`, screenshot `/tmp/desperation-commander-smoke2-20260704-boot.png` was captured, `START` input was accepted, `/tmp/desperation-commander-smoke2-20260704-after-start.png` was captured, `stop` returned `stopped:true`, and final `status --all` returned `[]`. |
| Commander Toxic Orb score coverage | `rtk make -j1 -O check TESTS='AI scores Commander Dondozo KO knowing poisoned Tatsugiri remains'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `commander-toxic-orb-score-smoke-20260705` | Pass on 2026-07-05. Adds a lower-level Commander Dondozo faint-bonus regression that does not execute the full Dondozo KO / Commander release turn. The test covers swallowed Tatsugiri holding Toxic Orb, already toxic-poisoned Tatsugiri, and regular poisoned Tatsugiri, verifying that Toxic Orb / toxic poison receive the stronger remaining-value bonus and regular poison receives the weaker remaining-value bonus. `ai_doubles.c` exited 0 with its existing `KNOWN_FAILING` / `TO_DO` labels unchanged, `ai_smart_gimmick.c` passed, and `AI_FLAG_READ_PLAYER_MOVE` passed 32 tests. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, `status --all` showed pid `525287` alive at 120 FPS, screenshot `/tmp/commander-toxic-orb-score-smoke-20260705-boot.png` was captured, a first parallel Lua attempt timed out and is non-evidence, the single-command retry returned `{ok=true, validation=commander_toxic_orb_score_smoke_20260705_retry}`, `START` input was accepted, the follow-up Lua bridge check returned `{ok=true, validation=commander_toxic_orb_score_after_start_20260705}`, screenshot `/tmp/commander-toxic-orb-score-smoke-20260705-after-start.png` was captured, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Short-horizon risk governor pass | `rtk make -j1 -O check TESTS='AI accepts a low-accuracy sleep swing when it is about to be KOed'`; `rtk make -j1 -O check TESTS='AI fishes for flinch against a read Perish Song only without a clean damage line'`; `rtk make -j1 -O check TESTS='AI logs hax out reason for desperate read Perish Song flinch'`; `rtk make -j1 -O check TESTS='AI does not fish for flinch against read Perish Song when it can switch out'`; `rtk make -j1 -O check TESTS='AI switches to Soundproof against a read Perish Song when it cannot stop it'`; `rtk make -j1 -O check TESTS='AI only sacrifices a partner with spread damage for desperation board pressure'`; `rtk make -j1 -O check TESTS='AI targets Commander Dondozo instead of swallowed Tatsugiri'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI does not aim redirectable single-target pressure into selected Rage Powder'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'` | Pass on 2026-07-04. Adds the shared `AiBoardSnapshot` / `AiThreatClass` / `AiShortHorizon` risk governor, centralizes low-accuracy / hax / OHKO / delayed-attack / partner-sacrifice / second-Protect / switch-survival unlocks, logs AI reason tags in `BattleActionLog` v2, and adds the Soundproof switch answer for read Perish Song. The selected-Rage-Powder regression covers the important negative: a doubles slot being exposed is not battle-level desperation while the AI has a live partner and no broader board collapse. `ai_switching.c` and `ai_doubles.c` still report their existing `KNOWN_FAILING` / `TO_DO` labels only. Docs, normal ROM, and mGBA Live handoff validation are recorded in the next row. |
| Battle action log trace v3 pass | `rtk make -j1 -O check TESTS='AI logs hax out reason for desperate read Perish Song flinch'`; `rtk make -j1 -O check TESTS='AI accepts a low-accuracy sleep swing when it is about to be KOed'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `battle-log-trace-v3-smoke-20260705`; `python3 -m json.tool /tmp/battle-log-trace-v3-smoke-20260705-empty.json` | Pass on 2026-07-05. Adds compact `aiThreatFlags` and `aiRiskKind` trace bytes to the existing 24-byte `BattleActionLogEntry`, asserts that size at compile time, and updates the mGBA Live exporter to schema `pokeemerald.battle_action_log.v3` with named threat / risk fields. The focused tests passed with new assertions for `AI_THREAT_PERISH_TRAP_CLOCK`, `AI_THREAT_DAMAGE_RACE`, `AI_THREAT_DESPERATION`, `AI_RISK_SECONDARY_HAX`, and `AI_RISK_LOW_ACCURACY_STATUS`. Full `ai.c`, `AI_FLAG_READ_PLAYER_MOVE` with 36 tests, full `ai_smart_gimmick.c`, and full `ai_switching.c` passed; `ai_switching.c` still reports its existing `KNOWN_FAILING` label while exiting 0. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, Lua returned `{ok=true, validation=battle_log_trace_v3_smoke_20260705}` before input and `{ok=true, validation=battle_log_trace_v3_after_start_20260705}` after `START`, screenshots `/tmp/battle-log-trace-v3-smoke-20260705-boot.png` and `/tmp/battle-log-trace-v3-smoke-20260705-after-start.png` were captured, and a direct Lua exporter run wrote `/tmp/battle-log-trace-v3-smoke-20260705-empty.json` with schema `pokeemerald.battle_action_log.v3`, `entry_size=24`, and valid JSON. This smoke did not enter a battle, so the exported v3 file has zero entries and does not validate non-empty trace fields through Lua; the C tests validate populated trace bytes. One `input-clear` attempt timed out after an R+START debug-menu attempt reset to title; a retry cleared input, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Read sound-pressure Throat Chop pass | `rtk make -j1 -O check TESTS='AI reads a selected sound status move and uses Throat Chop immediately'`; `rtk make -j1 -O check TESTS='AI reads a selected sound move and uses Throat Chop immediately'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_calc_best_move_score.c'`; `rtk make -j1 -O check TESTS='Belly Drum - physical move >50pct damage vs ice face already used'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `throat-chop-sound-pressure-smoke-20260705` | Pass / partial file-level on 2026-07-05. Adds a read-mode bridge in `MOVE_EFFECT_THROAT_CHOP` scoring so selected sound pressure is evaluated before generic best-move prediction. The status branch is deliberately narrow and covers selected Perish Song only; broader status sound denial remains planner work. The selected Perish Song regression failed before the fix with `Expected Throat Chop, got Psychic Fangs`; after the fix the selected Perish Song and selected Hyper Voice focused tests passed. `AI_FLAG_READ_PLAYER_MOVE` passed 36 tests and full `ai.c` passed. Full `ai_calc_best_move_score.c` is not accepted evidence because its unrelated existing Ice Face / Belly Drum check failed with `Expected Liquidation, got Icicle Crash`; the same Ice Face check also fails as a focused single test, while all Throat Chop checks in that file passed. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt ROM, Lua returned `{ok=true, validation=throat_chop_sound_pressure_smoke_20260705}` before input and `{ok=true, validation=throat_chop_sound_pressure_after_start_retry_20260705}` after `START`, captured `/tmp/throat-chop-sound-pressure-smoke-20260705-boot.png` and `/tmp/throat-chop-sound-pressure-smoke-20260705-after-start-late.png`, stopped cleanly, and final `status --all` returned `[]`. One parallel after-START Lua bridge timed out and is non-evidence; the single-command retry passed. |
| Short-horizon risk governor runtime handoff | `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `short-horizon-risk-smoke-20260704` | Pass / partial runtime on 2026-07-04. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live started pid `434147`, `status --all` showed the updated `pokeemerald.gba` alive at 120 FPS, Lua returned `{ok=true, validation=short_horizon_risk_smoke_20260704}`, screenshots `/tmp/short-horizon-risk-smoke-20260704-boot.png` and `/tmp/short-horizon-risk-smoke-20260704-after-start.png` were captured, `START` input was accepted, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Focused slot-collapse switch pass | `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI sacrifices Whimsicott to preserve Choice Kyogre from selected Z plus Fake Out focus'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk git diff --check`; `rtk make -j16 -O all`; mGBA Live session `focused-slot-collapse-smoke-20260704` | Pass / partial runtime on 2026-07-04. The focused regression failed before the fix with `Expected SWITCH, got MOVE`, matching the manual Read Double issue where Choice Specs Kyogre tried to act through selected Electric Z plus Fake Out pressure. After the fix, selected player gimmick damage is included in switching damage, known focus damage into a slot is summarized, and the AI can choose Focus Sash Whimsicott as a deliberate cushion when Kyogre has no safe Protect / Detect line and no Electric-immune reserve. Full `ai_smart_gimmick.c` passed, `AI_FLAG_READ_PLAYER_MOVE` passed 31 tests, and `ai_switching.c` exited 0 with its existing `KNOWN_FAILING` label unchanged. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, `status --all` showed pid `458282` alive at 120 FPS, Lua returned `{ok=true, validation=focused_slot_collapse_smoke_20260704}`, screenshot `/tmp/focused-slot-collapse-smoke-20260704-boot.png` was exported, `START` input was accepted, a follow-up Lua bridge check returned `{ok=true, validation=focused_slot_collapse_after_start_20260704}`, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. The post-START screenshot command timed out, so the runtime evidence is boot / bridge / input rather than a feature-specific battle screen. |
| Focused switch candidate payoff pass | `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI rejects a doomed sacrifice when it does not improve next board'`; `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI sacrifices Whimsicott to preserve Choice Kyogre from selected Z plus Fake Out focus'`; `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI prefers a nonfatal cushion over a doomed support sacrifice'`; `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI values a pivot-capable sacrifice in focused collapse'`; `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI accepts a 15-of-16 survival switch when focus pressure leaves no clean pivot'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `focused-switch-candidate-payoff-smoke-20260705` | Pass on 2026-07-05. Focused slot-collapse switch scoring now builds explicit safe / roll-survival / nonfatal-cushion / sacrifice candidates. The new negative regression covers low-HP Choice Specs Kyogre pinned by selected `Thunderbolt` plus selected `Bleakwind Storm` with only a no-item `Magikarp` reserve; because that doomed reserve does not consume `Fake Out`, carry support / pivot value, or have Focus Sash value, the AI rejects the sacrifice and attacks instead. Existing positive focused-collapse, nonfatal cushion, pivot-capable sacrifice, and 15/16 survival checks still pass. Full `ai_smart_gimmick.c`, full `ai_switching.c`, and `AI_FLAG_READ_PLAYER_MOVE` passed; `ai_switching.c` still reports its existing `KNOWN_FAILING` label while exiting 0. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/focused-switch-candidate-payoff-smoke-20260705-boot.png` and `/tmp/focused-switch-candidate-payoff-smoke-20260705-after-start.png`, accepted `START`, showed a nonblank continue-menu screenshot after input, stopped cleanly, and final `status --all` returned `[]`. A first Lua / screenshot attempt omitted `--session`, and a first input attempt omitted `--key`; those CLI-usage attempts are non-evidence and the corrected retries passed. |
| Short-horizon candidate-line suppression pass | `rtk make -j1 -O check TESTS='AI short horizon suppresses hax when clean damage answers read Perish Song'`; `rtk make -j1 -O check TESTS='AI fishes for flinch against a read Perish Song only without a clean damage line'`; `rtk make -j1 -O check TESTS='AI logs hax out reason for desperate read Perish Song flinch'`; `rtk make -j1 -O check TESTS='AI accepts a low-accuracy sleep swing when it is about to be KOed'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `short-horizon-candidate-line-smoke-20260705` | Pass on 2026-07-05. `AiShortHorizon` now stores the first stable `AiCandidateLine` and the first fallback risk line, and the risk governor blocks high-variance / secondary-hax risks when a clean stable line exists. The focused Perish Song regression uses `Stomp` as the hax line and `Boomburst` as the clean answer; the AI chooses clean damage and logs no risk family. Existing flinch-fishing, hax-reason, low-accuracy sleep, full `ai.c`, full `ai_smart_gimmick.c`, and `AI_FLAG_READ_PLAYER_MOVE` checks passed. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt ROM, returned Lua bridge checks before and after `START`, captured `/tmp/short-horizon-candidate-line-smoke-20260705-boot.png` and `/tmp/short-horizon-candidate-line-smoke-20260705-after-start.png`, accepted `START`, stopped cleanly, and final `status --all` returned `[]`. A parallel focused `make check` attempt is non-evidence because concurrent checks collided on shared test output; the accepted reruns were sequential. |
| Battle action log short-horizon trace v4 pass | `rtk make -j1 -O check TESTS='AI short horizon suppresses hax when clean damage answers read Perish Song'`; `rtk make -j1 -O check TESTS='AI logs hax out reason for desperate read Perish Song flinch'`; `rtk make -j1 -O check TESTS='AI accepts a low-accuracy sleep swing when it is about to be KOed'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `battle-log-short-horizon-v4-smoke-20260705`; `python3 -m json.tool /tmp/battle-log-short-horizon-v4-smoke-20260705-empty.json` | Pass on 2026-07-05. `BattleActionLogEntry` is now 28 bytes and records short-horizon line flags, stable line family, fallback risk line family, and loss clock beside the existing AI reason / threat / risk trace. The focused Perish Song regression now asserts populated clean-damage plus high-variance line facts in C. Full `ai.c`, `AI_FLAG_READ_PLAYER_MOVE`, full `ai_smart_gimmick.c`, and full `ai_switching.c` passed; `ai_switching.c` still reports its existing `KNOWN_FAILING` label while exiting 0. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt ROM, Lua returned `{ok=true, validation=battle_log_short_horizon_v4_smoke_20260705}` before input and `{ok=true, validation=battle_log_short_horizon_v4_after_start_20260705}` after `START`, screenshots `/tmp/battle-log-short-horizon-v4-smoke-20260705-boot.png` and `/tmp/battle-log-short-horizon-v4-smoke-20260705-after-start.png` were captured, and a direct Lua exporter retry wrote `/tmp/battle-log-short-horizon-v4-smoke-20260705-empty.json` with schema `pokeemerald.battle_action_log.v4`, `entry_size=28`, and valid JSON. This smoke did not enter a battle, so the Lua evidence covers schema / layout execution; populated trace bytes are covered by the C battle test. The first focused run after the header-size change was interrupted after a long no-output rebuild and is non-evidence; the rerun passed. The first exporter attempt inherited autosave `BATTLE_ACTION_LOG_SKIP_EMPTY` and returned `skipped_empty=true`; the retry set `_G.BATTLE_ACTION_LOG_SKIP_EMPTY=false` and wrote the JSON. `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Safe Protect counterplay switch pass | `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI does not trust Protect when selected Feint breaks the pinned Kyogre line'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='Protect: AI'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk git diff --check`; `rtk make -j16 -O all`; `rtk mdbook build docs`; mGBA Live session `safe-protect-counterplay-smoke-20260704` | Pass on 2026-07-04. The focused Feint regression failed before the fix with `Expected SWITCH, got MOVE`. After the fix, doubles switch preservation scans confirmed player choices for Feint, protection-ignoring moves, and Unseen Fist-style contact bypass before treating Protect / Detect as a safe answer. Protect availability also respects current move limitations such as disabled / Imprisoned / locked-out moves. Full `ai_smart_gimmick.c`, `Protect: AI`, and `AI_FLAG_READ_PLAYER_MOVE` passed; `ai_switching.c` exited 0 with its existing `KNOWN_FAILING` label unchanged. Normal ROM build passed with the existing linker RWX warning. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. mGBA Live booted the rebuilt `pokeemerald.gba`, `status --all` showed pid `465259` alive at 120 FPS, Lua returned `{ok=true, validation=safe_protect_counterplay_smoke_20260704}`, screenshots `/tmp/safe-protect-counterplay-smoke-20260704-boot.png` and `/tmp/safe-protect-counterplay-smoke-20260704-after-start.png` were captured, `START` input was accepted, a follow-up Lua bridge check returned `{ok=true, validation=safe_protect_counterplay_after_start_20260704}`, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Protect chip counterplay switch pass | `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI does not trust Protect when selected Z chip KOs through it'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `protect-chip-counterplay-smoke-20260705` | Pass on 2026-07-05. The focused regression covers low-HP Mystic Water Kyogre with Protect pinned by selected Electric Z `Thunderbolt` plus selected `Fake Out`: because the Z chip through Protect is lethal, the AI now switches Kyogre to Whimsicott instead of treating Protect as safe. Full `ai_smart_gimmick.c` passed, `AI_FLAG_READ_PLAYER_MOVE` passed 33 tests, and `ai_switching.c` exited 0 with its existing `KNOWN_FAILING` labels unchanged. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, Lua returned `{ok=true, validation=protect_chip_counterplay_smoke_20260705}` before input and `{ok=true, validation=protect_chip_counterplay_after_start_20260705}` after `START`, screenshots `/tmp/protect-chip-counterplay-smoke-20260705-boot.png` and `/tmp/protect-chip-counterplay-smoke-20260705-after-start.png` were captured, `START` input was accepted, the after-START screenshot showed the title screen, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Focused roll-survival switch pass | `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI accepts a 15-of-16 survival switch when focus pressure leaves no clean pivot'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `focused-roll-survival-smoke-20260705` | Pass on 2026-07-05. Adds a middle-tier imperfect switch-in for confirmed doubles focus pressure: if no fully safe pivot exists, the active high-value slot is about to collapse, and the reserve survives at an estimated 15/16 roll threshold, the AI can switch instead of accepting the active loss. The same helper may use the 14/16 threshold only under the shared desperation governor. The focused regression uses Choice Specs Kyogre pinned by selected `Thunderbolt` plus `Fake Out` and a Choice Koraidon reserve that is not a free support sacrifice. Full `ai_smart_gimmick.c` passed, `AI_FLAG_READ_PLAYER_MOVE` passed 34 tests, and `ai_switching.c` exited 0 with its existing `KNOWN_FAILING` label unchanged. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, Lua returned `{ok=true, validation=focused_roll_survival_smoke_20260705}` before input and `{ok=true, validation=focused_roll_survival_after_start_20260705}` after `START`, screenshots `/tmp/focused-roll-survival-smoke-20260705-boot.png` and `/tmp/focused-roll-survival-smoke-20260705-after-start.png` were captured, `START` input was accepted, the after-START screenshot showed the title screen, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Exact multi-move roll distribution switch pass | `rtk make -j1 -O check TESTS='Switch AI combines multiple damage rolls as an exact distribution'`; `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI accepts a 15-of-16 survival switch when focus pressure leaves no clean pivot'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `exact-roll-distribution-smoke-20260705` | Pass on 2026-07-05. Refines the focused roll-survival primitive so one- or two-source selected damage summaries are taken from the exact 16-roll joint distribution rather than adding independent per-hit quantiles. The low-level regression verifies a single 85-100 range as `85, 93, 98, 99, 100` and two independent 85-100 ranges as `170, 185, 193, 195, 200`, proving the 15/16 combined threshold is `195` rather than the old additive `198`. The existing focused 15/16 switch regression still passed, full `ai_switching.c` exited 0 with its existing `KNOWN_FAILING` label unchanged, `AI_FLAG_READ_PLAYER_MOVE` passed 36 tests, and full `ai_smart_gimmick.c` passed. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, returned Lua bridge checks before and after `START`, screenshots `/tmp/exact-roll-distribution-smoke-20260705-boot.png` and `/tmp/exact-roll-distribution-smoke-20260705-after-start.png` were captured, `START` input was accepted, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Focused nonfatal cushion switch pass | `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI prefers a nonfatal cushion over a doomed support sacrifice'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `nonfatal-cushion-switch-smoke-20260705` | Pass on 2026-07-05. Focused slot-collapse switching now ranks a nonfatal cushion above a doomed support / Focus Sash sacrifice, while still leaving safe pivots and accepted 15/16 or desperation 14/16 roll-survival pivots above it. The focused regression first failed with `Expected partyIndex 3, got 2`, matching Whimsicott being chosen over a Hariyama cushion, then passed after adding the middle tier. Full `ai_smart_gimmick.c` passed, `AI_FLAG_READ_PLAYER_MOVE` passed 35 tests, and `ai_switching.c` exited 0 with its existing `KNOWN_FAILING` label unchanged. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, Lua returned `{ok=true, validation=nonfatal_cushion_switch_smoke_20260705}` before input and `{ok=true, validation=nonfatal_cushion_switch_after_start_20260705}` after `START`, screenshots `/tmp/nonfatal-cushion-switch-smoke-20260705-boot.png` and `/tmp/nonfatal-cushion-switch-smoke-20260705-after-start.png` were captured, `START` input was accepted, the after-START screenshot showed a nonblank title frame, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Pivot-capable preserve switch pass | `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI values a pivot-capable sacrifice in focused collapse'`; `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI switches Mega Gengar to Incineroar against a known cross-slot Grassy Glide KO'`; `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI prefers a nonfatal cushion over a doomed support sacrifice'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `pivot-preserve-switch-smoke-20260705` | Pass on 2026-07-05. Adds `EFFECT_HIT_ESCAPE` follow-up value for `U-turn` / `Volt Switch` / `Flip Turn` style switch-ins in focused preserve ranking and in the selected-KO switch-in tie-break that can run before focused collapse. The focused regression first failed with `Expected partyIndex 3, got 2`, showing the AI picked the non-pivot Focus Sash Crobat before the fix, then passed. The existing cross-slot selected-KO and nonfatal cushion regressions still pass. Full `ai_smart_gimmick.c` passed, `AI_FLAG_READ_PLAYER_MOVE` passed 36 tests, and `ai_switching.c` exited 0 with its existing `KNOWN_FAILING` label unchanged. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, Lua returned `{ok=true, validation=pivot_preserve_switch_smoke_20260705}` before input and `{ok=true, validation=pivot_preserve_switch_after_start_20260705}` after `START`, screenshots `/tmp/pivot-preserve-switch-smoke-20260705-boot.png` and `/tmp/pivot-preserve-switch-smoke-20260705-after-start.png` were captured, `START` input was accepted, the after-START screenshot showed a nonblank title frame, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Focused delayed-attack risk pass | `rtk make -j1 -O check TESTS='AI accepts delayed Solar Beam when near-term loss has no clean hit'`; `rtk make -j1 -O check TESTS="AI won't use Solar Beam if there is no Sun up or the user is not holding Power Herb"`; `rtk make -j1 -O check TESTS='AI scores KOs with two turn moves correctly, considering Power Herb'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/check_bad_move.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `delayed-attack-risk-smoke-20260705` | Pass on 2026-07-05. Non-semi-invulnerable two-turn attacks now stay rejected when the opponent can KO before the charge resolves, but can become desperation lines when the AI survives the charge turn and the delayed hit is the only short-horizon payoff. The focused Solar Beam regression covers that case, while the existing Solar Beam no-sun / no-Power-Herb and Power Herb two-turn KO checks still pass. Full `ai.c` and full `check_bad_move.c` passed. One attempted parallel focused run is non-evidence because concurrent `make check` jobs collided on shared temporary files; the sequential reruns are the accepted evidence. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, Lua returned `{ok=true, validation=delayed_attack_risk_smoke_20260705}` before input and `{ok=true, validation=delayed_attack_risk_after_start_20260705}` after `START`, screenshots `/tmp/delayed-attack-risk-smoke-20260705-boot.png` and `/tmp/delayed-attack-risk-smoke-20260705-after-start.png` were captured, `START` input was accepted, the after-START screenshot showed a nonblank field frame, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Read sound-pressure Soundproof switch pass | `rtk make -j1 -O check TESTS='AI switches to Soundproof against'`; `rtk make -j1 -O check TESTS='AI does not switch to Soundproof against read sound pressure when it can KO first'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `read-soundproof-switch-smoke-20260705` | Pass on 2026-07-05. Adds a narrow Soundproof switch bridge for lethal confirmed sound moves such as `Boomburst`, including doubles spread sound pressure, while preserving the Perish Song Soundproof escape and staying in when the active battler can cleanly KO the sound-move source first. Full `ai_switching.c` exited 0 with its existing `KNOWN_FAILING` label unchanged, `AI_FLAG_READ_PLAYER_MOVE` passed 34 tests, and full `ai_smart_gimmick.c` passed. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, Lua returned `{ok=true, validation=read_soundproof_switch_smoke_20260705}` before input and `{ok=true, validation=read_soundproof_switch_after_start_20260705}` after `START`, screenshots `/tmp/read-soundproof-switch-smoke-20260705-boot.png` and `/tmp/read-soundproof-switch-smoke-20260705-after-start.png` were captured, `START` input was accepted, the after-START screenshot showed a nonblank field frame, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Nonlethal Soundproof switch pass | `rtk make -j1 -O check TESTS='AI switches to Soundproof against read nonlethal sound pressure'`; `rtk make -j1 -O check TESTS='AI does not switch to Soundproof against nonlethal sound pressure when it wins the race'`; `rtk make -j1 -O check TESTS='AI switches to Soundproof against a read lethal sound move'`; `rtk make -j1 -O check TESTS='AI switches to Soundproof against read spread sound pressure'`; `rtk make -j1 -O check TESTS='AI does not switch to Soundproof against read sound pressure when it can KO first'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `nonlethal-soundproof-switch-smoke-20260705` | Pass on 2026-07-05. Extends the Soundproof switch bridge from lethal selected sound damage to selected two-hit sound pressure when the active AI battler cannot win the short damage race and the Soundproof reserve has immediate attack or board value. The focused positive covers nonlethal selected `Hyper Voice` into an active Wobbuffet that cannot race it and a Soundproof `Thunderbolt` reserve. The focused negative confirms the AI stays in when faster `Strength` wins the nonlethal sound race. Existing lethal, spread, and clean-KO-first Soundproof checks remain passing. Full `ai_switching.c` exited 0 with its existing `KNOWN_FAILING` label unchanged, `AI_FLAG_READ_PLAYER_MOVE` passed 36 tests, and full `ai_smart_gimmick.c` passed. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, returned Lua bridge checks before and after `START`, screenshots `/tmp/nonlethal-soundproof-switch-smoke-20260705-boot.png` and `/tmp/nonlethal-soundproof-switch-smoke-20260705-after-start.png` were captured, `START` input was accepted, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Ally Speed drop Trick Room score pass | `rtk make -j1 -O check TESTS='AI scores ally Speed drop under Trick Room to reverse move order'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_ATTACKS_PARTNER'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `ally-speed-drop-trick-room-smoke-20260705` | Pass on 2026-07-05. Adds a narrow score-layer bridge for using Speed drops such as `Scary Face` on an ally under active Trick Room when the drop flips that ally behind a live foe and the ally can immediately KO that foe. Generic opponent Speed-drop scoring no longer gets its ordinary Speed-control bonus while Trick Room is active. The focused regression, full `ai_doubles.c`, `AI_FLAG_ATTACKS_PARTNER`, `AI_FLAG_READ_PLAYER_MOVE`, and full `ai_partner_synergy.c` passed. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. A first attempted parallel check involving `AI_FLAG_ATTACKS_PARTNER` and `ai_smart_gimmick.c` is non-evidence because concurrent `make check` jobs collided on shared test output; accepted checks were rerun sequentially. mGBA Live booted the rebuilt `pokeemerald.gba`, Lua returned `{ok=true, validation=ally_speed_drop_trick_room_smoke_20260705}` before input and `{ok=true, validation=ally_speed_drop_trick_room_after_start_20260705}` after `START`, screenshots `/tmp/ally-speed-drop-trick-room-smoke-20260705-boot.png` and `/tmp/ally-speed-drop-trick-room-smoke-20260705-after-start.png` were captured, `START` input was accepted, the after-START screenshot showed the continue menu, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. The first screenshot attempt used the wrong `--output` flag and is non-evidence; the retry with `--out` passed. |
| End-of-turn recovery Protect pass | `rtk make -j1 -O check TESTS='Protect: AI values singles Protect when Grassy Terrain recovery creates payoff'`; `rtk make -j16 -O check TESTS='Protect: AI'`; `rtk make -j16 -O check TESTS='test/battle/ai/check_bad_move.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `end-turn-recovery-protect-smoke-20260705` | Pass on 2026-07-05. The new Grassy Terrain Protect regression failed before the fix with `Expected 1.0 passes/successes, observed 0.0`, then passed after adding a one-turn end-of-turn recovery estimate. Singles Protect payoff now sees Leftovers, Black Sludge, Grassy Terrain, Poison Heal, Rain Dish, Ice Body, Aqua Ring, and Ingrain. Smart switch-in repeated-hit survival now includes Grassy Terrain recovery in addition to its existing item / Poison Heal / weather / Wish / status / hazard handling. `Protect: AI` passed 10 tests, `check_bad_move.c` passed 19 tests, full `ai_smart_gimmick.c` passed, `AI_FLAG_READ_PLAYER_MOVE` passed 32 tests, and `ai_switching.c` exited 0 with its existing `KNOWN_FAILING` label unchanged. One parallel `AI_FLAG_READ_PLAYER_MOVE` attempt is non-evidence because it collided on shared temporary test files with `open tmpfd failed`; the sequential rerun passed. `git diff --check` exited 0. Normal ROM build passed with the existing linker RWX warning. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. mGBA Live booted the rebuilt `pokeemerald.gba`, `status --all` showed pid `473254` alive at 120 FPS, Lua returned `{ok=true, validation=end_turn_recovery_protect_smoke_20260705}`, screenshots `/tmp/end-turn-recovery-protect-smoke-20260705-boot.png` and `/tmp/end-turn-recovery-protect-smoke-20260705-after-start.png` were captured, `START` input was accepted, a follow-up Lua bridge check returned `{ok=true, validation=end_turn_recovery_protect_after_start_20260705}`, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Tailwind timer-stall Protect pass | `rtk make -j1 -O check TESTS='Protect: AI values singles Protect to burn the last opposing Tailwind turn'`; `rtk make -j16 -O check TESTS='Protect: AI'`; `rtk make -j16 -O check TESTS='test/battle/ai/check_bad_move.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `tailwind-timer-protect-smoke-20260705` | Pass on 2026-07-05. The focused regression confirms actual move choice: under final-turn opposing Tailwind, a dangerous incoming fixed-damage move, and a speed relationship that flips after Tailwind expires, the AI chooses Protect to burn the turn. `Protect: AI` passed 11 tests, `check_bad_move.c` passed 20 tests, `AI_FLAG_READ_PLAYER_MOVE` passed 32 tests, full `ai_smart_gimmick.c` passed, and `ai_switching.c` exited 0 with its existing `KNOWN_FAILING` label unchanged. One parallel `make check` attempt is non-evidence because concurrent checks collided on shared temporary files with `open tmpfd failed`; sequential reruns passed. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, `status --all` showed pid `511906` alive at 120 FPS, Lua returned `{ok=true, validation=tailwind_timer_protect_smoke_20260705}`, screenshots `/tmp/tailwind-timer-protect-smoke-20260705-boot.png` and `/tmp/tailwind-timer-protect-smoke-20260705-after-start.png` were captured, `START` input was accepted, a follow-up Lua bridge check returned `{ok=true, validation=tailwind_timer_protect_after_start_20260705}`, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Trick Room timer-stall Protect pass | `rtk make -j1 -O check TESTS='Protect: AI values singles Protect when Leftovers recovery creates payoff'`; `rtk make -j1 -O check TESTS='Protect: AI values singles Protect when Grassy Terrain recovery creates payoff'`; `rtk make -j1 -O check TESTS='Protect: AI values singles Protect to burn the last opposing Tailwind turn'`; `rtk make -j1 -O check TESTS='Protect: AI values singles Protect to burn the last Trick Room turn'`; `rtk make -j1 -O check TESTS='Protect: AI does not waste its own final Tailwind while burning Trick Room'`; `rtk make -j16 -O check TESTS='Protect: AI'`; `rtk make -j16 -O check TESTS='test/battle/ai/check_bad_move.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `trick-room-timer-protect-smoke-20260705` | Pass on 2026-07-05. Adds final-Trick-Room Protect scoring, a self-sabotage guard for burning the AI's own final Tailwind alongside final Trick Room, and explicit Leftovers recovery coverage alongside Grassy Terrain. `Protect: AI` passed 14 tests, `check_bad_move.c` passed 23 tests, `AI_FLAG_READ_PLAYER_MOVE` passed 32 tests, full `ai_smart_gimmick.c` passed, `ai_switching.c` exited 0 with its existing `KNOWN_FAILING` label unchanged, and `ai_doubles.c` exited 0 with its existing `KNOWN_FAILING` / `TO_DO` labels unchanged. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, Lua returned `{ok=true, validation=trick_room_timer_protect_smoke_20260705}` before input and `{ok=true, validation=trick_room_timer_protect_smoke_20260705_after_start}` after `START`, screenshots `/tmp/trick-room-timer-protect-smoke-20260705-boot.png` and `/tmp/trick-room-timer-protect-smoke-20260705-after-start.png` were captured, `START` input was accepted, the after-START screenshot showed the continue menu, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| End-turn berry / Leech Seed recovery pass | `rtk make -j1 -O check TESTS='Protect: AI values singles Protect when Sitrus Berry threshold follows end-turn chip'`; `rtk make -j1 -O check TESTS='Protect: AI recovery estimate includes Leech Seed drain from a seeded target'`; `rtk make -j1 -O check TESTS='Switch AI counts Sitrus Berry as twenty-five percent single-use healing'`; `rtk make -j1 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI does not switch Mega Gengar to Incineroar against a punishing known cross-slot hit'`; `rtk make -j16 -O check TESTS='Protect: AI'`; `rtk make -j16 -O check TESTS='test/battle/ai/check_bad_move.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk git diff --check`; `rtk make -j16 -O all`; `rtk mdbook build docs`; mGBA Live session `end-turn-berry-leech-smoke-20260705` | Pass on 2026-07-05. Adds Protect recovery payoff for HP-restoring berries that become eligible after end-turn chip, adds Leech Seed drain recovery to the Protect recovery estimate, and fixes switch-in Sitrus healing from `maxHP / 25` to runtime's 25% HP. `Protect: AI` passed 16 tests, `check_bad_move.c` passed 25 tests, `ai_switching.c` exited 0 with its existing `KNOWN_FAILING` label unchanged, `AI_FLAG_READ_PLAYER_MOVE` passed 32 tests after strengthening the High Horsepower negative fixture to remain punishing with correct Sitrus healing, full `ai_smart_gimmick.c` passed, and `ai_doubles.c` exited 0 with existing `KNOWN_FAILING` / `TO_DO` labels unchanged. `git diff --check` exited 0. Normal ROM build passed with the existing linker RWX warning. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. mGBA Live booted the rebuilt `pokeemerald.gba`, Lua returned `{ok=true, validation=end_turn_berry_leech_smoke_20260705}` before input and `{ok=true, validation=end_turn_berry_leech_after_start_20260705}` after `START`, screenshots `/tmp/end-turn-berry-leech-smoke-20260705-boot.png` and `/tmp/end-turn-berry-leech-smoke-20260705-after-start.png` were captured, `START` input was accepted, the after-START screenshot showed the title screen, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Final screen timer-stall Protect pass | `rtk make -j1 -O check TESTS='Protect: AI values singles Protect to burn the last opposing screen turn'`; `rtk make -j1 -O check TESTS='Protect: AI does not burn the last opposing screen turn when it can break screens'`; `rtk make -j16 -O check TESTS='Protect: AI'`; `rtk make -j16 -O check TESTS='test/battle/ai/check_bad_move.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `final-screen-timer-protect-smoke-20260705` | Pass on 2026-07-05. Adds narrow singles Protect timer-stall scoring for final-turn opposing Reflect, Light Screen, and Aurora Veil when the incoming move is dangerous and wall expiry creates a meaningful follow-up attack payoff. The negative regression confirms the AI does not wait when it can already use a screen-breaking attack. `Protect: AI` passed 18 tests, `check_bad_move.c` passed 27 tests, `AI_FLAG_READ_PLAYER_MOVE` passed 32 tests, and full `ai_smart_gimmick.c` passed. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, Lua returned `{ok=true, validation=final_screen_timer_protect_smoke_20260705}` before input and `{ok=true, validation=final_screen_timer_protect_after_start_retry_20260705}` after `START` retry; one parallel post-START Lua attempt timed out and is non-evidence. Screenshots `/tmp/final-screen-timer-protect-smoke-20260705-boot.png` and `/tmp/final-screen-timer-protect-smoke-20260705-after-start.png` were captured, `START` input was accepted, the after-START screenshot showed the title screen, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Final weather / terrain timer-stall Protect pass | `rtk make -j1 -O check TESTS='Protect: AI values singles Protect to burn final rain before a boosted attack'`; `rtk make -j1 -O check TESTS='Protect: AI values singles Protect to burn final Electric Terrain before a boosted attack'`; `rtk make -j16 -O check TESTS='Protect: AI'`; `rtk make -j16 -O check TESTS='test/battle/ai/check_bad_move.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `final-weather-terrain-protect-smoke-20260705` | Pass on 2026-07-05. The rain regression confirms Protect can burn final rain when the boosted selected Water attack would KO but the post-rain hit is survivable. The Electric Terrain regression covers the same defensive timer-stall shape for terrain-boosted Electric damage. `Protect: AI` passed 20 tests, `check_bad_move.c` passed 29 tests, `AI_FLAG_READ_PLAYER_MOVE` passed 32 tests, and full `ai_smart_gimmick.c` passed after a non-evidence parallel run collided on shared temporary files with `open tmpfd failed`. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, Lua returned `{ok=true, validation=final_weather_terrain_protect_smoke_20260705}` before input and `{ok=true, validation=final_weather_terrain_protect_after_start_20260705}` after `START`, screenshots `/tmp/final-weather-terrain-protect-smoke-20260705-boot.png` and `/tmp/final-weather-terrain-protect-smoke-20260705-after-start.png` were captured, `START` input was accepted, the after-START screenshot showed the title screen, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Field-expiry speed guard Protect pass | `rtk make -j1 -O check TESTS='Protect: AI does not burn final rain if Swift Swim speed expires too'`; `rtk make -j1 -O check TESTS='Protect: AI values singles Protect to burn final rain before a boosted attack'`; `rtk make -j1 -O check TESTS='Protect: AI values singles Protect to burn final Electric Terrain before a boosted attack'`; `rtk make -j1 -O check TESTS='Protect: AI does not burn opposing screens when its own final screen is needed'`; `rtk make -j16 -O check TESTS='Protect: AI'`; `rtk make -j16 -O check TESTS='test/battle/ai/check_bad_move.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `field-expiry-speed-guard-smoke-20260705` | Pass on 2026-07-05. The focused Swift Swim regression failed before the fix because final-rain Protect burn reused the current rain-boosted Speed cache when checking whether the AI could attack first next turn. The fix recomputes both battlers' Speed under the post-expiry weather / terrain before accepting a line that still requires moving first. Existing final-rain and final-Electric-Terrain positives still pass, and the own-final-screen regression records a related self-sabotage guard that was already safe. `Protect: AI` passed 27 tests and full `check_bad_move.c` passed 36 tests. `AI_FLAG_READ_PLAYER_MOVE` and full `ai_smart_gimmick.c` passed. One parallel focused check attempt is non-evidence because concurrent `make check` jobs collided on the shared test ELF and one returned `No tests found`; the sequential rerun passed. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, returned Lua bridge checks before and after `START`, screenshots `/tmp/field-expiry-speed-guard-smoke-20260705-boot.png` and `/tmp/field-expiry-speed-guard-smoke-20260705-after-start.png` were captured, `START` input was accepted, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Final Perish timer-stall Protect pass | `rtk make -j1 -O check TESTS='Protect: AI values singles Protect while opposing Perish count expires'`; `rtk make -j1 -O check TESTS='Protect: AI does not value singles Protect when both Perish counts expire'`; `rtk make -j16 -O check TESTS='Protect: AI'`; `rtk make -j16 -O check TESTS='test/battle/ai/check_bad_move.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `perish-timer-protect-smoke-20260705` | Pass on 2026-07-05. The focused positive confirms Protect can wait out an already-active opposing Perish count at zero when the incoming move is dangerous and the AI is not also falling. The negative confirms same-count Perish endings are not scored as a Protect payoff. `Protect: AI` passed 22 tests, `check_bad_move.c` passed 31 tests, `AI_FLAG_READ_PLAYER_MOVE` passed 32 tests, `ai_switching.c` exited 0 with its existing `KNOWN_FAILING` labels unchanged, full `ai.c` exited 0, and full `ai_smart_gimmick.c` passed. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, Lua returned `{ok=true, validation=perish_timer_protect_smoke_20260705}` before input and `{ok=true, validation=perish_timer_protect_after_start_20260705}` after `START`, screenshots `/tmp/perish-timer-protect-smoke-20260705-boot.png` and `/tmp/perish-timer-protect-smoke-20260705-after-start.png` were captured, `START` input was accepted, the after-START screenshot showed the title screen, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Final Dynamax timer-stall Protect / Max Guard pass | `rtk make -j1 -O check TESTS='Protect: AI values Max Guard to burn the last opposing Dynamax turn'`; `rtk make -j1 -O check TESTS='Protect: AI values Protect to burn final opposing Dynamax when chip damage is survivable'`; `rtk make -j1 -O check TESTS='Protect: AI does not spend its own final Max Guard only to burn opposing Dynamax'`; `rtk make -j16 -O check TESTS='Protect: AI'`; `rtk make -j16 -O check TESTS='test/battle/ai/check_bad_move.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/gimmick_dynamax.c'`; `rtk git diff --check`; `rtk mdbook build docs`; `rtk make -j16 -O all`; mGBA Live session `final-dynamax-protect-smoke-20260705` | Pass on 2026-07-05. The Max Guard positive confirms it can burn the opponent's final Dynamax turn when the AI remains Dynamaxed afterward and the normal-HP follow-up is materially better. The regular Protect positive confirms the same final-Dynamax burn can be valued when the AI survives the maximum 1/4 Max Move damage that leaks through Protect. The negative confirms the narrow helper does not spend the AI's own final Max Guard only to trade final Dynamax turns. `Protect: AI` passed 25 tests, `check_bad_move.c` passed 34 tests, `AI_FLAG_READ_PLAYER_MOVE` passed 32 tests, full `ai_smart_gimmick.c` passed, and `ai/gimmick_dynamax.c` exited 0 with its existing 2 `KNOWN_FAILING` and 1 `TO_DO` labels unchanged. `git diff --check` exited 0. mdBook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large-search-index warning. Normal ROM build passed with the existing linker RWX warning. mGBA Live booted the rebuilt `pokeemerald.gba`, Lua returned `{ok=true, validation=final_dynamax_protect_smoke_20260705}` before input and `{ok=true, validation=final_dynamax_protect_after_start_20260705}` after `START`, screenshots `/tmp/final-dynamax-protect-smoke-20260705-boot.png` and `/tmp/final-dynamax-protect-smoke-20260705-after-start.png` were captured, `START` input was accepted, the after-START screenshot showed the title screen, `stop --grace 1` returned `stopped:true`, and final `status --all` returned `[]`. |
| Focused smart gimmick tests | `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV'` | Pass on 2026-06-06; 13 tests passed. Includes Z-Crystal-based all-gimmick Z-Move coverage plus Dynamax / Mega late-commit regressions. |
| Focused read / production fixture AI tests | `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_TERA_ONLY'`; `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_TERA'`; `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_ALL'` | Pass on 2026-06-06. `AI_FLAG_READ_PLAYER_MOVE` has 11 passing tests and `ai_smart_gimmick.c` has 31 passing tests after replacing placeholder Wobbuffet / Celebrate fixtures with production-style moves, items, abilities, natures, IVs, and fixed equivalent stats. Covers selected player Tera damage reads, actual selected switch-in reads, Fake Out Dynamax, command-log capture for all double-battle choices, retargeting single-target damage away from a confirmed selected Protect, read-mode Fake Out pressure, `Extreme Speed` priority ordering, priority-blocker guards, and the pid `278133` cross-slot Grassy Glide KO switch to Incineroar. The paired High Horsepower case confirms the AI does not force the same Incineroar switch when the known target move would punish the reserve. `TESTS='AI_FLAG_AGGRESSIVE_GIMMICK'` now returns `No tests found`; the former placeholder aggressive cases were replaced by deterministic production-style low-HP pressure checks. |
| Focused smart-gimmick / read adjusted rerun | `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_ONLY'`; `rtk make -j16 -O check TESTS='AI_FLAG_SMART_Z_MOVE'`; `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_ALL'`; `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_TERA_ONLY'`; `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_TERA'`; `rtk make -j16 -O check TESTS='AI_FLAG_SMART_MEGA'` | Pass on 2026-06-06 after the pid `317741` ally-benefit update. Counts were 11, 8, 5, 4, 1, 1, and 1 tests respectively, so the current adjusted read / smart-gimmick filter total is 31 tests. A broad exploratory `TESTS='AI_FLAG_'` run is non-evidence because it picked unrelated existing tests and failed at the then-unresolved `AI_FLAG_ATTACKS_PARTNER is willing to kill either the partner or the player 4/6`; that filter was resolved separately on 2026-07-04. |
| Z-Detect / Fake Out Protect payoff rerun | `rtk make -j16 -O check TESTS='AI uses Z-Moves -- Z-Detect'`; `rtk make -j16 -O check TESTS='AI uses Z-Moves'`; `rtk make -j16 -O check TESTS='AI_FLAG_SMART_Z_MOVE'`; `rtk make -j16 -O check TESTS='Protect: AI'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; partner-support focused checks | Pass on 2026-07-04. The older broad `Z-Detect` status-Z gap is resolved: confirmed / predicted `Fake Out` is a real singles Protect payoff, status Z-Protect gets a small Z-only score boost, same-type damaging Z-Move candidates are read from the user's actual moveset so `Close Combat` preserves Fightinium Z, and smart Dynamax still takes priority over passive Protect when it can keep a selected attack live through Fake Out. Counts: Z-Detect 1, broad Z-Move 14, Smart Z 5, Protect AI 9, READ_PLAYER_MOVE 25. Focused partner-support checks for Pollen Puff, Rage Powder, ally triggers, Haze, Clear Smog, Psych Up, and selected-KO Protect also passed. |
| Elite Single selected-move switching | `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: singles ignore unselected coverage'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: singles switch on selected KO'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O check TESTS='test/battle/trainer_control.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O debug`; `rtk make -j16 -O all`; `rtk mdbook build docs`; mGBA Live session `elite-single-switch-smoke-20260704` | Pass / partial runtime on 2026-07-04. The Elite Single trainer already has `Read Player Move`; the fix makes singles switch evaluation use the selected player command instead of unselected visible coverage. Focused tests confirm the AI stays in when the player selected a status move despite holding Water coverage, but still switches when the selected Water move would KO. `AI_FLAG_READ_PLAYER_MOVE` passed 28 tests, `ai_switching.c` exited 0 with existing `KNOWN_FAILING` labels unchanged, `trainer_control.c` passed 24 tests with an Elite-style Metagross / Milotic / Breloom adaptive-pool fixture, and `ai_smart_gimmick.c` passed 42 tests. Debug and normal ROM builds passed. mdbook exited 0 with the existing missing `CHANGELOG.md` include, `CREDITS.md` `</img>`, and large-search-index warnings. mGBA Live booted the updated normal ROM, Lua returned `{ok=true, validation=elite_single_switch_smoke_20260704}`, screenshot `/tmp/elite-single-switch-smoke-20260704.png` was exported, and the validation session stopped with `stopped:true`; the active manual session `manual-ai-watch-20260704d` was left running. The available `/tmp/manual-ai-watch-20260704*` autosaves for this pass were doubles logs, so there was no exact Elite Single turn log to cite. |
| Tera Single selected-KO stay-in | `/tmp/manual-ai-watch-20260704d-battle-action-log-autosave.json`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI stays and teras instead of switching from a selected KO it can outpace'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI may switch if tera still loses to the selected KO roll'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_tera.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O debug`; `rtk make -j16 -O all`; `rtk mdbook build docs`; mGBA Live session `tera-single-selected-ko-smoke-20260704` | Pass / partial runtime on 2026-07-04. The Tera Single log showed player Flutter Mane selecting `Shadow Ball` turn 0 and `Moonblast` turns 1-2 while the AI switched repeatedly, then spent Tera only on turn 3. The fix lets singles stay in when the AI can Terastallize before the selected hit lands, maximum-roll damage after Tera no longer KOs, and the Tera attacker has immediate pressure. A paired negative test confirms the AI may still switch when the selected hit can KO through Tera at maximum roll. `ai_smart_tera.c` passed 6 tests, `AI_FLAG_READ_PLAYER_MOVE` passed 30 tests, `ai_switching.c` exited 0 with existing `KNOWN_FAILING` labels unchanged, and `ai_smart_gimmick.c` passed 42 tests. Debug and normal ROM builds passed. mdbook exited 0 with existing warnings. mGBA Live booted the updated normal ROM, Lua returned `{ok=true, validation=tera_single_selected_ko_smoke_20260704}`, screenshot `/tmp/tera-single-selected-ko-smoke-20260704.png` was exported, and the validation session stopped with `stopped:true`; only the pre-existing stale `read-gimmick-pressure-20260630` entry remained in `status --all`. |
| Non-link NPC read-player policy | `rtk make -j16 -O check TESTS='NPC trainer AI automatically reads confirmed player commands outside link battles'`; `rtk make -j16 -O check TESTS='AI runtime knowledge'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai.c'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live session `npc-read-policy-smoke-20260704` | Pass / partial runtime on 2026-07-04. Runtime policy adds `AI_FLAG_READ_PLAYER_MOVE` in `GetAiFlags()` for non-recorded, non-link trainer AI that already has AI flags, covering Elite Four, Champion, normal NPCs, Frontier-style defaults, Trainer Hill, partners, and debug trainer battles without editing all trainer-party AI lines or partygen-owned fragments. The focused NPC policy test passed and confirms ordinary trainer / Frontier-style flags gain read-player mode while link, recorded, recorded-link, non-trainer, and zero-flag cases do not. `AI runtime knowledge` passed 4 tests, `AI_FLAG_READ_PLAYER_MOVE` passed 30 tests, and `test/battle/ai/ai.c` exited 0. Normal and debug ROM builds passed. mdbook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index warning. mGBA Live booted the updated ROM, Lua returned `{ok=true, validation=npc_read_policy_smoke_20260704}`, exported `/tmp/npc-read-policy-smoke-20260704-boot.png` and `/tmp/npc-read-policy-smoke-20260704-after-start.png`, accepted `START`, wrote `/tmp/npc-read-policy-smoke-20260704-battle-action-log-autosave.json`, and stopped with `stopped:true`; final `status --all` still listed only the pre-existing stale `read-gimmick-pressure-20260630` entry. |
| Z-Detect runtime / docs handoff | `rtk make -j16 -O all`; `rtk mdbook build docs`; mGBA Live session `zdetect-runtime-20260704` | Pass / partial runtime on 2026-07-04. Normal ROM build passed with the existing linker RWX warning. mdbook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>`, and large-search-index warnings. mGBA Live booted `pokeemerald.gba`, captured `/tmp/zdetect-runtime-20260704-boot.png`, accepted `START`, captured `/tmp/zdetect-runtime-20260704-after-start.png` at the title screen, and stopped with `stopped:true`. |
| pid `317741` ally-benefit regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; `rtk make -j16 -O check TESTS='AI heals a low HP setup ally with Pollen Puff'`; `rtk make -j16 -O check TESTS='AI uses Rage Powder'`; `rtk make -j16 -O check TESTS='AI triggers ally'`; `rtk make -j16 -O check TESTS='AI uses a guaranteed critical hit'`; `rtk make -j16 -O check TESTS='AI uses Psych Up'`; `rtk make -j16 -O check TESTS='AI uses Haze to reset an ally'`; `rtk make -j16 -O check TESTS='AI can score Clear Smog'`; Beat Up / Weakness Policy existing AI filters; `rtk make -j16 -O debug`; `rtk make -j16 -O all`; mGBA Live session `partner-tech-ai-20260606` | Pass / partial runtime on 2026-06-06. The pid `317741` log showed `Amoonguss` choosing `Spore`, offensive `Pollen Puff`, and `Protect` while not using `Rage Powder` / ally heal despite having the information. Focused tests now cover ally Pollen Puff healing, read-mode Rage Powder protection, ally `Steam Engine` + `Weakness Policy`, ally `Stamina` + `Body Press`, ally `Anger Point`, ally `Psych Up`, `Haze` after `Draco Meteor` drops, and low-priority ally `Clear Smog` scoring when Haze is unavailable. Existing AI Beat Up / Justified / Rage Fist and ally Weakness Policy tests passed. Debug and normal ROM builds passed. mGBA Live booted the updated ROM, accepted an A input to the title menu, captured `/tmp/partner-tech-ai-20260606-boot.png` and `/tmp/partner-tech-ai-20260606-after-a.png`, and stopped cleanly. |
| pid `354725` Pollen Puff self-target regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; `rtk make -j16 -O check TESTS='AI does not target itself'`; `rtk make -j16 -O check TESTS='AI heals'`; `rtk make -j16 -O check TESTS='AI uses Rage Powder'`; `rtk make -j16 -O check TESTS='AI triggers ally'`; `rtk make -j16 -O check TESTS='AI uses a guaranteed critical hit'`; `rtk make -j16 -O check TESTS='AI uses Psych Up'`; `rtk make -j16 -O check TESTS='AI uses Haze'`; `rtk make -j16 -O check TESTS='AI can score Clear Smog'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; mGBA Live session `pollen-self-target-guard-20260607` | Pass / partial runtime on 2026-06-07. Log sequence 26, turn 5 had opponent-right `MOVE_POLLEN_PUFF` targeting `B_POSITION_OPPONENT_RIGHT`, which is not a legal Pollen Puff target. The fix rejects self-targeting for non-self target classes through `CanTargetBattler`, stops double AI move slot 0 from bypassing target validity, and revalidates opponent AI targets before emitting the controller command. Focused Pollen Puff self-target regression passed, existing ally Pollen Puff and partner-synergy tests passed, and `AI_FLAG_READ_PLAYER_MOVE` passed 11 tests. The then-separate `AI_FLAG_ATTACKS_PARTNER` broad probe was later resolved on 2026-07-04 by accepting valid spread pressure and adding a production-style single-target target-selection regression. Normal and debug ROM builds passed. mGBA Live booted `pokeemerald.gba`, exported `/tmp/pollen-self-target-guard-boot.png`, accepted `A`, stopped with `stopped:true`, and final `status --all` returned `[]`. |
| Partner Tech debug battle and teammate-target scoring | `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk make -j16 -O check TESTS='test/battle/trainer_control.c'`; `rtk make -j16 -O check TESTS='Debug battles do not give exp or EVs'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; mGBA Live session `partner-tech-menu-20260630` | Pass / partial runtime on 2026-06-30. Added `Gauntlet Battles -> Partner Tech`, a no-legendary level-50 4v4 doubles route with production-style pools for `Charm` into `Contrary`, Lum-guarded `Swagger`, guaranteed-critical `Frost Breath` into `Anger Point`, low-damage priority activation, `Rage Powder`, and ally `Pollen Puff`. Added regression checks for useful `Charm` into a `Contrary` physical ally from minimum Attack, useful `Swagger` into a Lum Berry physical ally, and no `Swagger` reward without a confusion guard. Partner-synergy passed 15 tests, trainer-control passed 23 tests, debug battle EXP / EV suppression passed 1 test, read-mode passed 21 tests, smart-gimmick passed 38 tests, doubles exited 0 with existing known-failing / TO_DO labels unchanged, and normal / debug ROM builds passed. The older broad `AI_FLAG_ATTACKS_PARTNER` follow-up was resolved on 2026-07-04 and now passes separately. |
| Partner Tech docs build | `rtk mdbook build docs` | Exit 0 on 2026-06-30 after documenting `Partner Tech`. Existing warnings remain for the missing root `CHANGELOG.md` include, existing `CREDITS.md` `</img>` warning, and large search index. |
| pid `368099` HP-dependent power read regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; `rtk make -j16 -O check TESTS='AI discounts'`; `rtk make -j16 -O check TESTS=\"AI prefers Kyogre's\"`; `rtk make -j16 -O check TESTS=\"AI does not tunnel Kyogre's\"`; `rtk make -j16 -O check TESTS=test/battle/ai/ai_doubles.c`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live session `hp-dependent-ai-20260607` | Pass / partial runtime on 2026-06-07. The pid `368099` log showed opponent-left `Kyogre` selecting `Water Spout` while player-right had a faster confirmed `Dragon Ascent` into Kyogre, so the AI was valuing Water Spout from current HP instead of projected HP after known earlier damage. The fix discounts HP-dependent power when known earlier damage lowers the relevant user or target HP first. New production-style double tests cover `Water Spout` after a known faster `Dragon Ascent` and `Wring Out` after a known faster ally-spread `Surf`; existing Kyogre spread-pressure tests still pass so full-HP Water Spout remains preferred when it can fire before being damaged. `ai_doubles.c` exited 0 with existing known-failing / TO_DO labels unchanged, `AI_FLAG_READ_PLAYER_MOVE` passed 11 tests, normal and debug ROM builds passed, and mdbook exited 0 with the same existing `CHANGELOG.md`, `CREDITS.md`, and large-search-index warnings. mGBA Live booted `pokeemerald.gba`, returned Lua `{ok=true, validation=hp_dependent_ai_20260607}`, exported `/tmp/hp-dependent-ai-20260607-boot.png` and `/tmp/hp-dependent-ai-20260607-after-a.png`, accepted an `A` input, stopped with `stopped:true`, and final `status --all` returned `[]`. |
| pid `493358` future spread-pressure regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; `rtk make -j16 -O check TESTS='AI values future spread pressure over a switch read beside boosted Xerneas'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live session `future-pressure-ai-20260607` | Pass / partial runtime on 2026-06-07. The pid `493358` feedback identified that Kyogre's Ice Beam switch read into Amoonguss was too narrow while a Geomancy-boosted Xerneas remained beside the switch. The fix gives every damaging doubles candidate a lightweight future board-pressure score: safe opposing-side spread damage is summed across both opposing slots, Choice / Gorilla Tactics users compare that pressure over a three-turn same-move horizon, and narrow non-KO single-target reads are penalized when spread pressure is materially stronger. The focused Kyogre test passed and asserts both `Water Spout` and `Origin Pulse` score above `Ice Beam` under Choice Specs and Mystic Water. `ai_doubles.c` exited 0 with existing known-failing / TO_DO labels unchanged, smart-gimmick passed 31 tests, and partner-synergy passed 9 tests. A parallel `check` attempt for smart-gimmick plus partner-synergy produced a shared test-ELF `Bus error`, so only the sequential reruns are evidence. Normal and debug ROM builds passed. mdbook exited 0 with the same existing warnings. mGBA Live booted `pokeemerald.gba`, returned Lua `{ok=true, validation=future_pressure_ai_20260607}`, exported `/tmp/future-pressure-ai-20260607-boot.png` and `/tmp/future-pressure-ai-20260607-after-a.png`, accepted an `A` input, stopped with `stopped:true`, and final `status --all` returned `[]`. |
| pid `507917` Prankster / coverage regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; `rtk make -j16 -O check TESTS='AI uses Guard Split to improve its stats'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live session `prankster-coverage-ai-20260607` | Pass / partial runtime on 2026-06-07. The live `manual-ai-log` session matched pid `507917`, and turn 3 showed Prankster `Tornadus` choosing `Taunt` into Dark-type `Incineroar`, which has no Gen 7+ Prankster payoff. The fix applies the Prankster-Dark immunity check before Taunt is rewarded or accepted as a non-bad move. The same board showed why Kyogre must be valued as the Incineroar damage source when Tornadus has no meaningful Incineroar hit: the future board-pressure score now rewards a damaging candidate that meaningfully pressures a target its partner cannot damage. This is not limited to `AI_FLAG_READ_PLAYER_MOVE`; the regression is parametrized for confirmed read / omniscient context and normal prediction / known-party context, while a Guard Split rerun guards low-information support targeting from heuristic pollution. File-level doubles, smart-gimmick, partner-synergy, normal ROM, and debug ROM checks passed. mdbook exited 0 with the same existing warnings. mGBA Live booted `pokeemerald.gba`, returned Lua `{ok=true, validation=prankster_coverage_ai_20260607}`, exported `/tmp/prankster-coverage-ai-20260607-boot.png` and `/tmp/prankster-coverage-ai-20260607-after-a.png`, accepted an `A` input, stopped with `stopped:true`, and final `status --all` returned `[]`. Initial parallel long-title filters are non-evidence because they collided on shared hydra / temporary files and returned `No tests found`. |
| pid `528022` tactical partner activation regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; archived session `manual-ai-log-20260607-044252/session.json`; `rtk make -j16 -O check TESTS='AI triggers ally'`; `rtk make -j16 -O check TESTS='AI can score a harmless non-priority Fire hit'`; `rtk make -j16 -O check TESTS='AI does not use unsafe non-priority Water Gun'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk make -j16 -O check TESTS=\"AI will trigger its ally's Weakness Policy\"`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live session `partner-activation-ai-final2-20260607` | Pass / partial runtime on 2026-06-07. The archived session metadata matched pid `528022`, and the autosave had 38 entries through turn 7. The visible `Water Gun` actions targeted player-right, not the AI ally, but the feedback clarified the intended ally-trigger policy: do not treat arbitrary survivable ally hits as good just because they activate `Steam Engine` / `Weakness Policy`; prefer tactical triggers such as low-damage `Aqua Jet`, and still allow non-priority moves when the actual ally damage is harmless. The fix gives single-target partner activation a tactical trigger score: spread moves remain valid, priority moves score strongly, harmless non-priority hits score weakly, and unsafe non-priority 4x Water damage into `Coalossal` is not rewarded. `Partner Double` now uses `Primarina` `Aqua Jet` beside `Coalossal` instead of `Pelipper` `Water Gun`. Focused partner-activation tests, the full partner-synergy file, existing spread `Weakness Policy`, `ai_doubles.c`, normal ROM, and debug ROM passed. mdbook exited 0 with the same existing `CHANGELOG.md`, `CREDITS.md`, and large-search-index warnings. After a final normal ROM rebuild, mGBA Live booted `pokeemerald.gba`, returned Lua `{ok=true, validation=partner_activation_ai_final2_20260607}`, exported `/tmp/partner-activation-ai-final2-20260607-boot.png` and `/tmp/partner-activation-ai-final2-20260607-after-a.png`, accepted an `A` input, stopped with `stopped:true`, and final `status --all` returned `[]`. Parallel focused check attempts and parallel all/debug builds are non-evidence because they collided on shared temporary files / root ROM outputs; sequential reruns are the accepted evidence. |
| pid `549295` selected Protect read regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; archived session `manual-ai-log-20260607-055044/session.json`; `rtk make -j16 -O check TESTS='AI protects instead of choosing a slower attack into a selected KO'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live session `protect-read-ai-20260607` | Pass / partial runtime on 2026-06-07. The archived session metadata matched pid `549295`; the autosave had 23 entries through turn 4. Turn 3 showed player-right selected Dynamax `Wood Hammer` into opponent-right `Primarina`, while `Primarina` chose slower `Sparkling Aria` instead of `Protect`. The fix makes selected-target readers prefer confirmed `gBattleStruct->moveTarget`, adds a READ-player Protect payoff that scans all confirmed player commands for selected high-damage threats into the protecting battler, and penalizes ordinary actions that known earlier damage would KO before resolution. Focused Protect-read, partner-synergy, READ_PLAYER_MOVE, smart-gimmick, and doubles checks passed. Normal and debug ROM builds passed. mdbook exited 0 with the same existing `CHANGELOG.md`, `CREDITS.md`, and large-search-index warnings. mGBA Live booted `pokeemerald.gba`, returned Lua `{ok=true, validation=protect_read_ai_20260607}`, exported `/tmp/protect-read-ai-20260607-boot.png` and `/tmp/protect-read-ai-20260607-after-a.png`, accepted an `A` input, stopped with `stopped:true`, and final `status --all` returned `[]`. |
| pid `590887` Protect / Dragon Dance read discipline regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; active session `/home/jastin/.mgba-live-mcp/runtime/sessions/manual-ai-log/session.json`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI does not attack Protect or passively Protect into setup'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI pressures a selected offensive setup threat'`; `rtk make -j16 -O check TESTS="AI_FLAG_READ_PLAYER_MOVE: AI does not hide behind King's Shield while a setup threat is selected"`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk make -j16 -O check TESTS='Protect: AI'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live sessions `protect-read-discipline-20260607` and `setup-threat-read-20260607` | Pass / partial runtime on 2026-06-07. The first active-session autosave matched pid `590887` and was generated at `2026-06-07T07:53:05Z` with 41 entries through turn 9. Turn 0 showed player-left Mega `Dragon Dance` and player-right `Protect`, but opponent-left still chose Mega `Flash Cannon` into the protected player-right slot while opponent-right chose `Protect` with no confirmed incoming damage. The first fix raised the selected-Protect single-target damage penalty and removed passive read-mode doubles Protect scoring when there is no known protectable incoming hit. The follow-up autosave was generated at `2026-06-07T09:21:32Z` from the same `manual-ai-log` session after its process metadata had advanced to pid `597829`; it showed player-left Mega `Dragon Dance` plus player-right `Moonblast`, followed by too much `Protect` / `King's Shield` while Mega Dragonite kept attacking. The follow-up fix treats selected offensive setup moves as immediate future-pressure threats, rewards meaningful damage into the setup user, discounts narrow damage into the other slot when the setup threat could be pressured, discounts Protect that only shelters one AI slot while the other player slot is known to boost, and prevents Aegislash's Stance Change `King's Shield` bonus from overriding that setup read. Focused turn-0 Protect, selected-setup pressure, and King's Shield setup tests passed. READ_PLAYER_MOVE passed 17 tests, smart-gimmick passed 34 tests, partner-synergy passed 12 tests, Protect AI passed 9 tests, and doubles exited 0 with existing known-failing / TO_DO labels unchanged. Normal ROM, debug ROM, and docs passed; mdbook exited 0 with the same existing warnings. mGBA Live `protect-read-discipline-20260607` and `setup-threat-read-20260607` both booted `pokeemerald.gba`, returned Lua validation payloads, exported screenshots, accepted an `A` input, stopped with `stopped:true`, and final `status --all` returned `[]`. The analyzed `manual-ai-log` session was not stopped by this validation. |
| pid `608191` selected Geomancy / redirection read regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; active session `/home/jastin/.mgba-live-mcp/runtime/sessions/manual-ai-log/session.json`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI disrupts selected Geomancy instead of fearing unselected Encore'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI does not aim redirectable single-target pressure into selected Rage Powder'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O check TESTS='Protect: AI'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live session `read-redirection-ai-20260607` | Pass / partial runtime on 2026-06-07. The autosave matched pid `608191` and was generated at `2026-06-07T09:59:58Z` with 32 entries through turn 6. The key reversal was turn 0 `Protect` + `Geomancy` from the player side while AI `Amoonguss` chose offensive `Pollen Puff` instead of disruption, followed by later single-target pressure into a partner slot while player-side redirection was selected. The fix only suppresses Encore-vulnerable support moves when a selected player `Encore` actually targets the acting AI battler, scores legal setup disruption such as `Spore` into selected `Geomancy`, and penalizes ordinary redirectable single-target moves aimed at a partner of a selected `Follow Me` / `Rage Powder` user while preserving powder-immunity and redirection-bypass exceptions. The two focused regressions passed; `AI_FLAG_READ_PLAYER_MOVE` passed 19 tests; `ai_smart_gimmick.c` passed 36 tests; `ai_partner_synergy.c` passed 12 tests; `Protect: AI` passed 9 tests; `ai_doubles.c` exited 0 with existing known-failing / TO_DO labels unchanged. Normal and debug ROM builds passed. mdbook exited 0 with the same existing `CHANGELOG.md`, `CREDITS.md`, and large-search-index warnings. mGBA Live booted `pokeemerald.gba`, exported `/tmp/read-redirection-ai-20260607-boot.png` and `/tmp/read-redirection-ai-20260607-after-a.png`, accepted an `A` input, returned Lua `{ok=true, validation=read_redirection_ai_20260607}`, stopped with `stopped:true`, and final `status --all` returned `[]`. |
| pid `608191` known selected-KO smart-gimmick follow-up | `/tmp/double-battle-mgba-20260630-read-double-turn1.json`; `/tmp/double-battle-mgba-20260630-read-double-switch.png`; `/tmp/read-gimmick-pressure-20260630-turn1.json`; `/tmp/read-gimmick-pressure-20260630-turn1-final.png`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/gimmick_dynamax.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; PJCS2026 / NAIC / older VGC turn-shape review notes | Pass / partial runtime on 2026-06-30. The pre-fix `Read Double` route produced player `Tornadus` + `Incineroar` versus opponent `Tornadus` + `Tapu Koko`; player `Tailwind` plus `Fake Out` was known, and the exported log confirmed opponent `Tapu Koko` selected `GIMMICK_Z_MOVE` + `Volt Switch` into player-left. The post-fix route produced player `Tapu Koko` + `Incineroar` versus opponent `Tornadus` + `Incineroar`; player selected `Dazzling Gleam` plus `Fake Out`, and the exported log showed opponent-left selecting Dynamax in the recompute window, using `Protect` / visible `Max Guard`, opponent-right switching to `Rillaboom`, and `Rillaboom` using `Wood Hammer` to KO Koko. The runtime change now scans confirmed player damaging moves into each AI battler, compares AI survival with and without the gimmick, and lets Smart Mega, damaging Z-Move, Dynamax, and Tera spend when the gimmick prevents a known same-turn KO or turns that pressure into a late-commit / damage-race reason. Focused `AI_FLAG_READ_PLAYER_MOVE` passed 20 tests, `ai_smart_gimmick.c` passed 37 tests, `gimmick_dynamax.c` exited 0 with known-failing / TO_DO labels unchanged, `ai_partner_synergy.c` passed 12 tests, `ai_doubles.c` exited 0 with known-failing / TO_DO labels unchanged, and normal / debug ROM builds passed. Existing broad filters still expose separate known issues: `ai_smart_tera.c` exits 2 on `PASSES_RANDOMLY` metadata in `AI_FLAG_SMART_TERA: AI might tera if it gets saved from a ko (1/?)`, and `gimmick_z_move.c` exits 2 on `AI uses Z-Moves -- Z-Detect 1/2`. `read-gimmick-pressure-20260630` cleanup did not fully clear: `mgba-live-cli stop` returned `stopped:false`, `ps` showed pid `726608` as `Zs [mgba-qt] <defunct>`, and `status --all` still listed a stale alive session, so the cleanup caveat is recorded instead of treating mGBA as fully clean. |
| Singles known selected-KO smart-gimmick follow-up | `/tmp/single-gimmick-pressure-20260630-turn1.json`; `/tmp/single-gimmick-pressure-20260630-turn1-progress.png`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: smart Dynamax answers selected single KO pressure'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: Dmax singles switch-in stops pivoting after absorbing Astral Barrage'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; mGBA Live session `single-gimmick-pressure-20260630` | Pass / monitor on 2026-06-30 and 2026-07-04. Added a production-style singles regression where player `Miraidon` selects `Draco Meteor` into Dynamax-capable `Koraidon`, and the AI spends Dynamax on `Collision Course` instead of conserving while under confirmed selected-KO pressure. The mGBA route booted the debug ROM, opened `Party -> Gauntlet Battles -> Dmax Single`, reached player `Calyrex` versus opponent `Kyogre`, selected `Astral Barrage`, exported a six-entry action log, and showed opponent-side switch / resolved switch-in behavior to `Zacian`. The action log does not include enough HP / damage outcome state to prove the later switch was incorrect, so it remains a monitor candidate. The 2026-07-04 fixed production-style pivot guard passed and confirms the current AI can pivot once into Rillaboom under Calyrex's `Astral Barrage`, then attack with `Grassy Glide` instead of repeatedly switching. The original focused single test passed, `AI_FLAG_READ_PLAYER_MOVE` passed 21 tests at the time, and `ai_smart_gimmick.c` passed 38 tests. The `single-gimmick-pressure-20260630` session stopped cleanly with `stopped:true`; the only remaining mGBA Live status entry was the older stale `read-gimmick-pressure-20260630` zombie recorded above. |
| pid `565092` Choice role-done switch regression and Champions fixture | `/tmp/manual-ai-log-battle-action-log-autosave.json`; archived session `manual-ai-log-20260607-064700/session.json`; official PJCS2026 game-division streams; Victory Road 2026 Japan Championships page; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: choice-locked'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk make tools/trainerproc/trainerproc`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live session `champs-choice-ai-20260607` | Pass / partial runtime on 2026-06-07. The pid `565092` autosave had 58 entries through turn 12 and showed opponent-right using `MOVE_ICE_BEAM` nine consecutive times after switching on turn 0, then only switching much later. The fix adds a read-mode doubles pivot for already locked Choice / Gorilla Tactics users when all player commands are known, the active slot is not taking known damage, the locked move no longer creates meaningful 2HKO or spread-pressure progress, and an eligible reserve can enter without being punished by the confirmed move set. The follow-up guard makes the cushion policy explicit: if the confirmed player turn is already targeting the role-done Pokemon, the AI keeps it in instead of exposing the reserve. Focused Choice passed 3 tests, full read-mode passed 14 tests, and `ai_switching.c` exited 0 with existing known-failing labels unchanged. Earlier smart-gimmick, doubles, and partner-synergy file checks passed for this source state. Normal and debug ROM builds passed, trainerproc needed no rebuild, and mdbook exited 0 with the same existing warnings. mGBA Live booted the current `pokeemerald.gba`, returned Lua `{ok=true, validation=champs_choice_ai_20260607}`, exported `/tmp/champs-choice-ai-20260607-boot.png` and `/tmp/champs-choice-ai-20260607-after-a.png`, accepted `A`, stopped with `stopped:true`, and final `status --all` returned `[]`. The boot screenshot was captured during a white fade, but the after-A screenshot reached the title screen. This was only boot / bridge / input evidence: the current mGBA battery route exposed `NEW GAME` rather than a progressed overworld debug save, so `Gauntlet Battles -> Champs Double` menu reachability and in-battle behavior remain a manual check. The cushion-only follow-up did not rerun mGBA because the C source change is a comment clarifying the already-tested guard; runtime behavior is covered by the test-runner regression. |
| pid `354725` docs handoff | `rtk mdbook build docs` | Pass on 2026-06-07. mdbook exited 0 with the same existing warnings for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index. |
| pid `278133` cross-slot KO switch regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O all`; mGBA Live boot / bridge smoke | Pass on 2026-06-06. Log analysis showed turn 7 player-left `MOVE_GRASSY_GLIDE` targeted opponent-right Mega Gengar, but opponent-right stayed in for `MOVE_FOCUS_BLAST` and then disappeared from later turns. Root cause: switch context read the opposite slot instead of the known cross-slot selected target. Focused read-mode passed 11 tests; smart-gimmick file passed 31 tests; AI switching file exited 0 with only existing known-failing labels; normal ROM build passed; session `known-cross-slot-ko-switch-guard-20260606` booted updated `pokeemerald.gba`, screenshot `/tmp/known-cross-slot-ko-switch-guard-20260606.png` was nonblank, Lua returned `{ok=true, frame=1217}`, and `stop` returned `stopped:true`. Final `status --all` still listed the unrelated `manual-ai-log` session alive, so only the validation session was stopped. |
| Final branch build / docs / boot smoke | `rtk make -j16 -O all`; `rtk mdbook build docs`; MCP mGBA Live session `final-smart-gimmick-ai-20260606` | Pass / partial runtime on 2026-06-06 after the final source, production-style test fixture, and docs state. Normal ROM build passed. mdbook exited 0; existing warnings remained for missing root `CHANGELOG.md` include, `CREDITS.md` HTML end tag, and large search index. mGBA Live returned Lua `{ok=true, validation=smart_gimmick_ai_final_20260606}`, exported `/tmp/final-smart-gimmick-ai-20260606.png`, and stopped cleanly. |
| Additional pid `278133` AI smoke files | `rtk make -j16 -O check TESTS='test/battle/ai/ai.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_choice.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_calc_best_move_score.c'` | Pass / exit 0 on 2026-06-06. `ai_choice.c` passed 11 tests and `ai_calc_best_move_score.c` passed 17 tests. `ai.c` and `ai_doubles.c` exited 0; existing known-failing / TODO labels in doubles remain unchanged. `TESTS='test/battle/ai'` was attempted but returned `No tests found`, so it is non-evidence. |
| Focused double switch reservation regression | `rtk make -j16 -O check TESTS='AI will not try to switch for the same'` | Pass on 2026-06-06; 6 tests passed after the action-log / non-assert switch-in fallback update. Confirms existing double and 2v1 same-reserve switch guards still work. |
| Focused smart Dynamax environment tests | `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_ONLY'` | Pass on 2026-06-06; 7 tests passed. Covers conservation, last-Pokemon use, one-reserve low-HP late commit, Max Move payoff, Fake Out disruption prevention, and phazing disruption prevention. |
| Focused smart Z tests | `rtk make -j16 -O check TESTS='AI_FLAG_SMART_Z_MOVE'` | Pass on 2026-06-06; 4 tests passed. Adds one-reserve low-HP late commit. |
| Focused smart Mega tests | `rtk make -j16 -O check TESTS='AI_FLAG_SMART_MEGA'` | Pass on 2026-06-05; 1 test passed. |
| Focused all-gimmick environment tests | `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_ALL'` | Pass on 2026-06-06; 4 tests passed. Covers held Z-Crystal use, doubles Tera, full-HP setup-turn Mega delay, and low-HP one-reserve Mega late commit. |
| Focused Kyogre doubles spread-pressure checks | `rtk make -j16 -O check TESTS='AI does not tunnel Kyogre'`; `rtk make -j16 -O check TESTS='AI prefers Kyogre'` | Pass on 2026-06-06; 2 focused tests passed. Confirms rain-accurate `Thunder` no longer beats meaningful `Water Spout` spread pressure only due to accuracy bypass scoring. |
| Player double move-menu gimmick UI reset | `rtk make -j16 -O debug`; `rtk make -j16 -O all`; `rtk make -j16 -O check TESTS='(Z-MOVE) Every battler can use Z-Moves - Doubles'`; `rtk make -j16 -O check TESTS=\"Dynamax: Max Strike lowers both opponents' speed\"`; mGBA Live boot / screenshot | Pass / partial visual on 2026-06-06. Build and focused runtime checks passed after moving the player gimmick UI reset before move-menu drawing. mGBA Live session `player-gimmick-menu-reset-20260606` booted the ROM at 120 FPS, Lua bridge returned `{ok=true}`, screenshot exported to `/tmp/player-gimmick-menu-reset-20260606.png`, and the session stopped cleanly. The existing animation filter `TESTS=\"Z-Moves don't leak when used - Doubles\"` returned `No tests found`, so direct visual confirmation is still manual: in a double battle, select Z-Move or Dynamax / Max Move for the first player battler, then confirm the second player battler's initial move menu is normal unless that battler independently toggles its own gimmick. |
| Focused smart Tera tests | `rtk make -j16 -O check TESTS='AI_FLAG_SMART_TERA'` | Pass on 2026-06-05; 4 tests passed. |
| Focused smart switching tests | `rtk make -j16 -O check TESTS='AI_FLAG_SMART_SWITCHING'` | Pass on 2026-06-06. Covers double-position switching, partner-cover guard, weather / terrain reserve pivots, Tailwind / Trick Room reserve pivots, terrain seed plans, status-benefit pivots, direct and secondary status / confusion support pivots, Skill Swap bridge pivots, predicted-Taunt attacker pivots / stay-in guards, and the double-switch execution guard through the shared predicted-move immunity predicate. |
| Focused Protect scoring tests | `rtk make -j16 -O check TESTS='Protect: AI'` | Pass on 2026-06-05; 9 tests passed. Covers ignore-protection moves, Unseen Fist, passive singles Protect rejection, boosted-attacker rejection, residual payoff, and second Protect scoring in singles and doubles. |
| Focused Protect scoring tests | `rtk make -j16 -O check TESTS='Protect: AI'` | Pass on 2026-06-06 after the Protect-retarget update; 9 tests passed. Confirms the read-mode retarget penalty did not regress Protect-use scoring. |
| Focused Fake Out priority-pressure tests | `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: selected Fake Out'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: blocked selected Fake Out does not discount ordinary actions'`; `rtk make -j16 -O check TESTS='Fake Out'`; `rtk make -j16 -O check TESTS='AI will Fake Out'` | Pass on 2026-06-06. Covers read-mode scoring against selected Fake Out, lower-priority `Extreme Speed`, priority-blocker protection through `Armor Tail`, base Fake Out move-effect behavior, and existing AI Fake Out selection behavior. |
| Debug trainer fixture generation | `rtk make tools/trainerproc/trainerproc`; `rtk make -j16 -O debug` | Pass on 2026-06-06 after moving the 8 weighted gauntlet battles under `Gauntlet Battles`, rebalancing the player-side pools, and adding `Mix Single` / `Mix Double` all-gimmick gauntlets. `rtk make -j16 -O debug` also passes after the ability-popup refresh / form-change cleanup fix, after the shared ability-popup palette ownership fix, after preserving debug opponent gimmick bits across `CB2_InitBattle()`, and after the command-buffer read update. |
| Debug `Read Double` fixture generation | `rtk make tools/trainerproc/trainerproc`; `rtk make -j16 -O debug`; `rtk make -j16 -O all` | Pass on 2026-06-06 after adding `Gauntlet Battles -> Read Double`, a mirrored 8-Pokemon level-50 4v4 doubles pool on both sides. |
| Debug `Read Single` / `Champs Single` fixture generation | `rtk make tools/trainerproc/trainerproc`; `rtk make -j16 -O check TESTS='test/battle/trainer_control.c'`; `rtk make -j16 -O check TESTS='Debug battles do not give exp or EVs'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; mGBA Live session `more-debug-battles-20260630` | Pass on 2026-06-30. Added `Gauntlet Battles -> Read Single`, a mirrored 6-Pokemon level-50 3v3 singles read pool with all gimmick access, and the first `Gauntlet Battles -> Champs Single` level-50 3v3 Mega-only Champions-style route. That original narrow Champ route is superseded by the 2026-07-04 adaptive Champion / Elite pools, but the runtime menu and 3v3 route evidence remains valid. Trainer control passed 23 tests, debug battle EXP / EV suppression passed 1 test, and normal / debug ROM builds passed. mGBA reached the expanded gauntlet menu, showed `Read Single`, started `Champs Single`, reached `Dragonite` versus `Lucario`, selected `MOVE_DRAGON_DANCE`, exported `/tmp/more-debug-battles-20260630-champs-single-turn1.json`, and stopped the validation session cleanly. |
| Debug ROM build | `rtk make -j16 -O debug` | Pass on 2026-06-06 after the Protect-retarget update. |
| Debug ROM build | `rtk make -j16 -O debug` | Pass on 2026-06-06 after the read-mode Fake Out priority-pressure update. |
| mGBA Live command-buffer route | Direct MCP `mgba_live_start`; `DISPLAY=:0 rtk ... mgba-live-cli start --rom pokeemerald.gba --session-id smart-gimmick-command-read-20260606`; MCP input / screenshot tools | Pass on 2026-06-06 after CLI start. Direct MCP start without `DISPLAY` failed first with Qt `xcb` display initialization. The CLI-started session opened `R+START` debug menu, navigated `Party -> Gauntlet Battles -> Tera Double`, reached battle, accepted both player commands, resolved the turn to the party switch screen, exported `/tmp/smart-gimmick-command-read-after-turn.png`, and stopped cleanly. |
| mGBA Live Dmax Double action-log / assert route | `DISPLAY=:0 rtk ... mgba-live-cli start --rom pokeemerald.gba --session-id smart-gimmick-dmax-double-log-20260606`; MCP input / screenshot / memory read tools | Pass on 2026-06-06. Continued from the existing save, opened `R+START`, navigated `Party -> Gauntlet Battles -> Dmax Double`, entered multiple double-battle turns, observed opponent switch-in resolution from `Kyogre` to `Charizard`, opponent `Protect`, and later `Landorus` board state without hitting the prior `IsValidSwitchIn()` blue assert. Read `gBattleActionLog` at `0x020004cc`; header showed `sequence=15`, `lastRecordedTurn=2`, `cursor=15`, and `count=15`, confirming runtime command / switch entries were stored. Exported `/tmp/smart-gimmick-dmax-double-log-20260606.png`, stopped cleanly, and `mgba-live-cli status --all` returned `[]`. |
| mGBA Live battle action log JSON export | `tools/mgba_live/export_battle_action_log.sh battle-log-export-20260606 /tmp/battle-action-log-export-test.json`; `python3 -m json.tool /tmp/battle-action-log-export-test.json` | Pass on 2026-06-06 for the WSL / Linux wrapper. Started `Party -> Gauntlet Battles -> Dmax Double`, confirmed one double-battle turn, then exported schema `pokeemerald.battle_action_log.v1` with 6 entries. The JSON parsed successfully and included named `MOVE_ASTRAL_BARRAGE`, named `MOVE_FAKE_OUT`, opponent `B_ACTION_SWITCH` entries, and `resolved_switch_in` markers. First sandboxed export attempt failed because `mgba-live-cli` refreshes `~/.mgba-live-mcp/runtime/active_session`; rerunning with approval succeeded. Session stopped cleanly and `status --all` returned `[]`. Windows `.bat` help execution was attempted from WSL but could not be validated in this sandbox because `cmd.exe` could not enter the UNC workspace path; validate it from a normal Windows terminal if that route is used. |
| mGBA Live battle action log field coverage | `python3 -m json.tool /tmp/manual-battle-log-check-current.json`; focused JSON field summary | Pass on 2026-06-06. The current exported log had schema `pokeemerald.battle_action_log.v1`, header `count=17`, `sequence=17`, `last_recorded_turn=2`, and no missing required fields across 17 entries. It included raw codes plus decoded names for actions, moves, items, gimmicks, battler positions, side / slot, target, party index, and flags. Coverage included 10 move entries, 7 switch entries, `GIMMICK_Z_MOVE`, `GIMMICK_MEGA`, `selected_gimmick`, and `resolved_switch_in`. Because the backing buffer is EWRAM and cleared by battle initialization, export during the current battle before starting another battle. |
| mGBA Live pid `297174` / no-gimmick log audit | `/tmp/manual-ai-log-battle-action-log-autosave.json`; `/tmp/battle-action-log-export-test.json`; archived `manual-ai-log-*` session metadata | Partial on 2026-06-06. The pid `297174` autosave had 28 entries through turn 6 and recorded selected `GIMMICK_DYNAMAX` plus `GIMMICK_TERA`, so it was not the reported no-Mega / no-gimmick 4v4. It showed stronger lines after the cross-slot work, including early Tailwind / Dynamax pressure and later pivoting. The only gimmick-free `/tmp` candidate was `/tmp/battle-action-log-export-test.json`, but it had only six turn-0 entries from an export smoke, not a full 4v4 match. A complete no-Mega / no-gimmick 4v4 action log was not found in `/tmp` or archived `manual-ai-log-*` metadata. |
| mGBA Live manual-ai-log analysis | `/tmp/manual-ai-log-battle-action-log-autosave.json`; copied to `/tmp/manual-ai-log-captured-20260606.json`; Python JSON summary | Pass on 2026-06-06. Autosave retained a non-empty 20-entry `pokeemerald.battle_action_log.v1` log while a later direct export correctly skipped an empty current EWRAM buffer. The log covered 16 move actions, 4 resolved switch-ins, 2 selected `GIMMICK_TERA` markers, and turns 0-3. Useful reads: early pressure into player right after Tailwind, Tera capture, and switch-in recording. Main loss-pattern finding: turn 3 had opponent right use `Collision Course` into player right's selected `Protect`; this became the `AI_FLAG_READ_PLAYER_MOVE` Protect-retarget regression. |
| mGBA Live manual-ai-log pid 222151 analysis | Archived session `manual-ai-log-20260606-103907/session.json`; `/tmp/manual-ai-log-battle-action-log-autosave.json`; copied to `/tmp/manual-ai-log-latest-20260606.json` | Pass on 2026-06-06. Archive metadata matched pid `222151`, `fps_target=120.0`, and startup autosave. Heartbeat frame `37830` at `2026-06-06T10:38:14Z` matched the autosave `generated_at_utc` frame `37800`; SHA-256 matched between the autosave and copied latest JSON. The 20-entry log recorded 14 move actions, 6 switch actions, two selected Tera markers, and resolved switch-ins. Main finding: turn 0 had player left selected `Fake Out` into opponent right while opponent right planned ordinary `Draco Meteor`; this became the read-mode Fake Out pressure / priority regression. |
| mGBA Live 120 FPS start helper | `tools/mgba_live/start_mgba_live.sh fps-wrapper-check-20260606 120`; `mgba-live-cli status --all`; `mgba-live-cli run-lua --session fps-wrapper-check-20260606 --code "return 123" --timeout 3`; `mgba-live-cli stop --session fps-wrapper-check-20260606` | Pass on 2026-06-06 for the WSL / Linux wrapper. `status --all` reported an alive session for `pokeemerald.gba` with `fps_target=120.0` and advancing heartbeat frame. The simple Lua bridge call returned `{ result: 123 }`. The initial `emu.framecount()` probe failed because that function is not present in this mGBA Lua API; the session itself and bridge were healthy. Stop returned `stopped:true`. Windows `.bat` was updated but not runtime-validated in this WSL sandbox. |
| mGBA Live action log after session close | `tools/mgba_live/export_battle_action_log.sh manual-ai-retest-20260606 /tmp/manual-ai-retest-final-log.json`; `mgba-live-cli status --all`; archived session inspection | Failed to recover the just-finished battle on 2026-06-06 because the session was already gone before export. `status --all` returned `[]`; the archive at `~/.mgba-live-mcp/runtime/archived_sessions/manual-ai-retest-20260606-20260606-100152` had only session metadata, heartbeat / response, bridge script, and empty stdout / stderr logs. Existing manual exports such as `/tmp/manual-battle-log-check-current.json` still existed, but they were not the just-finished double battle. |
| mGBA Live action log autosave startup | `BATTLE_ACTION_LOG_OUT=/tmp/autosave-wrapper-check-20260606.json tools/mgba_live/start_mgba_live.sh autosave-wrapper-check-20260606 120`; `mgba-live-cli run-lua --session autosave-wrapper-check-20260606 --code "return {autosave=_G.BATTLE_ACTION_LOG_AUTOSAVE ~= nil, ...}"`; `mgba-live-cli stop --session autosave-wrapper-check-20260606` | Pass on 2026-06-06 for startup registration. `session.json` listed `tools/mgba_live/battle_action_log_autosave.lua`, and Lua returned `autosave=true`. No battle was started, so no non-empty autosave JSON was expected in this smoke. Future manual sessions started through `start_mgba_live.sh` should keep the latest non-empty action log at `/tmp/<session>-battle-action-log-autosave.json` unless overridden. |
| mGBA Live Protect-retarget boot smoke | `tools/mgba_live/start_mgba_live.sh ai-protect-retarget-20260606 120`; `mgba-live-cli run-lua`; `mgba-live-cli screenshot`; `mgba-live-cli stop` | Pass / partial runtime on 2026-06-06. Updated `pokeemerald.gba` booted at 120 FPS, Lua returned `{ok=true}`, screenshot exported to `/tmp/ai-protect-retarget-20260606.png`, and the validation session stopped cleanly. This was boot / bridge evidence; the actual Protect-retarget behavior is covered by focused test-runner validation to avoid interrupting manual play. |
| mGBA Live Fake Out priority boot smoke | `tools/mgba_live/start_mgba_live.sh read-fakeout-priority-smoke-20260606 120`; `mgba-live-cli status --all`; `mgba-live-cli run-lua`; `mgba-live-cli screenshot`; `mgba-live-cli stop` | Pass / partial runtime on 2026-06-06. Updated `pokeemerald.gba` booted at 120 FPS, Lua returned `{ok=true}`, screenshot exported to `/tmp/read-fakeout-priority-smoke-20260606.png`, stop returned `stopped:true`, and final `status --all` returned `[]`. This was boot / bridge evidence; the Fake Out priority behavior is covered by focused test-runner validation. |
| mGBA Live cross-slot switch boot smoke | `tools/mgba_live/start_mgba_live.sh known-cross-slot-ko-switch-guard-20260606 120`; `mgba-live-cli status --all`; `mgba-live-cli screenshot --out /tmp/known-cross-slot-ko-switch-guard-20260606.png`; `mgba-live-cli run-lua`; `mgba-live-cli stop`; `mgba-live-cli status --all` | Pass / partial runtime on 2026-06-06. Updated `pokeemerald.gba` booted at 120 FPS with pid `299495`, screenshot showed a nonblank in-game field frame, Lua returned `{ok=true, frame=1217}`, and stop returned `stopped:true`. Final `status --all` still listed the unrelated `manual-ai-log` session alive, so only the validation session was stopped. This was boot / bridge evidence; the exact switch behavior is covered by focused test-runner validation. |
| mGBA Live `Read Double` menu route | `tools/mgba_live/start_mgba_live.sh read-double-menu-20260606 120`; `mgba-live-cli input-tap`; MCP screenshot export; `mgba-live-cli stop`; `mgba-live-cli status --all` | Pass on 2026-06-06. Rebuilt the debug ROM, started the session at 120 FPS, captured `/tmp/read-double-menu-20260606-boot.png`, sent `START`, reached `Party -> Gauntlet Battles`, scrolled to the bottom, captured `/tmp/read-double-menu-20260606-read-double.png` showing `Read Double`, stopped cleanly, and `status --all` returned `[]`. This confirms menu availability; battle behavior remains for manual recheck and action-log session review. |
| mGBA Live `Read Single` / `Champs Single` route | `mgba-live-cli input-tap`; `mgba-live-cli screenshot`; `tools/mgba_live/export_battle_action_log.sh more-debug-battles-20260630 /tmp/more-debug-battles-20260630-champs-single-turn1.json`; `mgba-live-cli stop` | Pass on 2026-06-30. The progressed debug route reached `Party -> Gauntlet Battles`, screenshots showed `Read Single` and selected `Champs Single`, the `Champs Single` trainer loaded as `CHAMPION Champ Lab`, battle reached `Dragonite` versus `Lucario`, and one turn exported a 2-entry action log: player `MOVE_DRAGON_DANCE`, opponent `MOVE_NASTY_PLOT`. This became the `Champs Single` singles setup read regression because Lucario should attack a known selected Dragon Dance when meaningful damage is available. The validation session stopped with `stopped:true`; the older stale `read-gimmick-pressure-20260630` session still returned `stopped:false` and remains a cleanup caveat. |
| 2026-07-04 `Champs Single` Dragon Dance setup read guard | `/tmp/more-debug-battles-20260630-champs-single-turn1.json`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: Mega Lucario attacks selected Dragon Dance instead of setting up'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live session `champs-single-setup-guard-20260704` | Pass / partial runtime on 2026-07-04. The focused regression failed before the runtime fix with `Unmatched EXPECT_MOVE Flash Cannon, got Nasty Plot` and passed after `ShouldAvoidSelfSetupIntoReadPlayerSetup()` penalized self offensive setup in 1v1 read mode when the player selected an offensive setup move and the AI has meaningful direct damage. The focused test passed 1 test, `AI_FLAG_READ_PLAYER_MOVE` passed 24 tests, and `ai_smart_gimmick.c` passed 41 tests. Normal and debug ROM builds passed. mdbook exited 0 with the existing root `CHANGELOG.md`, `CREDITS.md`, and large-search-index warnings. mGBA Live booted the updated ROM, exported `/tmp/champs-single-setup-guard-20260704.png`, returned Lua `{ok=true, validation=champs_single_setup_guard_20260704}` on a 10-second retry after an initial 3-second timeout, stopped with `stopped:true`, and final `status --all` listed only the older stale `read-gimmick-pressure-20260630` zombie session already recorded above. |
| mGBA Live `Partner Tech` route | `tools/mgba_live/start_mgba_live.sh partner-tech-menu-20260630 120`; `mgba-live-cli input-tap`; `mgba-live-cli screenshot`; `mgba-live-cli stop`; `mgba-live-cli status --all` | Pass on 2026-06-30. The progressed debug route opened `R+START`, reached `Party -> Gauntlet Battles`, captured `/tmp/partner-tech-menu-20260630-partner-tech-menu.png` with `Partner Tech` visible, captured `/tmp/partner-tech-menu-20260630-partner-tech-selected.png` with `Partner Tech` selected, started `CHAMPION Tech Lab`, and reached an initial doubles board with player `Staraptor` + `Rillaboom` versus opponent `Lurantis` + `Staraptor`, proving the new trainer pool loads in runtime. The validation session stopped with `stopped:true`; the older stale `read-gimmick-pressure-20260630` zombie session still remains listed by `status --all` after its separate `stop` returned `stopped:false` and pid `726608` remained defunct. |
| 2026-07-04 Rage Powder ally-target / Max Guard-only Dynamax guard | `/tmp/manual-ai-watch-20260704-battle-action-log-autosave.json`; `/tmp/read-gimmick-pressure-20260630-turn1.json`; `rtk make -j16 -O check TESTS='AI does not target a Rage Powder ally with neutral attacks without payoff'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI does not spend Dynamax on Protect'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: AI does not spend Dynamax on Tailwind'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_multi.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live sessions `rage-powder-ally-target-guard-20260704` and `max-guard-dynamax-guard-20260704` | Pass / partial runtime on 2026-07-04. The manual watch session disappeared before a direct export, but autosave held a 44-entry action log through turn 9. Sequences 36 and 40 showed opponent-left `Coalossal` selecting `MOVE_BODY_PRESS` into ally `Amoonguss`; sequence 43 showed `MOVE_EARTH_POWER` into the same ally. Because the action log records selected targets before redirection, this was a real ally-target scoring mistake, not Rage Powder redirection. Pokemon Wiki's `ちゅうもくのまと` specification confirms same-side ally moves are not redirected by Follow Me / Rage Powder effects. Sequence 6 also showed opponent-left selecting `GIMMICK_DYNAMAX` with `MOVE_PROTECT` while opponent-right also protected, so the AI was spending Dynamax only to create Max Guard instead of a damaging / board-control Max Move. The older read-gimmick-pressure log showed the same status-move spend class with `GIMMICK_DYNAMAX` plus `MOVE_TAILWIND`. The fixes make non-`AI_FLAG_ATTACKS_PARTNER` single-target ally damage require a real partner payoff, and make smart Dynamax reject missing or status-class selected moves, including `Protect` and `Tailwind`, while preserving spread-move friendly-fire scoring, legitimate ally-benefit moves, and damaging Dynamax lines under Fake Out / phazing / KO pressure. Focused ally-target regression passed; focused Dynamax-on-Protect regression failed before the fix with `Expected gimmick N/A, got Dynamax` and passed after the fix; focused Dynamax-on-Tailwind regression passed after the same fix; `AI_FLAG_READ_PLAYER_MOVE` passed 23 tests; partner-synergy passed 16 tests; multi passed 13 tests; smart-gimmick passed 40 tests; doubles exited 0 with existing known-failing / TO_DO labels unchanged; normal and debug ROM builds passed. mGBA Live booted the updated ROM for the ally-target guard, Lua returned `{ok=true, validation=rage_powder_ally_target_guard_20260704}`, screenshot `/tmp/rage-powder-ally-target-guard-20260704.png` was exported, and the validation session stopped with `stopped:true`. A follow-up boot for the Max Guard-only guard returned `{ok=true, validation=max_guard_dynamax_guard_20260704}`, exported `/tmp/max-guard-dynamax-guard-20260704.png`, and stopped with `stopped:true`. Final `status --all` still listed only the older stale `read-gimmick-pressure-20260630` session, matching the previously recorded zombie cleanup caveat. A parallel check attempt for partner-synergy plus multi is non-evidence because the two make checks collided on shared test ROM / hydra temporary files; sequential reruns are the accepted evidence. A follow-up `TESTS='AI_FLAG_ATTACKS_PARTNER'` pass on 2026-07-04 resolved the older broad mismatch by accepting valid `Overdrive` spread pressure and adding a production-style Iron Hands / Rillaboom / Iron Boulder / Garganacl single-target target-selection regression. |
| 2026-07-04 `AI_FLAG_ATTACKS_PARTNER` spread-pressure expectation cleanup | `rtk make -j16 -O check TESTS='AI_FLAG_ATTACKS_PARTNER'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'` | Pass. The legacy `Mighty Cleave` case was failing because the AI selected `Overdrive`; score inspection showed `Mighty Cleave` still valued the weak target, but `Overdrive` took the KO while adding spread damage, which is the correct doubles pressure choice. The test now expects `Overdrive` in that spread-available branch and adds a production-style single-target regression where `Iron Boulder` must use `Mighty Cleave` into low-HP `Rillaboom` over healthy `Iron Hands` or ally `Garganacl`. `AI_FLAG_ATTACKS_PARTNER` passed 3 tests, partner-synergy passed 16 tests, and read-player passed 25 tests. |
| 2026-07-04 G-Max residual vs redundant Airstream guard | `/tmp/manual-ai-watch-20260704b-battle-action-log-autosave.json`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: G-Max Charizard'`; `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_ONLY: AI can spend Dynamax for Max Airstream'`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live sessions `adaptive-pool-smoke-20260704`, `adaptive-pool-elite-double-smoke-20260704`, and `adaptive-tag-map-smoke-20260704` | Pass / partial runtime. The log showed opponent Whimsicott establishing Tailwind, then opponent Charizard selecting `GIMMICK_DYNAMAX` with `MOVE_AIR_SLASH`, duplicating Speed control instead of applying G-Max Wildfire residual pressure. The focused regression passes with Charizard using Dynamax on `Heat Wave` / G-Max Wildfire when Tailwind is already active, while the existing Max Airstream spend test still passes when Speed control is actually valuable. Debug ROM build passed. mdbook exited 0 with the existing missing root `CHANGELOG.md` include, `CREDITS.md` `</img>`, and large search index warnings. The first mGBA Live smoke booted the rebuilt ROM, exported `/tmp/adaptive-pool-smoke-20260704.png`, returned Lua `{ok=true, validation=adaptive_pool_smoke_20260704}` on a 10-second retry after an initial 5-second timeout, and stopped cleanly. The follow-up smoke after adding the Elite double fixture exported `/tmp/adaptive-pool-elite-double-smoke-20260704.png`, returned Lua `{ok=true, validation=adaptive_pool_elite_double_smoke_20260704}`, and stopped cleanly. After correcting the adaptive tag map, `adaptive-tag-map-smoke-20260704` exported `/tmp/adaptive-tag-map-smoke-20260704.png`, returned Lua `{ok=true, validation=adaptive_tag_map_smoke_20260704}`, and stopped cleanly. The user/manual `manual-ai-watch-20260704c` session and stale `read-gimmick-pressure-20260630` session were left untouched. |
| mGBA Live mixed gauntlet menu runtime | Preserve running session `manual-ai-retest-20260606`; rebuild debug ROM separately | Not run on 2026-06-06 because the user was actively operating `manual-ai-retest-20260606` and requested that the session not be cut. The source and generated debug trainer data built successfully, but `Mix Single` / `Mix Double` need a later mGBA restart before they can be visually confirmed in `Party -> Gauntlet Battles`. |
| Debug battle EXP / EV gate | `rtk make -j16 -O check TESTS='Debug battles do not give exp or EVs'` | Pass on 2026-06-06; 1 test passed. Confirms `gIsDebugBattle` suppresses the EXP bar, EXP gain, and EV gain. |
| Trainer Party Pool regression | `rtk make -j16 -O check TESTS='Trainer Party Pool'` | Pass on 2026-06-06; 9 tests passed after the gauntlet submenu and same-grade player-pool rebalance. Covers role-filtered pool selection, weighted selection, and runtime RNG variation used by the debug battle fixtures. |
| Trainer Party Pool mixed gauntlet regression | `rtk make -j16 -O check TESTS='Trainer Party Pool'` | Pass on 2026-06-06; 9 tests passed after adding `Mix Single` / `Mix Double`. Covers the same weighted pool and tag rules used by the new all-gimmick pools. |
| Trainer Party Pool `Read Double` regression | `rtk make -j16 -O check TESTS='Trainer Party Pool'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'` | Pass on 2026-06-06 after adding `Read Double`; Trainer Party Pool passed 9 tests, and read-mode AI passed 8 tests. |
| Read Double Trick Room / Protect over-pivot regression | `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='AI uses Trick Room'`; `rtk make -j16 -O check TESTS='AI_FLAG_SMART_SWITCHING'`; `rtk make -j16 -O check TESTS='Protect: AI'`; `rtk make -j16 -O check TESTS='AI prefers to set up a powerful Status'` | Pass on 2026-06-06 after analyzing the pid `252732` Read Double feedback through the latest non-empty autosave. Read-mode AI passed 9 tests, Trick Room passed 3 tests, Smart Switching passed, Protect passed 9 tests, and the existing Powerful Status Trick Room preference still passes. The first attempted parallel rerun of two `check` filters collided on the shared test ELF and produced `mmap elffd failed`; the sequential reruns passed and are the evidence. |
| Ability popup / form-change focused checks | `rtk make -j16 -O check TESTS='Mega Evolution'`; `rtk make -j16 -O check TESTS='Pressure'`; `rtk make -j16 -O check TESTS='Delta Stream'`; `rtk make -j16 -O check TESTS='Terastallization'`; `rtk make -j16 -O check TESTS='Ultra Burst'`; `rtk make -j16 -O check TESTS='Primal Reversion'`; `rtk make -j16 -O check TESTS='Fairy Aura'`; `rtk make -j16 -O check TESTS='Dark Aura'`; `rtk make -j16 -O check TESTS='Aura Break'`; `rtk make -j16 -O check TESTS='Drizzle'` | Pass on 2026-06-06 after the ability-popup refresh / form-change cleanup fix. Sequential results: Mega Evolution 5 tests, Pressure 5 tests, Delta Stream 1 test, Terastallization 3 tests, Ultra Burst 5 tests, Primal Reversion 15 tests. After restoring the upstream normal-popup script timing and fixing shared palette ownership: Fairy Aura 2 tests, Dark Aura 2 tests, Aura Break 2 tests, and Drizzle 5 tests pass. Earlier parallel attempts are non-evidence because separate `make check` processes collided on the same test ELF / tmpfd artifacts. |
| Runtime knowledge catalog tool | `rtk cargo check --manifest-path tools/runtime_knowledge/Cargo.toml`; `rtk cargo run --manifest-path tools/runtime_knowledge/Cargo.toml -- --out /tmp/runtime_knowledge_catalog_rust --pretty`; JSON parse of `/tmp/runtime_knowledge_catalog_rust/*.json` | Pass on 2026-06-05. Generated valid catalogs for 935 moves, 319 abilities, 130 hold effects, 874 items, 4 gimmick policies, and `summary.json`. |
| Full battle / runtime checks | `rtk make -j16 -O check` | Historical pass on 2026-06-05 before the final Protect / Dmax-vs-Z debug update. Re-attempt after the final update exited 2; the visible output only showed existing test-runner / known-failing labels (`Tests resume after CRASH`, `Pokemon level up learnsets fit within MAX_LEVEL_UP_MOVES and MAX_RELEARNER_MOVES`), and both filtered checks return 0 individually. Focused checks above are the accepted evidence for this update. A `rtk make -j1 -O check` re-attempt did not progress beyond the initial link warning and was abandoned as non-evidence; stale `mgba-rom-test-hydra` / `mgba-rom-test` children were killed. |
| Full battle / runtime checks after `Read Double` | `rtk make -j16 -O check` | Exited 2 on 2026-06-06. `/tmp/read-double-check.log` recorded three unrelated existing broad-run AI failures: the then-unresolved `AI_FLAG_ATTACKS_PARTNER is willing to kill either the partner or the player 4/6`, `AI_FLAG_SMART_TERA: AI might tera if it gets saved from a ko (1/?)`, and `AI uses Z-Moves -- Z-Detect 1/2`. The focused Trainer Party Pool and read-mode checks above passed and are the accepted evidence for this debug-fixture-only update; the `AI_FLAG_ATTACKS_PARTNER` filter was resolved separately on 2026-07-04. |
| Broad existing Z-Move AI filter | `rtk make -j16 -O check TESTS='AI uses Z-Moves'` | Failed on 2026-06-06 in existing `AI uses Z-Moves -- Z-Detect 1/2`: expected Z-Move, got no gimmick. Focused smart-gimmick checks pass; this status-Z Protect heuristic is tracked as separate follow-up evidence. |
| Normal ROM build | `rtk make -j16 -O all` | Pass on 2026-06-06 after the ability-popup refresh / form-change cleanup fix, after the shared ability-popup palette ownership fix, after the debug opponent gimmick-bit preservation / Kyogre spread-pressure fix, and after the command-buffer read update. |
| Normal ROM build | `rtk make -j16 -O all` | Pass on 2026-06-06 after the Protect-retarget update. |
| Normal ROM build | `rtk make -j16 -O all` | Pass on 2026-06-06 after the read-mode Fake Out priority-pressure update. |
| Normal ROM build | `rtk make -j16 -O all` | Pass on 2026-06-06 after the Read Double Trick Room / Protect over-pivot tuning. |
| Debug ROM build | `rtk make -j16 -O debug` | Pass on 2026-06-06 after the Read Double Trick Room / Protect over-pivot tuning. |
| Docs build | `rtk mdbook build docs` | Exit 0 on 2026-06-06 after docs updates. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` HTML end tag, and large search index. |
| Docs build | `rtk mdbook build docs` | Pass on 2026-06-06 after documenting the ability-popup refresh / form-change cleanup fix, shared palette ownership fix, and debug opponent gimmick-bit preservation / Kyogre spread-pressure fix, with existing warnings: missing root `CHANGELOG.md` include, existing `CREDITS.md` `</img>` warning, large search index. |
| Docs build | `rtk mdbook build docs` | Exit 0 on 2026-06-06 after documenting the player double move-menu gimmick UI reset. Existing warnings remain: missing root `CHANGELOG.md` include, existing `CREDITS.md` `</img>` warning, and large search index. |
| Docs build | `rtk mdbook build docs` | Exit 0 on 2026-06-06 after documenting the Protect-retarget update. Existing warnings remain: missing root `CHANGELOG.md` include, existing `CREDITS.md` `</img>` warning, and large search index. |
| Docs build | `rtk mdbook build docs` | Exit 0 on 2026-06-06 after documenting the read-mode Fake Out priority-pressure update. Existing warnings remain: missing root `CHANGELOG.md` include, existing `CREDITS.md` `</img>` warning, and large search index. |
| Docs build | `rtk mdbook build docs` | Exit 0 on 2026-06-06 after documenting `Read Double`. Existing warnings remain: missing root `CHANGELOG.md` include, existing `CREDITS.md` `</img>` warning, and large search index. |
| Docs build | `rtk mdbook build docs` | Exit 0 on 2026-06-06 after documenting the Read Double Trick Room / Protect over-pivot tuning. Existing warnings remain: missing root `CHANGELOG.md` include, existing `CREDITS.md` `</img>` warning, and large search index. |
| Docs build | `rtk mdbook build docs` | Exit 0 on 2026-06-06 after documenting the pid `317741` ally-benefit update and `Partner Double`. Existing warnings remain: missing root `CHANGELOG.md` include, existing `CREDITS.md` `</img>` warning, and large search index. |
| mGBA Live runtime route | Boot debug ROM, enter the debug Party menu, start a target gauntlet battle, and capture at least one in-battle frame after player input / opponent response | Pass on 2026-06-06 for debug route and battle reachability. Session `smart-gimmick-read-aggressive-20260606` booted the debug ROM from `NEW GAME`, reached field control, opened `R+START` overworld debug menu, navigated `Party -> Gauntlet Battles -> Dmax Double`, started the battle, reached a move-selection screen, confirmed player input, and captured `/tmp/smart-gimmick-read-aggressive-after-input.png` on an in-battle response frame. `input-clear`, `stop`, and `status --all` cleaned up with `[]`. This was behavior-route evidence, not only title-screen smoke. Manual mGBA input did not capture a visible opponent Dynamax / Tera / Z-Move animation in this pass because early turns KO'd opposing slots quickly; focused AI tests are the direct evidence for aggressive/read logic. |
| mGBA Live Read Double Trick Room / Protect smoke | `tools/mgba_live/start_mgba_live.sh read-trick-protect-smoke-20260606 120`; `mgba-live-cli status --all`; `mgba-live-cli screenshot`; `mgba-live-cli run-lua --code 'return {ok=true}'`; `mgba-live-cli stop` | Pass on 2026-06-06. Started pid `274593` at 120 FPS, exported `/tmp/read-trick-protect-smoke-20260606.png`, confirmed the Lua bridge returned `{ok=true}`, stopped cleanly, and final `status --all` returned `[]`. This is boot / bridge evidence; the actual Trick Room mirror and Protect-over-pivot behavior is covered by focused `check` tests because replaying the exact manual loss line requires user-controlled input. |
| mGBA Live historical boot smokes | Boot current ROM and capture one screenshot / input state | Pass on 2026-06-06 for earlier fixes only. For the ability-popup refresh / form-change cleanup fix, sandboxed CLI startup first failed because it could not create `~/.mgba-live-mcp/runtime/sessions/ability-popup-formchange-20260606`; rerunning with approval and `DISPLAY=:0` booted `pokeemerald.gba` to the title screen, exported `/tmp/ability-popup-formchange-20260606.png`, `mgba-live-cli stop` returned `stopped:true`, and `status --all` returned `[]`. For the shared palette ownership fix, `DISPLAY=:0` booted `pokeemerald.gba` in session `ability-popup-palette-20260606`, exported `/tmp/ability-popup-palette-20260606.png`, `stop` returned `stopped:true`, and `status --all` returned `[]`. For the debug opponent gimmick-bit preservation fix, `DISPLAY=:0` booted `pokeemerald.gba` in session `smart-gimmick-pending-bits-20260606`, exported `/tmp/smart-gimmick-pending-bits-20260606.png`, `stop` returned `stopped:true`, and `status --all` returned `[]`. These were boot smokes only; they are not accepted as validation for the aggressive/read gauntlet behavior. |

## Manual Runtime Checks

### Debug Party Battle Fixtures

Build the debug ROM path first:

```bash
rtk make -j16 -O debug
```

In-game, open the overworld debug menu with `R + START` when `DEBUG_OVERWORLD_MENU` is enabled and `DEBUG_OVERWORLD_IN_MENU` is `FALSE`. Then use `Party`. The gauntlet fixtures are grouped under `Party -> Gauntlet Battles` so the top-level Party menu stays under the debug menu's 20-item limit. In this NPC fixture mode, singles are fixed 3v3 and doubles are fixed 4v4; 6v6 should only appear in explicitly named irregular fixtures:

- `Battle 3v3 Single`: starts a level-50 3v3 singles battle. Player side is fixed to `Dragonite`, `Gholdengo`, and `Garchomp`. AI side is selected from the `Ladder` 6-Pokemon weighted pool with 3 chosen Pokemon.
- `Battle 4v4 Double`: starts a level-50 4v4 doubles battle. Player side is fixed to `Incineroar`, `Rillaboom`, `Flutter Mane`, and `Urshifu-Rapid-Strike`. AI side is selected from the `VGC Test` 7-Pokemon weighted pool with 4 chosen Pokemon.
- `Battle Dmax/Z Single`: starts a level-50 3v3 singles battle. Player side is a Z-Move test team. AI side is a Dynamax / Gigantamax / Z-Crystal pressure team with smart gimmick flags.
- `Battle Dmax/Z Double`: starts a level-50 4v4 doubles battle. Player side is a VGC-style Z-Move test team. AI side is a Dynamax / Gigantamax / Z-Crystal doubles team with Tailwind and weather pressure.
- `Battle Gimmick Single`: starts a singles availability audit. Player side is passive. AI side leads `Gengarite` Mega and has reserve `Mimikium Z`, Gigantamax `Charizard`, and Normal Tera `Dragonite`.
- `Battle Gimmick Double`: starts a doubles availability audit. Player side is passive. AI side leads Gigantamax `Charizard` plus `Electrium Z` `Tapu Koko`, with reserve `Gengarite` Mega and Normal Tera `Dragonite`.
- `Gauntlet Battles -> Mega Single`: starts a level-50 3v3 singles battle with Mega-only debug access.
- `Gauntlet Battles -> Mega Double`: starts a level-50 4v4 doubles battle with Mega-only debug access.
- `Gauntlet Battles -> MegaZ Single`: starts a level-50 3v3 singles battle with Mega + Z-Power debug access.
- `Gauntlet Battles -> MegaZ Double`: starts a level-50 4v4 doubles battle with Mega + Z-Power debug access.
- `Gauntlet Battles -> Dmax Single`: starts a level-50 3v3 singles battle with Dynamax-only debug access.
- `Gauntlet Battles -> Dmax Double`: starts a level-50 4v4 doubles battle with Dynamax-only debug access.
- `Gauntlet Battles -> Tera Single`: starts a level-50 3v3 singles battle with Tera-only debug access.
- `Gauntlet Battles -> Tera Double`: starts a level-50 4v4 doubles battle with Tera-only debug access.
- `Gauntlet Battles -> Mix Single`: starts a level-50 3v3 singles battle with all gimmick access and mixed Mega / Z-Move / Dynamax / Tera weighted pools on both sides.
- `Gauntlet Battles -> Mix Double`: starts a level-50 4v4 doubles battle with all gimmick access and mixed Mega / Z-Move / Dynamax / Tera weighted pools on both sides.
- `Gauntlet Battles -> Read Single`: starts a level-50 3v3 singles battle with all gimmick access and mirrored 6-Pokemon weighted pools on both sides. Use this for full-information singles audits before exporting the action log from the supplied session ID.
- `Gauntlet Battles -> Read Double`: starts a level-50 4v4 doubles battle with all gimmick access and mirrored 8-Pokemon weighted pools on both sides. Use this for reset-style manual read audits before exporting the action log from the supplied session ID.
- `Gauntlet Battles -> Partner Double`: starts a fixed level-50 4v4 doubles battle with all gimmick access. Player side is `Miraidon`, `Incineroar`, `Rillaboom`, and Mega `Gengar`; AI side is `Amoonguss`, `Xerneas`, `Primarina`, and `Coalossal`. Use this for intentional ally-benefit audits: `Rage Powder`, ally `Pollen Puff`, and ally `Aqua Jet` into `Steam Engine` / `Weakness Policy`.
- `Gauntlet Battles -> Partner Tech`: starts a level-50 4v4 doubles battle with all gimmick access and focused ally-benefit pools. Player side can roll Mega `Staraptor`, `Incineroar`, `Rillaboom`, `Gardevoir`, `Amoonguss`, and `Dondozo`; AI side can roll `Whimsicott` / `Contrary` `Lurantis`, `Grimmsnarl` / Lum Berry `Staraptor`, `Froslass` / `Anger Point` `Tauros`, `Primarina` / `Coalossal`, and `Amoonguss`. Use this for broader teammate-target audits: ally `Charm`, Lum-guarded `Swagger`, critical `Frost Breath`, priority activation, redirection, and Pollen Puff legality.
- `Gauntlet Battles -> Champs Single`: starts a level-50 3v3 singles battle with Mega-only debug access. Player side stays compact, and AI side selects 3 from a 16-candidate Champion pool using `POOL_PRUNE_OPPONENT_ADAPTIVE`.
- `Gauntlet Battles -> Champs Double`: starts a level-50 4v4 doubles battle with Mega-only debug access. Player side stays compact, and AI side selects 4 from a 17-candidate Champion pool using `POOL_PRUNE_OPPONENT_ADAPTIVE`.
- `Gauntlet Battles -> Elite Single`: starts a level-50 3v3 singles battle with no gimmick access. AI side selects 3 from a 17-candidate Elite pool using `POOL_PRUNE_OPPONENT_ADAPTIVE`.
- `Gauntlet Battles -> Elite Double`: starts a level-50 4v4 doubles battle with no gimmick access. AI side selects 4 from a 17-candidate Elite pool using `POOL_PRUNE_OPPONENT_ADAPTIVE`.

Expected results:

- The battle starts from the Party debug menu without truncating the 4v4 player side to 3 Pokemon.
- The opposing side is generated from the `.party` pool, respecting `Party Size`, `Pool Rules`, tags, and `Pool Weight`.
- If `B_POOL_SETTING_CONSISTENT_RNG` is `FALSE`, repeated starts can produce different opposing selections. If it is `TRUE`, selection is deterministic for the same save OTID and trainer pointer.
- Fainting opposing Pokemon does not display the EXP bar and does not grant EXP or EVs.
- AI behavior should use the configured `Smart Trainer`, `Prediction`, `Smart Gimmick`, `Know Opponent Party`, and `Powerful Status` flags.
- In the Dmax/Z fixtures, the debug battle grants Z-Power Ring and Dynamax Band access through `gDebugGimmickAccessFlags`, so the player can test Z-Move pressure into AI Dynamax / Gigantamax timing without requiring those Bag key items. Z-Moves still require Z-Crystals. These fixtures are competitive timing fixtures and do not contain an AI Mega candidate.
- In the Gimmick audit fixtures, the debug battle grants all gimmick access through `gDebugGimmickAccessFlags`. Use these when the question is "can the opponent use Mega / Z / Dynamax / Tera at all?" rather than "does smart timing decide to conserve?"
- Opponent-side debug gimmick use does not depend on the opponent having Bag key items. Check trainer-party data instead: Mega Stone, Z-Crystal plus `Z Move: Yes`, `Dynamax Level` plus `Dynamax: Yes`, or `Tera Type`. The debug Party route now preserves those opponent bits across `CB2_InitBattle()` even though the party is built before `gBattleStruct` allocation.
- `Battle Gimmick Single` expected quick check: the opposing lead `Gengar` should be able to Mega Evolve early. Continue by fainting or forcing through reserves to inspect `Mimikyu` Z-Move, Gigantamax `Charizard`, and `Dragonite` Tera availability.
- `Battle Gimmick Double` expected quick check: the opposing lead pair exposes a Dynamax / Gigantamax candidate and a Z-Move candidate immediately. Charizard has `.shouldUseDynamax = TRUE`; Tapu Koko has `Electrium Z` plus `.shouldUseZMove = TRUE`. Continue through reserves to inspect `Gengar` Mega and `Dragonite` Tera availability.
- In the gauntlet fixtures, both the player and AI parties are generated from weighted pools. Repeated starts should be able to produce different teams while still respecting `Party Size`, item clause, species clause, and role tags.
- Gauntlet AI trainers use `Smart Trainer`, `Prediction`, `Smart Gimmick`, `Smart Switching`, `Smart Mon Choices`, `Omniscient`, `Know Opponent Party`, `Powerful Status`, `Aggressive Gimmick`, and `Read Player Move`. This is the "AI can read with full information plus confirmed player command input" route for checking whether predictions, pivots, and gimmick timing are actually good enough when the opposing team is known.
- Mega-only gauntlets should not expose Z-Move, Dynamax, or Tera access. Mega+Z gauntlets should expose Mega and Z only. Dynamax-only gauntlets should expose Dynamax / Gigantamax only. Tera-only gauntlets should expose Tera only.
- Mix gauntlets should expose every debug gimmick access bit at once while still relying on each Pokemon's data: Mega Stone for Mega, Z-Crystal plus `Z Move: Yes`, `Dynamax Level` / `Gigantamax: Yes`, or `Tera Type`.
- `Read Single` should keep both sides same-grade in singles: the mirrored pools can roll `Miraidon`, `Koraidon`, `Gengar`, `Dragonite`, `Rillaboom`, and `Tapu Koko`, with lead / ace / support tags and weights. The AI's advantage should come from `Read Player Move`, known player party data, switching, setup-pressure handling, and timing quality, not a stronger roster.
- `Read Double` should keep both sides same-grade: the mirrored pools can roll `Tornadus`, `Incineroar`, `Farigiraf`, `Rillaboom`, `Miraidon`, `Koraidon`, `Gengar`, and `Tapu Koko`, with lead / ace tags and weights. The AI's advantage should come from `Read Player Move`, known player party data, switching, and timing quality, not a stronger roster.
- `Partner Double` should start with the ally-benefit board visible instead of waiting for a pool roll. The first AI side is `Amoonguss` + `Xerneas`, so a selected player single-target attack into Xerneas should make `Rage Powder` a real candidate, and low-HP / boosted Xerneas states should make ally `Pollen Puff` valuable. The reserve pair `Primarina` + `Coalossal` is intended for low-damage `Aqua Jet` triggering `Steam Engine` and `Weakness Policy`.
- `Partner Tech` should expose multiple possible ally-benefit boards from production-style candidates. A useful `Contrary` ally stat reversal, a Lum-guarded `Swagger`, a guaranteed critical hit into `Anger Point`, and low-damage priority activation can be good candidates; self-targeting Pollen Puff or arbitrary unsafe friendly fire should not be chosen.
- `Champs Single` should stay Mega-only and 3v3. Its AI side should adapt from the Champion pool after reading the player party rather than staying on one fixed three-Pokemon answer.
- `Champs Double` should stay Mega-only and 4v4. Its AI side should adapt from the Champion pool after reading the player party, including fast-pressure, weather / legendary, and slower disruption responses.
- `Elite Single` should stay no-gimmick and 3v3. It is the stronger normal NPC singles route, so restricted legends are allowed when the adaptive pool chooses that branch.
- `Elite Single` AI has `Read Player Move`; switching should key off the confirmed selected player command. If the player selected a status / setup move, the AI should not switch only because the visible moveset contains unselected coverage. If the selected move would KO and a real defensive switch-in exists, the AI can pivot.
- `Elite Single` pool entries should use singles-valid support sets. Doubles-only utility such as `Rage Powder`, `Helping Hand`, ally `Pollen Puff`, and routine `Protect` should stay in doubles pools instead of becoming dead singles turns.
- `Elite Double` should stay no-gimmick and 4v4. It is the stronger normal NPC doubles route and should test full-information reads without relying on Mega, Z-Move, Dynamax, or Tera access.
- `Tera Single` should not always preserve Tera through an obvious selected-KO turn. If the active Pokemon can act first, Tera changes the incoming selected move from a KO into a guaranteed survival even at maximum roll, and the Tera turn still deals meaningful damage, staying in and spending Tera is preferred over cycling reserves. If maximum-roll damage can still KO after Tera, switching remains acceptable.
- The gauntlet sides should be roughly same-grade after pool selection. The player side can roll restricted legends, mythicals, strong support, and high-value gimmick anchors too; the AI's intended edge comes from full-information prediction, switching, and timing, not from having a categorically stronger team.
- The top-level Party menu should render cleanly with no corrupted text. Entering `Gauntlet Battles` should show the eighteen gauntlet entries without mojibake, including the scroll-bottom `Read Single`, `Read Double`, `Partner Double`, `Partner Tech`, `Champs Single`, `Champs Double`, `Elite Single`, and `Elite Double` entries.
- Ability popups should not linger over form-change messages. In Mega gauntlets, a switch-in `Pressure` popup from `Mewtwo` should close before the `Mewtwonite Y is reacting` Mega Evolution message, and repeated same-battler ability activations should refresh the existing popup instead of leaving black OBJ-tile rectangles.
- Normal ability popups should keep the upstream timing. `Fairy Aura`, `Dark Aura`, and switch-in weather popups should not be cut short; the popup palette should also stay stable while Move Info Window / Last Used Ball Window sprites are closing.
- In Rayquaza / Delta Stream scenarios, after strong winds dissipate or the active Pokemon changes, the battlefield should not show a black rectangle where an old ability popup sprite or shared OBJ tile used to be.
- Double battles should not hit the `IsValidSwitchIn()` ASSERT when the AI tries to reposition. If both active AI Pokemon want to switch but only one legal reserve exists, or a partner already reserved the best target, the second switch request should be canceled or redirected to a legal reserve.
- Opponent gimmicks are not forced to fire immediately in smart-timing fixtures. They are expected to activate only when the smart timing checks find a concrete payoff: last-Pokemon pressure, one-reserve low-HP late commit, KO conversion, Max Move board control, disruption prevention, useful Mega / Tera form timing, or a valid Z-Move payoff.
- In gauntlet fixtures, `Aggressive Gimmick` should make Dynamax / Tera / damaging Z-Move use visibly more proactive than the normal smart-conservation fixtures. A good manual target is roughly "often spends on real pressure, sometimes holds," not "always press immediately" and not "never spend until the last Pokemon."
- In gauntlet fixtures, `Read Player Move` can only read player commands that have already been confirmed by the controller. In doubles, if only one player-side battler has confirmed when the opponent recomputes, the read is intentionally partial until a later full turn-tree evaluator exists.
- Player-side double battle move selection should not leak a first battler's selected gimmick state into the second battler's first rendered move menu. After choosing Z-Move or Dynamax for the first player battler, the second player battler should initially show its normal move names, PP, and type text unless START is pressed for that second battler and that battler is eligible for the selected gimmick.

For mGBA Live validation, do not stop at title-screen boot. Use a progressed debug save or scripted input route, open `Party -> Gauntlet Battles`, start one target gauntlet, choose a player move, and capture the opponent response. If the automation cannot reach that state, record the exact navigation failure here instead of marking the runtime check complete.

Use the debug trainer battle flow and Trainer 1 IDs documented in `docs/tutorials/ai_flags.md`.

- `855`: no-payoff Dynamax conserve.
- `856`: last-Pokemon Dynamax.
- `858`: Z-Move viability and smart timing.
- `859`: offensive Tera.
- `860`: Dynamax + Tera environment with separate candidates.
- `861`: all-gimmick single battle.
- `862`: all-gimmick double battle.
- `863`: Smart Switching debug double battle.

For double switching behavior, use any debug double battle with `AI_FLAG_SMART_SWITCHING` and a passive pinned AI lead pair. Expected behavior is that the AI can hard switch one or both active Pokemon if neither active slot has useful pressure and the partner cannot cover the threatened position.

For board-control switching, use an AI reserve that can change the field or speed state. Expected examples:

- `Pelipper` / `Drizzle` can be chosen over staying in when rain creates stronger reserve pressure.
- `Rillaboom` / `Grassy Surge` can be chosen when Grassy Terrain plus Grass pressure changes the board.
- A reserve with `Tailwind` can be chosen when doubled Speed flips relevant matchups.
- A reserve with `Trick Room` can be chosen when the AI side is slower and the current room state does not already cover that plan.
- A reserve terrain setter holding the matching seed can be chosen when the field activates a stat plan.
- A reserve with status-benefit ability or `Facade` / `Psycho Shift` can be chosen into a predicted status move.
- In doubles, a reserve with direct status pressure, secondary status / confusion pressure, status prevention, status cure, or Skill Swap-style ability bridge support can be chosen when the active slot is pinned and the bench role can create board pressure.

For the `863` Smart Switching debug fixture:

- Target the AI left `Zigzagoon` with `Taunt`. Expected: when prediction fires and `Zigzagoon` cannot punish in place, the AI can pivot into `Gengar` to attack the Taunt user instead of staying as disabled utility.
- Target the AI left `Zigzagoon` with `Will-O-Wisp`. Expected: when prediction fires, `Guts` `Ursaring` is an intended burn-benefit reserve.
- Pressure the AI left slot with an unfavorable Fighting or strong-damage line. Expected: the AI can pivot toward `Slowbro`, whose `Scald` / `Psybeam` pressure represents secondary-status / confusion-style board support.
- Negative guard to keep in mind while testing custom parties: the AI should not switch only because a Pokemon was Taunted. It should stay if the active Pokemon can already punish with damage, if Taunt is blocked by `Aroma Veil` / Gen 6+ `Oblivious`, or if an enabled Gen 5+ `Mental Herb` can absorb the Taunt.

For Dynamax disruption timing, use a Dynamax-capable AI active Pokemon with a reserve remaining and a selected damaging move. Expected examples:

- If the known / predicted opposing move is `Fake Out` or another relevant flinch move, smart Dynamax can spend to keep the selected attack live.
- If the known / predicted opposing move is `Roar` or `Whirlwind`, smart Dynamax can spend to avoid losing the active Pokemon to phazing.

For Protect timing, use singles and doubles battles where the AI has `Protect` plus a predicted damaging move. Expected examples:

- In singles, the AI should usually avoid passive Protect if it only gives the opponent a free turn, especially after the opponent has already boosted.
- In singles, the AI can use Protect when it gains a real turn payoff: residual target damage, incoming Wish, Poison Heal / Leftovers / Black Sludge / Grassy Terrain / Rain Dish / Ice Body / Aqua Ring / Ingrain recovery, Substitute threshold, choice-lock scouting, or a next-turn Disable / Encore line.
- A second consecutive Protect is not forbidden. It should be penalized for reduced success odds, but can remain viable when the same payoff still matters.
- In doubles, second Protect should also remain available as a risky positioning option under pressure rather than being removed from consideration.
- In doubles, a pressured active Pokemon with usable `Protect` should not be forced into optional all-moves-bad / delayed speed-control / bad-position / bad-odds switching if Protect can buy the turn. This guard does not apply when the Pokemon is trying to escape status, Yawn, perish, Encore, similar volatile pressure, or a move that ignores Protect.

For focused-slot collapse switching, use production-style Read Double boards where a high-value active Pokemon is pinned by confirmed damage and cannot safely Protect:

- If a real defensive switch-in exists, the AI should prefer that safe pivot before considering a sacrifice.
- If no immune or safe switch-in exists and the active Pokemon lacks a safe Protect / Detect line, the AI may switch to a lower-value Focus Sash or support reserve to preserve the active Pokemon and consume one-turn disruption.
- The main regression is Choice Specs Kyogre under selected Electric Z `Thunderbolt` plus `Fake Out`. With no Electric-immune reserve, the AI should switch Kyogre to Whimsicott instead of staying in with `Water Spout`.
- A Protect / Detect move in the set is not enough to cancel this behavior. If a selected Feint, protection-ignoring move, Unseen Fist-style contact move, disabled / Imprisoned Protect, or similar known limitation beats the Protect line, the AI should still run the no-safe-Protect switch ladder.
- This is not a blanket "always sack support" rule. Do not count a test that only gives the AI an obvious Electric immunity as evidence for the no-safe-switch behavior.

For desperation comeback timing, use boards where the AI is likely to lose within roughly two to three turns unless a disruptive or high-variance line connects. Expected examples:

- If a normal damaging move can KO, likely KO, or win the short damage race, the AI should prefer that clean line over flinch, crit, OHKO, or low-accuracy status fishing.
- If the AI has no reserve or is trapped under Perish Song pressure, a faster flinch, paralysis, sleep, OHKO, or critical-hit line can become viable as a last-chance out.
- If a reserve can still switch out of Perish Song and there is no opposing setup pressure, the AI should not treat Perish Song alone as permission to fish for flinch.
- In doubles, near-term loss pressure can justify Fake Out-style flinch, Taunt into a known status move, Tailwind, Trick Room, speed drops, or Throat Chop against sound moves when those lines can stop the immediate checkmate. Soundproof switching is available for the narrow read-Perish escape case. Ally-target Speed manipulation now has a narrow active-Trick-Room / immediate-KO score bridge, but broader switch / Protect / order-planner checks remain future work.
- Spread moves that KO an ally should remain rejected unless the AI is already under near-term board pressure, the spread move creates real opposing-side pressure, and a reserve can use the opened slot.
- Commander Dondozo should be the target, not swallowed Tatsugiri. If Dondozo can be KOed, the AI should remember that Tatsugiri remains afterward, especially when Tatsugiri is poisoned / burned or carrying Toxic Orb / Flame Orb.

## Accepted Risks Before Deeper Tuning

- G-Max unique effects are now exposed as broad reusable move knowledge flags, with residual G-Max effects separated. Most non-residual unique effects still need separate scoring if they become important to trainer fixtures.
- Smart Z-Move status tactics still depend on the existing status Z-Move checks. `Z-Detect` / Fake Out timing is covered, but broader status Z-Moves such as Z-Haze, Z-Tailwind, and Z-Trick Room still need tactic-specific models before relying on them as competitive lines.
- The AI now applies a lightweight future board-pressure heuristic to damaging doubles candidates, including a three-turn same-move horizon for Choice / Gorilla Tactics users and a partner-coverage gap reward when reliable future context is available. This is intentionally broader than `AI_FLAG_READ_PLAYER_MOVE`, but it still does not perform a full turn-tree search.
- Tactical partner activation is still heuristic. It separates spread, priority, and low-damage non-priority triggers, but it does not yet solve every multi-turn line where an ally accepts damage now for a later-positioning payoff.
- `AI_FLAG_READ_PLAYER_MOVE` is now also a non-link NPC trainer policy for trainers that already have AI flags. The rollout is intentionally runtime-only so it does not duplicate partygen's ownership of generated trainer-party / tactic-pool content. It still evaluates one chosen command set plus bounded future-pressure heuristics instead of searching a full turn tree.
- Choice-role-done switching is intentionally conservative. It currently requires a confirmed read turn, an already locked Choice / Gorilla Tactics move, no known damage into the active slot, weak locked-move progress, and a switch-in that is not punished by known damage. If the role-done Pokemon is already being hit, the AI can keep it as a cushion rather than exposing the reserve. It does not yet model every status-target, PP, or two-turn sacrifice line.
- Focused-slot collapse switching is a targeted doubles slice, not a full 2-3 turn planner. It recognizes selected-gimmick focus damage, Fake Out-style one-turn disruption, high-value active preservation, safe switch-ins, Focus Sash / support cushions, selected Protect counterplay such as Feint / protection bypass, and one-turn Grassy Terrain recovery in switch-in survival. It does not yet model every Volt Switch pivot, timer-stall Protect, unconfirmed Protect counterplay prediction, or multi-turn recovery / chip line requested in `goal.md`.
- Protect scoring is payoff-based, not a full opponent turn-tree read. It recognizes common turn-gain reasons, consecutive-use risk, and one-turn deterministic recovery such as Grassy Terrain and Leftovers, but it does not yet solve every PP-stall, double-target, "Protect to bait a switch", or full recovery-plus-timer line.
- Desperation comeback scoring is payoff-gated through the shared risk governor and deliberately narrow, but it is still a heuristic. It does not yet solve full 2-3 turn forced lines, broad sound-pressure switching, or the broader ally-order planner beyond the narrow Scary Face-style active-Trick-Room / immediate-KO score bridge.
- Double switching and board-control pivoting are heuristic position checks, not a full VGC turn solver. Board-control pivots can identify weather, terrain, Tailwind, and Trick Room roles, but they do not yet search full multi-turn lines such as "switch setter now, protect partner next turn, then reposition again."
- Status and ability-bridge switching is intentionally conservative in singles. Non-immediate utility pivots are double-battle only in this slice; singles keep only immediate status-benefit pivots plus the existing bad-odds / bad-matchup gates.
- The latest Elite Single concern did not have an exact single-battle action log in `/tmp`; the available `manual-ai-watch-20260704*` exports for this pass were doubles battles. Focused selected-command switch tests cover the suspected runtime bug, and the next manual Elite Single run should export a fresh log for turn-by-turn review.

## GitHub Actions

Long GitHub Actions were not re-waited for this local handoff. Local `check`, `all`, docs build, and mGBA Live smoke provide the branch evidence for this update.
