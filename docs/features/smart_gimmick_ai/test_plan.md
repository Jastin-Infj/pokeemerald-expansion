# Smart Gimmick AI Test Plan

## Required Validation

| Check | Command | Status |
| --- | --- | --- |
| Focused runtime knowledge tests | `rtk make -j16 -O check TESTS='AI runtime knowledge'` | Pass on 2026-06-05; 4 tests passed. Covers move-category, ability-category, item / hold-effect category mapping, and predicted-move immunity bridges. |
| Focused smart gimmick tests | `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV'` | Pass on 2026-06-06; 13 tests passed. Includes Z-Crystal-based all-gimmick Z-Move coverage plus Dynamax / Mega late-commit regressions. |
| Focused read / production fixture AI tests | `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_TERA_ONLY'`; `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_TERA'`; `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_ALL'` | Pass on 2026-06-06. `AI_FLAG_READ_PLAYER_MOVE` has 11 passing tests and `ai_smart_gimmick.c` has 31 passing tests after replacing placeholder Wobbuffet / Celebrate fixtures with production-style moves, items, abilities, natures, IVs, and fixed equivalent stats. Covers selected player Tera damage reads, actual selected switch-in reads, Fake Out Dynamax, command-log capture for all double-battle choices, retargeting single-target damage away from a confirmed selected Protect, read-mode Fake Out pressure, `Extreme Speed` priority ordering, priority-blocker guards, and the pid `278133` cross-slot Grassy Glide KO switch to Incineroar. The paired High Horsepower case confirms the AI does not force the same Incineroar switch when the known target move would punish the reserve. `TESTS='AI_FLAG_AGGRESSIVE_GIMMICK'` now returns `No tests found`; the former placeholder aggressive cases were replaced by deterministic production-style low-HP pressure checks. |
| Focused smart-gimmick / read adjusted rerun | `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_ONLY'`; `rtk make -j16 -O check TESTS='AI_FLAG_SMART_Z_MOVE'`; `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_ALL'`; `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_TERA_ONLY'`; `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_TERA'`; `rtk make -j16 -O check TESTS='AI_FLAG_SMART_MEGA'` | Pass on 2026-06-06 after the pid `317741` ally-benefit update. Counts were 11, 8, 5, 4, 1, 1, and 1 tests respectively, so the current adjusted read / smart-gimmick filter total is 31 tests. A broad exploratory `TESTS='AI_FLAG_'` run is non-evidence because it picked unrelated existing tests and failed at `AI_FLAG_ATTACKS_PARTNER is willing to kill either the partner or the player 4/6`. |
| pid `317741` ally-benefit regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; `rtk make -j16 -O check TESTS='AI heals a low HP setup ally with Pollen Puff'`; `rtk make -j16 -O check TESTS='AI uses Rage Powder'`; `rtk make -j16 -O check TESTS='AI triggers ally'`; `rtk make -j16 -O check TESTS='AI uses a guaranteed critical hit'`; `rtk make -j16 -O check TESTS='AI uses Psych Up'`; `rtk make -j16 -O check TESTS='AI uses Haze to reset an ally'`; `rtk make -j16 -O check TESTS='AI can score Clear Smog'`; Beat Up / Weakness Policy existing AI filters; `rtk make -j16 -O debug`; `rtk make -j16 -O all`; mGBA Live session `partner-tech-ai-20260606` | Pass / partial runtime on 2026-06-06. The pid `317741` log showed `Amoonguss` choosing `Spore`, offensive `Pollen Puff`, and `Protect` while not using `Rage Powder` / ally heal despite having the information. Focused tests now cover ally Pollen Puff healing, read-mode Rage Powder protection, ally `Steam Engine` + `Weakness Policy`, ally `Stamina` + `Body Press`, ally `Anger Point`, ally `Psych Up`, `Haze` after `Draco Meteor` drops, and low-priority ally `Clear Smog` scoring when Haze is unavailable. Existing AI Beat Up / Justified / Rage Fist and ally Weakness Policy tests passed. Debug and normal ROM builds passed. mGBA Live booted the updated ROM, accepted an A input to the title menu, captured `/tmp/partner-tech-ai-20260606-boot.png` and `/tmp/partner-tech-ai-20260606-after-a.png`, and stopped cleanly. |
| pid `354725` Pollen Puff self-target regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; `rtk make -j16 -O check TESTS='AI does not target itself'`; `rtk make -j16 -O check TESTS='AI heals'`; `rtk make -j16 -O check TESTS='AI uses Rage Powder'`; `rtk make -j16 -O check TESTS='AI triggers ally'`; `rtk make -j16 -O check TESTS='AI uses a guaranteed critical hit'`; `rtk make -j16 -O check TESTS='AI uses Psych Up'`; `rtk make -j16 -O check TESTS='AI uses Haze'`; `rtk make -j16 -O check TESTS='AI can score Clear Smog'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; mGBA Live session `pollen-self-target-guard-20260607` | Pass / partial runtime on 2026-06-07. Log sequence 26, turn 5 had opponent-right `MOVE_POLLEN_PUFF` targeting `B_POSITION_OPPONENT_RIGHT`, which is not a legal Pollen Puff target. The fix rejects self-targeting for non-self target classes through `CanTargetBattler`, stops double AI move slot 0 from bypassing target validity, and revalidates opponent AI targets before emitting the controller command. Focused Pollen Puff self-target regression passed, existing ally Pollen Puff and partner-synergy tests passed, and `AI_FLAG_READ_PLAYER_MOVE` passed 11 tests. A sequential `TESTS='AI_FLAG_ATTACKS_PARTNER'` probe still exposes the pre-existing `is willing to kill either the partner or the player 4/6` target mismatch, matching earlier non-evidence broad runs and not counted against this fix. Normal and debug ROM builds passed. mGBA Live booted `pokeemerald.gba`, exported `/tmp/pollen-self-target-guard-boot.png`, accepted `A`, stopped with `stopped:true`, and final `status --all` returned `[]`. |
| pid `368099` HP-dependent power read regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; `rtk make -j16 -O check TESTS='AI discounts'`; `rtk make -j16 -O check TESTS=\"AI prefers Kyogre's\"`; `rtk make -j16 -O check TESTS=\"AI does not tunnel Kyogre's\"`; `rtk make -j16 -O check TESTS=test/battle/ai/ai_doubles.c`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live session `hp-dependent-ai-20260607` | Pass / partial runtime on 2026-06-07. The pid `368099` log showed opponent-left `Kyogre` selecting `Water Spout` while player-right had a faster confirmed `Dragon Ascent` into Kyogre, so the AI was valuing Water Spout from current HP instead of projected HP after known earlier damage. The fix discounts HP-dependent power when known earlier damage lowers the relevant user or target HP first. New production-style double tests cover `Water Spout` after a known faster `Dragon Ascent` and `Wring Out` after a known faster ally-spread `Surf`; existing Kyogre spread-pressure tests still pass so full-HP Water Spout remains preferred when it can fire before being damaged. `ai_doubles.c` exited 0 with existing known-failing / TO_DO labels unchanged, `AI_FLAG_READ_PLAYER_MOVE` passed 11 tests, normal and debug ROM builds passed, and mdbook exited 0 with the same existing `CHANGELOG.md`, `CREDITS.md`, and large-search-index warnings. mGBA Live booted `pokeemerald.gba`, returned Lua `{ok=true, validation=hp_dependent_ai_20260607}`, exported `/tmp/hp-dependent-ai-20260607-boot.png` and `/tmp/hp-dependent-ai-20260607-after-a.png`, accepted an `A` input, stopped with `stopped:true`, and final `status --all` returned `[]`. |
| pid `493358` future spread-pressure regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; `rtk make -j16 -O check TESTS='AI values future spread pressure over a switch read beside boosted Xerneas'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live session `future-pressure-ai-20260607` | Pass / partial runtime on 2026-06-07. The pid `493358` feedback identified that Kyogre's Ice Beam switch read into Amoonguss was too narrow while a Geomancy-boosted Xerneas remained beside the switch. The fix gives every damaging doubles candidate a lightweight future board-pressure score: safe opposing-side spread damage is summed across both opposing slots, Choice / Gorilla Tactics users compare that pressure over a three-turn same-move horizon, and narrow non-KO single-target reads are penalized when spread pressure is materially stronger. The focused Kyogre test passed and asserts both `Water Spout` and `Origin Pulse` score above `Ice Beam` under Choice Specs and Mystic Water. `ai_doubles.c` exited 0 with existing known-failing / TO_DO labels unchanged, smart-gimmick passed 31 tests, and partner-synergy passed 9 tests. A parallel `check` attempt for smart-gimmick plus partner-synergy produced a shared test-ELF `Bus error`, so only the sequential reruns are evidence. Normal and debug ROM builds passed. mdbook exited 0 with the same existing warnings. mGBA Live booted `pokeemerald.gba`, returned Lua `{ok=true, validation=future_pressure_ai_20260607}`, exported `/tmp/future-pressure-ai-20260607-boot.png` and `/tmp/future-pressure-ai-20260607-after-a.png`, accepted an `A` input, stopped with `stopped:true`, and final `status --all` returned `[]`. |
| pid `507917` Prankster / coverage regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; `rtk make -j16 -O check TESTS='AI uses Guard Split to improve its stats'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live session `prankster-coverage-ai-20260607` | Pass / partial runtime on 2026-06-07. The live `manual-ai-log` session matched pid `507917`, and turn 3 showed Prankster `Tornadus` choosing `Taunt` into Dark-type `Incineroar`, which has no Gen 7+ Prankster payoff. The fix applies the Prankster-Dark immunity check before Taunt is rewarded or accepted as a non-bad move. The same board showed why Kyogre must be valued as the Incineroar damage source when Tornadus has no meaningful Incineroar hit: the future board-pressure score now rewards a damaging candidate that meaningfully pressures a target its partner cannot damage. This is not limited to `AI_FLAG_READ_PLAYER_MOVE`; the regression is parametrized for confirmed read / omniscient context and normal prediction / known-party context, while a Guard Split rerun guards low-information support targeting from heuristic pollution. File-level doubles, smart-gimmick, partner-synergy, normal ROM, and debug ROM checks passed. mdbook exited 0 with the same existing warnings. mGBA Live booted `pokeemerald.gba`, returned Lua `{ok=true, validation=prankster_coverage_ai_20260607}`, exported `/tmp/prankster-coverage-ai-20260607-boot.png` and `/tmp/prankster-coverage-ai-20260607-after-a.png`, accepted an `A` input, stopped with `stopped:true`, and final `status --all` returned `[]`. Initial parallel long-title filters are non-evidence because they collided on shared hydra / temporary files and returned `No tests found`. |
| pid `528022` tactical partner activation regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; archived session `manual-ai-log-20260607-044252/session.json`; `rtk make -j16 -O check TESTS='AI triggers ally'`; `rtk make -j16 -O check TESTS='AI can score a harmless non-priority Fire hit'`; `rtk make -j16 -O check TESTS='AI does not use unsafe non-priority Water Gun'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk make -j16 -O check TESTS=\"AI will trigger its ally's Weakness Policy\"`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live session `partner-activation-ai-final2-20260607` | Pass / partial runtime on 2026-06-07. The archived session metadata matched pid `528022`, and the autosave had 38 entries through turn 7. The visible `Water Gun` actions targeted player-right, not the AI ally, but the feedback clarified the intended ally-trigger policy: do not treat arbitrary survivable ally hits as good just because they activate `Steam Engine` / `Weakness Policy`; prefer tactical triggers such as low-damage `Aqua Jet`, and still allow non-priority moves when the actual ally damage is harmless. The fix gives single-target partner activation a tactical trigger score: spread moves remain valid, priority moves score strongly, harmless non-priority hits score weakly, and unsafe non-priority 4x Water damage into `Coalossal` is not rewarded. `Partner Double` now uses `Primarina` `Aqua Jet` beside `Coalossal` instead of `Pelipper` `Water Gun`. Focused partner-activation tests, the full partner-synergy file, existing spread `Weakness Policy`, `ai_doubles.c`, normal ROM, and debug ROM passed. mdbook exited 0 with the same existing `CHANGELOG.md`, `CREDITS.md`, and large-search-index warnings. After a final normal ROM rebuild, mGBA Live booted `pokeemerald.gba`, returned Lua `{ok=true, validation=partner_activation_ai_final2_20260607}`, exported `/tmp/partner-activation-ai-final2-20260607-boot.png` and `/tmp/partner-activation-ai-final2-20260607-after-a.png`, accepted an `A` input, stopped with `stopped:true`, and final `status --all` returned `[]`. Parallel focused check attempts and parallel all/debug builds are non-evidence because they collided on shared temporary files / root ROM outputs; sequential reruns are the accepted evidence. |
| pid `549295` selected Protect read regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; archived session `manual-ai-log-20260607-055044/session.json`; `rtk make -j16 -O check TESTS='AI protects instead of choosing a slower attack into a selected KO'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live session `protect-read-ai-20260607` | Pass / partial runtime on 2026-06-07. The archived session metadata matched pid `549295`; the autosave had 23 entries through turn 4. Turn 3 showed player-right selected Dynamax `Wood Hammer` into opponent-right `Primarina`, while `Primarina` chose slower `Sparkling Aria` instead of `Protect`. The fix makes selected-target readers prefer confirmed `gBattleStruct->moveTarget`, adds a READ-player Protect payoff that scans all confirmed player commands for selected high-damage threats into the protecting battler, and penalizes ordinary actions that known earlier damage would KO before resolution. Focused Protect-read, partner-synergy, READ_PLAYER_MOVE, smart-gimmick, and doubles checks passed. Normal and debug ROM builds passed. mdbook exited 0 with the same existing `CHANGELOG.md`, `CREDITS.md`, and large-search-index warnings. mGBA Live booted `pokeemerald.gba`, returned Lua `{ok=true, validation=protect_read_ai_20260607}`, exported `/tmp/protect-read-ai-20260607-boot.png` and `/tmp/protect-read-ai-20260607-after-a.png`, accepted an `A` input, stopped with `stopped:true`, and final `status --all` returned `[]`. |
| pid `565092` Choice role-done switch regression and Champions fixture | `/tmp/manual-ai-log-battle-action-log-autosave.json`; archived session `manual-ai-log-20260607-064700/session.json`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: choice-locked'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_partner_synergy.c'`; `rtk make tools/trainerproc/trainerproc`; `rtk make -j16 -O all`; `rtk make -j16 -O debug`; `rtk mdbook build docs`; mGBA Live session `champs-choice-ai-20260607` | Pass / partial runtime on 2026-06-07. The pid `565092` autosave had 58 entries through turn 12 and showed opponent-right using `MOVE_ICE_BEAM` nine consecutive times after switching on turn 0, then only switching much later. The fix adds a read-mode doubles pivot for already locked Choice / Gorilla Tactics users when all player commands are known, the active slot is not taking known damage, the locked move no longer creates meaningful 2HKO or spread-pressure progress, and an eligible reserve can enter without being punished by the confirmed move set. The paired tests cover the desired pivot and the negative case where an immune active would be safe but the reserve would be hit by known spread damage. Focused Choice, full read-mode, switching, smart-gimmick, doubles, and partner-synergy checks passed; `ai_switching.c` / `ai_doubles.c` exited 0 with existing known-failing / TO_DO labels unchanged. Normal and debug ROM builds passed, trainerproc needed no rebuild, and mdbook exited 0 with the same existing warnings. mGBA Live booted the current `pokeemerald.gba`, returned Lua `{ok=true, validation=champs_choice_ai_20260607}`, exported `/tmp/champs-choice-ai-20260607-boot.png` and `/tmp/champs-choice-ai-20260607-after-a.png`, accepted `A`, stopped with `stopped:true`, and final `status --all` returned `[]`. The boot screenshot was captured during a white fade, but the after-A screenshot reached the title screen. This was only boot / bridge / input evidence: the current mGBA battery route exposed `NEW GAME` rather than a progressed overworld debug save, so `Gauntlet Battles -> Champs Double` menu reachability and in-battle behavior remain a manual check. |
| pid `354725` docs handoff | `rtk mdbook build docs` | Pass on 2026-06-07. mdbook exited 0 with the same existing warnings for the missing root `CHANGELOG.md` include, `CREDITS.md` `</img>` warning, and large search index. |
| pid `278133` cross-slot KO switch regression | `/tmp/manual-ai-log-battle-action-log-autosave.json`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_smart_gimmick.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_switching.c'`; `rtk make -j16 -O all`; mGBA Live boot / bridge smoke | Pass on 2026-06-06. Log analysis showed turn 7 player-left `MOVE_GRASSY_GLIDE` targeted opponent-right Mega Gengar, but opponent-right stayed in for `MOVE_FOCUS_BLAST` and then disappeared from later turns. Root cause: switch context read the opposite slot instead of the known cross-slot selected target. Focused read-mode passed 11 tests; smart-gimmick file passed 31 tests; AI switching file exited 0 with only existing known-failing labels; normal ROM build passed; session `known-cross-slot-ko-switch-guard-20260606` booted updated `pokeemerald.gba`, screenshot `/tmp/known-cross-slot-ko-switch-guard-20260606.png` was nonblank, Lua returned `{ok=true, frame=1217}`, and `stop` returned `stopped:true`. Final `status --all` still listed the unrelated `manual-ai-log` session alive, so only the validation session was stopped. |
| Final branch build / docs / boot smoke | `rtk make -j16 -O all`; `rtk mdbook build docs`; MCP mGBA Live session `final-smart-gimmick-ai-20260606` | Pass / partial runtime on 2026-06-06 after the final source, production-style test fixture, and docs state. Normal ROM build passed. mdbook exited 0; existing warnings remained for missing root `CHANGELOG.md` include, `CREDITS.md` HTML end tag, and large search index. mGBA Live returned Lua `{ok=true, validation=smart_gimmick_ai_final_20260606}`, exported `/tmp/final-smart-gimmick-ai-20260606.png`, and stopped cleanly. |
| Additional pid `278133` AI smoke files | `rtk make -j16 -O check TESTS='test/battle/ai/ai.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_doubles.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_choice.c'`; `rtk make -j16 -O check TESTS='test/battle/ai/ai_calc_best_move_score.c'` | Pass / exit 0 on 2026-06-06. `ai_choice.c` passed 11 tests and `ai_calc_best_move_score.c` passed 17 tests. `ai.c` and `ai_doubles.c` exited 0; existing known-failing / TODO labels in doubles remain unchanged. `TESTS='test/battle/ai'` was attempted but returned `No tests found`, so it is non-evidence. |
| Focused double switch reservation regression | `rtk make -j16 -O check TESTS='AI will not try to switch for the same'` | Pass on 2026-06-06; 6 tests passed after the action-log / non-assert switch-in fallback update. Confirms existing double and 2v1 same-reserve switch guards still work. |
| Focused smart Dynamax environment tests | `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_DYNAMAX_ONLY'` | Pass on 2026-06-06; 7 tests passed. Covers conservation, last-Pokemon use, one-reserve low-HP late commit, Max Move payoff, Fake Out disruption prevention, and phazing disruption prevention. |
| Focused smart Z tests | `rtk make -j16 -O check TESTS='AI_FLAG_SMART_Z_MOVE'` | Pass on 2026-06-06; 4 tests passed. Adds one-reserve low-HP late commit. |
| Focused smart Mega tests | `rtk make -j16 -O check TESTS='AI_FLAG_SMART_MEGA'` | Pass on 2026-06-05; 1 test passed. |
| Focused all-gimmick environment tests | `rtk make -j16 -O check TESTS='AI_FLAG_GIMMICK_ENV_ALL'` | Pass on 2026-06-06; 4 tests passed. Covers held Z-Crystal use, doubles Tera, full-HP setup-turn Mega delay, and low-HP one-reserve Mega late commit. |
| Focused Kyogre doubles spread-pressure checks | `rtk make -j16 -O check TESTS='AI does not tunnel Kyogre'`; `rtk make -j16 -O check TESTS='AI prefers Kyogre'` | Pass on 2026-06-06; 2 focused tests passed. Confirms rain-accurate `Thunder` no longer beats meaningful `Water Spout` spread pressure only due to accuracy bypass scoring. |
| Player double move-menu gimmick UI reset | `rtk make -j16 -O debug`; `rtk make -j16 -O all`; `rtk make -j16 -O check TESTS='(Z-MOVE) Every battler can use Z-Moves - Doubles'`; `rtk make -j16 -O check TESTS=\"Dynamax: Max Strike lowers both opponents' speed\"`; mGBA Live boot / screenshot | Pass / partial visual on 2026-06-06. Build and focused runtime checks passed after moving the player gimmick UI reset before move-menu drawing. mGBA Live session `player-gimmick-menu-reset-20260606` booted the ROM at 120 FPS, Lua bridge returned `{ok=true}`, screenshot exported to `/tmp/player-gimmick-menu-reset-20260606.png`, and the session stopped cleanly. The existing animation filter `TESTS=\"Z-Moves don't leak when used - Doubles\"` returned `No tests found`, so direct visual confirmation is still manual: in a double battle, select Z-Move or Dynamax / Max Move for the first player battler, then confirm the second player battler's initial move menu is normal unless that battler independently toggles its own gimmick. |
| Focused smart Tera tests | `rtk make -j16 -O check TESTS='AI_FLAG_SMART_TERA'` | Pass on 2026-06-05; 4 tests passed. |
| Focused smart switching tests | `rtk make -j16 -O check TESTS='AI_FLAG_SMART_SWITCHING'` | Pass on 2026-06-06. Covers double-position switching, partner-cover guard, weather / terrain reserve pivots, Tailwind / Trick Room reserve pivots, terrain seed plans, status-benefit pivots, direct and secondary status / confusion support pivots, Skill Swap bridge pivots, predicted-Taunt attacker pivots / stay-in guards, and the double-switch execution guard through the shared predicted-move immunity predicate. |
| Focused Protect scoring tests | `rtk make -j16 -O check TESTS='Protect: AI'` | Pass on 2026-06-05; 9 tests passed. Covers ignore-protection moves, Unseen Fist, passive singles Protect rejection, boosted-attacker rejection, residual payoff, and second Protect scoring in singles and doubles. |
| Focused Protect scoring tests | `rtk make -j16 -O check TESTS='Protect: AI'` | Pass on 2026-06-06 after the Protect-retarget update; 9 tests passed. Confirms the read-mode retarget penalty did not regress Protect-use scoring. |
| Focused Fake Out priority-pressure tests | `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: selected Fake Out'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE: blocked selected Fake Out does not discount ordinary actions'`; `rtk make -j16 -O check TESTS='Fake Out'`; `rtk make -j16 -O check TESTS='AI will Fake Out'` | Pass on 2026-06-06. Covers read-mode scoring against selected Fake Out, lower-priority `Extreme Speed`, priority-blocker protection through `Armor Tail`, base Fake Out move-effect behavior, and existing AI Fake Out selection behavior. |
| Debug trainer fixture generation | `rtk make tools/trainerproc/trainerproc`; `rtk make -j16 -O debug` | Pass on 2026-06-06 after moving the 8 weighted gauntlet battles under `Gauntlet Battles`, rebalancing the player-side pools, and adding `Mix Single` / `Mix Double` all-gimmick gauntlets. `rtk make -j16 -O debug` also passes after the ability-popup refresh / form-change cleanup fix, after the shared ability-popup palette ownership fix, after preserving debug opponent gimmick bits across `CB2_InitBattle()`, and after the command-buffer read update. |
| Debug `Read Double` fixture generation | `rtk make tools/trainerproc/trainerproc`; `rtk make -j16 -O debug`; `rtk make -j16 -O all` | Pass on 2026-06-06 after adding `Gauntlet Battles -> Read Double`, a mirrored 8-Pokemon level-50 4v4 doubles pool on both sides. |
| Debug ROM build | `rtk make -j16 -O debug` | Pass on 2026-06-06 after the Protect-retarget update. |
| Debug ROM build | `rtk make -j16 -O debug` | Pass on 2026-06-06 after the read-mode Fake Out priority-pressure update. |
| mGBA Live command-buffer route | Direct MCP `mgba_live_start`; `DISPLAY=:0 rtk ... mgba-live-cli start --rom pokeemerald.gba --session-id smart-gimmick-command-read-20260606`; MCP input / screenshot tools | Pass on 2026-06-06 after CLI start. Direct MCP start without `DISPLAY` failed first with Qt `xcb` display initialization. The CLI-started session opened `R+START` debug menu, navigated `Party -> Gauntlet Battles -> Tera Double`, reached battle, accepted both player commands, resolved the turn to the party switch screen, exported `/tmp/smart-gimmick-command-read-after-turn.png`, and stopped cleanly. |
| mGBA Live Dmax Double action-log / assert route | `DISPLAY=:0 rtk ... mgba-live-cli start --rom pokeemerald.gba --session-id smart-gimmick-dmax-double-log-20260606`; MCP input / screenshot / memory read tools | Pass on 2026-06-06. Continued from the existing save, opened `R+START`, navigated `Party -> Gauntlet Battles -> Dmax Double`, entered multiple double-battle turns, observed opponent switch-in resolution from `Kyogre` to `Charizard`, opponent `Protect`, and later `Landorus` board state without hitting the prior `IsValidSwitchIn()` blue assert. Read `gBattleActionLog` at `0x020004cc`; header showed `sequence=15`, `lastRecordedTurn=2`, `cursor=15`, and `count=15`, confirming runtime command / switch entries were stored. Exported `/tmp/smart-gimmick-dmax-double-log-20260606.png`, stopped cleanly, and `mgba-live-cli status --all` returned `[]`. |
| mGBA Live battle action log JSON export | `tools/mgba_live/export_battle_action_log.sh battle-log-export-20260606 /tmp/battle-action-log-export-test.json`; `python3 -m json.tool /tmp/battle-action-log-export-test.json` | Pass on 2026-06-06 for the WSL / Linux wrapper. Started `Party -> Gauntlet Battles -> Dmax Double`, confirmed one double-battle turn, then exported schema `pokeemerald.battle_action_log.v1` with 6 entries. The JSON parsed successfully and included named `MOVE_ASTRAL_BARRAGE`, named `MOVE_FAKE_OUT`, opponent `B_ACTION_SWITCH` entries, and `resolved_switch_in` markers. First sandboxed export attempt failed because `mgba-live-cli` refreshes `~/.mgba-live-mcp/runtime/active_session`; rerunning with approval succeeded. Session stopped cleanly and `status --all` returned `[]`. Windows `.bat` help execution was attempted from WSL but could not be validated in this sandbox because `cmd.exe` could not enter the UNC workspace path; validate it from a normal Windows terminal if that route is used. |
| mGBA Live battle action log field coverage | `python3 -m json.tool /tmp/manual-battle-log-check-current.json`; focused JSON field summary | Pass on 2026-06-06. The current exported log had schema `pokeemerald.battle_action_log.v1`, header `count=17`, `sequence=17`, `last_recorded_turn=2`, and no missing required fields across 17 entries. It included raw codes plus decoded names for actions, moves, items, gimmicks, battler positions, side / slot, target, party index, and flags. Coverage included 10 move entries, 7 switch entries, `GIMMICK_Z_MOVE`, `GIMMICK_MEGA`, `selected_gimmick`, and `resolved_switch_in`. Because the backing buffer is EWRAM and cleared by battle initialization, export during the current battle before starting another battle. |
| mGBA Live pid `297174` / no-gimmick log audit | `/tmp/manual-ai-log-battle-action-log-autosave.json`; `/tmp/battle-action-log-export-test.json`; archived `manual-ai-log-*` session metadata | Partial on 2026-06-06. The pid `297174` autosave had 28 entries through turn 6 and recorded selected `GIMMICK_DYNAMAX` plus `GIMMICK_TERA`, so it was not the reported no-Mega / no-gimmick 4v4. It showed stronger lines after the cross-slot work, including early Tailwind / Dynamax pressure and later pivoting. The only gimmick-free `/tmp` candidate was `/tmp/battle-action-log-export-test.json`, but it had only six turn-0 entries from an export smoke, not a full 4v4 match. A complete no-Mega / no-gimmick 4v4 action log was not found in `/tmp` or archived `manual-ai-log-*` metadata. |
| mGBA Live manual-ai-log analysis | `/tmp/manual-ai-log-battle-action-log-autosave.json`; copied to `/tmp/manual-ai-log-captured-20260606.json`; Python JSON summary | Pass on 2026-06-06. Autosave retained a non-empty 20-entry `pokeemerald.battle_action_log.v1` log while a later direct export correctly skipped an empty current EWRAM buffer. The log covered 16 move actions, 4 resolved switch-ins, 2 selected `GIMMICK_TERA` markers, and turns 0-3. Useful reads: early pressure into player right after Tailwind, Tera capture, and switch-in recording. Main loss-pattern finding: turn 3 had opponent right use `Collision Course` into player right's selected `Protect`; this became the `AI_FLAG_READ_PLAYER_MOVE` Protect-retarget regression. |
| mGBA Live manual-ai-log pid 222151 analysis | Archived session `manual-ai-log-20260606-103907/session.json`; `/tmp/manual-ai-log-battle-action-log-autosave.json`; copied to `/tmp/manual-ai-log-latest-20260606.json` | Pass on 2026-06-06. Archive metadata matched pid `222151`, `fps_target=120.0`, and startup autosave. Heartbeat frame `37830` at `2026-06-06T10:38:14Z` matched the autosave `generated_at_utc` frame `37800`; SHA-256 matched between the autosave and copied latest JSON. The 20-entry log recorded 14 move actions, 6 switch actions, two selected Tera markers, and resolved switch-ins. Main finding: turn 0 had player left selected `Fake Out` into opponent right while opponent right planned ordinary `Draco Meteor`; this became the read-mode Fake Out pressure / priority regression. |
| mGBA Live 120 FPS start helper | `tools/mgba_live/start_mgba_live.sh fps-wrapper-check-20260606 120`; `mgba-live-cli status --all`; `mgba-live-cli run-lua --session fps-wrapper-check-20260606 --code "return 123" --timeout 3`; `mgba-live-cli stop --session fps-wrapper-check-20260606` | Pass on 2026-06-06 for the WSL / Linux wrapper. `status --all` reported an alive session for `pokeemerald.gba` with `fps_target=120.0` and advancing heartbeat frame. The simple Lua bridge call returned `{ result: 123 }`. The initial `emu.framecount()` probe failed because that function is not present in this mGBA Lua API; the session itself and bridge were healthy. Stop returned `stopped:true`. Windows `.bat` was updated but not runtime-validated in this WSL sandbox. |
| mGBA Live action log after session close | `tools/mgba_live/export_battle_action_log.sh manual-ai-retest-20260606 /tmp/manual-ai-retest-final-log.json`; `mgba-live-cli status --all`; archived session inspection | Failed to recover the just-finished battle on 2026-06-06 because the session was already gone before export. `status --all` returned `[]`; the archive at `~/.mgba-live-mcp/runtime/archived_sessions/manual-ai-retest-20260606-20260606-100152` had only session metadata, heartbeat / response, bridge script, and empty stdout / stderr logs. Existing manual exports such as `/tmp/manual-battle-log-check-current.json` still existed, but they were not the just-finished double battle. |
| mGBA Live action log autosave startup | `BATTLE_ACTION_LOG_OUT=/tmp/autosave-wrapper-check-20260606.json tools/mgba_live/start_mgba_live.sh autosave-wrapper-check-20260606 120`; `mgba-live-cli run-lua --session autosave-wrapper-check-20260606 --code "return {autosave=_G.BATTLE_ACTION_LOG_AUTOSAVE ~= nil, ...}"`; `mgba-live-cli stop --session autosave-wrapper-check-20260606` | Pass on 2026-06-06 for startup registration. `session.json` listed `tools/mgba_live/battle_action_log_autosave.lua`, and Lua returned `autosave=true`. No battle was started, so no non-empty autosave JSON was expected in this smoke. Future manual sessions started through `start_mgba_live.sh` should keep the latest non-empty action log at `/tmp/<session>-battle-action-log-autosave.json` unless overridden. |
| mGBA Live Protect-retarget boot smoke | `tools/mgba_live/start_mgba_live.sh ai-protect-retarget-20260606 120`; `mgba-live-cli run-lua`; `mgba-live-cli screenshot`; `mgba-live-cli stop` | Pass / partial runtime on 2026-06-06. Updated `pokeemerald.gba` booted at 120 FPS, Lua returned `{ok=true}`, screenshot exported to `/tmp/ai-protect-retarget-20260606.png`, and the validation session stopped cleanly. This was boot / bridge evidence; the actual Protect-retarget behavior is covered by focused test-runner validation to avoid interrupting manual play. |
| mGBA Live Fake Out priority boot smoke | `tools/mgba_live/start_mgba_live.sh read-fakeout-priority-smoke-20260606 120`; `mgba-live-cli status --all`; `mgba-live-cli run-lua`; `mgba-live-cli screenshot`; `mgba-live-cli stop` | Pass / partial runtime on 2026-06-06. Updated `pokeemerald.gba` booted at 120 FPS, Lua returned `{ok=true}`, screenshot exported to `/tmp/read-fakeout-priority-smoke-20260606.png`, stop returned `stopped:true`, and final `status --all` returned `[]`. This was boot / bridge evidence; the Fake Out priority behavior is covered by focused test-runner validation. |
| mGBA Live cross-slot switch boot smoke | `tools/mgba_live/start_mgba_live.sh known-cross-slot-ko-switch-guard-20260606 120`; `mgba-live-cli status --all`; `mgba-live-cli screenshot --out /tmp/known-cross-slot-ko-switch-guard-20260606.png`; `mgba-live-cli run-lua`; `mgba-live-cli stop`; `mgba-live-cli status --all` | Pass / partial runtime on 2026-06-06. Updated `pokeemerald.gba` booted at 120 FPS with pid `299495`, screenshot showed a nonblank in-game field frame, Lua returned `{ok=true, frame=1217}`, and stop returned `stopped:true`. Final `status --all` still listed the unrelated `manual-ai-log` session alive, so only the validation session was stopped. This was boot / bridge evidence; the exact switch behavior is covered by focused test-runner validation. |
| mGBA Live `Read Double` menu route | `tools/mgba_live/start_mgba_live.sh read-double-menu-20260606 120`; `mgba-live-cli input-tap`; MCP screenshot export; `mgba-live-cli stop`; `mgba-live-cli status --all` | Pass on 2026-06-06. Rebuilt the debug ROM, started the session at 120 FPS, captured `/tmp/read-double-menu-20260606-boot.png`, sent `START`, reached `Party -> Gauntlet Battles`, scrolled to the bottom, captured `/tmp/read-double-menu-20260606-read-double.png` showing `Read Double`, stopped cleanly, and `status --all` returned `[]`. This confirms menu availability; battle behavior remains for manual recheck and action-log session review. |
| mGBA Live mixed gauntlet menu runtime | Preserve running session `manual-ai-retest-20260606`; rebuild debug ROM separately | Not run on 2026-06-06 because the user was actively operating `manual-ai-retest-20260606` and requested that the session not be cut. The source and generated debug trainer data built successfully, but `Mix Single` / `Mix Double` need a later mGBA restart before they can be visually confirmed in `Party -> Gauntlet Battles`. |
| Debug battle EXP / EV gate | `rtk make -j16 -O check TESTS='Debug battles do not give exp or EVs'` | Pass on 2026-06-06; 1 test passed. Confirms `gIsDebugBattle` suppresses the EXP bar, EXP gain, and EV gain. |
| Trainer Party Pool regression | `rtk make -j16 -O check TESTS='Trainer Party Pool'` | Pass on 2026-06-06; 9 tests passed after the gauntlet submenu and same-grade player-pool rebalance. Covers role-filtered pool selection, weighted selection, and runtime RNG variation used by the debug battle fixtures. |
| Trainer Party Pool mixed gauntlet regression | `rtk make -j16 -O check TESTS='Trainer Party Pool'` | Pass on 2026-06-06; 9 tests passed after adding `Mix Single` / `Mix Double`. Covers the same weighted pool and tag rules used by the new all-gimmick pools. |
| Trainer Party Pool `Read Double` regression | `rtk make -j16 -O check TESTS='Trainer Party Pool'`; `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'` | Pass on 2026-06-06 after adding `Read Double`; Trainer Party Pool passed 9 tests, and read-mode AI passed 8 tests. |
| Read Double Trick Room / Protect over-pivot regression | `rtk make -j16 -O check TESTS='AI_FLAG_READ_PLAYER_MOVE'`; `rtk make -j16 -O check TESTS='AI uses Trick Room'`; `rtk make -j16 -O check TESTS='AI_FLAG_SMART_SWITCHING'`; `rtk make -j16 -O check TESTS='Protect: AI'`; `rtk make -j16 -O check TESTS='AI prefers to set up a powerful Status'` | Pass on 2026-06-06 after analyzing the pid `252732` Read Double feedback through the latest non-empty autosave. Read-mode AI passed 9 tests, Trick Room passed 3 tests, Smart Switching passed, Protect passed 9 tests, and the existing Powerful Status Trick Room preference still passes. The first attempted parallel rerun of two `check` filters collided on the shared test ELF and produced `mmap elffd failed`; the sequential reruns passed and are the evidence. |
| Ability popup / form-change focused checks | `rtk make -j16 -O check TESTS='Mega Evolution'`; `rtk make -j16 -O check TESTS='Pressure'`; `rtk make -j16 -O check TESTS='Delta Stream'`; `rtk make -j16 -O check TESTS='Terastallization'`; `rtk make -j16 -O check TESTS='Ultra Burst'`; `rtk make -j16 -O check TESTS='Primal Reversion'`; `rtk make -j16 -O check TESTS='Fairy Aura'`; `rtk make -j16 -O check TESTS='Dark Aura'`; `rtk make -j16 -O check TESTS='Aura Break'`; `rtk make -j16 -O check TESTS='Drizzle'` | Pass on 2026-06-06 after the ability-popup refresh / form-change cleanup fix. Sequential results: Mega Evolution 5 tests, Pressure 5 tests, Delta Stream 1 test, Terastallization 3 tests, Ultra Burst 5 tests, Primal Reversion 15 tests. After restoring the upstream normal-popup script timing and fixing shared palette ownership: Fairy Aura 2 tests, Dark Aura 2 tests, Aura Break 2 tests, and Drizzle 5 tests pass. Earlier parallel attempts are non-evidence because separate `make check` processes collided on the same test ELF / tmpfd artifacts. |
| Runtime knowledge catalog tool | `rtk cargo check --manifest-path tools/runtime_knowledge/Cargo.toml`; `rtk cargo run --manifest-path tools/runtime_knowledge/Cargo.toml -- --out /tmp/runtime_knowledge_catalog_rust --pretty`; JSON parse of `/tmp/runtime_knowledge_catalog_rust/*.json` | Pass on 2026-06-05. Generated valid catalogs for 935 moves, 319 abilities, 130 hold effects, 874 items, 4 gimmick policies, and `summary.json`. |
| Full battle / runtime checks | `rtk make -j16 -O check` | Historical pass on 2026-06-05 before the final Protect / Dmax-vs-Z debug update. Re-attempt after the final update exited 2; the visible output only showed existing test-runner / known-failing labels (`Tests resume after CRASH`, `Pokemon level up learnsets fit within MAX_LEVEL_UP_MOVES and MAX_RELEARNER_MOVES`), and both filtered checks return 0 individually. Focused checks above are the accepted evidence for this update. A `rtk make -j1 -O check` re-attempt did not progress beyond the initial link warning and was abandoned as non-evidence; stale `mgba-rom-test-hydra` / `mgba-rom-test` children were killed. |
| Full battle / runtime checks after `Read Double` | `rtk make -j16 -O check` | Exited 2 on 2026-06-06. `/tmp/read-double-check.log` recorded three unrelated existing broad-run AI failures: `AI_FLAG_ATTACKS_PARTNER is willing to kill either the partner or the player 4/6`, `AI_FLAG_SMART_TERA: AI might tera if it gets saved from a ko (1/?)`, and `AI uses Z-Moves -- Z-Detect 1/2`. The focused Trainer Party Pool and read-mode checks above passed and are the accepted evidence for this debug-fixture-only update. |
| Broad existing Z-Move AI filter | `rtk make -j16 -O check TESTS='AI uses Z-Moves'` | Failed on 2026-06-06 in existing `AI uses Z-Moves -- Z-Detect 1/2`: expected Z-Move, got no gimmick. Focused smart-gimmick checks pass; this status-Z Protect heuristic is tracked as separate follow-up evidence. |
| Normal ROM build | `rtk make -j16 -O all` | Pass on 2026-06-06 after the ability-popup refresh / form-change cleanup fix, after the shared ability-popup palette ownership fix, after the debug opponent gimmick-bit preservation / Kyogre spread-pressure fix, and after the command-buffer read update. |
| Normal ROM build | `rtk make -j16 -O all` | Pass on 2026-06-06 after the Protect-retarget update. |
| Normal ROM build | `rtk make -j16 -O all` | Pass on 2026-06-06 after the read-mode Fake Out priority-pressure update. |
| Normal ROM build | `rtk make -j16 -O all` | Pass on 2026-06-06 after the Read Double Trick Room / Protect over-pivot tuning. |
| Debug ROM build | `rtk make -j16 -O debug` | Pass on 2026-06-06 after the Read Double Trick Room / Protect over-pivot tuning. |
| Docs build | `rtk mdbook build docs` | Exit 0 on 2026-06-06 after docs updates. Existing warnings remain for the missing root `CHANGELOG.md` include, `CREDITS.md` HTML end tag, and large search index. |
| Docs build | `rtk mdbook build docs` | Pass on 2026-06-06 after documenting the ability-popup refresh / form-change cleanup fix, shared palette ownership fix, and debug opponent gimmick-bit preservation / Kyogre spread-pressure fix, with existing warnings: missing root `CHANGELOG.md` include, existing `CREDITS.md` `</img>` warning, large search index. |
| Docs build | `rtk mdbook build docs` | Exit 0 on 2026-06-06 after documenting the player double move-menu gimmick UI reset. Existing warnings remain: missing root `CHANGELOG.md` include, existing `CREDITS.md` `</img>` warning, and large search index. |
| Docs build | `rtk mdbook build docs` | Exit 0 on 2026-06-06 after documenting the Protect-retarget update. Existing warnings remain: missing root `CHANGELOG.md` include, existing `CREDITS.md` `</img>` warning, and large search index. |
| Docs build | `rtk mdbook build docs` | Exit 0 on 2026-06-06 after documenting the read-mode Fake Out priority-pressure update. Existing warnings remain: missing root `CHANGELOG.md` include, existing `CREDITS.md` `</img>` warning, and large search index. |
| Docs build | `rtk mdbook build docs` | Exit 0 on 2026-06-06 after documenting `Read Double`. Existing warnings remain: missing root `CHANGELOG.md` include, existing `CREDITS.md` `</img>` warning, and large search index. |
| Docs build | `rtk mdbook build docs` | Exit 0 on 2026-06-06 after documenting the Read Double Trick Room / Protect over-pivot tuning. Existing warnings remain: missing root `CHANGELOG.md` include, existing `CREDITS.md` `</img>` warning, and large search index. |
| Docs build | `rtk mdbook build docs` | Exit 0 on 2026-06-06 after documenting the pid `317741` ally-benefit update and `Partner Double`. Existing warnings remain: missing root `CHANGELOG.md` include, existing `CREDITS.md` `</img>` warning, and large search index. |
| mGBA Live runtime route | Boot debug ROM, enter the debug Party menu, start a target gauntlet battle, and capture at least one in-battle frame after player input / opponent response | Pass on 2026-06-06 for debug route and battle reachability. Session `smart-gimmick-read-aggressive-20260606` booted the debug ROM from `NEW GAME`, reached field control, opened `R+START` overworld debug menu, navigated `Party -> Gauntlet Battles -> Dmax Double`, started the battle, reached a move-selection screen, confirmed player input, and captured `/tmp/smart-gimmick-read-aggressive-after-input.png` on an in-battle response frame. `input-clear`, `stop`, and `status --all` cleaned up with `[]`. This was behavior-route evidence, not only title-screen smoke. Manual mGBA input did not capture a visible opponent Dynamax / Tera / Z-Move animation in this pass because early turns KO'd opposing slots quickly; focused AI tests are the direct evidence for aggressive/read logic. |
| mGBA Live Read Double Trick Room / Protect smoke | `tools/mgba_live/start_mgba_live.sh read-trick-protect-smoke-20260606 120`; `mgba-live-cli status --all`; `mgba-live-cli screenshot`; `mgba-live-cli run-lua --code 'return {ok=true}'`; `mgba-live-cli stop` | Pass on 2026-06-06. Started pid `274593` at 120 FPS, exported `/tmp/read-trick-protect-smoke-20260606.png`, confirmed the Lua bridge returned `{ok=true}`, stopped cleanly, and final `status --all` returned `[]`. This is boot / bridge evidence; the actual Trick Room mirror and Protect-over-pivot behavior is covered by focused `check` tests because replaying the exact manual loss line requires user-controlled input. |
| mGBA Live historical boot smokes | Boot current ROM and capture one screenshot / input state | Pass on 2026-06-06 for earlier fixes only. For the ability-popup refresh / form-change cleanup fix, sandboxed CLI startup first failed because it could not create `~/.mgba-live-mcp/runtime/sessions/ability-popup-formchange-20260606`; rerunning with approval and `DISPLAY=:0` booted `pokeemerald.gba` to the title screen, exported `/tmp/ability-popup-formchange-20260606.png`, `mgba-live-cli stop` returned `stopped:true`, and `status --all` returned `[]`. For the shared palette ownership fix, `DISPLAY=:0` booted `pokeemerald.gba` in session `ability-popup-palette-20260606`, exported `/tmp/ability-popup-palette-20260606.png`, `stop` returned `stopped:true`, and `status --all` returned `[]`. For the debug opponent gimmick-bit preservation fix, `DISPLAY=:0` booted `pokeemerald.gba` in session `smart-gimmick-pending-bits-20260606`, exported `/tmp/smart-gimmick-pending-bits-20260606.png`, `stop` returned `stopped:true`, and `status --all` returned `[]`. These were boot smokes only; they are not accepted as validation for the aggressive/read gauntlet behavior. |

## Manual Runtime Checks

### Debug Party Battle Fixtures

Build the debug ROM path first:

```bash
rtk make -j16 -O debug
```

In-game, open the overworld debug menu with `R + START` when `DEBUG_OVERWORLD_MENU` is enabled and `DEBUG_OVERWORLD_IN_MENU` is `FALSE`. Then use `Party`. The gauntlet fixtures are grouped under `Party -> Gauntlet Battles` so the top-level Party menu stays under the debug menu's 20-item limit:

- `Battle 3v3 Single`: starts a level-50 3v3 singles battle. Player side is fixed to `Dragonite`, `Gholdengo`, and `Garchomp`. AI side is selected from the `Ladder` 6-Pokemon weighted pool with 3 chosen Pokemon.
- `Battle 4v4 Double`: starts a level-50 4v4 doubles battle. Player side is fixed to `Incineroar`, `Rillaboom`, `Flutter Mane`, and `Urshifu-Rapid-Strike`. AI side is selected from the `VGC Test` 7-Pokemon weighted pool with 4 chosen Pokemon.
- `Battle Dmax/Z Single`: starts a level-50 3v3 singles battle. Player side is a Z-Move test team. AI side is a Dynamax / Gigantamax / Z-Crystal pressure team with smart gimmick flags.
- `Battle Dmax/Z Double`: starts a level-50 4v4 doubles battle. Player side is a VGC-style Z-Move test team. AI side is a Dynamax / Gigantamax / Z-Crystal doubles team with Tailwind and weather pressure.
- `Battle Gimmick Single`: starts a singles availability audit. Player side is passive. AI side leads `Gengarite` Mega and has reserve `Mimikium Z`, Gigantamax `Charizard`, and Normal Tera `Dragonite`.
- `Battle Gimmick Double`: starts a doubles availability audit. Player side is passive. AI side leads Gigantamax `Charizard` plus `Electrium Z` `Tapu Koko`, with reserve `Gengarite` Mega and Normal Tera `Dragonite`.
- `Gauntlet Battles -> Mega Single`: starts a level-50 3v3 singles battle with Mega-only debug access.
- `Gauntlet Battles -> Mega Double`: starts a level-50 4v4 doubles battle with Mega-only debug access.
- `Gauntlet Battles -> MegaZ Single`: starts a level-50 3v3 singles battle with Mega + Z-Power debug access.
- `Gauntlet Battles -> MegaZ Double`: starts a level-50 4v4 doubles battle with Mega + Z-Power debug access.
- `Gauntlet Battles -> Dmax Single`: starts a level-50 3v3 singles battle with Dynamax-only debug access.
- `Gauntlet Battles -> Dmax Double`: starts a level-50 4v4 doubles battle with Dynamax-only debug access.
- `Gauntlet Battles -> Tera Single`: starts a level-50 3v3 singles battle with Tera-only debug access.
- `Gauntlet Battles -> Tera Double`: starts a level-50 4v4 doubles battle with Tera-only debug access.
- `Gauntlet Battles -> Mix Single`: starts a level-50 3v3 singles battle with all gimmick access and mixed Mega / Z-Move / Dynamax / Tera weighted pools on both sides.
- `Gauntlet Battles -> Mix Double`: starts a level-50 4v4 doubles battle with all gimmick access and mixed Mega / Z-Move / Dynamax / Tera weighted pools on both sides.
- `Gauntlet Battles -> Read Double`: starts a level-50 4v4 doubles battle with all gimmick access and mirrored 8-Pokemon weighted pools on both sides. Use this for reset-style manual read audits before exporting the action log from the supplied session ID.
- `Gauntlet Battles -> Partner Double`: starts a fixed level-50 4v4 doubles battle with all gimmick access. Player side is `Miraidon`, `Incineroar`, `Rillaboom`, and Mega `Gengar`; AI side is `Amoonguss`, `Xerneas`, `Primarina`, and `Coalossal`. Use this for intentional ally-benefit audits: `Rage Powder`, ally `Pollen Puff`, and ally `Aqua Jet` into `Steam Engine` / `Weakness Policy`.
- `Gauntlet Battles -> Champs Double`: starts a fixed level-50 4v4 no-legendary doubles battle with Mega-only debug access. Player side is `Dragonite` with `Dragoninite`, `Gardevoir`, `Dondozo`, and `Hydrapple`; AI side is `Lucario` with `Lucarionite`, `Charizard`, `Aegislash`, and Hisuian `Samurott`. Use this for Pokemon Champions-style read audits without restricted legends or mixed gimmicks.

Expected results:

- The battle starts from the Party debug menu without truncating the 4v4 player side to 3 Pokemon.
- The opposing side is generated from the `.party` pool, respecting `Party Size`, `Pool Rules`, tags, and `Pool Weight`.
- If `B_POOL_SETTING_CONSISTENT_RNG` is `FALSE`, repeated starts can produce different opposing selections. If it is `TRUE`, selection is deterministic for the same save OTID and trainer pointer.
- Fainting opposing Pokemon does not display the EXP bar and does not grant EXP or EVs.
- AI behavior should use the configured `Smart Trainer`, `Prediction`, `Smart Gimmick`, `Know Opponent Party`, and `Powerful Status` flags.
- In the Dmax/Z fixtures, the debug battle grants Z-Power Ring and Dynamax Band access through `gDebugGimmickAccessFlags`, so the player can test Z-Move pressure into AI Dynamax / Gigantamax timing without requiring those Bag key items. Z-Moves still require Z-Crystals. These fixtures are competitive timing fixtures and do not contain an AI Mega candidate.
- In the Gimmick audit fixtures, the debug battle grants all gimmick access through `gDebugGimmickAccessFlags`. Use these when the question is "can the opponent use Mega / Z / Dynamax / Tera at all?" rather than "does smart timing decide to conserve?"
- Opponent-side debug gimmick use does not depend on the opponent having Bag key items. Check trainer-party data instead: Mega Stone, Z-Crystal plus `Z Move: Yes`, `Dynamax Level` plus `Dynamax: Yes`, or `Tera Type`. The debug Party route now preserves those opponent bits across `CB2_InitBattle()` even though the party is built before `gBattleStruct` allocation.
- `Battle Gimmick Single` expected quick check: the opposing lead `Gengar` should be able to Mega Evolve early. Continue by fainting or forcing through reserves to inspect `Mimikyu` Z-Move, Gigantamax `Charizard`, and `Dragonite` Tera availability.
- `Battle Gimmick Double` expected quick check: the opposing lead pair exposes a Dynamax / Gigantamax candidate and a Z-Move candidate immediately. Charizard has `.shouldUseDynamax = TRUE`; Tapu Koko has `Electrium Z` plus `.shouldUseZMove = TRUE`. Continue through reserves to inspect `Gengar` Mega and `Dragonite` Tera availability.
- In the gauntlet fixtures, both the player and AI parties are generated from weighted pools. Repeated starts should be able to produce different teams while still respecting `Party Size`, item clause, species clause, and role tags.
- Gauntlet AI trainers use `Smart Trainer`, `Prediction`, `Smart Gimmick`, `Smart Switching`, `Smart Mon Choices`, `Omniscient`, `Know Opponent Party`, `Powerful Status`, `Aggressive Gimmick`, and `Read Player Move`. This is the "AI can read with full information plus confirmed player command input" route for checking whether predictions, pivots, and gimmick timing are actually good enough when the opposing team is known.
- Mega-only gauntlets should not expose Z-Move, Dynamax, or Tera access. Mega+Z gauntlets should expose Mega and Z only. Dynamax-only gauntlets should expose Dynamax / Gigantamax only. Tera-only gauntlets should expose Tera only.
- Mix gauntlets should expose every debug gimmick access bit at once while still relying on each Pokemon's data: Mega Stone for Mega, Z-Crystal plus `Z Move: Yes`, `Dynamax Level` / `Gigantamax: Yes`, or `Tera Type`.
- `Read Double` should keep both sides same-grade: the mirrored pools can roll `Tornadus`, `Incineroar`, `Farigiraf`, `Rillaboom`, `Miraidon`, `Koraidon`, `Gengar`, and `Tapu Koko`, with lead / ace tags and weights. The AI's advantage should come from `Read Player Move`, known player party data, switching, and timing quality, not a stronger roster.
- `Partner Double` should start with the ally-benefit board visible instead of waiting for a pool roll. The first AI side is `Amoonguss` + `Xerneas`, so a selected player single-target attack into Xerneas should make `Rage Powder` a real candidate, and low-HP / boosted Xerneas states should make ally `Pollen Puff` valuable. The reserve pair `Primarina` + `Coalossal` is intended for low-damage `Aqua Jet` triggering `Steam Engine` and `Weakness Policy`.
- `Champs Double` should stay no-legendary and Mega-only. It intentionally uses Pokemon visible on official Pokemon Champions pages, including `Dragonite`, `Gardevoir`, `Dondozo`, `Hydrapple`, `Lucario`, `Charizard`, `Aegislash`, and Hisuian `Samurott`: https://champions.pokemon.com/en-us/pokemon/ and https://champions.pokemon.com/en-us/gameplay/
- The gauntlet sides should be roughly same-grade after pool selection. The player side can roll restricted legends, mythicals, strong support, and high-value gimmick anchors too; the AI's intended edge comes from full-information prediction, switching, and timing, not from having a categorically stronger team.
- The top-level Party menu should render cleanly with no corrupted text. Entering `Gauntlet Battles` should show the thirteen gauntlet entries without mojibake, including the scroll-bottom `Read Double`, `Partner Double`, and `Champs Double` entries.
- Ability popups should not linger over form-change messages. In Mega gauntlets, a switch-in `Pressure` popup from `Mewtwo` should close before the `Mewtwonite Y is reacting` Mega Evolution message, and repeated same-battler ability activations should refresh the existing popup instead of leaving black OBJ-tile rectangles.
- Normal ability popups should keep the upstream timing. `Fairy Aura`, `Dark Aura`, and switch-in weather popups should not be cut short; the popup palette should also stay stable while Move Info Window / Last Used Ball Window sprites are closing.
- In Rayquaza / Delta Stream scenarios, after strong winds dissipate or the active Pokemon changes, the battlefield should not show a black rectangle where an old ability popup sprite or shared OBJ tile used to be.
- Double battles should not hit the `IsValidSwitchIn()` ASSERT when the AI tries to reposition. If both active AI Pokemon want to switch but only one legal reserve exists, or a partner already reserved the best target, the second switch request should be canceled or redirected to a legal reserve.
- Opponent gimmicks are not forced to fire immediately in smart-timing fixtures. They are expected to activate only when the smart timing checks find a concrete payoff: last-Pokemon pressure, one-reserve low-HP late commit, KO conversion, Max Move board control, disruption prevention, useful Mega / Tera form timing, or a valid Z-Move payoff.
- In gauntlet fixtures, `Aggressive Gimmick` should make Dynamax / Tera / damaging Z-Move use visibly more proactive than the normal smart-conservation fixtures. A good manual target is roughly "often spends on real pressure, sometimes holds," not "always press immediately" and not "never spend until the last Pokemon."
- In gauntlet fixtures, `Read Player Move` can only read player commands that have already been confirmed by the controller. In doubles, if only one player-side battler has confirmed when the opponent recomputes, the read is intentionally partial until a later full turn-tree evaluator exists.
- Player-side double battle move selection should not leak a first battler's selected gimmick state into the second battler's first rendered move menu. After choosing Z-Move or Dynamax for the first player battler, the second player battler should initially show its normal move names, PP, and type text unless START is pressed for that second battler and that battler is eligible for the selected gimmick.

For mGBA Live validation, do not stop at title-screen boot. Use a progressed debug save or scripted input route, open `Party -> Gauntlet Battles`, start one target gauntlet, choose a player move, and capture the opponent response. If the automation cannot reach that state, record the exact navigation failure here instead of marking the runtime check complete.

Use the debug trainer battle flow and Trainer 1 IDs documented in `docs/tutorials/ai_flags.md`.

- `855`: no-payoff Dynamax conserve.
- `856`: last-Pokemon Dynamax.
- `858`: Z-Move viability and smart timing.
- `859`: offensive Tera.
- `860`: Dynamax + Tera environment with separate candidates.
- `861`: all-gimmick single battle.
- `862`: all-gimmick double battle.
- `863`: Smart Switching debug double battle.

For double switching behavior, use any debug double battle with `AI_FLAG_SMART_SWITCHING` and a passive pinned AI lead pair. Expected behavior is that the AI can hard switch one or both active Pokemon if neither active slot has useful pressure and the partner cannot cover the threatened position.

For board-control switching, use an AI reserve that can change the field or speed state. Expected examples:

- `Pelipper` / `Drizzle` can be chosen over staying in when rain creates stronger reserve pressure.
- `Rillaboom` / `Grassy Surge` can be chosen when Grassy Terrain plus Grass pressure changes the board.
- A reserve with `Tailwind` can be chosen when doubled Speed flips relevant matchups.
- A reserve with `Trick Room` can be chosen when the AI side is slower and the current room state does not already cover that plan.
- A reserve terrain setter holding the matching seed can be chosen when the field activates a stat plan.
- A reserve with status-benefit ability or `Facade` / `Psycho Shift` can be chosen into a predicted status move.
- In doubles, a reserve with direct status pressure, secondary status / confusion pressure, status prevention, status cure, or Skill Swap-style ability bridge support can be chosen when the active slot is pinned and the bench role can create board pressure.

For the `863` Smart Switching debug fixture:

- Target the AI left `Zigzagoon` with `Taunt`. Expected: when prediction fires and `Zigzagoon` cannot punish in place, the AI can pivot into `Gengar` to attack the Taunt user instead of staying as disabled utility.
- Target the AI left `Zigzagoon` with `Will-O-Wisp`. Expected: when prediction fires, `Guts` `Ursaring` is an intended burn-benefit reserve.
- Pressure the AI left slot with an unfavorable Fighting or strong-damage line. Expected: the AI can pivot toward `Slowbro`, whose `Scald` / `Psybeam` pressure represents secondary-status / confusion-style board support.
- Negative guard to keep in mind while testing custom parties: the AI should not switch only because a Pokemon was Taunted. It should stay if the active Pokemon can already punish with damage, if Taunt is blocked by `Aroma Veil` / Gen 6+ `Oblivious`, or if an enabled Gen 5+ `Mental Herb` can absorb the Taunt.

For Dynamax disruption timing, use a Dynamax-capable AI active Pokemon with a reserve remaining and a selected damaging move. Expected examples:

- If the known / predicted opposing move is `Fake Out` or another relevant flinch move, smart Dynamax can spend to keep the selected attack live.
- If the known / predicted opposing move is `Roar` or `Whirlwind`, smart Dynamax can spend to avoid losing the active Pokemon to phazing.

For Protect timing, use singles and doubles battles where the AI has `Protect` plus a predicted damaging move. Expected examples:

- In singles, the AI should usually avoid passive Protect if it only gives the opponent a free turn, especially after the opponent has already boosted.
- In singles, the AI can use Protect when it gains a real turn payoff: residual target damage, incoming Wish, Poison Heal / Leftovers / Black Sludge recovery, Substitute threshold, choice-lock scouting, or a next-turn Disable / Encore line.
- A second consecutive Protect is not forbidden. It should be penalized for reduced success odds, but can remain viable when the same payoff still matters.
- In doubles, second Protect should also remain available as a risky positioning option under pressure rather than being removed from consideration.
- In doubles, a pressured active Pokemon with usable `Protect` should not be forced into optional all-moves-bad / delayed speed-control / bad-position / bad-odds switching if Protect can buy the turn. This guard does not apply when the Pokemon is trying to escape status, Yawn, perish, Encore, similar volatile pressure, or a move that ignores Protect.

## Accepted Risks Before Deeper Tuning

- G-Max unique effects need separate scoring if they become important to trainer fixtures.
- Air Lock / Cloud Nine preservation still needs a focused pre-weather AI unit once a clean weather fixture exists for this file.
- Smart Z-Move status tactics still depend on the existing status Z-Move checks.
- Broad existing `AI uses Z-Moves` validation currently exposes a separate `Z-Detect` status-Z timing gap. Keep the focused `AI_FLAG_GIMMICK_ENV` and `AI_FLAG_SMART_Z_MOVE` checks covered until that broader heuristic is retuned.
- The AI now applies a lightweight future board-pressure heuristic to damaging doubles candidates, including a three-turn same-move horizon for Choice / Gorilla Tactics users and a partner-coverage gap reward when reliable future context is available. This is intentionally broader than `AI_FLAG_READ_PLAYER_MOVE`, but it still does not perform a full turn-tree search.
- Tactical partner activation is still heuristic. It separates spread, priority, and low-damage non-priority triggers, but it does not yet solve every multi-turn line where an ally accepts damage now for a later-positioning payoff.
- `AI_FLAG_READ_PLAYER_MOVE` is a debug/testing input-read layer, not a fair normal-NPC policy. It now waits for all live player-side commands before the opponent recomputes action / move choice, but it still evaluates one chosen command set plus bounded future-pressure heuristics instead of searching a full turn tree.
- Choice-role-done switching is intentionally conservative. It currently requires a confirmed read turn, an already locked Choice / Gorilla Tactics move, no known damage into the active slot, weak locked-move progress, and a switch-in that is not punished by known damage. It does not yet model every status-target, PP, or two-turn sacrifice line.
- Protect scoring is payoff-based, not a full opponent turn-tree read. It recognizes common turn-gain reasons and consecutive-use risk, but it does not yet solve every PP-stall, double-target, or "Protect to bait a switch" line.
- Double switching and board-control pivoting are heuristic position checks, not a full VGC turn solver. Board-control pivots can identify weather, terrain, Tailwind, and Trick Room roles, but they do not yet search full multi-turn lines such as "switch setter now, protect partner next turn, then reposition again."
- Status and ability-bridge switching is intentionally conservative in singles. Non-immediate utility pivots are double-battle only in this slice; singles keep only immediate status-benefit pivots plus the existing bad-odds / bad-matchup gates.

## GitHub Actions

Long GitHub Actions were not re-waited for this local handoff. Local `check`, `all`, docs build, and mGBA Live smoke provide the branch evidence for this update.
