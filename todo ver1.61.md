欲しいのは、このトレーナー線においてタグ付けをできるようにして検索向上を高めます。今はランダムのアンダーバーCというような記載をしていますが、これをランダムマイザーのランクの方で、例えば何か戦略性の高いものを実現するために、例えばタイプ別に並べるとかではなく、タグをつけることで、そのポケモンしか出ないように検索をかけることができます。そしてそれを重みをもちろんつけた状態でできます。ただこちらの重みに関しては計算方法が狂うかもしれないので、そこは別途ご相談になります。プログラムの内容をご覧ください。合計を取得し、そこから抽選をします。もちろんそのWaterというようなタグがついていないポケモンは除外します。なので、あらかじめその設定が親元のTrainer.Partyの方に定義がされている場合は、もうあらかじめそこからフィルターをかける必要があります。ただ、これの大きなメリットとしまして、そういった戦略性の高いパーティーがもともと組めるようになることです。こちらはあくまでオリジナルのタグの名前をつけることによって検索が向上できます。なので、レベルが100以上とかそういったところも自分たちで補うことができます。もちろん条件としてレベル100以上みたいなことももちろん設定ができますが、それはタグ付けのセンスを問われるということにしときます。

› では懸念点について話していきましょう。まず決めておくこと。まず既存の案Aでいきます。なのでタグをを使います。このタグなんだけど、既存のトレーナー.partyの方にタグっていうのを書いて、例えばvoter、そしてfireっていう2つのもしタグ複数あった場合は
  スラッシュで書き込んでいきます。で、そのランク、例えばランクがAだったとして、あ、ごめん、固定シンボルのときにしかこれ使えないから、あ、いや、どちらでも使えてもいいかもね。じゃあタグなんで、例えばランクunderbarCの後にタグっていうのをつけま
  す。で、そのタグのところにfireっていうのを例えばつけたとして、そしたらランクの指定された、例えばものがランクunderbarCというような形であれば、そのランクCの.partyのところからタグのfireっていうのを見つけてもらい、それに合致したところから抽選
  を行います。重みに関しては、もともとの抽選のときから抽選する前に、まずはフィルターをかけることを優先します。そのフィルターから通した後に重みを再計算、調整してもらい、そこから抽選をします。なので勝手にファイヤーのやつだけフィルターをして、
  重みを全く無視することは許しません。ちゃんと重みに合ったものに調整をしてほしいです。その差額分、例えば100のうちの10しかファイヤーがなくて、合計が残りがほぼ90だった場合は、それを全部安分してちょっとずつファイヤーの方に預けるようなロジックに
  なるかと思います。一旦これでどうでしょうか。
